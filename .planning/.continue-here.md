---
phase: between-phases
task: 0
total_tasks: 3
status: between_phases
last_updated: 2026-03-04T23:15:52.623Z
---

<current_state>
Phase 38 merged to main (PR #7, squash merged, branch cleaned up). Deliberation "signal-lifecycle-closed-loop-gap" concluded with adoption plan. Three quick tasks remain to implement the deliberation's recommendations (Options A, B, and design principle). User wants all three done as a single /gsd:quick session.
</current_state>

<completed_work>

- Deliberation concluded: signal-lifecycle-closed-loop-gap (status: Concluded, 4 predictions recorded)
- Option C (deliberation epistemic hardening): Already adopted — Step 2.5 (Severe Testing), Prior Deliberation Check, AskUserQuestion phantom guard all committed and merged via phase-38
- Phase 38 PR #7 created, CI fix (wiring-validation.test.js exemption for deliberate.md), CI passed, squash merged to main
- Post-merge cleanup: local branch deleted, remote branch deleted, main pulled, local reinstalled
</completed_work>

<remaining_work>

All three items should be done as ONE /gsd:quick task:

**Item 1 — Option A: Programmatic Signal Lifecycle Reconciliation**
- Create shell script `get-shit-done/bin/reconcile-signal-lifecycle.sh`
  - Input: phase directory path
  - Finds all `*-PLAN.md` files in the directory
  - Extracts `resolves_signals` via `gsd-tools.js frontmatter get --field resolves_signals --raw`
  - Validates result is a JSON array (starts with `[`), not error object
  - For each signal ID, finds signal file in `~/.gsd/knowledge/signals/`, updates `lifecycle_state: "remediated"`, appends `lifecycle_log` entry with resolved_by_plan, approach, timestamp
  - Rebuilds KB index via `kb-rebuild-index.sh`
- Add one-liner call to `get-shit-done/workflows/execute-phase.md` AFTER the verification step (post-verifier, pre-"phase complete" declaration)
- The script must also be runnable standalone for backfill

**Item 2 — Option B: KB Health Watchdog**
- Add lifecycle consistency check to `get-shit-done/workflows/health-check.md`
- Cross-reference: plans with `resolves_signals` declarations vs referenced signals' `lifecycle_state`
- Flag inconsistencies: "Plan X-Y declared it resolves sig-Z, but sig-Z is still in detected state"
- Severity: warning (not blocking)

**Item 3 — Design Principle in PROJECT.md**
- Add to PROJECT.md Key Decisions table:
  - Decision: "Critical state transitions must be programmatic (scripts/hooks), not agent instructions"
  - Rationale: "Agent instructions are unreliable at ensuring every step fires in long sequences — executor skipped update_resolved_signals despite code existing in execute-plan.md"
  - Outcome: "Deliberation concluded — reconcile-signal-lifecycle.sh replaces agent-instruction-based transitions"
- Reference: deliberation signal-lifecycle-closed-loop-gap.md

After all three: run `node bin/install.js --local` to sync .claude/ and run `npm test` to verify.
</remaining_work>

<decisions_made>

- Deliberation concluded with A+C+B recommendation. Option C already adopted. Options A and B as quick tasks.
- Option A implementation: shell script (not gsd-tools.js modification, which is upstream). Per-phase granularity (one call after all plans complete, not per-plan). Integration point: execute-phase.md after verifier returns.
- Option D (backfill of 101 signals) deferred until forward fix proves reliable.
- Design principle: "Critical state transitions should be programmatic, not agent instructions" — meta-lesson from this deliberation.
- gsd-tools.js frontmatter parsing: use `frontmatter get --field resolves_signals --raw`, validate starts with `[` (not error JSON object `{"error":"Field not found",...}`).
</decisions_made>

<blockers>
- None
</blockers>

<context>
This work implements the concluded deliberation "signal-lifecycle-closed-loop-gap". The deliberation found that lifecycle transitions (detected -> triaged -> remediated -> verified) exist as agent instructions in execute-plan.md but agents don't reliably follow them. The fix moves the remediation transition to a deterministic shell script called at the orchestrator level. The health watchdog (Option B) detects when transitions fail silently. The design principle ensures future automation phases (39-43) prefer programmatic triggers over agent instructions.

Key files to reference during implementation:
- Deliberation: .planning/deliberations/signal-lifecycle-closed-loop-gap.md
- execute-plan.md update_resolved_signals step (lines 335-397) — the agent instruction being replaced
- execute-phase.md — where the script call goes (after verification step)
- health-check.md — where the watchdog check goes
- PROJECT.md Key Decisions table — where the design principle goes
</context>

<next_action>
Resume from: run /gsd:quick with all three items as one task. The task description: "Implement signal lifecycle deliberation: reconciliation script (Option A), health check watchdog (Option B), and design principle (PROJECT.md)". After completing: reinstall locally, run tests, create PR, merge. Then clear context and plan Phase 39.
</next_action>
