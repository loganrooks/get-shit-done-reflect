---
phase: 47-fork-module-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/sensors.cjs
  - get-shit-done/bin/lib/backlog.cjs
  - get-shit-done/bin/lib/health-probe.cjs
  - get-shit-done/bin/gsd-tools.cjs
autonomous: true
must_haves:
  truths:
    - "sensors-list and sensors-blind-spots commands produce identical CLI output before and after extraction"
    - "All 7 backlog subcommands produce identical CLI output before and after extraction"
    - "All 3 health-probe commands produce identical CLI output before and after extraction"
    - "The 4 duplicate frontmatter helpers are removed from gsd-tools.cjs"
    - "cmdForkFrontmatterValidate still works after dead code removal"
  artifacts:
    - path: "get-shit-done/bin/lib/sensors.cjs"
      provides: "cmdSensorsList, cmdSensorsBlindSpots"
      exports: ["cmdSensorsList", "cmdSensorsBlindSpots"]
    - path: "get-shit-done/bin/lib/backlog.cjs"
      provides: "7 backlog commands + 3 helpers"
      exports: ["cmdBacklogAdd", "cmdBacklogList", "cmdBacklogUpdate", "cmdBacklogStats", "cmdBacklogGroup", "cmdBacklogPromote", "cmdBacklogIndex"]
    - path: "get-shit-done/bin/lib/health-probe.cjs"
      provides: "3 probe commands + 3 KB helpers"
      exports: ["cmdHealthProbeSignalMetrics", "cmdHealthProbeSignalDensity", "cmdHealthProbeAutomationWatchdog"]
  key_links:
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/sensors.cjs"
      via: "require('./lib/sensors.cjs') + dispatcher case 'sensors'"
      pattern: "sensors\\.cmdSensors"
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/backlog.cjs"
      via: "require('./lib/backlog.cjs') + dispatcher case 'backlog'"
      pattern: "backlog\\.cmdBacklog"
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/health-probe.cjs"
      via: "require('./lib/health-probe.cjs') + dispatcher case 'health-probe'"
      pattern: "healthProbe\\.cmdHealthProbe"
    - from: "get-shit-done/bin/lib/backlog.cjs"
      to: "get-shit-done/bin/lib/frontmatter.cjs"
      via: "require('./frontmatter.cjs')"
      pattern: "frontmatter\\.extractFrontmatter"
---

<objective>
Extract sensors.cjs, backlog.cjs, and health-probe.cjs from gsd-tools.cjs inline functions into dedicated lib/ modules, then remove the 4 duplicate frontmatter helpers (dead code) and update cmdForkFrontmatterValidate to use the frontmatter module.

Purpose: Move 3 of 5 fork command groups into dedicated modules, continuing Phase 46's modularization. These 3 modules are the simpler set (no shared constants section). Dead code removal cleans up 213 lines of duplicate frontmatter helpers that are byte-identical to frontmatter.cjs exports.

Output: 3 new modules in get-shit-done/bin/lib/, updated dispatcher routing, dead code removed.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-fork-module-extraction/47-CONTEXT.md
@.planning/phases/47-fork-module-extraction/47-RESEARCH.md
@get-shit-done/bin/gsd-tools.cjs
@get-shit-done/bin/lib/core.cjs
@get-shit-done/bin/lib/frontmatter.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sensors.cjs, backlog.cjs, and health-probe.cjs modules</name>
  <files>
    get-shit-done/bin/lib/sensors.cjs
    get-shit-done/bin/lib/backlog.cjs
    get-shit-done/bin/lib/health-probe.cjs
  </files>
  <action>
Create 3 new CJS modules in get-shit-done/bin/lib/. Copy function bodies VERBATIM from gsd-tools.cjs -- do not rename parameters, change logic, or "improve" code. Follow the upstream module convention: docstring, requires, constants, helpers, commands, exports.

IMPORTANT: Always edit get-shit-done/bin/lib/ (npm source), NEVER .claude/get-shit-done-reflect/bin/lib/ (install target).

**sensors.cjs** (~145 lines):
- Requires: `fs`, `path`, `{ error, output }` from `./core.cjs`
- Commands: Copy `cmdSensorsList` (lines 2219-2307) and `cmdSensorsBlindSpots` (lines 2309-2351) verbatim
- Exports: `{ cmdSensorsList, cmdSensorsBlindSpots }`

**backlog.cjs** (~330 lines):
- Requires: `fs`, `path`, `os`, `{ error, output, generateSlugInternal }` from `./core.cjs`, `frontmatter` from `./frontmatter.cjs`
- Helpers: Copy `resolveBacklogDir` (lines 351-357), `readBacklogItems` (lines 359-388), `regenerateBacklogIndex` (lines 631-675) verbatim
- Commands: Copy `cmdBacklogAdd` (lines 390-442), `cmdBacklogList` (lines 444-459), `cmdBacklogUpdate` (lines 461-513), `cmdBacklogStats` (lines 515-536), `cmdBacklogGroup` (lines 538-571), `cmdBacklogPromote` (lines 573-629), `cmdBacklogIndex` (lines 677-682) verbatim
- CRITICAL: In all copied functions, replace bare `extractFrontmatter(content)` calls with `frontmatter.extractFrontmatter(content)` and bare `reconstructFrontmatter(obj)` calls with `frontmatter.reconstructFrontmatter(obj)`. Research confirms 6 extractFrontmatter calls and 3 reconstructFrontmatter calls in backlog functions.
- Exports: `{ cmdBacklogAdd, cmdBacklogList, cmdBacklogUpdate, cmdBacklogStats, cmdBacklogGroup, cmdBacklogPromote, cmdBacklogIndex }`
- Do NOT export helpers (resolveBacklogDir, readBacklogItems, regenerateBacklogIndex are private)

**health-probe.cjs** (~467 lines):
- Requires: `fs`, `path`, `os`, `{ output, loadManifest }` from `./core.cjs`
- Helpers: Copy `resolveKBDir` (lines 1744-1754), `findLatestRegimeChange` (lines 1756-1804), `collectRegimeSignals` (lines 1806-1873) verbatim
- Commands: Copy `cmdHealthProbeSignalMetrics` (lines 1875-1982), `cmdHealthProbeSignalDensity` (lines 1984-2090), `cmdHealthProbeAutomationWatchdog` (lines 2092-2217) verbatim
- Exports: `{ cmdHealthProbeSignalMetrics, cmdHealthProbeSignalDensity, cmdHealthProbeAutomationWatchdog }`
- Do NOT export KB helpers (they are private)
  </action>
  <verify>
Verify each module loads without error:
```
node -e "const m = require('./get-shit-done/bin/lib/sensors.cjs'); console.log(Object.keys(m))"
node -e "const m = require('./get-shit-done/bin/lib/backlog.cjs'); console.log(Object.keys(m))"
node -e "const m = require('./get-shit-done/bin/lib/health-probe.cjs'); console.log(Object.keys(m))"
```
Each should print its exported function names without errors.
  </verify>
  <done>3 new modules exist in get-shit-done/bin/lib/, each loads cleanly with correct exports, function bodies are verbatim copies with only frontmatter import path changes in backlog.cjs</done>
</task>

<task type="auto">
  <name>Task 2: Rewire dispatcher, remove dead code, and verify all tests pass</name>
  <files>get-shit-done/bin/gsd-tools.cjs</files>
  <action>
Make 4 changes to gsd-tools.cjs:

**1. Add 3 new requires** after line 40 (after `const frontmatter = require('./lib/frontmatter.cjs');`):
```javascript
const sensors = require('./lib/sensors.cjs');
const backlog = require('./lib/backlog.cjs');
const healthProbe = require('./lib/health-probe.cjs');
```

**2. Rewire 12 dispatcher calls** in the CLI router:
- In case 'sensors' (around line 3168): Change `cmdSensorsList(` to `sensors.cmdSensorsList(` and `cmdSensorsBlindSpots(` to `sensors.cmdSensorsBlindSpots(` (2 calls)
- In case 'backlog' (around line 3053): Change all 7 bare `cmdBacklog*` calls to `backlog.cmdBacklog*` calls: cmdBacklogAdd, cmdBacklogList, cmdBacklogUpdate, cmdBacklogStats, cmdBacklogGroup, cmdBacklogPromote, cmdBacklogIndex (7 calls)
- In case 'health-probe' (around line 3181): Change `cmdHealthProbeSignalMetrics(` to `healthProbe.cmdHealthProbeSignalMetrics(`, `cmdHealthProbeSignalDensity(` to `healthProbe.cmdHealthProbeSignalDensity(`, `cmdHealthProbeAutomationWatchdog(` to `healthProbe.cmdHealthProbeAutomationWatchdog(` (3 calls)

**3. Remove extracted functions and dead code from gsd-tools.cjs:**
- Remove the 4 duplicate frontmatter helpers: extractFrontmatter (lines 44-117), reconstructFrontmatter (lines 119-181), spliceFrontmatter (lines 183-190), parseMustHavesBlock (lines 192-256)
- Remove backlog helpers + commands: resolveBacklogDir, readBacklogItems, cmdBacklogAdd, cmdBacklogList, cmdBacklogUpdate, cmdBacklogStats, cmdBacklogGroup, cmdBacklogPromote, regenerateBacklogIndex, cmdBacklogIndex (lines 351-682)
- Remove sensor commands: cmdSensorsList, cmdSensorsBlindSpots (lines 2219-2351)
- Remove KB helpers + health-probe commands: resolveKBDir, findLatestRegimeChange, collectRegimeSignals, cmdHealthProbeSignalMetrics, cmdHealthProbeSignalDensity, cmdHealthProbeAutomationWatchdog (lines 1744-2217)

**4. Update cmdForkFrontmatterValidate** (stays inline, around line 2387 before removal offsets):
Change `extractFrontmatter(content)` to `frontmatter.extractFrontmatter(content)`. The `frontmatter` module is already required at the top of the file. This enables safe removal of the duplicate extractFrontmatter function.

After all removals, gsd-tools.cjs should be approximately 1,800-1,900 lines (still contains manifest+automation inline functions, fork init overrides, fork command overrides, and the router).

Run `node bin/install.js --local` to sync to .claude/ install target. Then verify:
- `ls .claude/get-shit-done-reflect/bin/lib/ | wc -l` should show 14 files (11 upstream + 3 new fork modules)
- All test suites pass
  </action>
  <verify>
```
npm test
npm run test:upstream
npm run test:upstream:fork
node bin/install.js --local
ls .claude/get-shit-done-reflect/bin/lib/sensors.cjs .claude/get-shit-done-reflect/bin/lib/backlog.cjs .claude/get-shit-done-reflect/bin/lib/health-probe.cjs
```
All 534 tests pass (350 vitest + 174 upstream + 10 fork). Installer succeeds. 3 new modules present in install target.
  </verify>
  <done>Dispatcher routes sensors/backlog/health-probe through modules. Dead frontmatter helpers removed. cmdForkFrontmatterValidate uses frontmatter module. All 534 tests pass. Installer deploys 14 lib/ modules.</done>
</task>

</tasks>

<verification>
1. `npm test` -- 350 vitest tests pass
2. `npm run test:upstream` -- 174 upstream tests pass
3. `npm run test:upstream:fork` -- 10 fork tests pass
4. `node -e "require('./get-shit-done/bin/lib/sensors.cjs')"` -- no errors
5. `node -e "require('./get-shit-done/bin/lib/backlog.cjs')"` -- no errors
6. `node -e "require('./get-shit-done/bin/lib/health-probe.cjs')"` -- no errors
7. `grep -c "extractFrontmatter\|reconstructFrontmatter\|spliceFrontmatter\|parseMustHavesBlock" get-shit-done/bin/gsd-tools.cjs` -- only the frontmatter.extractFrontmatter call in cmdForkFrontmatterValidate remains (1 occurrence)
8. `node bin/install.js --local` succeeds
9. `ls .claude/get-shit-done-reflect/bin/lib/*.cjs | wc -l` -- 14 modules
</verification>

<success_criteria>
- sensors.cjs, backlog.cjs, health-probe.cjs exist in get-shit-done/bin/lib/ and are correctly required by gsd-tools.cjs
- All 12 dispatcher calls rewired (2 sensors + 7 backlog + 3 health-probe)
- 4 duplicate frontmatter helpers removed from gsd-tools.cjs
- cmdForkFrontmatterValidate uses frontmatter.extractFrontmatter(content) instead of bare extractFrontmatter(content)
- All 534 tests pass without modification
- Installer deploys all 14 lib/ modules
</success_criteria>

<output>
After completion, create `.planning/phases/47-fork-module-extraction/47-01-SUMMARY.md`
</output>
