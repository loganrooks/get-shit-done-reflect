---
phase: 47-fork-module-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/manifest.cjs
  - get-shit-done/bin/lib/automation.cjs
  - get-shit-done/bin/gsd-tools.cjs
autonomous: true
must_haves:
  truths:
    - "All 6 manifest subcommands produce identical CLI output before and after extraction"
    - "All 7 automation subcommands produce identical CLI output before and after extraction"
    - "FEATURE_CAPABILITY_MAP is exported from automation.cjs per MOD-07"
    - "validateFieldType and validateFieldEnum live in manifest.cjs as private helpers"
    - "gsd-tools.cjs is approximately 1,237 lines after both plans complete"
  artifacts:
    - path: "get-shit-done/bin/lib/manifest.cjs"
      provides: "6 manifest commands + 5 helpers/constants"
      exports: ["cmdManifestDiffConfig", "cmdManifestValidate", "cmdManifestGetPrompts", "cmdManifestApplyMigration", "cmdManifestLogMigration", "cmdManifestAutoDetect"]
    - path: "get-shit-done/bin/lib/automation.cjs"
      provides: "7 automation commands + FEATURE_CAPABILITY_MAP"
      exports: ["cmdAutomationResolveLevel", "cmdAutomationTrackEvent", "cmdAutomationLock", "cmdAutomationUnlock", "cmdAutomationCheckLock", "cmdAutomationRegimeChange", "cmdAutomationReflectionCounter", "FEATURE_CAPABILITY_MAP"]
  key_links:
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/manifest.cjs"
      via: "require('./lib/manifest.cjs') + dispatcher case 'manifest'"
      pattern: "manifest\\.cmdManifest"
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/automation.cjs"
      via: "require('./lib/automation.cjs') + dispatcher case 'automation'"
      pattern: "automation\\.cmdAutomation"
    - from: "get-shit-done/bin/lib/manifest.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "require('./core.cjs') for loadManifest, loadProjectConfig, atomicWriteJson"
      pattern: "require\\('./core\\.cjs'\\)"
---

<objective>
Extract manifest.cjs and automation.cjs from gsd-tools.cjs inline functions into dedicated lib/ modules, including their shared constants section and private helpers.

Purpose: Complete the Phase 47 fork module extraction by moving the final 2 command groups (manifest and automation) into dedicated modules. After this plan, gsd-tools.cjs retains only the requires block, fork init overrides, fork command overrides, and the CLI router (~1,237 lines, down from 3,200).

Output: 2 new modules in get-shit-done/bin/lib/, fully updated dispatcher, gsd-tools.cjs at final target line count.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-fork-module-extraction/47-CONTEXT.md
@.planning/phases/47-fork-module-extraction/47-RESEARCH.md
@get-shit-done/bin/gsd-tools.cjs
@get-shit-done/bin/lib/core.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create manifest.cjs and automation.cjs modules</name>
  <files>
    get-shit-done/bin/lib/manifest.cjs
    get-shit-done/bin/lib/automation.cjs
  </files>
  <action>
Create 2 new CJS modules in get-shit-done/bin/lib/. Copy function bodies VERBATIM from gsd-tools.cjs -- do not rename parameters, change logic, or "improve" code. Follow the upstream module convention: docstring, requires, constants, helpers, commands, exports.

IMPORTANT: Always edit get-shit-done/bin/lib/ (npm source), NEVER .claude/get-shit-done-reflect/bin/lib/ (install target).

**manifest.cjs** (~410 lines):
- Requires: `fs`, `path`, `{ execSync }` from `child_process`, `{ error, output, loadManifest, loadProjectConfig, atomicWriteJson }` from `./core.cjs`
- Constants: Copy `KNOWN_TOP_LEVEL_KEYS` (lines 276-281) verbatim
- Private helpers: Copy these verbatim as module-private functions (NOT exported):
  - `validateFieldType` (lines 258-267) -- RESEARCH CORRECTION: CONTEXT.md said these stay inline, but code analysis proved they are used ONLY by manifest commands (cmdManifestDiffConfig lines 1069/1077, cmdManifestValidate lines 1130/1138), NOT by cmdForkFrontmatterValidate
  - `validateFieldEnum` (lines 269-272)
  - `coerceValue` (lines 304-326)
  - `formatMigrationEntry` (lines 328-349)
- Commands: Copy verbatim:
  - `cmdManifestDiffConfig` (lines 1026-1099)
  - `cmdManifestValidate` (lines 1101-1171)
  - `cmdManifestGetPrompts` (lines 1173-1185)
  - `cmdManifestApplyMigration` (lines 1187-1255)
  - `cmdManifestLogMigration` (lines 1257-1304)
  - `cmdManifestAutoDetect` (lines 1306-1359)
- Exports: `{ cmdManifestDiffConfig, cmdManifestValidate, cmdManifestGetPrompts, cmdManifestApplyMigration, cmdManifestLogMigration, cmdManifestAutoDetect }`
- Do NOT export private helpers or KNOWN_TOP_LEVEL_KEYS (module-internal only)

**automation.cjs** (~402 lines):
- Requires: `fs`, `path`, `os`, `{ execSync }` from `child_process`, `{ error, output, loadProjectConfig }` from `./core.cjs`
- Constants: Copy `FEATURE_CAPABILITY_MAP` (lines 283-302) verbatim
- Commands: Copy verbatim:
  - `cmdAutomationResolveLevel` (lines 1361-1457)
  - `cmdAutomationTrackEvent` (lines 1459-1505)
  - `cmdAutomationLock` (lines 1507-1553)
  - `cmdAutomationUnlock` (lines 1555-1569)
  - `cmdAutomationCheckLock` (lines 1571-1602)
  - `cmdAutomationRegimeChange` (lines 1604-1688)
  - `cmdAutomationReflectionCounter` (lines 1690-1742)
- Exports: `{ cmdAutomationResolveLevel, cmdAutomationTrackEvent, cmdAutomationLock, cmdAutomationUnlock, cmdAutomationCheckLock, cmdAutomationRegimeChange, cmdAutomationReflectionCounter, FEATURE_CAPABILITY_MAP }`
- FEATURE_CAPABILITY_MAP IS exported per MOD-07 requirement (enables Phase 53 deep integration consumers)
  </action>
  <verify>
Verify each module loads without error:
```
node -e "const m = require('./get-shit-done/bin/lib/manifest.cjs'); console.log(Object.keys(m))"
node -e "const m = require('./get-shit-done/bin/lib/automation.cjs'); console.log(Object.keys(m)); console.log('FEATURE_CAPABILITY_MAP keys:', Object.keys(m.FEATURE_CAPABILITY_MAP))"
```
manifest.cjs should print its 6 command exports. automation.cjs should print its 7 command exports + FEATURE_CAPABILITY_MAP with its feature keys.
  </verify>
  <done>2 new modules exist in get-shit-done/bin/lib/, each loads cleanly with correct exports, function bodies are verbatim copies, FEATURE_CAPABILITY_MAP is exported from automation.cjs</done>
</task>

<task type="auto">
  <name>Task 2: Rewire dispatcher, remove extracted code, run final verification</name>
  <files>get-shit-done/bin/gsd-tools.cjs</files>
  <action>
Make 3 changes to gsd-tools.cjs:

**1. Add 2 new requires** (after the sensors/backlog/healthProbe requires added by Plan 01, or after line 40 if Plan 01 has not yet run):
```javascript
const manifest = require('./lib/manifest.cjs');
const automation = require('./lib/automation.cjs');
```

**2. Rewire 13 dispatcher calls** in the CLI router:
- In case 'manifest' (around line 3032 before removal offsets): Change all 6 bare `cmdManifest*` calls to `manifest.cmdManifest*`: cmdManifestDiffConfig, cmdManifestValidate, cmdManifestGetPrompts, cmdManifestApplyMigration, cmdManifestLogMigration, cmdManifestAutoDetect (6 calls)
- In case 'automation' (around line 3115 before removal offsets): Change all 7 bare `cmdAutomation*` calls to `automation.cmdAutomation*`: cmdAutomationResolveLevel, cmdAutomationTrackEvent, cmdAutomationLock, cmdAutomationUnlock, cmdAutomationCheckLock, cmdAutomationRegimeChange, cmdAutomationReflectionCounter (7 calls)

**3. Remove extracted functions and constants from gsd-tools.cjs:**
- Remove `validateFieldType` and `validateFieldEnum` (lines 258-272) -- moved to manifest.cjs
- Remove `KNOWN_TOP_LEVEL_KEYS` (lines 276-281) -- moved to manifest.cjs
- Remove `FEATURE_CAPABILITY_MAP` (lines 283-302) -- moved to automation.cjs
- Remove `coerceValue` and `formatMigrationEntry` (lines 304-349) -- moved to manifest.cjs
- Remove all 6 manifest command functions (lines 1026-1359) -- moved to manifest.cjs
- Remove all 7 automation command functions (lines 1361-1742) -- moved to automation.cjs

After all removals across BOTH plans, gsd-tools.cjs should be approximately 1,237 lines. Verify with `wc -l`. If significantly different, investigate what was missed or retained incorrectly.

Run `node bin/install.js --local` to sync to .claude/ install target. Then verify:
- `ls .claude/get-shit-done-reflect/bin/lib/ | wc -l` should show 16 files (11 upstream + 5 fork)
- All test suites pass
- No circular dependency warnings
  </action>
  <verify>
```
npm test
npm run test:upstream
npm run test:upstream:fork
node bin/install.js --local
ls .claude/get-shit-done-reflect/bin/lib/*.cjs | wc -l
wc -l get-shit-done/bin/gsd-tools.cjs
```
All 534 tests pass (350 vitest + 174 upstream + 10 fork). Installer succeeds. 16 lib/ modules present. gsd-tools.cjs is approximately 1,237 lines.
  </verify>
  <done>Dispatcher routes manifest/automation through modules. All extracted code removed from gsd-tools.cjs. All 534 tests pass. Installer deploys 16 lib/ modules. gsd-tools.cjs is ~1,237 lines (down from 3,200).</done>
</task>

</tasks>

<verification>
1. `npm test` -- 350 vitest tests pass
2. `npm run test:upstream` -- 174 upstream tests pass
3. `npm run test:upstream:fork` -- 10 fork tests pass
4. `node -e "require('./get-shit-done/bin/lib/manifest.cjs')"` -- no errors
5. `node -e "require('./get-shit-done/bin/lib/automation.cjs')"` -- no errors
6. `wc -l get-shit-done/bin/gsd-tools.cjs` -- approximately 1,237 lines
7. `ls .claude/get-shit-done-reflect/bin/lib/*.cjs | wc -l` -- 16 modules (11 upstream + 5 fork)
8. No inline manifest/automation/sensor/backlog/health-probe command functions remain in gsd-tools.cjs (only fork init overrides + fork command overrides + router)
9. `grep "FEATURE_CAPABILITY_MAP" get-shit-done/bin/lib/automation.cjs` -- exported
</verification>

<success_criteria>
- manifest.cjs and automation.cjs exist in get-shit-done/bin/lib/ and are correctly required by gsd-tools.cjs
- All 13 dispatcher calls rewired (6 manifest + 7 automation)
- validateFieldType, validateFieldEnum, KNOWN_TOP_LEVEL_KEYS, FEATURE_CAPABILITY_MAP, coerceValue, formatMigrationEntry all removed from gsd-tools.cjs
- All 534 tests pass without modification
- Installer deploys all 16 lib/ modules (11 upstream + 5 fork)
- gsd-tools.cjs is approximately 1,237 lines
- FEATURE_CAPABILITY_MAP is exported from automation.cjs per MOD-07
</success_criteria>

<output>
After completion, create `.planning/phases/47-fork-module-extraction/47-02-SUMMARY.md`
</output>
