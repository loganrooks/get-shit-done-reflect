---
phase: 52-feature-adoption
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/discuss-phase.md
  - get-shit-done/workflows/quick.md
autonomous: true

must_haves:
  truths:
    - "Discuss-phase workflow has codebase scouting (scout_codebase step) and produces <code_context> in CONTEXT.md"
    - "Discuss-phase workflow has prior context loading, cross-reference todos, and enhanced write_context template"
    - "Quick.md workflow has --discuss flag that enables lightweight discussion before planning"
    - "Fork's steering brief model (Working Model, Derived Constraints, Open Questions, Epistemic Guardrails) is NOT lost -- it lives in commands/gsdr/discuss-phase.md, not in the workflow file"
  artifacts:
    - path: "get-shit-done/workflows/discuss-phase.md"
      provides: "Code-aware discuss-phase with codebase scouting"
      contains: "scout_codebase"
    - path: "get-shit-done/workflows/quick.md"
      provides: "Quick workflow with --discuss flag"
      contains: "--discuss"
  key_links:
    - from: "get-shit-done/workflows/discuss-phase.md"
      to: "CONTEXT.md output"
      via: "write_context step produces code_context section"
      pattern: "code_context"
    - from: "get-shit-done/workflows/quick.md"
      to: "get-shit-done/workflows/discuss-phase.md"
      via: "--discuss flag chains to discuss-phase workflow"
      pattern: "discuss"
---

<objective>
Wholesale-replace the fork's discuss-phase.md with upstream's code-aware version (+641 lines, codebase scouting), and wholesale-replace quick.md with upstream's version (+406 lines, --discuss flag).

Purpose: Covers ADT-06 (code-aware discuss-phase) and ADT-10 (quick.md --discuss flag). Research confirmed wholesale replace is safe for both files: the fork's steering brief model lives in the command layer (commands/gsdr/discuss-phase.md), not in the workflow file. The upstream versions add purely additive functionality.
Output: Two workflow files with upstream's enhanced capabilities.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-feature-adoption/52-RESEARCH.md
@get-shit-done/workflows/discuss-phase.md
@get-shit-done/workflows/quick.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wholesale-replace discuss-phase.md with upstream's code-aware version (ADT-06)</name>
  <files>get-shit-done/workflows/discuss-phase.md</files>
  <action>
Replace the fork's discuss-phase.md (408 lines) with upstream's version (1049 lines):
```bash
git show upstream/main:get-shit-done/workflows/discuss-phase.md > get-shit-done/workflows/discuss-phase.md
```

The upstream version adds 6 new execution steps:
- `load_prior_context`: reads PROJECT.md, REQUIREMENTS.md, prior CONTEXT.md files
- `cross_reference_todos`: matches pending todos to phase scope
- `scout_codebase`: lightweight code scan for reusable assets (produces `<code_context>`)
- `advisor_research`: spawns parallel research agents per gray area
- `auto_advance`: chains discuss -> plan -> execute in auto mode
- `update_state`: records session in STATE.md

Also adds: batch mode (--batch), analyze mode (--analyze), text mode (--text), auto mode (--auto), discussion log generation, answer validation with retry logic, enhanced CONTEXT.md template with `<canonical_refs>`, `<code_context>`, and `<folded_todos>`.

**Critical verification after replace (DC-4):**
The fork's steering brief model (Working Model & Assumptions, Derived Constraints, Open Questions, Epistemic Guardrails) is NOT part of this workflow file. It lives in the fork's custom command layer. Verify:
1. The upstream workflow's `write_context` template does NOT conflict with the fork's command additions
2. The upstream workflow uses standard CONTEXT.md sections (`<domain>`, `<decisions>`, `<specifics>`, `<deferred>`) that the fork's command layer extends

**Source convention check:**
The upstream file uses `~/.claude/get-shit-done/` paths and `gsd:` prefixes, which is correct for source (installer handles rewriting). Verify no `gsdr-` references in the file.

**Post-replace: check fork's command layer is intact:**
```bash
# The fork's steering brief is in the command file, not the workflow
# This command file should NOT be modified by this task
grep -c "Epistemic Guardrails\|Derived Constraints\|Working Model\|Open Questions" commands/gsdr/discuss-phase.md 2>/dev/null || echo "No gsdr command file -- check commands/gsd/discuss-phase.md"
grep -c "Epistemic Guardrails\|Derived Constraints\|Working Model\|Open Questions" commands/gsd/discuss-phase.md 2>/dev/null || echo "Steering brief sections in source command"
```
The command file (where the steering brief lives) is NOT touched by this task.
  </action>
  <verify>
File replaced: `wc -l get-shit-done/workflows/discuss-phase.md` returns ~1049 lines (not 408).
Codebase scouting: `grep "scout_codebase" get-shit-done/workflows/discuss-phase.md` returns match.
Code context output: `grep "code_context" get-shit-done/workflows/discuss-phase.md` returns match.
Prior context loading: `grep "load_prior_context" get-shit-done/workflows/discuss-phase.md` returns match.
Source convention: `grep "gsdr-" get-shit-done/workflows/discuss-phase.md` returns nothing.
Steering brief safe: The commands/gsd/discuss-phase.md file is unchanged (git status shows no modification).
  </verify>
  <done>
Discuss-phase workflow replaced with upstream's 1049-line version including codebase scouting, code_context output, prior context loading, batch/analyze/text/auto modes, and enhanced CONTEXT.md template. Fork's steering brief model is preserved in the command layer (untouched).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wholesale-replace quick.md with upstream's --discuss version (ADT-10)</name>
  <files>get-shit-done/workflows/quick.md</files>
  <action>
Replace the fork's quick.md (351 lines) with upstream's version (757 lines):
```bash
git show upstream/main:get-shit-done/workflows/quick.md > get-shit-done/workflows/quick.md
```

The upstream version adds:
- `--discuss` flag: lightweight discussion phase before planning (surfaces assumptions, clarifies gray areas, captures decisions in CONTEXT.md)
- Composable flags: `--discuss --research --full` gives discussion + research + plan-checking + verification
- Enhanced flow control based on flag combinations
- Discussion-derived decision injection into CONTEXT.md template

**Source convention check:**
Verify the replaced file uses `~/.claude/get-shit-done/` paths and `gsd:` prefixes (source convention). No `gsdr-` references should be present.

**Compatibility check:**
The fork's quick.md command stub (`commands/gsd/quick.md`) references the workflow. Verify the upstream quick.md workflow is compatible with the fork's existing command structure. The command stub typically just calls the workflow, so compatibility should be automatic.

Note: The upstream quick.md comes from post-C2 (shell robustness) and post-C4 (worktree isolation), so it already has `|| true` guards and `isolation="worktree"` where needed. No additional patching required.
  </action>
  <verify>
File replaced: `wc -l get-shit-done/workflows/quick.md` returns ~757 lines (not 351).
Discuss flag: `grep "\-\-discuss" get-shit-done/workflows/quick.md` returns match.
Composable flags: `grep "DISCUSS_MODE" get-shit-done/workflows/quick.md` returns match.
Source convention: `grep "gsdr-" get-shit-done/workflows/quick.md` returns nothing.
Worktree isolation included: `grep "isolation.*worktree" get-shit-done/workflows/quick.md` returns match (C4 already applied upstream).
  </verify>
  <done>
Quick.md workflow replaced with upstream's 757-line version including --discuss flag, composable flag system, and discussion-derived decision injection. Includes C2 shell robustness and C4 worktree isolation from upstream.
  </done>
</task>

</tasks>

<verification>
- discuss-phase.md is ~1049 lines with codebase scouting and code_context output
- quick.md is ~757 lines with --discuss flag and composable flag system
- Fork's steering brief model preserved in command layer (commands/gsd/discuss-phase.md unchanged)
- Both files use source-convention paths (gsd- prefix, ~/.claude/get-shit-done/)
- No gsdr- references in either file
</verification>

<success_criteria>
- ADT-06: discuss-phase.md has scout_codebase step and produces code_context in CONTEXT.md
- ADT-10: quick.md has --discuss flag with composable flag system
- DC-4: fork's steering brief model in command layer is untouched
- Both files ready for installer's replacePathsInContent() at install time
</success_criteria>

<output>
After completion, create `.planning/phases/52-feature-adoption/52-03-SUMMARY.md`
</output>
