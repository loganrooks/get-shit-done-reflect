---
phase: 52-feature-adoption
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-nyquist-auditor.md
  - agents/gsd-integration-checker.md
  - get-shit-done/workflows/add-tests.md
  - get-shit-done/workflows/cleanup.md
  - get-shit-done/workflows/health.md
  - get-shit-done/workflows/validate-phase.md
autonomous: true

must_haves:
  truths:
    - "Nyquist auditor agent exists in source with agent-protocol.md reference"
    - "Integration-checker agent is updated to upstream's latest version with agent-protocol.md preserved"
    - "All 4 new workflows exist in source and use standard upstream structure"
    - "All adopted files use gsd- prefix in source (installer handles rewriting to gsdr-)"
  artifacts:
    - path: "agents/gsd-nyquist-auditor.md"
      provides: "Nyquist validation gap auditor agent"
      contains: "agent-protocol.md"
    - path: "agents/gsd-integration-checker.md"
      provides: "Updated integration checker agent"
      contains: "agent-protocol.md"
    - path: "get-shit-done/workflows/add-tests.md"
      provides: "Test generation workflow for completed phases"
      contains: "<purpose>"
    - path: "get-shit-done/workflows/cleanup.md"
      provides: "Phase directory archival workflow"
      contains: "<purpose>"
    - path: "get-shit-done/workflows/health.md"
      provides: "Planning directory health check workflow"
      contains: "<purpose>"
    - path: "get-shit-done/workflows/validate-phase.md"
      provides: "Nyquist validation gap audit workflow"
      contains: "<purpose>"
  key_links:
    - from: "agents/gsd-nyquist-auditor.md"
      to: "get-shit-done/workflows/validate-phase.md"
      via: "Agent spawned by validate-phase workflow"
      pattern: "gsd-nyquist-auditor"
    - from: "agents/gsd-integration-checker.md"
      to: "get-shit-done/references/agent-protocol.md"
      via: "Required reading reference"
      pattern: "agent-protocol.md"
---

<objective>
Adopt the Nyquist auditor agent, update the integration-checker agent, and copy 4 new upstream workflows (add-tests, cleanup, health, validate-phase) to fork source.

Purpose: Covers ADT-05 (nyquist auditor), ADT-07 (4 workflows), ADT-08 (integration-checker update). All follow the copy-and-namespace-rewrite pattern -- files are copied to source with gsd- prefix and the installer handles rewriting to gsdr- at install time.
Output: 6 source files ready for installer registration (Plan 05 handles CODEX_AGENT_SANDBOX registration).
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-feature-adoption/52-RESEARCH.md
@agents/gsd-integration-checker.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt Nyquist auditor agent and update integration-checker (ADT-05, ADT-08)</name>
  <files>agents/gsd-nyquist-auditor.md, agents/gsd-integration-checker.md</files>
  <action>
**Nyquist auditor (ADT-05) -- net new file:**

Copy from upstream:
```bash
git show upstream/main:agents/gsd-nyquist-auditor.md > agents/gsd-nyquist-auditor.md
```

Then verify and add agent-protocol.md reference if missing (DC-7). Research confirms upstream's version does NOT have the agent-protocol reference (grep returned 0). Add the reference in the `<required_reading>` section (or create one if absent):
```markdown
<required_reading>
@~/.claude/get-shit-done/references/agent-protocol.md
</required_reading>
```
Note: The path uses `~/.claude/get-shit-done/` (source convention). The installer rewrites to `./.claude/get-shit-done-reflect/` at install time.

**Integration-checker (ADT-08) -- update existing file:**

The fork has 427 lines, upstream has 443 lines (+16 delta). Compare and apply upstream additions:
```bash
diff <(cat agents/gsd-integration-checker.md) <(git show upstream/main:agents/gsd-integration-checker.md)
```

Key upstream additions per research:
- CRITICAL mandatory initial read block
- Requirements Integration Map requirement

Apply the upstream additions while preserving the fork's existing agent-protocol.md reference (currently at line 399). If the diff is clean (no conflicting sections), wholesale-replace with upstream version and then re-add the agent-protocol reference.

For both agents, verify:
- `grep -c "agent-protocol.md" agents/gsd-nyquist-auditor.md` returns >= 1
- `grep -c "agent-protocol.md" agents/gsd-integration-checker.md` returns >= 1
  </action>
  <verify>
Files exist: `test -f agents/gsd-nyquist-auditor.md && test -f agents/gsd-integration-checker.md && echo "OK"`
Agent-protocol refs: `grep -l "agent-protocol" agents/gsd-nyquist-auditor.md agents/gsd-integration-checker.md` returns both files.
Nyquist agent content: `grep "Nyquist\|nyquist\|validation" agents/gsd-nyquist-auditor.md | head -3` returns meaningful matches.
No gsdr- in source: `grep "gsdr-" agents/gsd-nyquist-auditor.md | grep -v "gsd-tools"` returns nothing (source uses gsd- prefix).
  </verify>
  <done>
Nyquist auditor agent exists at agents/gsd-nyquist-auditor.md with agent-protocol.md reference. Integration-checker updated to upstream's latest version with agent-protocol reference preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Copy 4 upstream workflows (ADT-07)</name>
  <files>get-shit-done/workflows/add-tests.md, get-shit-done/workflows/cleanup.md, get-shit-done/workflows/health.md, get-shit-done/workflows/validate-phase.md</files>
  <action>
Copy all 4 workflows from upstream:
```bash
git show upstream/main:get-shit-done/workflows/add-tests.md > get-shit-done/workflows/add-tests.md
git show upstream/main:get-shit-done/workflows/cleanup.md > get-shit-done/workflows/cleanup.md
git show upstream/main:get-shit-done/workflows/health.md > get-shit-done/workflows/health.md
git show upstream/main:get-shit-done/workflows/validate-phase.md > get-shit-done/workflows/validate-phase.md
```

These are net-new files in the fork (none currently exist in the fork's workflows directory). They use `~/.claude/get-shit-done/` paths and `gsd:` command prefixes in source, which the installer's `replacePathsInContent()` handles at install time.

After copying, verify each file:
1. Has a `<purpose>` section (standard workflow structure)
2. Uses `~/.claude/get-shit-done/` paths (NOT `~/.claude/get-shit-done-reflect/`)
3. Uses `/gsd:` command prefixes (NOT `/gsdr:`)
4. Has no `gsdr-` references (source convention: gsd- only)

These workflows reference the gsd-nyquist-auditor agent (validate-phase.md) and other agents. Those references will be namespace-rewritten by the installer.

Note: These files come from upstream post-C2 (shell robustness), so they already have `|| true` guards where needed. No additional C2 patching required for these files.
  </action>
  <verify>
All 4 files exist:
```bash
for f in add-tests.md cleanup.md health.md validate-phase.md; do test -f "get-shit-done/workflows/$f" && echo "OK: $f" || echo "MISSING: $f"; done
```
Each has purpose section: `grep -l "<purpose>" get-shit-done/workflows/{add-tests,cleanup,health,validate-phase}.md` returns all 4.
Source convention preserved: `grep -l "gsdr-" get-shit-done/workflows/{add-tests,cleanup,health,validate-phase}.md` returns nothing.
  </verify>
  <done>
All 4 workflows (add-tests.md, cleanup.md, health.md, validate-phase.md) exist in get-shit-done/workflows/ with correct source-convention paths. Each has a purpose section and uses gsd- prefix (installer handles rewriting).
  </done>
</task>

</tasks>

<verification>
- 2 agent files and 4 workflow files exist in source
- All files use gsd- prefix and source-convention paths
- Agent-protocol.md referenced in both agents (DC-7)
- All 4 workflows have standard structure (<purpose> sections)
- validate-phase.md references gsd-nyquist-auditor agent
</verification>

<success_criteria>
- ADT-05: agents/gsd-nyquist-auditor.md exists with agent-protocol ref
- ADT-07: all 4 workflows exist in get-shit-done/workflows/
- ADT-08: agents/gsd-integration-checker.md updated to upstream's latest with agent-protocol ref preserved
- All 6 files follow source-convention paths (gsd- prefix, ~/.claude/get-shit-done/)
</success_criteria>

<output>
After completion, create `.planning/phases/52-feature-adoption/52-02-SUMMARY.md`
</output>
