---
phase: 49-config-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/plan-phase.md
  - get-shit-done/workflows/reflect.md
  - get-shit-done/workflows/new-project.md
  - get-shit-done/workflows/run-spike.md
  - get-shit-done/references/spike-integration.md
  - get-shit-done/references/milestone-reflection.md
  - get-shit-done/references/reflection-patterns.md
  - get-shit-done/references/spike-execution.md
  - get-shit-done/references/health-probes/config-validity.md
  - get-shit-done/references/version-migration.md
  - get-shit-done/templates/roadmap.md
autonomous: true

must_haves:
  truths:
    - "No workflow or reference file references depth as a config field name (discovery-phase.md excluded -- it uses depth as workflow parameter)"
    - "Shell grep commands in reflect.md search for granularity with coarse|standard|fine enum values"
    - "Spike sensitivity derivation references granularity: coarse/standard/fine instead of depth: quick/standard/comprehensive"
    - "Config template in new-project.md shows granularity instead of depth"
    - "Roadmap template references granularity setting with coarse/standard/fine enum values"
    - "Health probe config-validity.md checks granularity field with correct enum values"
    - "version-migration.md documents the controlled-exception mechanism for field renames"
  artifacts:
    - path: "get-shit-done/workflows/reflect.md"
      provides: "Shell grep updated to search for granularity"
      contains: "granularity"
    - path: "get-shit-done/workflows/plan-phase.md"
      provides: "Spike sensitivity derived from config.granularity"
      contains: "config.granularity"
    - path: "get-shit-done/references/version-migration.md"
      provides: "Controlled-exception mechanism for field renames documented"
      contains: "rename"
    - path: "get-shit-done/templates/roadmap.md"
      provides: "Roadmap template uses granularity terminology"
      contains: "granularity"
  key_links:
    - from: "get-shit-done/workflows/reflect.md"
      to: ".planning/config.json"
      via: "Shell grep for granularity field"
      pattern: "granularity"
    - from: "get-shit-done/references/health-probes/config-validity.md"
      to: ".planning/config.json"
      via: "Shell validation of granularity enum"
      pattern: "coarse.*standard.*fine"
---

<objective>
Update all workflow and reference files that reference `depth` as a config field to use `granularity` with the new enum values, and document the controlled-exception mechanism in version-migration.md.

Purpose: CFG-04 (workflow file updates), CFG-07 (version-migration.md controlled-exception documentation). After this plan, zero workflow/reference files will reference `depth` as a config key.
Output: 9 updated markdown files + version-migration.md with rename migration documentation.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-config-migration/49-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update workflow files from depth to granularity terminology</name>
  <files>get-shit-done/workflows/plan-phase.md, get-shit-done/workflows/reflect.md, get-shit-done/workflows/new-project.md, get-shit-done/workflows/run-spike.md, get-shit-done/templates/roadmap.md</files>
  <action>
**IMPORTANT: Do NOT modify `get-shit-done/workflows/discovery-phase.md`.** That file uses "depth" as a workflow execution parameter (depth=verify, depth=standard, depth=deep), NOT as a config field reference.

Also do NOT modify `get-shit-done/workflows/discuss-phase.md` -- its references to "depth" are about content depth/tone, not the config field.

1. **plan-phase.md** (lines 179-182): Update spike sensitivity derivation:
   - Change `config.depth` to `config.granularity`
   - Change the mapping from:
     ```
     depth: quick       -> conservative
     depth: standard    -> balanced
     depth: comprehensive -> aggressive
     ```
     To:
     ```
     granularity: coarse    -> conservative
     granularity: standard  -> balanced
     granularity: fine      -> aggressive
     ```

2. **reflect.md**:
   - Line 20: Change "Read config.json for mode and depth settings" to "Read config.json for mode and granularity settings"
   - Line 90: Change the shell grep from:
     ```bash
     DEPTH=$(cat .planning/config.json 2>/dev/null | grep -o '"depth"[[:space:]]*:[[:space:]]*"[^"]*"' | grep -o '"[^"]*"$' | tr -d '"' || echo "standard")
     ```
     To:
     ```bash
     GRANULARITY=$(cat .planning/config.json 2>/dev/null | grep -o '"granularity"[[:space:]]*:[[:space:]]*"[^"]*"' | grep -o '"[^"]*"$' | tr -d '"' || echo "standard")
     ```
   - Lines 100-106: Update the enum documentation:
     ```
     - `granularity: coarse` - Check only current project, skip drift check
     - `granularity: standard` - Current project with drift check
     - `granularity: fine` - Cross-project with full drift analysis
     ```
     And update line 106 to use `granularity` instead of `depth`. Update all subsequent references to `DEPTH` variable to `GRANULARITY` in this file.

3. **new-project.md**:
   - Line 323: Change `"depth": "quick|standard|comprehensive"` to `"granularity": "coarse|standard|fine"`
   - Line 979: Change "config.json has workflow mode, depth, parallelization" to "config.json has workflow mode, granularity, parallelization"

4. **run-spike.md**:
   - Line 20: Change `derived from depth` to `derived from granularity`

5. **templates/roadmap.md** (CFG-04):
   - Line 108: Change `"- Phase count depends on depth setting (quick: 3-5, standard: 5-8, comprehensive: 8-12)"` to `"- Phase count depends on granularity setting (coarse: 3-5, standard: 5-8, fine: 8-12)"`
   - Check for any other `depth` config-field references in the template and update to `granularity`.
  </action>
  <verify>
Verify no remaining config-field depth references in workflows (excluding discovery-phase.md and discuss-phase.md):
```bash
grep -rn '"depth"' get-shit-done/workflows/ get-shit-done/templates/ --include="*.md" | grep -v discovery-phase | grep -v discuss-phase
grep -rn 'config\.depth' get-shit-done/workflows/ get-shit-done/templates/ --include="*.md"
grep -rn 'depth:.*quick\|depth:.*comprehensive' get-shit-done/workflows/ get-shit-done/templates/ --include="*.md"
```
All three should return empty.
  </verify>
  <done>
All 4 workflow files and roadmap template updated. Zero references to `depth` as a config field remain in workflows or templates (discovery-phase.md and discuss-phase.md correctly unchanged). Shell grep in reflect.md searches for `granularity`. Spike sensitivity derivation in plan-phase.md uses `granularity: coarse/standard/fine`. Roadmap template uses `granularity setting (coarse: 3-5, standard: 5-8, fine: 8-12)`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update reference files and document controlled-exception mechanism in version-migration.md</name>
  <files>get-shit-done/references/spike-integration.md, get-shit-done/references/milestone-reflection.md, get-shit-done/references/reflection-patterns.md, get-shit-done/references/spike-execution.md, get-shit-done/references/health-probes/config-validity.md, get-shit-done/references/version-migration.md</files>
  <action>
1. **spike-integration.md**:
   - Line 118: Change `config.depth` to `config.granularity`
   - Lines 207-210: Update derivation mapping:
     ```
     granularity: coarse -> spike.sensitivity: conservative
     granularity: standard -> spike.sensitivity: balanced
     granularity: fine -> spike.sensitivity: aggressive
     ```

2. **milestone-reflection.md**:
   - Line 169: Change `"depth": "quick"` to `"granularity": "coarse"`
   - Line 176: Change `"depth": "comprehensive"` to `"granularity": "fine"`
   - Line 192: Change `depth=comprehensive` to `granularity=fine`

3. **reflection-patterns.md**:
   - Line 692: Change `depth` to `granularity` in the config field list

4. **spike-execution.md**:
   - Lines 229-232: Update derivation:
     ```
     **Derivation from granularity (default):**
     - `granularity: coarse` -> `spike_sensitivity: conservative`
     - `granularity: standard` -> `spike_sensitivity: balanced`
     - `granularity: fine` -> `spike_sensitivity: aggressive`
     ```
   - Line 237: Change `"depth": "standard"` to `"granularity": "standard"`

5. **health-probes/config-validity.md**:
   - Line 37: Change `for field in mode depth; do` to `for field in mode granularity; do`
   - Line 46: Change the node command from reading `c.depth` to `c.granularity`
   - Line 48: Change from checking `depth=$DEPTH` with `quick|standard|comprehensive` to checking `granularity=$GRANULARITY` with `coarse|standard|fine`. Update the variable name from `DEPTH` to `GRANULARITY` throughout.

6. **version-migration.md** (CFG-07): Add a new section after "### Migration Actions" (around line 63) titled "### Controlled Exception: Field Renames":
   ```markdown
   ### Controlled Exception: Field Renames

   While migrations are additive-only by default, **field renames** are a controlled exception to this principle. A rename simultaneously adds a new field and removes the old one, which appears to violate "nothing is removed." This is permitted under strict conditions:

   **Conditions for a field rename migration:**
   1. The rename is declared in `feature-manifest.json` under the `migrations[]` array
   2. The entry specifies `type: "rename_field"`, the old key (`from`), the new key (`to`), and an optional `value_map` for enum changes
   3. The migration is idempotent: if the old key is absent, no action is taken; if both keys exist, the new key wins and the old is removed
   4. The `version` field records when the rename was introduced for audit

   **Example: depth to granularity (v1.15.0)**

   The `depth` config field was renamed to `granularity` with enum value changes (`quick` -> `coarse`, `comprehensive` -> `fine`). This is expressed as:

   ```json
   {
     "type": "rename_field",
     "version": "1.15.0",
     "from": "depth",
     "to": "granularity",
     "scope": "top_level",
     "value_map": {
       "quick": "coarse",
       "standard": "standard",
       "comprehensive": "fine"
     }
   }
   ```

   **Runtime safety net:** In addition to the manifest migration, `loadConfig()` in `core.cjs` and `cmdConfigEnsureSection()` in `config.cjs` perform the same rename inline as a belt-and-suspenders fallback for configs that have not yet been explicitly migrated.

   **Why both mechanisms coexist:** The manifest migration is the authoritative, auditable path. The inline runtime migration ensures that even if `apply-migration` has not run (e.g., fresh clone without upgrade step), the config is usable. Neither mechanism alone is sufficient.
   ```
  </action>
  <verify>
Verify no remaining config-field depth references in references (excluding knowledge-surfacing.md which uses "depth" in a non-config sense):
```bash
grep -rn '"depth"' get-shit-done/references/ --include="*.md" | grep -v knowledge-surfacing | grep -v 'depth over'
grep -rn 'config\.depth' get-shit-done/references/ --include="*.md"
grep -rn 'depth:.*quick\|depth:.*comprehensive' get-shit-done/references/ --include="*.md"
```
All three should return empty (or only contain the version-migration.md example showing the migration mechanism).

Verify version-migration.md has the controlled-exception section:
```bash
grep -c "Controlled Exception" get-shit-done/references/version-migration.md
```
Should return 1.
  </verify>
  <done>
All 5 reference files updated from depth to granularity. Health probe config-validity.md checks granularity with coarse|standard|fine enum. version-migration.md documents the controlled-exception mechanism for field renames with the depth-to-granularity example. Zero remaining config-field depth references in workflow or reference files.
  </done>
</task>

</tasks>

<verification>
1. Full grep sweep across get-shit-done/ for config-field depth references returns zero hits (excluding discovery-phase.md workflow parameter, discuss-phase.md content depth, knowledge-surfacing.md, and version-migration.md's historical example)
2. version-migration.md contains "Controlled Exception: Field Renames" section
3. Health probe validates granularity with coarse|standard|fine enum values
</verification>

<success_criteria>
- Zero workflow/reference files reference depth as a config field (CFG-04)
- Shell grep in reflect.md searches for granularity
- Spike sensitivity derivation uses granularity enum values
- Config template in new-project.md uses granularity
- Health probe checks granularity field
- version-migration.md documents controlled-exception mechanism (CFG-07)
</success_criteria>

<output>
After completion, create `.planning/phases/49-config-migration/49-02-SUMMARY.md`
</output>
