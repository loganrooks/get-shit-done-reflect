---
phase: 43-plan-intelligence-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-plan-checker.md
autonomous: true

must_haves:
  truths:
    - "Plan checker validates gsd-tools.js subcommand references in plan actions and reports unrecognized commands as advisory findings"
    - "Plan checker validates config key references against feature-manifest.json schema and reports mismatches as advisory findings"
    - "Plan checker validates directory existence for files_modified paths with temporal awareness for intra-plan creates"
    - "Plan checker validates resolves_signals IDs against KB signal index and reports unmatched IDs as advisory findings"
    - "All semantic validation findings use advisory severity with typed finding IDs (TOOL-NNN, CFG-NNN, DIR-NNN, SIG-NNN)"
  artifacts:
    - path: "agents/gsd-plan-checker.md"
      provides: "Plan checker agent spec with 11 dimensions (7 structural + 4 semantic)"
      contains: "Dimension 8"
  key_links:
    - from: "agents/gsd-plan-checker.md Dimension 8"
      to: "gsd-tools.js subcommands"
      via: "embedded tool command allowlist"
      pattern: "tool_subcommand"
    - from: "agents/gsd-plan-checker.md Dimension 9"
      to: "get-shit-done/feature-manifest.json"
      via: "config key path validation"
      pattern: "config_key"
    - from: "agents/gsd-plan-checker.md Dimension 11"
      to: ".planning/knowledge/index.md"
      via: "signal ID lookup"
      pattern: "signal_reference"
---

<objective>
Add four semantic validation dimensions (8-11) to the plan checker agent spec, covering tool subcommand validation (PLAN-01), config key validation (PLAN-02), directory existence validation (PLAN-03), and signal reference validation (PLAN-04). All findings use advisory severity with typed IDs per PLAN-05.

Purpose: The plan checker has been identified across three separate signal occurrences (phases 34, 35, 42) as having a structural capability gap. These semantic dimensions catch tool/config/directory/signal reference errors before execution.
Output: Updated `agents/gsd-plan-checker.md` with Dimensions 8-11 and advisory severity policy.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-plan-intelligence-templates/43-RESEARCH.md
@agents/gsd-plan-checker.md
@get-shit-done/feature-manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add advisory severity policy and finding ID schema to plan checker</name>
  <files>agents/gsd-plan-checker.md</files>
  <action>
Add a new section after the existing `</verification_dimensions>` closing tag but before `<verification_process>`, titled "Advisory Semantic Dimensions". This section establishes the framework for Dimensions 8-11.

Add a preamble section explaining:
1. **Advisory Severity Policy (PLAN-05):** All semantic validation findings use `severity: advisory` -- never blocker. Plans describe future state (a plan that creates dir X in Task 1 and references it in Task 2 would incorrectly fail strict directory validation at plan-check time). Advisory findings surface information without blocking execution.
2. **Finding ID Schema:** Format is `[TYPE]-[NNN]` where TYPE is one of: TOOL, CFG, DIR, SIG. Sequential numbering per dimension per plan-check run. These typed IDs enable future correlation with execution signals.
3. **Finding Output Format:**
```yaml
issue:
  dimension: "[dimension_name]"
  severity: advisory
  finding_id: "[TYPE]-[NNN]"
  description: "..."
  plan: "[plan-id]"
  task: [N]
  resolution_hint: "..."
```

Then add Dimension 8 (Tool Subcommand Validation - PLAN-01) inside the advisory semantic dimensions section:

**Dimension 8: Tool Subcommand Validation**
- Question: Do plan actions reference valid gsd-tools.js subcommands?
- Severity: advisory
- Process:
  1. Scan all `<action>` blocks for patterns matching `gsd-tools.js <command> [<subcommand>]` or `node .*/gsd-tools.js <command> [<subcommand>]`
  2. Check `<command>` against the top-level command allowlist
  3. If command has subcommands, check `<subcommand>` against the subcommand allowlist
  4. Report unmatched commands/subcommands as advisory findings with TOOL-NNN IDs

Embed the tool command allowlist (verified from gsd-tools.js source 2026-03-06):
```
Top-level commands:
  state, resolve-model, find-phase, commit, verify-summary, template,
  frontmatter, verify, generate-slug, current-timestamp, list-todos,
  verify-path-exists, config-ensure-section, config-set, history-digest,
  phases, roadmap, phase, milestone, validate, progress, todo, scaffold,
  init, phase-plan-index, state-snapshot, summary-extract, websearch,
  manifest, backlog, automation, sensors, health-probe

Subcommand trees:
  frontmatter: get, set, merge, validate
  verify: plan-structure, phase-completeness, references, commits, artifacts, key-links
  automation: resolve-level, track-event, lock, unlock, check-lock, regime-change, reflection-counter
  sensors: list, blind-spots
  health-probe: signal-metrics, signal-density, automation-watchdog
  init: execute-phase, plan-phase, new-project, new-milestone, quick, resume, verify-work, phase-op, todos, milestone-op, map-codebase, progress
  template: (accepts template name arg)
  roadmap: get-phase
  manifest: selftest, describe
```

Include a maintenance note: "This allowlist must be updated when gsd-tools.js adds new subcommands. When you encounter an unrecognized top-level command, issue a TOOL finding -- do not silently ignore."

Include an example finding:
```yaml
issue:
  dimension: tool_subcommand
  severity: advisory
  finding_id: "TOOL-001"
  description: "Plan references 'frontmatter extract' -- valid subcommands are: get, set, merge, validate"
  plan: "43-01"
  task: 1
  resolution_hint: "Did you mean 'frontmatter get'?"
```

Then add Dimension 9 (Config Key Validation - PLAN-02):

**Dimension 9: Config Key Validation**
- Question: Do plan actions reference valid config keys from feature-manifest.json?
- Severity: advisory
- Process:
  1. Read `get-shit-done/feature-manifest.json` (or `~/.claude/get-shit-done/feature-manifest.json` at runtime)
  2. Build valid key set by walking schema: for each feature, `config_key + "." + schema_field`; for nested objects, `config_key + "." + field + "." + nested_field`
  3. Scan `<action>` blocks for config key references: after `config-set` commands, dotted paths near "config", "config.json", "feature-manifest" context
  4. Validate extracted keys against valid set
  5. Advisory finding for unrecognized keys with CFG-NNN IDs
- Note: Narrow extraction to context where config is being discussed to avoid false positives on version numbers (e.g., "v1.17.0") or file paths (e.g., "path.to.file.ts")

Include an example finding referencing the known spike_sensitivity vs spike.sensitivity confusion.

CRITICAL: Edit the npm source file `agents/gsd-plan-checker.md`, NOT `.claude/agents/gsd-plan-checker.md`. Per project conventions (CLAUDE.md), always edit npm source directories.
  </action>
  <verify>
1. `grep -c "Dimension 8" agents/gsd-plan-checker.md` returns 1
2. `grep -c "Dimension 9" agents/gsd-plan-checker.md` returns 1
3. `grep -c "advisory" agents/gsd-plan-checker.md` returns at least 8 (new advisory references)
4. `grep "TOOL-001" agents/gsd-plan-checker.md` shows the example finding
5. `grep "CFG-001" agents/gsd-plan-checker.md` shows the example finding
6. `git diff agents/gsd-plan-checker.md | head -5` confirms changes are in npm source, not .claude/
  </verify>
  <done>
Plan checker agent spec has Dimensions 8 (tool subcommand) and 9 (config key) with advisory severity policy, finding ID schema, embedded tool allowlist, and extraction guidance. All findings are advisory severity with typed IDs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add directory existence and signal reference dimensions to plan checker</name>
  <files>agents/gsd-plan-checker.md</files>
  <action>
Continue adding to the advisory semantic dimensions section in `agents/gsd-plan-checker.md`.

Add Dimension 10 (Directory Existence Validation - PLAN-03):

**Dimension 10: Directory Existence Validation**
- Question: Do `files_modified` paths in plan frontmatter have valid parent directories?
- Severity: advisory
- Process:
  1. Parse `files_modified` from plan frontmatter
  2. For each path, extract parent directory
  3. Build a "will exist" set from task `<files>` and `<action>` blocks (directories explicitly created via mkdir -p or listed as directory creates)
  4. Also build a "dependency creates" set: if plan depends on earlier plans, include directories those plans create (from their `files_modified`)
  5. Check: does the parent directory exist on disk OR appear in the "will exist" set OR appear in the "dependency creates" set?
  6. Advisory finding for missing directories with DIR-NNN IDs
- Temporal awareness is critical: a plan that creates `src/new-feature/` in Task 1 and references `src/new-feature/component.ts` in Task 2 should NOT produce a finding for the parent directory
- When checking disk: use simple path existence check (ls or test -d), not recursive search

Include an example finding:
```yaml
issue:
  dimension: directory_existence
  severity: advisory
  finding_id: "DIR-001"
  description: "files_modified includes 'src/new-module/index.ts' but parent 'src/new-module/' does not exist on disk and is not created by any task in this plan or its dependencies"
  plan: "43-02"
  task: 2
  resolution_hint: "Add directory creation to an earlier task or verify the path is correct"
```

Add Dimension 11 (Signal Reference Validation - PLAN-04):

**Dimension 11: Signal Reference Validation**
- Question: Do `resolves_signals` IDs in plan frontmatter exist in the KB signal index?
- Severity: advisory
- Process:
  1. Parse `resolves_signals` from plan frontmatter (YAML list of signal IDs)
  2. Read `.planning/knowledge/index.md` (project-local primary, `~/.gsd/knowledge/index.md` fallback per Phase 38.1 convention)
  3. Extract all signal IDs from the index (both `sig-*` and legacy `SIG-*` format)
  4. For each `resolves_signals` ID, check if it appears in the index
  5. Advisory finding for unmatched IDs with SIG-NNN IDs -- signal may not yet have been collected
- Note: signals may be collected after plan creation. An unmatched ID is an advisory note, not a definitive error.

Include an example finding:
```yaml
issue:
  dimension: signal_reference
  severity: advisory
  finding_id: "SIG-001"
  description: "resolves_signals includes 'sig-2026-03-10-missing-signal' which is not found in KB index"
  plan: "43-01"
  resolution_hint: "Verify signal ID exists in .planning/knowledge/index.md -- signal may not yet be collected"
```

Finally, update the verification_process section to integrate the new dimensions:
- After Step 8 (Assess Scope), add **Step 8.5: Semantic Validation** that runs Dimensions 8-11 and collects advisory findings
- Update Step 10 (Determine Overall Status) to note: "Advisory findings from semantic validation are reported in the output but do NOT affect the passed/issues_found determination. Only blocker and warning severity issues from Dimensions 1-7 determine status."

Update the `<success_criteria>` section to add:
- `[ ] Semantic validation dimensions checked (Dimensions 8-11, advisory only)`

CRITICAL: Edit the npm source file `agents/gsd-plan-checker.md`, NOT `.claude/agents/gsd-plan-checker.md`.
  </action>
  <verify>
1. `grep -c "Dimension 10" agents/gsd-plan-checker.md` returns 1
2. `grep -c "Dimension 11" agents/gsd-plan-checker.md` returns 1
3. `grep "DIR-001" agents/gsd-plan-checker.md` shows the example finding
4. `grep "SIG-001" agents/gsd-plan-checker.md` shows the example finding
5. `grep "Semantic Validation" agents/gsd-plan-checker.md` shows the new verification process step
6. `grep -c "Dimension" agents/gsd-plan-checker.md` returns at least 11 (all dimensions referenced)
7. No changes in `.claude/` directory: `git diff --name-only | grep -c "^\.claude/"` returns 0
  </verify>
  <done>
Plan checker agent spec has all 11 dimensions (7 structural + 4 semantic). Dimensions 8-11 cover tool subcommands, config keys, directory existence (with temporal awareness), and signal references. All semantic findings are advisory severity with typed IDs. Verification process integrates semantic validation as a non-blocking step.
  </done>
</task>

</tasks>

<verification>
1. `agents/gsd-plan-checker.md` contains Dimensions 1-11 (7 structural + 4 semantic)
2. All semantic dimensions use `severity: advisory` (never blocker)
3. Each dimension has: extraction pattern, validation source, finding format with typed IDs
4. Tool allowlist is embedded and complete (matches gsd-tools.js source)
5. Temporal awareness documented for directory validation
6. KB path fallback pattern used for signal reference validation
7. Advisory findings do not affect passed/issues_found status determination
8. No changes to `.claude/` directory (npm source only)
</verification>

<success_criteria>
- Plan checker spec validates tool subcommand existence with embedded allowlist (PLAN-01)
- Plan checker spec validates config key paths against feature-manifest.json schema (PLAN-02)
- Plan checker spec validates directory existence with intra-plan temporal awareness (PLAN-03)
- Plan checker spec validates signal reference IDs against KB index (PLAN-04)
- All findings use advisory severity with typed IDs for future correlation (PLAN-05)
- Changes are in npm source `agents/gsd-plan-checker.md` only
</success_criteria>

<output>
After completion, create `.planning/phases/43-plan-intelligence-templates/43-01-SUMMARY.md`
</output>
