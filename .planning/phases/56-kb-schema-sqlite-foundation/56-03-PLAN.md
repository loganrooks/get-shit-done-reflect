---
phase: 56-kb-schema-sqlite-foundation
plan: 03
type: execute
wave: 3
depends_on: ["56-02"]
files_modified:
  - tests/unit/kb.test.js
autonomous: true

must_haves:
  truths:
    - "kb rebuild on the 199-signal corpus produces a SQLite index with all frontmatter fields indexed and 0 errors"
    - "Signal files with old schema (missing lifecycle_state, missing polarity, legacy SIG-format) parse successfully with correct defaults"
    - "source field migration produces correct detection_method + origin for all 7 source variants in corpus"
    - "kb.db can be deleted and rebuilt without data loss (dual-write invariant holds)"
    - "Node version guard fires actionable error on Node <22.5.0 without crashing non-KB commands"
  artifacts:
    - path: "tests/unit/kb.test.js"
      provides: "Unit and integration tests for kb.cjs module"
      min_lines: 150
  key_links:
    - from: "tests/unit/kb.test.js"
      to: "get-shit-done/bin/lib/kb.cjs"
      via: "require('../../get-shit-done/bin/lib/kb.cjs')"
      pattern: "cmdKbRebuild|cmdKbStats|cmdKbMigrate"
---

<objective>
Create the test suite for kb.cjs and run the source field migration on the real corpus. Tests validate schema handling, rebuild correctness, migration mapping, and the dual-write invariant.

Purpose: KB-09 requires migration of the `source` field. KB-10 requires backward compatibility testing against the full corpus. This plan delivers both the migration execution and the test evidence that the entire KB substrate works correctly.

Output: `tests/unit/kb.test.js` with passing tests, migrated signal files committed
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-kb-schema-sqlite-foundation/56-CONTEXT.md
@.planning/phases/56-kb-schema-sqlite-foundation/56-01-SUMMARY.md
@.planning/phases/56-kb-schema-sqlite-foundation/56-02-SUMMARY.md
@get-shit-done/bin/lib/kb.cjs
@tests/unit/sensors.test.js
@tests/integration/kb-infrastructure.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create kb.test.js with unit tests for schema, mapping, rebuild, stats, and migrate</name>
  <files>tests/unit/kb.test.js</files>
  <action>
Create `tests/unit/kb.test.js` using vitest, following the pattern in `tests/unit/sensors.test.js` and `tests/integration/kb-infrastructure.test.js`. Use the `tmpdirTest` helper from `tests/helpers/tmpdir.js` for isolated test directories.

**Test structure:**

```javascript
import { describe, it, expect } from 'vitest'
import { tmpdirTest } from '../helpers/tmpdir.js'
import path from 'node:path'
import fs from 'node:fs'
import { createRequire } from 'node:module'

const require = createRequire(import.meta.url)
```

Require kb.cjs functions. If the module exports internal helpers for testing, use those; otherwise test through the cmd* functions with `--raw` flag to get JSON output, capturing stdout.

**Helper function:** `writeSignalFixture(kbDir, project, filename, frontmatterFields)` -- creates a signal .md file with YAML frontmatter in `{kbDir}/signals/{project}/{filename}`. Use the same pattern as `tests/integration/kb-infrastructure.test.js` writeSignal helper.

**Test groups:**

1. **Schema generation handling (4 tests):**
   - Legacy SIG-format signal (type: observation, status: open, no lifecycle_state, no signal_category) -- verify defaults to lifecycle_state: detected, polarity: negative, status: active
   - Early standard signal (type: signal, source: manual, no lifecycle_state) -- verify source maps to detection_method: manual, origin: user-observation
   - Phase 31 enriched signal (lifecycle_state: triaged, evidence, triage, signal_category: negative) -- verify all fields preserved as-is
   - Latest signal (has response_disposition) -- verify disposition field populated

2. **Source field migration mapping (7 tests, one per source variant):**
   - source: auto -> detection_method: automated, origin: collect-signals
   - source: manual -> detection_method: manual, origin: user-observation
   - source: automated -> detection_method: automated, origin: collect-signals
   - source: deliberation-trigger -> detection_method: manual, origin: deliberation-trigger
   - source: auto-collected -> detection_method: automated, origin: collect-signals
   - source: (plan summary reference like "55-01-SUMMARY.md") -> detection_method: automated, origin: plan-summary
   - source: (absent) -> detection_method: unknown, origin: unknown

3. **Rebuild correctness (3 tests):**
   - Empty corpus: rebuild on empty signals dir produces 0 entries, no errors
   - Mixed corpus: 5 signals across 2 projects + 1 root-level signal + 1 spike -> correct counts in meta table
   - Incremental rebuild: modify 1 file after initial rebuild, run rebuild again -> only 1 updated, rest skipped (content hash check)

4. **Stats output (2 tests):**
   - Stats on populated corpus returns counts by severity, lifecycle, polarity, project
   - Stats on missing db returns error message (not crash)

5. **Migrate (3 tests):**
   - File with source: auto -> adds detection_method + origin, removes source
   - File without source field -> skipped, not modified
   - File already migrated (has detection_method) -> skipped, not modified

6. **Dual-write invariant (1 test, KB-05):**
   - Build index, delete kb.db, rebuild -> same counts as original build. Proves SQLite is derived cache.

7. **Tags and links (2 tests):**
   - Signal with tags: [tag1, tag2] -> signal_tags table has 2 entries
   - Signal with qualified_by: [sig-xxx] and related_signals: [sig-yyy] -> signal_links table has entries with correct link_types

8. **Status normalization (2 tests, RESEARCH.md Pitfall 4):**
   - status: resolved -> normalized to status: active, lifecycle_state: remediated
   - status: remediated -> normalized to status: active, lifecycle_state: remediated

**Handling node:sqlite ExperimentalWarning (RESEARCH.md Pitfall 3):**
Do NOT assert on stderr content. The warning is harmless. If tests capture stderr, filter out ExperimentalWarning lines.

**Test execution note:** These tests require Node >=22.5.0 (which this machine has: v22.22.1). Tests that call into kb.cjs will trigger the node:sqlite lazy require. Use `{ timeout: 10000 }` for rebuild tests that process multiple files.
  </action>
  <verify>Run `npm test -- tests/unit/kb.test.js` and confirm all tests pass. Check test count is >= 20.</verify>
  <done>All kb.test.js tests pass. Tests cover all 4 schema generations, all 7 source field variants, rebuild correctness, stats output, migrate behavior, dual-write invariant, tags/links extraction, and status normalization.</done>
</task>

<task type="auto">
  <name>Task 2: Run kb migrate on real corpus and rebuild index</name>
  <files>.planning/knowledge/signals/**/*.md</files>
  <action>
Run the source field migration on the actual signal corpus:

1. First, take a count of files with `source:` field before migration:
   `grep -rl "^source:" .planning/knowledge/signals/ | wc -l`
   Expected: approximately 183 files (from RESEARCH.md corpus analysis)

2. Run migration:
   `node get-shit-done/bin/gsd-tools.cjs kb migrate`
   This modifies signal files: replaces `source: X` with `detection_method: Y` and `origin: Z` per the mapping defined in kb.cjs.

3. Verify migration results:
   - `grep -rl "^source:" .planning/knowledge/signals/ | wc -l` should be 0 (all migrated)
   - `grep -rl "^detection_method:" .planning/knowledge/signals/ | wc -l` should match pre-migration count
   - Spot-check 3 files across different source values to verify correct mapping

4. Rebuild the index after migration:
   `node get-shit-done/bin/gsd-tools.cjs kb rebuild`
   This rebuilds kb.db with the migrated frontmatter. Should process all files with 0 errors.

5. Run stats to verify corpus integrity:
   `node get-shit-done/bin/gsd-tools.cjs kb stats`
   Verify signal counts match expectations (~199 signals, ~1 spike).

6. Run the full test suite to confirm nothing is broken:
   `npm test`
   All existing tests plus the new kb.test.js must pass.

**Commit strategy:** The migrated signal files (~183 files) should be committed in a single commit: `fix(56): migrate signal source field to detection_method + origin (KB-09)`. The kb.test.js should be committed separately: `test(56): add kb module unit tests`.

**If migration produces errors:** Investigate the specific files. Common issues:
- Signals with unusual `source` values (plan summary references) -- verify mapSourceToOrigin handles them
- Frontmatter reconstruction changing formatting -- verify reconstructFrontmatter preserves content after frontmatter block
- Files with no frontmatter at all -- these should be skipped by extractFrontmatter returning empty/null
  </action>
  <verify>
Run: `grep -rl "^source:" .planning/knowledge/signals/ | wc -l` -- must be 0 (all migrated).
Run: `node get-shit-done/bin/gsd-tools.cjs kb rebuild` -- must complete with 0 errors.
Run: `node get-shit-done/bin/gsd-tools.cjs kb stats` -- must show 190+ signals indexed.
Run: `npm test` -- all tests pass including new kb.test.js.
  </verify>
  <done>All ~183 signal files with source field migrated to detection_method + origin. kb rebuild on full corpus succeeds with 0 errors. All tests pass. Signal files committed with migration commit.</done>
</task>

</tasks>

<verification>
- All kb.test.js tests pass (20+ tests covering schema generations, migration, rebuild, stats, dual-write invariant)
- Source field migration applied to all ~183 signal files in the real corpus
- `kb rebuild` on migrated corpus produces 0 errors and 190+ indexed signals
- `kb stats` shows meaningful corpus breakdown
- Full test suite (`npm test`) passes including existing 661 tests + new kb tests
- kb.db can be deleted and rebuilt with identical results (dual-write invariant)
</verification>

<success_criteria>
- tests/unit/kb.test.js exists with 20+ passing tests
- No signal files contain `source:` field (all migrated to detection_method + origin)
- `gsd-tools kb rebuild` on full corpus: 0 errors, 190+ signals, 1+ spikes
- `gsd-tools kb stats` shows counts by severity, lifecycle, polarity, project, detection_method
- `npm test` passes all tests (existing + new)
- kb.db delete + rebuild produces identical stats (KB-05 dual-write invariant proven)
</success_criteria>

<output>
After completion, create `.planning/phases/56-kb-schema-sqlite-foundation/56-03-SUMMARY.md`
</output>
