---
phase: 56-kb-schema-sqlite-foundation
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - get-shit-done/bin/gsd-tools.cjs
  - package.json
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "gsd-tools kb rebuild invokes cmdKbRebuild and produces kb.db"
    - "gsd-tools kb stats invokes cmdKbStats and shows corpus statistics"
    - "gsd-tools kb migrate invokes cmdKbMigrate"
    - "package.json engines.node is >=22.5.0"
    - "kb.db is in .gitignore and will not be committed"
    - "gsd-tools usage message includes kb in the command list"
  artifacts:
    - path: "get-shit-done/bin/gsd-tools.cjs"
      provides: "Router case for kb subcommands"
      contains: "case 'kb'"
    - path: "package.json"
      provides: "Updated Node version requirement"
      contains: ">=22.5.0"
    - path: ".gitignore"
      provides: "kb.db exclusion"
      contains: "kb.db"
  key_links:
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/kb.cjs"
      via: "require('./lib/kb.cjs')"
      pattern: "kb\\.cmdKbRebuild|kb\\.cmdKbStats|kb\\.cmdKbMigrate"
---

<objective>
Wire the kb.cjs module into gsd-tools.cjs routing, update package.json engines.node to >=22.5.0, and add kb.db to .gitignore.

Purpose: Plan 01 creates the module; this plan makes it callable via `gsd-tools kb rebuild|stats|migrate` and establishes the Node version requirement and gitignore entry that KB-04a, KB-05, and KB-11 require.

Output: Working `gsd-tools kb` commands, correct package.json engines, kb.db gitignored
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-kb-schema-sqlite-foundation/56-CONTEXT.md
@.planning/phases/56-kb-schema-sqlite-foundation/56-01-SUMMARY.md
@get-shit-done/bin/gsd-tools.cjs
@package.json
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire kb.cjs into gsd-tools.cjs router and update package.json</name>
  <files>get-shit-done/bin/gsd-tools.cjs, package.json</files>
  <action>
**gsd-tools.cjs changes:**

1. Add require at top with other lib modules (around line 52, after the automation require):
```javascript
const kb = require('./lib/kb.cjs');
```
IMPORTANT: This is a lazy-guarded module -- node:sqlite is NOT required at module load time per Plan 01's version guard pattern. The require of kb.cjs itself is safe on any Node version.

2. Update the usage/error message (line 87) to include 'kb' in the commands list. Add it after 'health-probe' in the list.

3. Add a new case block before the `default:` case (before line 683). Follow the exact pattern used by sensors/manifest/automation cases:

```javascript
case 'kb': {
  const subcommand = args[1];
  if (subcommand === 'rebuild') {
    kb.cmdKbRebuild(cwd, raw);
  } else if (subcommand === 'stats') {
    kb.cmdKbStats(cwd, raw);
  } else if (subcommand === 'migrate') {
    kb.cmdKbMigrate(cwd, raw);
  } else {
    error('Unknown kb subcommand. Available: rebuild, stats, migrate');
  }
  break;
}
```

**package.json changes:**

Update the engines.node field from `>=16.7.0` to `>=22.5.0` (line 42). This is required by KB-11 because node:sqlite is not available before Node 22.5.0.

The CHANGELOG entry for this breaking change will be handled at release time, not in this plan.
  </action>
  <verify>
Run: `node get-shit-done/bin/gsd-tools.cjs 2>&1 | grep -o 'kb'` to confirm kb appears in usage message.
Run: `node -e "const pkg = require('./package.json'); console.log(pkg.engines.node)"` to confirm `>=22.5.0`.
Run: `grep "case 'kb'" get-shit-done/bin/gsd-tools.cjs` to confirm router case exists.
  </verify>
  <done>gsd-tools.cjs routes `kb rebuild`, `kb stats`, `kb migrate` to kb.cjs functions. package.json engines.node is >=22.5.0.</done>
</task>

<task type="auto">
  <name>Task 2: Add kb.db to .gitignore and run kb rebuild on real corpus</name>
  <files>.gitignore</files>
  <action>
**gitignore changes:**

Add `kb.db` to .gitignore. Place it in a new section after the existing reentrancy lockfiles section:

```
# Knowledge base SQLite index (derived cache, rebuildable from files)
kb.db
```

This ensures kb.db is never committed regardless of where it lives (.planning/knowledge/kb.db or ~/.gsd/knowledge/kb.db). The comment documents that it is a derived cache per KB-05.

**Validate by running kb rebuild on the real corpus:**

After the gitignore change, run `node get-shit-done/bin/gsd-tools.cjs kb rebuild` against the actual 199-signal corpus at `.planning/knowledge/`. This is the smoke test that proves the entire pipeline works end-to-end:
- File discovery finds all signals and spikes
- extractFrontmatter parses all 4 schema generations
- signalToRow applies defaults for missing fields
- SQLite INSERT succeeds for all entries
- No crashes on malformed signals

Expected output: approximately 199 files processed (194+ project-scoped signals, 5 root-level signals, 1 spike), 0 errors.

If any errors occur, investigate and fix in kb.cjs. Common issues per RESEARCH.md:
- Missing field defaults (Pitfall 1)
- Status/lifecycle_state conflation (Pitfall 4)
- Root-level signal discovery (Pitfall 6)

After rebuild succeeds, verify kb.db exists at `.planning/knowledge/kb.db` and run `node get-shit-done/bin/gsd-tools.cjs kb stats` to see the corpus breakdown.

Then verify kb.db is correctly gitignored: `git status` should NOT show kb.db as an untracked file.
  </action>
  <verify>
Run: `node get-shit-done/bin/gsd-tools.cjs kb rebuild` -- must complete with 0 errors and 190+ signals indexed.
Run: `node get-shit-done/bin/gsd-tools.cjs kb stats` -- must show counts by severity, lifecycle, polarity, project.
Run: `git status .planning/knowledge/kb.db` -- must show no output (gitignored).
Run: `test -f .planning/knowledge/kb.db && echo "exists" || echo "missing"` -- must print "exists".
  </verify>
  <done>kb.db is gitignored. `kb rebuild` processes the full 199-signal corpus without errors. `kb stats` shows corpus statistics. kb.db exists as a derived cache that can be deleted and rebuilt.</done>
</task>

</tasks>

<verification>
- `gsd-tools kb rebuild` runs successfully against real corpus
- `gsd-tools kb stats` shows meaningful statistics
- `gsd-tools kb migrate` is callable (actual migration in Plan 03)
- package.json engines.node is >=22.5.0
- kb.db is gitignored
- gsd-tools usage message includes kb
</verification>

<success_criteria>
- `gsd-tools kb rebuild` processes 190+ signal files with 0 errors on the real corpus
- `gsd-tools kb stats` displays counts by severity, lifecycle_state, polarity, project
- package.json engines.node updated from >=16.7.0 to >=22.5.0
- kb.db appears in .gitignore with explanatory comment
- gsd-tools.cjs routes all 3 kb subcommands correctly
</success_criteria>

<output>
After completion, create `.planning/phases/56-kb-schema-sqlite-foundation/56-02-SUMMARY.md`
</output>
