---
phase: 60-sensor-pipeline-codex-parity
plan: "01"
signature:
  role: planner
  harness: claude-code
  platform: claude-code-cli
  vendor: anthropic
  model: claude-opus-4-7
  reasoning_effort: not_available
  profile: quality
  gsd_version: 1.19.8+dev
  generated_at: "2026-04-21T19:30:00Z"
  session_id: not_available
  provenance_status:
    role: derived
    harness: derived
    platform: derived_from_harness
    vendor: derived_from_harness
    model: exposed
    reasoning_effort: not_available
    profile: derived
    gsd_version: derived
    generated_at: exposed
    session_id: not_available
  provenance_source:
    role: artifact_role
    harness: runtime_context
    platform: derived_from_harness
    vendor: derived_from_harness
    model: runtime_env
    reasoning_effort: claude_not_exposed
    profile: config
    gsd_version: installed_harness
    generated_at: writer_clock
    session_id: claude_not_exposed
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/cross-runtime-parity-research.md
  - bin/install.js
  - tests/unit/install.test.js
autonomous: true
user_setup: []
must_haves:
  truths:
    - "`cross-runtime-parity-research.md` frontmatter reflects live-audit facts: `last_audited: 2026-04-21`, `last_audited_codex_version: 0.121.0`, `next_audit_due: 2026-05-05`."
    - "`cross-runtime-parity-research.md` §1.2 cites Codex `v0.121.0` (not `0.118.0`); §7.2 directory layout lists `logs_2.sqlite` (not `logs_1.sqlite`, with a note that Codex revs the filename suffix); §1.3 event-type enumeration names all 17 live event_msg types and all response_item types including `custom_tool_call`, `custom_tool_call_output`, `web_search_call`; Validation Commands table has rows for `Codex logs DB filename` and `Session event-type enumeration` that re-run the audit commands."
    - "`bin/install.js` `module.exports` exposes the installer primitives Plan 60-03 / 60-04 / 60-05 / 60-06 need: `fileHash`, `generateManifest`, `writeManifest`, `saveLocalPatches`, `pruneRedundantPatches`, `reportLocalPatches`, `getGlobalDir`, `claudeToCodexTools`, `PATCHES_DIR_NAME`, `MANIFEST_NAME`. `replacePathsInContent`, `extractFrontmatterAndBody`, `extractFrontmatterField`, `convertClaudeToCodexMarkdown`, `convertClaudeToCodexAgentToml` are already exported and remain exported."
    - "`require('bin/install.js')` from a downstream module successfully returns non-null bindings for every freshly-exported helper (regression test asserts this)."
    - "No behavioral change to `install.js` — the export list is additive; existing tests still pass."
  artifacts:
    - path: ".planning/research/cross-runtime-parity-research.md"
      provides: "G-1 reconciled research doc: Codex 0.121.0, logs_2.sqlite, 12+3 new event types enumerated, multi_agent_v2 row added, Validation Commands extended."
      contains: "0.121.0"
    - path: "bin/install.js"
      provides: "Extended module.exports with the 10 helpers downstream plans import. No other behavioral change."
      contains: "claudeToCodexTools"
    - path: "tests/unit/install.test.js"
      provides: "Regression test asserting every newly-exported helper is non-null and callable on `require('../../bin/install.js')`."
      contains: "claudeToCodexTools"
  key_links:
    - from: "bin/install.js module.exports"
      to: "claudeToCodexTools (internal const at line 517)"
      via: "add identifier to the `module.exports = { ... }` object at line 2627"
      pattern: "claudeToCodexTools"
    - from: "tests/unit/install.test.js"
      to: "bin/install.js"
      via: "`const installExports = require('../../bin/install.js'); expect(installExports.fileHash).toBeTypeOf('function');` for each new export"
      pattern: "installExports\\.(fileHash|generateManifest|claudeToCodexTools)"
---

<objective>
Prepare the ground for Wave 2 of Phase 60: reconcile the G-1 audit drift in `cross-runtime-parity-research.md` (Codex 0.118.0 → 0.121.0, `logs_1.sqlite` → `logs_2.sqlite`, 12 new `event_msg` types + 3 new `response_item` types, `multi_agent_v2` feature flag), and extend `bin/install.js` `module.exports` with the installer primitives that Plans 60-03 / 60-04 / 60-05 / 60-06 import via `require('../../bin/install.js')`. These are small, mechanical, low-risk changes with zero behavioral impact — the goal is strictly substrate preparation.

Purpose: Wave 2 plans (log-sensor adapter, patch sensor + classifier, post-install parity) and Wave 3 plans (XRT-02 validator) all build on two preconditions: (1) the research doc reflects live-audit reality so downstream `[evidenced:cited]` claims remain honest, and (2) the installer primitives `fileHash`, `generateManifest`, `saveLocalPatches`, `reportLocalPatches`, `getGlobalDir`, `claudeToCodexTools`, etc. are reachable via `require()`. Today `claudeToCodexTools` and several patch-related primitives are internal-only — consumers would either re-implement or fork the file. Per RESEARCH.md Don't-Hand-Roll §DC-6, that is exactly what Phase 60 must not do. This plan makes the primitives callable.
Output: A research doc aligned with the 2026-04-21 audit, an `install.js` whose exports match the Phase 60 consumer surface, and a regression test that fails if a future refactor silently de-exports any of them.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-sensor-pipeline-codex-parity/60-CONTEXT.md
@.planning/phases/60-sensor-pipeline-codex-parity/60-RESEARCH.md
@.planning/research/cross-runtime-parity-research.md
@bin/install.js
@tests/unit/install.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile G-1 audit drift in cross-runtime-parity-research.md</name>
  <files>.planning/research/cross-runtime-parity-research.md</files>
  <action>Apply the seven updates enumerated in RESEARCH.md §G-1 Audit Gate Finding verbatim. This is a doc-only edit — no code, no tests.

(a) **Frontmatter:**
- Bump `last_audited` to `"2026-04-21"`.
- Bump `last_audited_codex_version` to `"0.121.0"`.
- Set `next_audit_due` to `"2026-05-05"`.
- If the frontmatter currently has `last_audited_claude_code_version`, leave it or bump only if you can re-run `claude --version` in this session; otherwise leave the existing value with a comment `# not-re-audited 2026-04-21`.

(b) **§1.2 Codex version reference:** Replace any `v0.118.0` / `0.118.0` string referencing Codex CLI with `v0.121.0` / `0.121.0`. Do NOT rewrite text around the version — just swap the tokens so the paragraph still reads coherently.

(c) **§7.2 Codex directory layout:** Replace `logs_1.sqlite` with `logs_2.sqlite` and append this sentence after the first mention: "Codex revs this filename suffix on schema changes (`logs_1.sqlite` → `logs_2.sqlite` observed 2026-04-21, size ~1.1GB); consumers should glob `logs_*.sqlite` rather than hardcode the suffix." This codes the Pitfall 3 insight into the doc itself.

(d) **§1.3 Codex CLI event type enumeration:** Extend the existing enumeration with the 12 new `event_msg.payload.type` values found in the live audit: `exec_command_end`, `turn_aborted`, `patch_apply_end`, `context_compacted`, `collab_waiting_end`, `collab_agent_spawn_end`, `collab_close_end`, `collab_agent_interaction_end`, `mcp_tool_call_end`, `item_completed`, `thread_rolled_back`, `web_search_end`. Extend the `response_item.payload.type` list with the 3 new values: `custom_tool_call`, `custom_tool_call_output`, `web_search_call`. Mark these with a note: "Added 2026-04-21 after live-corpus survey of 100 sessions / 12,204 events. Adapters must recognize these as KNOWN (counted or ignored) — classifying them as unknown triggers SENS-07 for every occurrence (Pitfall 1 in phase-60 RESEARCH.md)."

(e) **§1.2 / Validation Commands:** Add a row for Codex `multi_agent_v2` with command `codex features list | grep multi_agent_v2` expecting `multi_agent_v2 under development false` per the 2026-04-21 audit. Leave the existing `multi_agent` row intact.

(f) **Validation Commands table — two new rows:**
| Codex logs DB filename | `ls ~/.codex/logs_*.sqlite` | Expect `logs_2.sqlite` (as of 2026-04-21); Codex revs the suffix |
| Session event-type enumeration | `for f in $(find ~/.codex/sessions -name '*.jsonl' | head -100); do jq -rcs '.[] | .payload.type // .type' < "$f"; done | sort -u` | Expect the 17 event_msg types + 5 response_item types enumerated in §1.3 |

(g) **Update the "Valid until" line / next-audit block** to `2026-05-05`. If there is no such line, add one at the bottom of the doc: `**Next audit due:** 2026-05-05 or at the next Codex version bump, whichever comes first.`

Do NOT rewrite the doc; only make the seven targeted edits above. Do NOT introduce new sections or remove existing content. The edit is a tight audit reconciliation, not a refactor. Preserve existing formatting, Markdown shape, and citation style.

Verify by re-reading the modified doc and confirming each of the seven changes above is present and internally consistent.</action>
  <verify>`grep -c "0.121.0" .planning/research/cross-runtime-parity-research.md` returns ≥ 2 (frontmatter + §1.2). `grep -c "logs_2.sqlite" .planning/research/cross-runtime-parity-research.md` returns ≥ 1. `grep -c "exec_command_end" .planning/research/cross-runtime-parity-research.md` returns ≥ 1. `grep -c "custom_tool_call" .planning/research/cross-runtime-parity-research.md` returns ≥ 1. `grep -c "multi_agent_v2" .planning/research/cross-runtime-parity-research.md` returns ≥ 1. `grep -c "2026-05-05" .planning/research/cross-runtime-parity-research.md` returns ≥ 1. Manual re-read confirms frontmatter `last_audited` / `last_audited_codex_version` / `next_audit_due` are set per (a).</verify>
  <done>G-1 audit gate closes — the living research doc reflects the 2026-04-21 audit. Downstream plans' `[evidenced:cited]` claims citing this doc no longer reference stale Codex versions or missing event types.</done>
</task>

<task type="auto">
  <name>Task 2: Extend bin/install.js module.exports with installer primitives Phase 60 consumers import, and ship regression coverage</name>
  <files>bin/install.js, tests/unit/install.test.js</files>
  <action>Two coordinated changes:

(a) **Extend `module.exports` at `bin/install.js:2627`:** Add the following identifiers to the existing `module.exports = { ... }` object in alphabetical order within the existing list. Per RESEARCH.md Don't-Hand-Roll §, these are the exact helpers that Plan 60-03 (log-sensor adapter), Plan 60-04 (patch classifier library), Plan 60-05 (post-install parity), and Plan 60-06 (XRT-02 validator) import via `require('../../bin/install.js')`:
- `fileHash` (defined at line 1750 — SHA256 wrapper)
- `generateManifest` (line 1758 — manifest walker)
- `writeManifest` (line 1778 — manifest writer)
- `saveLocalPatches` (line 1810 — patch backup)
- `pruneRedundantPatches` (line 1854 — patch pruning)
- `reportLocalPatches` (line 1911 — patch report)
- `getGlobalDir` (line 130 — centralized runtime path resolution, per Phase 58.1 DC-3 + G-6)
- `claudeToCodexTools` (line 517 — tool-name map used by Layer-2 content compare)
- `PATCHES_DIR_NAME` (constant — the `'gsdr-local-patches'` directory name)
- `MANIFEST_NAME` (constant — the `'gsd-file-manifest.json'` filename)

The existing exports (`replacePathsInContent`, `injectVersionScope`, `getGsdHome`, `migrateKB`, `countKBEntries`, `installKBScripts`, `createProjectLocalKB`, `convertClaudeToCodexSkill`, `convertClaudeToCodexMarkdown`, `convertClaudeToCodexAgentToml`, `copyCodexSkills`, `generateCodexAgentsMd`, `generateCodexMcpConfig`, `generateCodexConfigBlock`, `stripGsdFromCodexConfig`, `mergeCodexConfig`, `CODEX_AGENT_SANDBOX`, `GSD_CODEX_MARKER`, `safeFs`, `buildLocalHookCommand`, `extractFrontmatterAndBody`, `extractFrontmatterField`, `readSettings`, `writeSettings`, `copyWithPathReplacement`, `generateMigrationGuide`, `isVersionInRange`, `compareVersions`, `cleanupOrphanedFiles`, `validateHookFields`, `getCodexCompactPromptPath`) MUST NOT be removed or reordered — this is a strictly additive change.

If any named export (e.g., `PATCHES_DIR_NAME`, `MANIFEST_NAME`) is currently declared as a `const` that is NOT hoisted above `module.exports`, do NOT introduce new declarations — simply add them to the export object once you confirm they exist at module-top-level. If `PATCHES_DIR_NAME` / `MANIFEST_NAME` do NOT currently exist as module-level constants (they might be inline string literals in functions like `saveLocalPatches`), introduce them as module-top-level `const PATCHES_DIR_NAME = 'gsdr-local-patches'; const MANIFEST_NAME = 'gsd-file-manifest.json';` near the top of the file (after the imports), replacing any inline usage of those string literals. Grep before introducing: `grep -n "'gsdr-local-patches'" bin/install.js` — if hits exist in function bodies, refactor them to use the top-level constants so the exported constants are a single source of truth.

Do NOT change behavior of any function being exported. Do NOT rename. Do NOT change function signatures. If a function being exported references closures or module-internal helpers, verify those closures are not themselves dependent on state that only exists when `install()` is running — all of the listed helpers are pure-or-filesystem functions safe to call from a downstream module.

(b) **Regression test `tests/unit/install.test.js`:** Add a new `describe('Phase 60: module.exports surface')` block with a single `it('exports every installer primitive Phase 60 consumers import', ...)` test that does:
```js
const installExports = require('../../bin/install.js');
const required = [
  'fileHash', 'generateManifest', 'writeManifest',
  'saveLocalPatches', 'pruneRedundantPatches', 'reportLocalPatches',
  'getGlobalDir', 'claudeToCodexTools',
  'PATCHES_DIR_NAME', 'MANIFEST_NAME',
  // Pre-existing exports Phase 60 also uses — guard against silent removal
  'replacePathsInContent', 'convertClaudeToCodexMarkdown',
  'convertClaudeToCodexAgentToml', 'extractFrontmatterAndBody',
  'extractFrontmatterField',
];
for (const name of required) {
  expect(installExports[name], `install.js missing export: ${name}`).toBeDefined();
}
expect(typeof installExports.fileHash).toBe('function');
expect(typeof installExports.generateManifest).toBe('function');
expect(typeof installExports.claudeToCodexTools).toBe('object');
expect(installExports.PATCHES_DIR_NAME).toBe('gsdr-local-patches');
expect(installExports.MANIFEST_NAME).toBe('gsd-file-manifest.json');
```

This test is the STRUCTURAL PREVENTION layer per the adopted `cross-runtime-parity-testing.md` Option D: it fails at test-time if a future refactor silently de-exports one of the Phase 60 primitives. No downstream plan needs to own this guard — it lives here at the export surface.

Do NOT write an integration test for this plan — the regression test is unit-level by design.</action>
  <verify>`npx vitest run tests/unit/install.test.js` passes including the new Phase 60 module.exports surface test. `node -e "const e = require('./bin/install.js'); console.log(Object.keys(e).filter(k => ['fileHash','generateManifest','claudeToCodexTools','PATCHES_DIR_NAME','MANIFEST_NAME'].includes(k)).length)"` prints `5`. `grep -n "claudeToCodexTools" bin/install.js` shows the identifier both at line 517 (definition) AND in the `module.exports` object. `npm test` passes (no regressions).</verify>
  <done>All ten Phase 60 installer primitives are reachable via `require('../../bin/install.js')`. A structural test guards the surface against silent regression. No behavioral change to the installer.</done>
</task>

</tasks>

<verification>
- [ ] `grep -c "0.121.0" .planning/research/cross-runtime-parity-research.md` returns ≥ 2
- [ ] `grep -c "logs_2.sqlite" .planning/research/cross-runtime-parity-research.md` returns ≥ 1
- [ ] `grep -c "exec_command_end" .planning/research/cross-runtime-parity-research.md` returns ≥ 1
- [ ] `grep -c "multi_agent_v2" .planning/research/cross-runtime-parity-research.md` returns ≥ 1
- [ ] `grep -n "claudeToCodexTools" bin/install.js` shows 2+ occurrences (definition + export)
- [ ] `node -e "const e = require('./bin/install.js'); for (const k of ['fileHash','generateManifest','writeManifest','saveLocalPatches','pruneRedundantPatches','reportLocalPatches','getGlobalDir','claudeToCodexTools','PATCHES_DIR_NAME','MANIFEST_NAME']) { if (!e[k]) { console.error('MISSING:', k); process.exit(1) } } console.log('OK')"` prints `OK` and exits 0
- [ ] `npx vitest run tests/unit/install.test.js` passes (existing tests + new Phase 60 module.exports surface test)
- [ ] `npm test` passes (no regressions)
</verification>

<narrowing_decisions>
## Narrowing Decisions

None. The plan implements the Phase 60 research recommendation verbatim for Wave 1 (G-1 audit doc bump + installer export surface). The research recommendation originally bundled the doc bump and the export prep into one plan (RESEARCH.md §"Wave 1 — G-1 Audit + Installer Export Preparation"); the orchestrator's `research_recommendation` split them into 60-01 (doc bump) + 60-02 (behavior matrix sidecar), which this plan preserves. The exports themselves are colocated with the doc bump (both mechanical, small, independent of the sidecar) — this is the orchestrator's shape. No CONTEXT.md obligation is narrowed, deferred, or declined.
</narrowing_decisions>

<success_criteria>
- All tasks completed
- All verification checks pass
- `cross-runtime-parity-research.md` reflects the 2026-04-21 audit reality (Codex 0.121.0, logs_2.sqlite, 17 event_msg types, 5 response_item types, multi_agent_v2 row)
- `bin/install.js` exports the ten primitives Plan 60-03/60-04/60-05/60-06 import
- Structural regression test guards against silent de-export
- Zero behavioral change to the installer; `npm test` passes unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/60-sensor-pipeline-codex-parity/60-01-SUMMARY.md`
</output>
