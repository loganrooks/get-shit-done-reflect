---
phase: 53-deep-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/automation.cjs
  - tests/unit/automation.test.js
  - tests/unit/install.test.js
autonomous: true

must_haves:
  truths:
    - "When no --context-pct flag is passed, resolve-level reads bridge file from /tmp/claude-ctx-*.json and uses used_pct for deferral"
    - "When bridge file is stale (>120s), resolve-level ignores it and falls back to skip-deferral"
    - "When --context-pct is explicitly passed, it takes precedence over bridge file (bridge file only fills in when contextPct is undefined)"
    - "resolve-level recognizes nyquist_validation as a valid feature with task_tool_dependent=true"
    - "All 405+ existing tests continue to pass"
  artifacts:
    - path: "get-shit-done/bin/lib/automation.cjs"
      provides: "Bridge file reading logic + nyquist_validation in FEATURE_CAPABILITY_MAP"
      contains: "claude-ctx-"
    - path: "tests/unit/automation.test.js"
      provides: "Bridge file reading tests"
      contains: "bridge"
    - path: "tests/unit/install.test.js"
      provides: "Updated FEATURE_CAPABILITY_MAP test with 5 features"
      contains: "nyquist_validation"
  key_links:
    - from: "get-shit-done/bin/lib/automation.cjs"
      to: "/tmp/claude-ctx-*.json"
      via: "fs.readdirSync glob + JSON.parse"
      pattern: "claude-ctx-.*\\.json"
    - from: "get-shit-done/bin/lib/automation.cjs"
      to: "FEATURE_CAPABILITY_MAP"
      via: "nyquist_validation entry"
      pattern: "nyquist_validation"
---

<objective>
Wire bridge file context data into automation deferral and expand FEATURE_CAPABILITY_MAP with nyquist_validation.

Purpose: INT-01 replaces wave-count estimation with real context usage data from the statusline bridge file. INT-08 ensures resolve-level recognizes the newly adopted nyquist_validation feature for level-based triggering.

Output: Updated automation.cjs with bridge file reading + new map entry, plus tests covering both changes.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-deep-integration/53-RESEARCH.md
@get-shit-done/bin/lib/automation.cjs
@tests/unit/automation.test.js
@tests/unit/install.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bridge file reading in resolve-level + nyquist_validation map entry</name>
  <files>get-shit-done/bin/lib/automation.cjs</files>
  <action>
Two changes to automation.cjs:

**Change 1 (INT-01): Bridge file reading in cmdAutomationResolveLevel.**
Insert a new block BEFORE the existing Step 3 (context-aware deferral, lines 62-70). The new block runs only when `options.contextPct === undefined` (i.e., no explicit --context-pct flag was passed):

1. Read `/tmp/` directory (via `os.tmpdir()`) and filter for files matching `claude-ctx-*.json` excluding any with `-warned` in the name.
2. Sort by mtime descending, pick the most recent.
3. Parse JSON: expects `{ remaining_percentage, used_pct, timestamp }`.
4. Staleness check: if `(now - bridgeData.timestamp) > 120` seconds, discard the file (do NOT use it).
5. If fresh and valid: set `options.contextPct = bridgeData.used_pct` and push `bridge_file: used_pct=${bridgeData.used_pct}%` to `reasons`.
6. Entire block wrapped in try/catch with silent failure -- bridge is best-effort.

Per locked decision: fallback chain is bridge file -> contextPct option -> skip deferral. The existing Step 3 logic handles steps 2-3 of the chain unchanged.

Note: `os` is already imported at line 8. `fs` and `path` are already imported at lines 5-6.

**Change 2 (INT-08): Add nyquist_validation to FEATURE_CAPABILITY_MAP.**
After the existing `ci_status` entry (line 29), add:

```javascript
nyquist_validation: {
  hook_dependent_above: null,   // workflow-triggered, not hook-based
  task_tool_dependent: true,    // spawns gsd-nyquist-auditor via Task()
},
```

Per research: only nyquist_validation needs a map entry. context_monitor is always-on infrastructure (per Claude's discretion decision). KB surfacing is inline workflow logic.
  </action>
  <verify>Run `npm test -- --run` and verify all 405+ tests pass. Then manually inspect automation.cjs to confirm both changes are present.</verify>
  <done>automation.cjs has bridge file reading block before context-aware deferral, FEATURE_CAPABILITY_MAP has 5 entries including nyquist_validation, all existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Tests for bridge file reading + updated FEATURE_CAPABILITY_MAP test</name>
  <files>tests/unit/automation.test.js, tests/unit/install.test.js</files>
  <action>
**In tests/unit/automation.test.js**, add a new `describe('bridge file context reading (INT-01)')` block after the existing `describe('context-aware deferral (AUTO-04)')` block (after line 241). Use the established `tmpdirTest` + `setupAndResolve` pattern. Tests:

1. **"bridge file populates contextPct when --context-pct not provided"**: Create a fresh bridge file in `os.tmpdir()` with `{ session_id: "test-session", remaining_percentage: 30, used_pct: 70, timestamp: Math.floor(Date.now()/1000) }`. Run resolve-level with level=3 and NO --context-pct flag. Expect effective=1 (deferred because 70% > 60% default threshold). Expect reasons to contain 'bridge_file'. Clean up the bridge file after test.

2. **"stale bridge file is ignored"**: Create a bridge file with `timestamp` set to 200 seconds ago. Run resolve-level with level=3 and NO --context-pct. Expect effective=3 (no deferral because bridge was stale). Clean up after.

3. **"explicit --context-pct takes precedence over bridge file"**: Create a fresh bridge file with used_pct=70. Run resolve-level with level=3 AND `--context-pct 40`. Expect effective=3 (40% < 60% threshold, so no deferral despite bridge saying 70%). The explicit flag prevents the bridge block from running (contextPct is already defined).

4. **"no bridge file: graceful fallback"**: Ensure no bridge files exist in tmpdir. Run resolve-level with level=3 and NO --context-pct. Expect effective=3 (no deferral, no crash).

IMPORTANT: Bridge files go to `os.tmpdir()` which is system-wide, not per-project. Tests must create uniquely named bridge files and clean up in a finally block to avoid polluting other tests. Use `claude-ctx-test-${randomSuffix}.json` naming.

**In tests/unit/install.test.js**, update the test at line 2713:
- Change test name from `'FEATURE_CAPABILITY_MAP exports all four automation features'` to `'FEATURE_CAPABILITY_MAP exports all five automation features'`
- Change `expectedFeatures` from `['signal_collection', 'reflection', 'health_check', 'ci_status']` to `['signal_collection', 'reflection', 'health_check', 'ci_status', 'nyquist_validation']`

Also update the `cmdAutomationResolveLevel accepts all FEATURE_CAPABILITY_MAP feature names` test (line 2779) to handle `nyquist_validation` the same way `ci_status` is handled -- it is valid in FEATURE_CAPABILITY_MAP as an automation capability but is not a separate top-level manifest feature. Add `nyquist_validation` to the same `if` clause as `ci_status` (line 2798).
  </action>
  <verify>Run `npm test -- --run` and verify all tests pass (should now be 409+ with the new bridge file tests). Specifically verify: `npx vitest run tests/unit/automation.test.js` and `npx vitest run tests/unit/install.test.js`.</verify>
  <done>4 new bridge file tests pass. FEATURE_CAPABILITY_MAP test updated to expect 5 features. All existing tests continue to pass.</done>
</task>

</tasks>

<verification>
1. `npm test -- --run` passes with 409+ tests (405 existing + 4 new bridge file tests)
2. `node get-shit-done/bin/gsd-tools.cjs automation resolve-level nyquist_validation --raw --cwd .` returns valid JSON with `feature: "nyquist_validation"`
3. Bridge file reading code in automation.cjs references `claude-ctx-` pattern
4. FEATURE_CAPABILITY_MAP has exactly 5 entries
</verification>

<success_criteria>
INT-01 satisfied: resolve-level reads bridge file when --context-pct is absent, with staleness check and graceful fallback.
INT-08 satisfied: FEATURE_CAPABILITY_MAP includes nyquist_validation with task_tool_dependent=true.
All 405+ existing tests continue to pass, plus 4 new bridge file tests.
</success_criteria>

<output>
After completion, create `.planning/phases/53-deep-integration/53-01-SUMMARY.md`
</output>
