---
phase: 53-deep-integration
plan: 04
type: execute
wave: 2
depends_on: ["53-01", "53-02", "53-03"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "All installed command stubs under .claude/commands/gsdr/ reference gsdr- prefixed workflows and agents"
    - "All source files under agents/ and get-shit-done/workflows/ use gsd- prefix (not gsdr-)"
    - "The installer replacePathsInContent() correctly rewrites gsd- to gsdr- for adopted files"
    - "npm test passes with all tests from plans 01-03 plus existing baseline"
    - "Every INT-0x requirement has at least one verifiable artifact"
  artifacts: []
  key_links:
    - from: ".claude/commands/gsdr/"
      to: "agents/ and get-shit-done/workflows/"
      via: "installer replacePathsInContent()"
      pattern: "gsdr-"
---

<objective>
Verify INT-06 namespace correctness (re-verification of Phase 52 work) and run cross-plan integration verification for all INT requirements.

Purpose: INT-06 was already satisfied by Phase 52's namespace rewriting. This plan re-verifies that all adopted files maintain correct namespace after the changes made in plans 01-03. It also serves as the phase-level integration check, ensuring all eight INT requirements are satisfied together.

Output: Verification results confirming all INT requirements are met.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-deep-integration/53-RESEARCH.md
@.planning/phases/53-deep-integration/53-01-SUMMARY.md
@.planning/phases/53-deep-integration/53-02-SUMMARY.md
@.planning/phases/53-deep-integration/53-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: INT-06 namespace re-verification</name>
  <files></files>
  <action>
Re-verify namespace correctness for all adopted files. No files are modified -- this is a verification-only task.

**Step 1: Install locally to regenerate .claude/ from source.**
```bash
node bin/install.js --local
```

**Step 2: Verify source files use gsd- prefix (not gsdr-).**
Check all source agent and workflow files:
```bash
# Source agents should use gsd- prefix
grep -l "gsdr-" agents/*.md 2>/dev/null || echo "PASS: No gsdr- in source agents"

# Source workflows should use gsd- prefix
grep -l "gsdr-" get-shit-done/workflows/*.md 2>/dev/null || echo "PASS: No gsdr- in source workflows"
```

**Step 3: Verify installed files use gsdr- prefix (after path rewriting).**
```bash
# Installed command stubs should reference gsdr- prefixed files
grep -rl "gsd-" .claude/commands/gsdr/ 2>/dev/null | grep -v "get-shit-done" || echo "PASS: Installed commands use gsdr- prefix"

# Installed agents should have gsdr- subagent_types
grep "subagent_type:" .claude/agents/gsdr-*.md 2>/dev/null | grep -v "gsdr-" || echo "PASS: Installed agents use gsdr- subagent types"
```

**Step 4: Verify the 4 adopted workflows from Phase 52 are installed correctly.**
Check that these exist under `.claude/commands/gsdr/`:
- gsdr:add-tests
- gsdr:cleanup
- gsdr:health
- gsdr:validate-phase

```bash
ls .claude/commands/gsdr/add-tests.md .claude/commands/gsdr/cleanup.md .claude/commands/gsdr/health.md .claude/commands/gsdr/validate-phase.md 2>/dev/null
```

If any verification fails, report the specific failure. If all pass, INT-06 is confirmed satisfied.
  </action>
  <verify>All four verification steps produce PASS results. No stale gsd- references in installed files. All gsdr: command stubs exist.</verify>
  <done>INT-06 re-verified: all adopted workflows and agents are namespace-correct with gsdr- prefix after installation.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-plan integration verification</name>
  <files></files>
  <action>
Run the full test suite and verify each INT requirement has a corresponding artifact or test:

**Step 1: Full test suite.**
```bash
npm test -- --run
```
Verify total test count is >= 411 (405 baseline + 4 bridge file + 6 health probe = 415 minimum expected, but verify actual).

**Step 2: INT requirement coverage matrix.**
Verify each requirement by checking its specific artifact:

- **INT-01**: `grep "claude-ctx-" get-shit-done/bin/lib/automation.cjs` (bridge file reading code exists)
- **INT-02**: `grep "VALIDATION.md" agents/gsd-artifact-sensor.md` (scan target added)
- **INT-03**: `grep "SGNL-04\|SGNL-05" agents/gsd-artifact-sensor.md` (detection rules exist)
- **INT-04**: `grep "surface_kb_knowledge" get-shit-done/workflows/discuss-phase.md` (KB surfacing step exists)
- **INT-05**: `grep "FORK_PROTECTED_DIRS" get-shit-done/workflows/cleanup.md` (protection guard exists)
- **INT-06**: Verified in Task 1 above (namespace correctness)
- **INT-07**: `grep "validation-coverage" get-shit-done/bin/gsd-tools.cjs` (CLI routing exists)
- **INT-08**: `grep "nyquist_validation" get-shit-done/bin/lib/automation.cjs` (map entry exists)

**Step 3: Verify phase success criteria from ROADMAP.md.**
Check each success criterion:
1. "When context usage exceeds threshold, automation deferral triggers using bridge file data" -- confirmed by INT-01 tests (bridge file populates contextPct)
2. "Nyquist validation gaps in VALIDATION.md are detected by the artifact sensor" -- confirmed by SGNL-04/SGNL-05 in sensor spec
3. "The discuss-phase workflow surfaces relevant KB knowledge" -- confirmed by surface_kb_knowledge step
4. "Running the cleanup workflow does NOT delete knowledge/deliberations/backlog" -- confirmed by FORK_PROTECTED_DIRS guard
5. "The automation framework's resolve-level recognizes newly adopted features" -- confirmed by nyquist_validation in FEATURE_CAPABILITY_MAP

If any verification fails, report the specific gap. If all pass, Phase 53 is complete.
  </action>
  <verify>All tests pass. All 8 INT requirements have verified artifacts. All 5 phase success criteria from ROADMAP.md are satisfied.</verify>
  <done>Phase 53 integration verified: all 8 INT requirements have corresponding artifacts, all tests pass, all 5 ROADMAP success criteria met.</done>
</task>

</tasks>

<verification>
1. `npm test -- --run` passes with 411+ tests
2. INT-01 through INT-08 each have at least one verified artifact
3. All 5 phase success criteria from ROADMAP.md are confirmed
4. No namespace regressions (INT-06 re-verified)
</verification>

<success_criteria>
INT-06 re-verified: namespace rewriting correct for all adopted files.
All 8 INT requirements verified with specific artifacts.
Full test suite passes.
Phase 53 success criteria from ROADMAP.md all confirmed TRUE.
</success_criteria>

<output>
After completion, create `.planning/phases/53-deep-integration/53-04-SUMMARY.md`
</output>
