---
phase: 53-deep-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/discuss-phase.md
  - get-shit-done/workflows/cleanup.md
autonomous: true

must_haves:
  truths:
    - "The discuss-phase workflow has a surface_kb_knowledge step between scout_codebase and analyze_phase"
    - "KB surfacing reads index.md, matches tags against phase keywords, reads top 5 entries, and builds kb_context"
    - "KB surfacing degrades gracefully when no KB exists (skips silently)"
    - "The cleanup workflow has an explicit FORK_PROTECTED_DIRS guard before any move operations"
    - "The cleanup workflow aborts if any move target matches a protected directory"
  artifacts:
    - path: "get-shit-done/workflows/discuss-phase.md"
      provides: "KB knowledge surfacing step"
      contains: "surface_kb_knowledge"
    - path: "get-shit-done/workflows/cleanup.md"
      provides: "Fork protection guard"
      contains: "FORK_PROTECTED_DIRS"
  key_links:
    - from: "get-shit-done/workflows/discuss-phase.md"
      to: ".planning/knowledge/index.md"
      via: "cat + grep for tag matches"
      pattern: "knowledge/index.md"
    - from: "get-shit-done/workflows/cleanup.md"
      to: ".planning/knowledge/"
      via: "FORK_PROTECTED_DIRS exclusion check"
      pattern: "FORK_PROTECTED_DIRS"
---

<objective>
Add KB knowledge surfacing to discuss-phase and explicit fork directory protection to the cleanup workflow.

Purpose: INT-04 ensures prior knowledge (lessons, signals, spikes) informs gray area identification during phase discussion, making the system genuinely learn from its own observations. INT-05 provides defense-in-depth protection for fork-specific directories during milestone cleanup.

Output: Updated discuss-phase.md with surface_kb_knowledge step, updated cleanup.md with FORK_PROTECTED_DIRS guard.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-deep-integration/53-RESEARCH.md
@get-shit-done/workflows/discuss-phase.md
@get-shit-done/workflows/cleanup.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: KB knowledge surfacing step in discuss-phase</name>
  <files>get-shit-done/workflows/discuss-phase.md</files>
  <action>
Insert a new step `<step name="surface_kb_knowledge">` between the existing `scout_codebase` step (ends at line 339) and the `analyze_phase` step (starts at line 341).

The new step should contain:

```markdown
<step name="surface_kb_knowledge">
Check for relevant knowledge base entries that might inform gray area identification and discussion.

**Step 1: Locate KB index**
```bash
KB_DIR=""
if [ -d ".planning/knowledge" ]; then
  KB_DIR=".planning/knowledge"
elif [ -d "$HOME/.gsd/knowledge" ]; then
  KB_DIR="$HOME/.gsd/knowledge"
fi
if [ -n "$KB_DIR" ]; then
  cat "$KB_DIR/index.md" 2>/dev/null || true
fi
```

If no KB directory exists or index.md is missing: skip this step silently. No workflow slowdown for projects without a knowledge base.

**Step 2: Extract phase keywords**
From the phase goal in ROADMAP.md (already loaded), identify 3-5 domain keywords relevant to this phase's scope. For example, a phase about "authentication" yields keywords like "auth", "login", "session", "JWT".

**Step 3: Scan index for matches**
Grep the index for entries whose tags overlap with phase keywords. Use LLM judgment for semantic relevance -- exact substring match is not required. Prioritize:
- Lessons (les-*) over signals (sig-*) -- lessons are refined knowledge
- Spikes (spk-*) with resolved status -- spikes answer uncertainty
- Signals with severity >= notable -- skip minor/trace signals

**Step 4: Read top matches (max 5)**
For the top 5 matching entries by relevance, read the full entry files from `$KB_DIR/signals/` or `$KB_DIR/spikes/` or `$KB_DIR/lessons/`.

**Step 5: Build internal kb_context**
If matches were found, build an internal context block:

```
<kb_context>
## Relevant KB Entries

- [entry-id]: One-line summary of finding relevant to this phase
- [entry-id]: One-line summary of finding relevant to this phase
</kb_context>
```

Cap at 3-5 items (per guardrail G3). Each item is a one-liner, not the full entry content. Total KB context should stay under ~500 tokens.

If no relevant entries found: set `<kb_context>` to empty. Do not slow down the workflow.

Store as internal variable for use in `analyze_phase` step alongside `codebase_context` and `prior_decisions`.
</step>
```

Then update the `analyze_phase` step description (line 342) to reference kb_context. Change:
```
Analyze the phase to identify gray areas worth discussing. **Use both `prior_decisions` and `codebase_context` to ground the analysis.**
```
To:
```
Analyze the phase to identify gray areas worth discussing. **Use `prior_decisions`, `codebase_context`, and `kb_context` to ground the analysis.**
```

IMPORTANT: Per DC-6, the fork's steering brief model (Working Model, Derived Constraints, Open Questions, Epistemic Guardrails sections in CONTEXT.md) must remain intact. KB surfacing adds alongside these sections, not replaces them. The kb_context is used internally to inform the discussion, not written as a separate CONTEXT.md section.

Per guardrail G3: start conservative with 3-5 items. Per pitfall 5 from research: check existence before reading, skip silently if KB does not exist.
  </action>
  <verify>Read the updated discuss-phase.md and verify: (1) surface_kb_knowledge step exists between scout_codebase and analyze_phase, (2) step reads index.md with fallback, (3) step handles missing KB gracefully, (4) analyze_phase references kb_context, (5) existing steering brief model sections are preserved (grep for "Working Model" and "Derived Constraints" in the file to confirm).</verify>
  <done>discuss-phase.md has surface_kb_knowledge step that reads KB index, matches tags, reads top 5 entries, builds internal kb_context capped at 3-5 items. Existing steering brief model untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fork directory protection guard in cleanup workflow</name>
  <files>get-shit-done/workflows/cleanup.md</files>
  <action>
Add an explicit `FORK_PROTECTED_DIRS` guard to cleanup.md. Per locked decision: use an explicit constant/list rather than relying on implicit safety-by-construction.

**Change 1: Add protection constant definition.**
Insert a new step `<step name="verify_fork_protection">` BEFORE the existing `archive_phases` step (which starts at line 102). The new step:

```markdown
<step name="verify_fork_protection">

Before archiving, verify no fork-protected directories will be affected.

**Fork-protected directories** (MUST NOT be moved, deleted, or archived):
- `.planning/knowledge/`
- `.planning/deliberations/`
- `.planning/backlog/`

```bash
# Verify none of the phase directories to be archived match protected paths
FORK_PROTECTED_DIRS=("knowledge" "deliberations" "backlog")

for dir in "${PHASE_DIRS_TO_ARCHIVE[@]}"; do
  dirname=$(basename "$dir")
  for protected in "${FORK_PROTECTED_DIRS[@]}"; do
    if [ "$dirname" = "$protected" ]; then
      echo "SAFETY: Cannot archive fork-protected directory: $dir"
      echo "Aborting cleanup."
      exit 1
    fi
  done
done
```

If any directory in the archive set matches a protected directory name, ABORT with a clear error message. This is a defense-in-depth guard -- the cleanup workflow currently only moves `.planning/phases/{dir}` directories, but this guard protects against future upstream changes that might widen the scope.

</step>
```

**Change 2: Reference protection in archive_phases step.**
At the beginning of the `archive_phases` step (line 102), add a note:

```markdown
**Pre-condition:** verify_fork_protection step confirmed no protected directories in the archive set.
```

Per pitfall 4 from research: use relative directory basenames (not absolute paths) so the guard works regardless of where the workflow file lives.
  </action>
  <verify>Read the updated cleanup.md and verify: (1) verify_fork_protection step exists before archive_phases, (2) FORK_PROTECTED_DIRS lists knowledge, deliberations, and backlog, (3) step aborts on match, (4) archive_phases references the pre-condition.</verify>
  <done>cleanup.md has explicit FORK_PROTECTED_DIRS guard with knowledge, deliberations, and backlog directories. Guard aborts cleanup if any protected directory would be archived. Defense-in-depth per locked decision.</done>
</task>

</tasks>

<verification>
1. discuss-phase.md has `surface_kb_knowledge` step between `scout_codebase` and `analyze_phase`
2. discuss-phase.md `analyze_phase` step references `kb_context`
3. cleanup.md has `verify_fork_protection` step with `FORK_PROTECTED_DIRS` listing 3 directories
4. cleanup.md protection guard aborts on match with clear error message
5. Neither file has structural issues (valid markdown, proper step nesting)
6. `npm test -- --run` passes (no test regressions from workflow file changes)
</verification>

<success_criteria>
INT-04 satisfied: discuss-phase surfaces relevant KB knowledge alongside codebase scouting results, capped at 3-5 items, with graceful degradation when KB does not exist.
INT-05 satisfied: cleanup workflow has explicit FORK_PROTECTED_DIRS guard that prevents archiving knowledge/, deliberations/, or backlog/ directories.
</success_criteria>

<output>
After completion, create `.planning/phases/53-deep-integration/53-03-SUMMARY.md`
</output>
