---
phase: 50-migration-test-hardening
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/core.cjs
  - get-shit-done/bin/gsd-tools-fork.test.js
autonomous: true

must_haves:
  truths:
    - "findProjectRoot resolves project root from subdirectories when .planning/ exists at an ancestor"
    - "findProjectRoot returns startDir when it already contains .planning/ (early-return fix from upstream)"
    - "findProjectRoot stops at HOME and does not walk into unrelated parent projects"
    - "Each extracted module produces valid CLI output for its command set"
    - "Commands run from repo roots and subdirectories keep project-local authority when .planning/ exists"
  artifacts:
    - path: "get-shit-done/bin/lib/core.cjs"
      provides: "findProjectRoot function adopted from upstream (C2 partial)"
      contains: "findProjectRoot"
    - path: "get-shit-done/bin/gsd-tools-fork.test.js"
      provides: "TST-06 module equivalence + project-root authority tests"
      contains: "TST-06"
  key_links:
    - from: "get-shit-done/bin/gsd-tools-fork.test.js"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "CLI subprocess + direct import"
      pattern: "findProjectRoot"
    - from: "get-shit-done/bin/lib/core.cjs"
      to: "get-shit-done/bin/gsd-tools.cjs"
      via: "module.exports"
      pattern: "findProjectRoot"
---

<objective>
Adopt findProjectRoot from upstream (C2 partial routing from Phase 48.1 drift ledger), then add module behavioral equivalence tests (TST-06) and project-root authority tests to the fork test suite.

Purpose: The fork currently lacks findProjectRoot (uses resolveWorktreeRoot for worktree cases only). Upstream's findProjectRoot (with the early-return fix from commit c16b874) handles subdirectory-to-root resolution, HOME boundary, and .planning/-detection heuristics. TST-06 verifies that extracted modules produce valid CLI output and that project-root authority is maintained from subdirectories.
Output: findProjectRoot in core.cjs + test cases in gsd-tools-fork.test.js covering TST-06 and C2 partial.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-migration-test-hardening/50-RESEARCH.md
@.planning/phases/48.1-post-audit-upstream-drift-retriage-and-roadmap-reconciliation/UPSTREAM-DRIFT-LEDGER.md
@get-shit-done/bin/lib/core.cjs
@get-shit-done/bin/gsd-tools-fork.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt findProjectRoot from upstream into core.cjs (C2 partial)</name>
  <files>get-shit-done/bin/lib/core.cjs</files>
  <action>
Add the `findProjectRoot` function to core.cjs, adopted from upstream with the early-return fix (commit c16b874).

Place the function after `resolveWorktreeRoot` (after line 223) since they are related root-resolution functions. Use the `module.exports.funcName` extension pattern (consistent with Phase 46 convention -- append after the main exports block, do not modify the upstream exports block).

Implementation (adapted from upstream `upstream/main:get-shit-done/bin/lib/core.cjs`):

```javascript
/**
 * Find the project root by walking up from startDir looking for .planning/.
 * Adopted from upstream (C2 partial, commit c16b874) with early-return fix.
 *
 * Key behaviors:
 * - If startDir already has .planning/, returns startDir immediately (early-return fix)
 * - Walks up directories looking for parent with .planning/
 * - Respects sub_repos config and multiRepo flag
 * - Never walks above HOME directory
 * - Falls back to .git heuristic when no explicit config
 */
function findProjectRoot(startDir) {
  // ... full implementation from upstream
}
```

Copy the full implementation from upstream, including the `isInsideGitRepo` inner function and the early-return check. The function uses `fs`, `path`, and `require('os').homedir()` -- all already available in core.cjs.

Add to the module.exports using the fork extension pattern:
```javascript
module.exports.findProjectRoot = findProjectRoot;
```

Do NOT modify the existing upstream exports block. Append after it, consistent with how `loadManifest`, `loadProjectConfig`, `atomicWriteJson`, and `parseIncludeFlag` were added in Phase 46.

Important: The upstream implementation uses `fs.existsSync`, `fs.statSync`, `fs.readFileSync`, and `JSON.parse` -- all available in core.cjs scope. Verify the `path` and `os` modules are already required at the top of the file.
  </action>
  <verify>Run `node -e "const c = require('./get-shit-done/bin/lib/core.cjs'); console.log(typeof c.findProjectRoot)"` and confirm it prints `function`. Then run all existing tests to ensure no regressions: `node --test get-shit-done/bin/gsd-tools.test.js get-shit-done/bin/gsd-tools-fork.test.js && npm test`.</verify>
  <done>findProjectRoot function exists in core.cjs exports, adopted from upstream with early-return fix. All existing tests pass (zero behavioral changes to existing code).</done>
</task>

<task type="auto">
  <name>Task 2: TST-06 module behavioral equivalence and project-root authority tests</name>
  <files>get-shit-done/bin/gsd-tools-fork.test.js</files>
  <action>
Add two new describe blocks to gsd-tools-fork.test.js.

**Block 1: `describe('TST-06: module behavioral equivalence - CLI output validation')`**

Per research accept-risk: the pre-extraction monolith no longer exists, so test that the router produces valid JSON output for each command category. Use the existing `runGsdTools` helper pattern (already in gsd-tools.test.js -- replicate or import the pattern in gsd-tools-fork.test.js).

Create a helper at the top of the file if not already present:
```javascript
function runGsdToolsFromDir(args, cwd) {
  try {
    const result = execSync(
      `node "${path.join(__dirname, 'gsd-tools.cjs')}" ${args}`,
      { cwd, encoding: 'utf-8', timeout: 10000, env: { ...process.env, HOME: cwd } }
    );
    return { success: true, output: result.trim() };
  } catch (e) {
    return { success: false, error: e.stderr || e.message, output: (e.stdout || '').trim() };
  }
}
```

Create tests for each major module category that verify valid CLI output:

1. **`test('manifest commands produce valid JSON output via router')`**
   - Create temp project with manifest + config (createTempProject + write manifest).
   - Run `manifest apply-migration --raw` via the router.
   - Assert: output parses as valid JSON, has expected keys (`total_changes`, `changes`).

2. **`test('frontmatter validate produces structured output via router')`**
   - Create temp dir with a plan file containing valid frontmatter.
   - Run `frontmatter validate <file> --schema plan` via the router.
   - Assert: output parses as valid JSON, has expected keys (`valid`, `missing`, `present`).

3. **`test('config get produces output via router')`**
   - Create temp project with config.json.
   - Run `config get mode` via the router.
   - Assert: success, output is non-empty.

4. **`test('init plan-phase produces JSON output via router')`**
   - Create temp project with ROADMAP.md containing a phase entry.
   - Run `init plan-phase "01-test"` via the router.
   - Assert: output parses as valid JSON, has expected keys (`phase_found`, `phase_dir`).

**Block 2: `describe('TST-06: project-root authority from subdirectories')`**

Test that findProjectRoot correctly resolves roots from subdirectories.

5. **`test('findProjectRoot returns startDir when .planning/ exists at startDir')`**
   - Import findProjectRoot from core.cjs: `const { findProjectRoot } = require('./lib/core.cjs');`
   - Create temp dir with `.planning/` in it.
   - Call `findProjectRoot(tmpDir)`.
   - Assert: returns tmpDir (early-return fix).

6. **`test('findProjectRoot resolves parent when called from subdirectory')`**
   - Create temp dir structure: `project/.planning/`, `project/.git/`, `project/src/components/`.
   - Call `findProjectRoot(path.join(tmpDir, 'project', 'src', 'components'))`.
   - Assert: returns `path.join(tmpDir, 'project')`.

7. **`test('findProjectRoot stops at HOME boundary')`**
   - Create temp dir as HOME: `fakehome/.planning/` does NOT exist, `fakehome/project/src/` exists.
   - Set `os.homedir` return or use env to mock HOME.
   - Call findProjectRoot from `fakehome/project/src/` where neither project nor fakehome has `.planning/`.
   - Assert: does NOT return paths above HOME. Returns the startDir or a reasonable fallback.

8. **`test('findProjectRoot does not walk into unrelated parent .planning/')`**
   - Create: `workspace/.planning/` (parent project), `workspace/other-project/src/` (no .planning/, no .git).
   - Call findProjectRoot from `workspace/other-project/src/`.
   - Assert: Does NOT return `workspace/` since `other-project` is not inside workspace's git repo and not in sub_repos config.

For tests 5-8: Use the node:test pattern (test/beforeEach/afterEach with temp dirs) consistent with gsd-tools-fork.test.js conventions.
  </action>
  <verify>Run `node --test get-shit-done/bin/gsd-tools-fork.test.js` and confirm all 8 new tests pass. Then run the full suite: `node --test get-shit-done/bin/gsd-tools.test.js get-shit-done/bin/gsd-tools-fork.test.js && npm test`.</verify>
  <done>4 module equivalence tests verify valid CLI output for manifest, frontmatter, config, and init command categories. 4 project-root authority tests verify findProjectRoot early-return, subdirectory resolution, HOME boundary, and unrelated-parent protection.</done>
</task>

</tasks>

<verification>
1. `node -e "const c = require('./get-shit-done/bin/lib/core.cjs'); console.log(typeof c.findProjectRoot)"` prints `function`
2. `node --test get-shit-done/bin/gsd-tools-fork.test.js` passes with all existing + 8 new tests
3. `node --test get-shit-done/bin/gsd-tools.test.js` still passes (no regressions)
4. `npm test` still passes (no vitest regressions)
5. findProjectRoot handles early-return, subdirectory walk-up, HOME boundary, and unrelated parent correctly
</verification>

<success_criteria>
- C2 partial: findProjectRoot adopted from upstream with early-return fix, exported from core.cjs
- TST-06 equivalence: Router produces valid output for manifest, frontmatter, config, and init commands
- TST-06 authority: findProjectRoot resolves correctly from subdirectories, respects HOME boundary
- All 543+ tests (193 node:test + 350 vitest) still pass
</success_criteria>

<output>
After completion, create `.planning/phases/50-migration-test-hardening/50-05-SUMMARY.md`
</output>
