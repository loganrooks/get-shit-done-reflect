---
phase: 50-migration-test-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/install.test.js
autonomous: true

must_haves:
  truths:
    - "Running the full installer twice on the same HOME produces identical installed output"
    - "Installer re-run does not duplicate settings.json hook entries or create extra files"
    - "manifest apply-migration output is consumed by automation resolve-level (integration depth verified)"
  artifacts:
    - path: "tests/unit/install.test.js"
      provides: "TST-03 installer re-run idempotency + TST-08 integration depth tests"
      contains: "TST-03"
  key_links:
    - from: "tests/unit/install.test.js"
      to: "bin/install.js"
      via: "subprocess install twice + file comparison"
      pattern: "execSync.*install\\.js"
---

<objective>
Add installer re-run idempotency tests (TST-03) and integration depth tests (TST-08) to the vitest installer test suite.

Purpose: TST-03 verifies that running the full installer twice over the same installation produces identical output (no duplicated hooks, no extra files, no settings drift). Existing idempotency tests cover individual operations (KB symlink, settings section) but not the full end-to-end installer. TST-08 verifies that adopted features connect to the fork pipeline by checking that manifest apply-migration output structure is consumable by automation resolve-level.
Output: New test cases in tests/unit/install.test.js covering TST-03 and TST-08.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-migration-test-hardening/50-RESEARCH.md
@tests/unit/install.test.js
@bin/install.js
@get-shit-done/bin/lib/automation.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: TST-03 full installer re-run idempotency</name>
  <files>tests/unit/install.test.js</files>
  <action>
Add a new `describe('TST-03: full installer re-run idempotency')` block in the installer integration section of install.test.js (near the existing `describe('upgrade path: gsd to gsdr namespace')` block around line 2380).

Create these `tmpdirTest` tests:

1. **`tmpdirTest('TST-03: two consecutive --claude --global installs produce identical file trees')`**
   - Run `node "${installScript}" --claude --global` with `HOME=${tmpdir}`.
   - Recursively collect all file paths under `${tmpdir}/.claude/` into a sorted array. For each file, record `{ path, size, content_hash }` (use a simple hash: `content.length + '-' + content.slice(0, 100)` is sufficient -- no need for crypto).
   - Run the same install command again.
   - Collect the same file inventory.
   - Assert: File count is identical. File paths are identical. File sizes are identical. No new files appeared. No files disappeared.
   - Important: Exclude any timestamped files (e.g., `knowledge.backup-*` directories) from comparison since backup timestamps will differ.

2. **`tmpdirTest('TST-03: installer re-run does not duplicate settings.json hook entries')`**
   - Run install once, read `${tmpdir}/.claude/settings.json`, count hook entries in the `hooks` object.
   - Run install again, read settings.json again.
   - Assert: Hook entry count is identical. No duplicate hook command strings. The hooks object is structurally identical (deep equal).

3. **`tmpdirTest('TST-03: installer re-run does not duplicate agent files')`**
   - Run install once, list agent files in `${tmpdir}/.claude/agents/`.
   - Run install again, list agent files again.
   - Assert: Same file set. No `*-copy.md` or `* (1).md` duplicates.

For all tests: Use `{ env: { ...process.env, HOME: tmpdir }, cwd: tmpdir, stdio: ['pipe', 'pipe', 'pipe'], timeout: 15000 }` for the execSync calls, matching the existing pattern.
  </action>
  <verify>Run `npm test -- --reporter=verbose tests/unit/install.test.js` and confirm all 3 new TST-03 tests pass.</verify>
  <done>3 installer re-run idempotency tests verifying file tree identity, hook entry non-duplication, and agent file set stability across consecutive installs.</done>
</task>

<task type="auto">
  <name>Task 2: TST-08 integration depth tests</name>
  <files>tests/unit/install.test.js</files>
  <action>
Add a new `describe('TST-08: integration depth - adopted features connect to fork pipeline')` block after the TST-03 block.

TST-08 verifies that adopted features are not isolated patches but connect to the fork's signal/automation pipeline. The research specifically recommends testing that "manifest apply-migration output is consumed by automation resolve-level."

Create these tests:

1. **`it('manifest apply-migration JSON output has structure consumable by automation pipeline')`**
   - This is a structural integration test, not a subprocess test. Import `FEATURE_CAPABILITY_MAP` from `get-shit-done/bin/lib/automation.cjs`.
   - Verify that `FEATURE_CAPABILITY_MAP` includes a mapping for the migration-related features (e.g., `manifest` or `apply-migration` commands).
   - This confirms that the automation framework knows about manifest capabilities for level-based triggering.

2. **`tmpdirTest('installed feature-manifest.json contains migrations array for automation consumption')`**
   - Run installer. Read the installed `feature-manifest.json` from `${tmpdir}/.claude/get-shit-done-reflect/feature-manifest.json`.
   - Parse JSON. Assert: `manifest_version` is >= 2, `migrations` is an array with at least one entry, each migration entry has `type` and `version` fields.
   - This verifies the manifest structure that downstream automation uses.

3. **`tmpdirTest('installed config.json defaults include automation-relevant fields')`**
   - Run installer. Check if a default config is created or if the manifest features define the config shape.
   - Verify the feature-manifest.json `features` section includes `signal_lifecycle` and `automation` features with their config keys.
   - This ensures the migration system's feature definitions connect to the automation pipeline's expectations.

4. **`it('FEATURE_CAPABILITY_MAP covers all fork module command categories')`**
   - Import FEATURE_CAPABILITY_MAP from automation.cjs.
   - Verify it maps capabilities for the major command categories: sensors, backlog, manifest, automation, health-probe.
   - This is a structural assertion that the automation framework's capability awareness spans the full fork module set.

For tests 1 and 4: These are pure unit tests (no tmpdir needed). Use regular `it()` blocks.
For tests 2 and 3: Use `tmpdirTest` with installer subprocess.
  </action>
  <verify>Run `npm test -- --reporter=verbose tests/unit/install.test.js` and confirm all 4 new TST-08 tests pass.</verify>
  <done>4 integration depth tests verifying automation pipeline awareness of manifest capabilities, installed manifest structure, config defaults shape, and FEATURE_CAPABILITY_MAP coverage across all fork module categories.</done>
</task>

</tasks>

<verification>
1. `npm test -- tests/unit/install.test.js` passes with all existing + 7 new tests
2. TST-03 tests prove installer is safe to run multiple times without drift
3. TST-08 tests prove adopted features connect to automation pipeline
4. No existing tests broken
</verification>

<success_criteria>
- TST-03: Two consecutive installs produce identical file trees, hook entries, and agent files
- TST-08: FEATURE_CAPABILITY_MAP covers fork modules, installed manifest has migrations array, feature definitions include automation-relevant config keys
- All 196+ install tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/50-migration-test-hardening/50-04-SUMMARY.md`
</output>
