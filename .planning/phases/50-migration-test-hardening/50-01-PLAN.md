---
phase: 50-migration-test-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/install.test.js
autonomous: true

must_haves:
  truths:
    - "A full-corpus scan of ALL installed file types finds zero stale gsd:/gsd-/get-shit-done/ references"
    - "Snapshot regression tests catch namespace rewriting changes on a representative file corpus"
  artifacts:
    - path: "tests/unit/install.test.js"
      provides: "TST-01 full-corpus namespace scan + TST-09 snapshot regression tests"
      contains: "TST-01"
  key_links:
    - from: "tests/unit/install.test.js"
      to: "bin/install.js"
      via: "subprocess install + file scan"
      pattern: "execSync.*install\\.js"
---

<objective>
Add full-corpus namespace scan (TST-01) and snapshot regression tests for namespace rewriting (TST-09) to the vitest installer test suite.

Purpose: Existing namespace tests only cover specific file categories (.md agents, .md workflows, .js hooks). TST-01 ensures ALL installed file types (.cjs, .json, .sh, .toml, settings) are scanned for stale references. TST-09 adds vitest snapshots capturing replacePathsInContent output so that any future rewriting changes are caught as snapshot diffs.
Output: New test cases in tests/unit/install.test.js covering TST-01 and TST-09.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-migration-test-hardening/50-RESEARCH.md
@tests/unit/install.test.js
@bin/install.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: TST-01 full-corpus namespace scan test</name>
  <files>tests/unit/install.test.js</files>
  <action>
Add a new test inside the existing `describe('installed content namespace verification')` block (around line 2479).

Create a `tmpdirTest` named `'TST-01: full-corpus scan of all installed files finds zero stale namespace references'` that:

1. Runs `node "${installScript}" --claude --global` in the tmpdir (same pattern as existing tests at line 2482).
2. Recursively walks ALL files under `${tmpdir}/.claude/` (not just .md or .js -- include .cjs, .json, .sh, .toml, and any other file types).
3. For each file, reads content and checks for three stale patterns:
   - `/gsd:` command prefix (regex: `/\/gsd:/g`) -- should be `/gsdr:` instead
   - `gsd-` prefix that is NOT `gsd-tools` and NOT `gsd-reflect` (regex: `/\bgsd-(?!tools|reflect)\w+/g`)
   - `get-shit-done/` directory path NOT followed by `reflect` (regex: `/get-shit-done\/(?!reflect)/g`)
4. Collects all violations into an array of `{ file, type, matches }` objects.
5. Asserts the violations array is empty, with JSON.stringify of violations as the assertion message for debugging.
6. Also verify the scan actually checked files (assert file count > 0) to prevent false-green from empty install.

Important: Skip binary files (.png, .jpg, .ico). Only scan text files. The existing namespace tests at lines 2482-2578 cover subsets; this test covers the full corpus including lib/*.cjs, hooks/*.js, settings.json, and any .toml/.sh files.
  </action>
  <verify>Run `npm test -- --reporter=verbose tests/unit/install.test.js` and confirm the new TST-01 test passes. The test should scan 50+ files across all installed categories.</verify>
  <done>Full-corpus namespace scan test exists and passes, covering ALL installed file types including .cjs, .json, .sh, .toml -- not just .md and .js.</done>
</task>

<task type="auto">
  <name>Task 2: TST-09 snapshot regression tests for namespace rewriting</name>
  <files>tests/unit/install.test.js</files>
  <action>
Add a new `describe('TST-09: namespace rewriting snapshot regression')` block near the existing `describe('gsdr namespace rewriting')` block (around line 264).

Create snapshot tests that capture `replacePathsInContent` output for a representative corpus of input patterns. Import `replacePathsInContent` from `bin/install.js` (already imported at the top of the file).

Create these snapshot test cases using `toMatchInlineSnapshot()`:

1. **Agent file corpus** -- Input containing: `subagent_type="gsd-executor"`, `@./.claude/get-shit-done/workflows/execute-plan.md`, `/gsd:execute-phase`, `gsd-tools.cjs`. Call `replacePathsInContent(input, './.claude/')` and snapshot the output.

2. **Workflow file corpus** -- Input containing: `@~/.claude/get-shit-done/references/agent-protocol.md`, `node ~/.claude/get-shit-done/bin/gsd-tools.cjs`, `/gsd:plan-phase`, `/gsd:research-phase`. Call `replacePathsInContent(input, './.claude/')` and snapshot.

3. **Hook file corpus** -- Input containing: `path.join(homedir, '.claude', 'get-shit-done', 'bin', 'gsd-tools.cjs')`, `'get-shit-done'` (quoted string in path.join). Call `replacePathsInContent(input, './.claude/')` and snapshot.

4. **False-positive boundary corpus** -- Input that must NOT be rewritten: `gsd-tools.cjs` (tool name), `gsd-tools-fork.test.js`, `get-shit-done-reflect` (already correct namespace). Snapshot to confirm these are preserved unchanged.

5. **Mixed content corpus** -- A realistic agent file snippet with interleaved rewritable and non-rewritable patterns. Snapshot the full transformation.

Use `toMatchInlineSnapshot()` (not `toMatchSnapshot()`) so the expected output is visible in the test file itself. On first run, vitest will populate the inline snapshots automatically -- let it do so via `npm test -- -u tests/unit/install.test.js`.

Each test should be a plain `it(...)` inside the describe block (not tmpdirTest -- no filesystem needed for these unit tests).
  </action>
  <verify>Run `npm test -- --reporter=verbose tests/unit/install.test.js` and confirm all TST-09 snapshot tests pass. The inline snapshots should be populated in the test file.</verify>
  <done>5 snapshot regression tests exist capturing replacePathsInContent behavior for agent, workflow, hook, false-positive boundary, and mixed content corpora. Future namespace rewriting changes will show as snapshot diffs.</done>
</task>

</tasks>

<verification>
1. `npm test -- tests/unit/install.test.js` passes with all existing + new tests
2. TST-01 test scans 50+ installed files and finds zero stale references
3. TST-09 snapshots are populated and match expected rewriting behavior
4. No existing tests broken
</verification>

<success_criteria>
- TST-01: Full-corpus scan covers ALL installed file types, not just .md/.js subsets
- TST-09: Inline snapshots capture representative namespace rewriting transformations
- All 350+ vitest tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/50-migration-test-hardening/50-01-SUMMARY.md`
</output>
