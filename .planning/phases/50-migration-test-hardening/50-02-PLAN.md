---
phase: 50-migration-test-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/gsd-tools.test.js
autonomous: true

must_haves:
  truths:
    - "Running apply-migration N times (N>=5) on the same config produces identical output after the first run"
    - "Config file is byte-identical between run 2 and run N (no metadata accumulation)"
    - "coerceValue handles null, undefined, empty string, and NaN-producing inputs without crashing"
    - "Type coercion edge cases produce expected results or graceful passthrough"
  artifacts:
    - path: "get-shit-done/bin/gsd-tools.test.js"
      provides: "TST-02 N-run idempotency + TST-07 coercion edge case tests"
      contains: "TST-02"
  key_links:
    - from: "get-shit-done/bin/gsd-tools.test.js"
      to: "get-shit-done/bin/lib/manifest.cjs"
      via: "CLI subprocess via runGsdTools"
      pattern: "runGsdTools.*manifest apply-migration"
---

<objective>
Add N-run idempotency tests for apply-migration (TST-02) and type coercion edge case tests (TST-07) to the node:test CLI test suite.

Purpose: Phase 49 added 2-run idempotency but the research identified that state accumulation bugs only manifest on run 3+. TST-02 extends to N=5 runs with byte-identical config comparison. TST-07 tests coerceValue edge cases (null, undefined, empty string, NaN strings, boolean-to-number) that have a known signal (SIG-260222-002).
Output: New test cases in gsd-tools.test.js covering TST-02 and TST-07.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-migration-test-hardening/50-RESEARCH.md
@get-shit-done/bin/gsd-tools.test.js
@get-shit-done/bin/lib/manifest.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: TST-02 N-run idempotency tests</name>
  <files>get-shit-done/bin/gsd-tools.test.js</files>
  <action>
Add a new `describe('manifest apply-migration N-run idempotency (TST-02)')` block after the existing `describe('manifest apply-migration rename migrations')` block (after line 2870).

Create these tests using the existing `createManifestTestEnv`, `runGsdTools`, `createTempProject`, and `cleanup` helpers:

1. **`test('apply-migration is idempotent over N=5 runs with rename migration')`**
   - Set up env with `{ mode: 'yolo', depth: 'comprehensive', manifest_version: 1 }`, version 2, and `[renameMigration()]`.
   - Run 1: assert success, assert total_changes > 0.
   - Capture config after run 1 via `fs.readFileSync(path.join(tmpDir, '.planning', 'config.json'), 'utf-8')`.
   - Runs 2-5: for each run, assert success, assert total_changes === 0, assert config is byte-identical to post-run-1 config.
   - This catches metadata accumulation (timestamps, version counters) that the 2-run test misses.

2. **`test('apply-migration is idempotent over N=5 runs with full feature reconciliation')`**
   - Set up env with `healthCheckFeature()` and a config missing some health_check fields (so feature reconciliation adds them on run 1).
   - Use manifest_version 2, no explicit migrations array (uses default from healthCheckFeature).
   - Run 1: assert total_changes > 0 (missing fields added).
   - Capture config after run 1.
   - Runs 2-5: assert total_changes === 0 and config byte-identical.

3. **`test('apply-migration is idempotent over N=5 runs on multi-version upgrade')`**
   - Read real production manifest from disk (same pattern as the existing CFG-05 test at line 2887).
   - Start with v1.14-era config: `{ mode: 'yolo', depth: 'comprehensive', parallelization: true, commit_docs: true, model_profile: 'balanced', workflow: { research: true, plan_checker: true, verifier: true } }`.
   - Run 1: applies all migrations + feature reconciliation.
   - Capture config.
   - Runs 2-5: assert zero changes and byte-identical config.
  </action>
  <verify>Run `node --test get-shit-done/bin/gsd-tools.test.js` and confirm all 3 new TST-02 tests pass. Total should be 196 tests.</verify>
  <done>3 N-run idempotency tests verify byte-identical config stability over 5 runs for rename migration, feature reconciliation, and multi-version upgrade scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: TST-07 config type coercion edge case tests</name>
  <files>get-shit-done/bin/gsd-tools.test.js</files>
  <action>
Add a new `describe('manifest apply-migration type coercion edge cases (TST-07)')` block after the TST-02 block.

These tests exercise the `coerceValue` function (manifest.cjs line 37) through the CLI interface by setting up configs with edge-case values and running apply-migration.

Create these tests:

1. **`test('null config value does not crash apply-migration (TST-07)')`**
   - Config: `{ manifest_version: 1, health_check: { frequency: 'milestone-only', stale_threshold_days: null, blocking_checks: false } }`
   - Run apply-migration --raw. Assert success (no crash). The null value should pass through coerceValue unchanged (it returns the value as-is when no type conversion matches).

2. **`test('string "true"/"false" coerced to boolean in boolean fields')`**
   - Config: `{ manifest_version: 1, health_check: { frequency: 'milestone-only', stale_threshold_days: 30, blocking_checks: "false" } }`
   - Run apply-migration --raw. Assert success. Read config and verify `blocking_checks` is boolean `false` (not string "false").
   - Also test with `"true"` -> boolean `true`.

3. **`test('empty string in number field is not coerced to 0')`**
   - Config: `{ manifest_version: 1, health_check: { frequency: 'milestone-only', stale_threshold_days: "", blocking_checks: false } }`
   - Run apply-migration --raw. Assert success. The empty string should NOT become 0 (coerceValue checks `trimmed !== ''` before number conversion). Verify the field value is still "" or has been set to the default.

4. **`test('NaN-producing string in number field passes through unchanged')`**
   - Config: `{ manifest_version: 1, health_check: { frequency: 'milestone-only', stale_threshold_days: "not-a-number", blocking_checks: false } }`
   - Run apply-migration --raw. Assert success (no crash). The value "not-a-number" fails `!isNaN(trimmed)` so coerceValue returns it as-is.

5. **`test('numeric string in number field is coerced to number')`**
   - Config: `{ manifest_version: 1, health_check: { frequency: 'milestone-only', stale_threshold_days: "30", blocking_checks: false } }`
   - Run apply-migration --raw. Assert success. Read config and verify `stale_threshold_days` is number `30` (not string "30").

For all tests, use `healthCheckFeature()` for the manifest features and `createManifestTestEnv` to set up the environment. Each test uses its own `tmpDir` via `createTempProject()`/`cleanup()`.
  </action>
  <verify>Run `node --test get-shit-done/bin/gsd-tools.test.js` and confirm all 5 new TST-07 tests pass. Total should be 201 tests.</verify>
  <done>5 type coercion edge case tests covering null, string-boolean, empty string, NaN-producing string, and numeric string inputs through the CLI migration path.</done>
</task>

</tasks>

<verification>
1. `node --test get-shit-done/bin/gsd-tools.test.js` passes with all existing + 8 new tests
2. TST-02 tests verify byte-identical configs across runs 2-5 (not just 2-run convergence)
3. TST-07 tests exercise all documented coerceValue edge cases
4. No existing tests broken
</verification>

<success_criteria>
- TST-02: N=5 idempotency verified for rename, feature reconciliation, and multi-version upgrade
- TST-07: null, string-boolean, empty-string, NaN-string, numeric-string all handled correctly
- All 193+ node:test tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/50-migration-test-hardening/50-02-SUMMARY.md`
</output>
