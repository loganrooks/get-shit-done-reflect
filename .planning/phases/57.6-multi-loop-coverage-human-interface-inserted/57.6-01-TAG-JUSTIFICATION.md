# Phase 57.6 Tag Justification Matrix

Per G-1 enforcement: each `serves_loop` addition is justified against the loop's `theory_of_change` text in `get-shit-done/bin/lib/measurement/registry.cjs` `LOOP_DEFINITIONS` (lines 12-83 at execution time).

This plan adds 11 loop memberships across 9 existing extractors.

| Extractor | File:Line | Loop Added | theory_of_change Fragment | Justification (why this extractor discriminates) |
|---|---|---|---|---|
| `runtime_session_identity` | `runtime.cjs:172` | `agent_performance` | "Relate execution outcomes to model, profile, reasoning level, and runtime metadata" | `value.model` and derived `value.profile` already carry the model/profile provenance axis this loop needs for attribution. |
| `runtime_session_identity` | `runtime.cjs:172` | `cross_session_patterns` | "Compare sessions across time while preserving era boundaries and coverage differences" | `value.session_id` plus `value.era_boundary` provide the clustering and era-preservation fields needed for cross-session comparison. |
| `claude_settings_at_start` | `runtime.cjs:211` | `agent_performance` | "Relate execution outcomes to model, profile, reasoning level, and runtime metadata" | `showThinkingSummaries` and `effective_effort_level` are the runtime-settings context and reasoning-level proxy this loop needs. |
| `runtime_era_boundary_registry` | `runtime.cjs:452` | `cross_session_patterns` | "Compare sessions across time while preserving era boundaries" | The extractor's entire payload is an era partition/comparability registry, which is the loop's named era-boundary axis. |
| `session_meta_provenance` | `derived.cjs:97` | `signal_quality` | "Evaluate whether the system is producing timely, trustworthy, and actionable signals" | `scope`, `lifecycle`, `refresh_policy`, and `write_path` distinguish derived `/insights` provenance from direct observation, which is exactly the loop's provenance discriminator. |
| `session_jsonl_coverage_audit` | `derived.cjs:162` | `cross_session_patterns` | "Compare sessions across time while preserving era boundaries and coverage differences" | The matched/unmatched/missing breakdown is the coverage-stratification evidence required to separate real trends from corpus drift. |
| `automation_signal_yield` | `gsdr.cjs:141` | `signal_quality` | "Evaluate whether the system is producing timely, trustworthy, and actionable signals" | The extractor already measures persisted `last_signal_count` totals by sensor, which is the loop's signal-yield discriminator. |
| `kb_signal_stats` | `gsdr.cjs:291` | `signal_quality` | "Evaluate whether the system is producing timely, trustworthy, and actionable signals" | Freshness plus signal totals/breakdowns show whether the KB surface is timely and trustworthy instead of silently stale. |
| `codex_runtime_metadata` | `codex.cjs:206` | `agent_performance` | "Relate execution outcomes to model, profile, reasoning level, and runtime metadata" | `reasoning_effort`, `sandbox_policy`, `model`, and derived `profile` provide the Codex runtime stratification variables for agent-performance attribution. |
| `codex_runtime_metadata` | `codex.cjs:206` | `cross_runtime_comparison` | "Compare Claude, Codex, and project-local measurement surfaces without collapsing runtime asymmetry" | This is the Codex runtime-provenance surface itself, so tagging it makes the cross-runtime loop compare real Codex metadata rather than only presence markers. |
| `runtime_source_presence` | `registry.cjs:326` | `cross_runtime_comparison` | "Compare Claude, Codex, and project-local measurement surfaces without collapsing runtime asymmetry" | The extractor exposes per-runtime source presence plus a shared symmetry marker, which is the loop's availability-marker axis. |

## Counter-Check (G-1 Rigor)

For every row above, the answer to "does this extractor's existing payload already contain data that discriminates between competing interpretations relevant to the tagged loop?" is yes.

- `runtime_session_identity`, `claude_settings_at_start`, and `codex_runtime_metadata` discriminate attribution questions by model/profile/runtime-setting differences instead of leaving them as guesses.
- `session_meta_provenance`, `automation_signal_yield`, and `kb_signal_stats` discriminate trustworthy signal surfaces from stale or weakly-grounded ones.
- `runtime_era_boundary_registry`, `session_jsonl_coverage_audit`, and `runtime_source_presence` discriminate real cross-session or cross-runtime patterns from artifacts caused by boundary, coverage, or availability differences.

No tag was added to inflate loop coverage. Every added membership points at an already-emitted payload axis that the target loop's theory of change explicitly needs.

## Registry Parity Verification

Post-edit expectation:

- `node ./get-shit-done/bin/gsd-tools.cjs measurement rebuild`
- `node -e "const { buildRegistry } = require('./get-shit-done/bin/lib/measurement/registry.cjs'); console.log(buildRegistry().extractors.length)"`

Expected result: extractor count remains `17`. No extractors added, none removed. This plan changes loop dispatch only.
