---
phase: 57.6-multi-loop-coverage-human-interface-inserted
plan: 06
model: gpt-5-codex
context_used_pct: 60
subsystem: measurement-reporting
tags: [measurement, reporting, markdown, ascii, cli]
requires:
  - phase: 57.6-04
    provides: runtime/Codex extractor coverage for agent-performance and cross-runtime reports
  - phase: 57.6-05
    provides: derived and GSDR extractor coverage for signal-quality and cross-session reports
provides:
  - `measurement report <loop>` CLI surface for all six loop templates
  - Shared markdown/ASCII report primitives and dispatch layer
  - Pipe-friendly human-readable rendering over the existing query contract
affects: [measurement, cli, diagnostics, human-interface]
tech-stack:
  added: []
  patterns: [query-contract-wrapper, markdown-table-rendering, ascii-bar-rendering, loop-specific-report-templates]
key-files:
  created:
    - get-shit-done/bin/lib/measurement/report/primitives.cjs
    - get-shit-done/bin/lib/measurement/report/dispatch.cjs
    - get-shit-done/bin/lib/measurement/report/loops/intervention_lifecycle.cjs
    - get-shit-done/bin/lib/measurement/report/loops/pipeline_integrity.cjs
    - get-shit-done/bin/lib/measurement/report/loops/agent_performance.cjs
    - get-shit-done/bin/lib/measurement/report/loops/signal_quality.cjs
    - get-shit-done/bin/lib/measurement/report/loops/cross_session_patterns.cjs
    - get-shit-done/bin/lib/measurement/report/loops/cross_runtime_comparison.cjs
    - tests/unit/measurement-report-primitives.test.js
    - tests/unit/measurement-report-dispatch.test.js
    - .planning/phases/57.6-multi-loop-coverage-human-interface-inserted/57.6-06-SUMMARY.md
  modified:
    - get-shit-done/bin/lib/measurement.cjs
key-decisions:
  - "The report layer wraps `queryMeasurement()` directly; it does not fork or reimplement the query pipeline."
  - "Signal-quality stratification is on by default and `--no-stratification` emits a warning instead of silently flattening biased data."
patterns-established:
  - "Text-first dual interface: markdown tables plus ASCII bars render the JSON contract without introducing a second epistemic path."
  - "Loop template rendering: each loop gets a focused renderer over shared primitives instead of a generic DSL."
duration: 25min
completed: 2026-04-16
---

# Phase 57.6 Plan 06: Report Surface Summary

**Added the human-facing `measurement report <loop>` surface, with six loop templates and shared markdown/ASCII primitives that render directly from the measurement query contract.**

## Performance
- **Duration:** 25min
- **Tasks:** 2
- **Files modified:** 12

## Accomplishments
- Added the `report` subcommand to `measurement`, plus shared primitives/dispatch and six loop-specific renderers for `intervention_lifecycle`, `pipeline_integrity`, `agent_performance`, `signal_quality`, `cross_session_patterns`, and `cross_runtime_comparison`.
- Kept the report layer thin over `queryMeasurement()`: the CLI now renders provenance, coverage, reliability, anomalies, markdown tables, and ASCII bars without forking the query semantics.
- Verified the user-facing contract directly: the signal-quality report stratifies by default, `--no-stratification` emits an explicit warning, the agent-performance report carries the effort-not-quality caveat, and the cross-runtime report shows Claude/Codex compaction side-by-side.

## Task Commits
1. **Tasks 1-2: report surface + router extension** - `4640de50`

## Files Created/Modified
- `get-shit-done/bin/lib/measurement.cjs` - Added `report` as a first-class measurement subcommand.
- `get-shit-done/bin/lib/measurement/report/primitives.cjs` - Shared markdown table, ASCII bar, and structured header helpers.
- `get-shit-done/bin/lib/measurement/report/dispatch.cjs` - Loop dispatch and CLI entrypoint over `queryMeasurement()`.
- `get-shit-done/bin/lib/measurement/report/loops/*.cjs` - Six loop renderers matching the measurement loop catalog.
- `tests/unit/measurement-report-primitives.test.js` - Primitive rendering coverage.
- `tests/unit/measurement-report-dispatch.test.js` - Dispatch/CLI behavior coverage.
- `.planning/phases/57.6-multi-loop-coverage-human-interface-inserted/57.6-06-SUMMARY.md` - Execution record for this plan.

## Decisions & Deviations
- **[Rule 3 - Blocking] Shared staged-worktree closeout.** The executor had already staged the report-layer files in the shared phase branch when I committed the requested `max`-profile todo. As a result, commit `4640de50` contains both the todo capture and the complete Plan 06 code changes. The code landed atomically, but the commit message reflects the combined closeout rather than a pure Plan 06 feat commit.
- No query-pipeline fork was introduced: all rendered reports come from `queryMeasurement()` output.

## Verification Evidence
- `npx vitest run tests/unit/measurement-report-primitives.test.js tests/unit/measurement-report-dispatch.test.js` passed: `2` files, `15` tests.
- `measurement --help` now lists `report` with `usage: gsd-tools measurement report <loop> [--no-stratification] [--runtime name]`.
- `measurement report agent_performance` renders the required header block and the literal marker-density caveat: "effort tracking, not reasoning quality".
- `measurement report signal_quality` defaults to a stratified size-bucket table, and `measurement report signal_quality --no-stratification` emits the required warning banner in the header.
- `measurement report cross_runtime_comparison` renders Claude/Codex compaction side-by-side with asymmetry caveats and runtime-dimension coverage data.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
Plan 07 can now generate committed diagnostic artifacts from real `measurement report` output instead of ad hoc shell snippets, which satisfies the phase’s text-first interface requirement and unlocks the end-to-end diagnostic demonstration.

## Self-Check: PASSED
- FOUND: `get-shit-done/bin/lib/measurement/report/primitives.cjs`
- FOUND: `get-shit-done/bin/lib/measurement/report/dispatch.cjs`
- FOUND: `tests/unit/measurement-report-primitives.test.js`
- FOUND: `tests/unit/measurement-report-dispatch.test.js`
- FOUND: `measurement --help` exposes `report`
