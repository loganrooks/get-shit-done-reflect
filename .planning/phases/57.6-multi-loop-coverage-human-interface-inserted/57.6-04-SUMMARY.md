---
phase: 57.6-multi-loop-coverage-human-interface-inserted
plan: 04
model: gpt-5-codex
context_used_pct: 56
subsystem: measurement-runtime
tags: [measurement, runtime-extractors, codex, compaction, vitest]
requires:
  - phase: 57.6-02
    provides: stratification helper and canonical skip_reason vocabulary
  - phase: 57.6-03
    provides: Claude/Codex loader scan helpers for thinking, clear invocations, and compaction
provides:
  - Five new runtime-facing extractors across Claude and Codex
  - Extended Codex runtime metadata payload with effort and sandbox distributions
  - Unit coverage for thinking gates, marker density, clear detection, and cross-runtime compaction
affects: [measurement, agent_performance, cross_session_patterns, cross_runtime_comparison]
tech-stack:
  added: []
  patterns: [loader-backed-runtime-extractors, runtime-symmetry-via-report-layer, effort-not-quality-caveats]
key-files:
  created:
    - tests/unit/measurement-thinking-composite.test.js
    - tests/unit/measurement-marker-density.test.js
    - tests/unit/measurement-compaction-events.test.js
    - tests/unit/measurement-clear-invocation.test.js
    - .planning/phases/57.6-multi-loop-coverage-human-interface-inserted/57.6-04-SUMMARY.md
  modified:
    - get-shit-done/bin/lib/measurement/extractors/runtime.cjs
    - get-shit-done/bin/lib/measurement/extractors/codex.cjs
    - tests/unit/measurement-codex.test.js
key-decisions:
  - "Kept cross-runtime symmetry at the query/report layer; each extractor row still emits runtime-local asymmetric markers."
  - "Updated the shared Codex extractor test to assert on the metadata row explicitly instead of assuming a single extractor."
patterns-established:
  - "Runtime loop coverage via loader-backed extractors: new runtime features consume loader surfaces rather than reparsing raw artifacts."
  - "Effort caveat discipline: marker density remains explicitly framed as effort tracking, not reasoning quality."
duration: 24min
completed: 2026-04-16
---

# Phase 57.6 Plan 04: Runtime Extractors Summary

**Added the Wave 2 runtime/Codex extractor layer: thinking, marker density, clear invocation, and cross-runtime compaction are now queryable on top of the loader helpers from Plan 03.**

## Performance
- **Duration:** 24min
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments
- Added `thinking_composite`, `marker_density`, `clear_invocation`, and `claude_compaction_events` to `runtime.cjs`, and added `codex_compaction_events` plus an extended `codex_runtime_metadata` payload to `codex.cjs`.
- Added dedicated unit coverage for the new extractor behavior, including the three-level thinking gate, the effort-only marker-density framing, Claude/Codex compaction emission semantics, and Claude `/clear` detection.
- Reconciled the shared `measurement-codex.test.js` expectation so the suite now understands that Codex runtime metadata and Codex compaction are separate extractor rows.

## Task Commits
1. **Tasks 1-2: runtime + codex extractors with shared Codex test alignment** - `018d192b`

## Files Created/Modified
- `get-shit-done/bin/lib/measurement/extractors/runtime.cjs` - Added the four Claude/runtime extractors and their loop wiring.
- `get-shit-done/bin/lib/measurement/extractors/codex.cjs` - Added `codex_compaction_events` and extended `codex_runtime_metadata` with effort and sandbox breadth fields.
- `tests/unit/measurement-thinking-composite.test.js` - Covers gate semantics and emitted value structure.
- `tests/unit/measurement-marker-density.test.js` - Covers density computation and the effort-not-quality caveat.
- `tests/unit/measurement-compaction-events.test.js` - Covers Claude and Codex compaction rows and runtime-local symmetry markers.
- `tests/unit/measurement-clear-invocation.test.js` - Covers Claude `/clear` detection and availability semantics.
- `tests/unit/measurement-codex.test.js` - Updated shared expectations to select the metadata row explicitly now that two Codex extractors exist.
- `.planning/phases/57.6-multi-loop-coverage-human-interface-inserted/57.6-04-SUMMARY.md` - Execution record for this plan.

## Decisions & Deviations
- **[Rule 3 - Blocking] Shared Codex test expectation.** The plan's new `codex_compaction_events` extractor made `CODEX_EXTRACTORS` length `2`, so `tests/unit/measurement-codex.test.js` had to stop assuming a single row and instead assert on the `codex_runtime_metadata` row directly.
- **Concurrent-wave verification note.** By the time final verification ran, Plan 05 had already landed on the shared phase branch, so registry rebuild reported `26` extractors instead of the isolated Plan 04 expectation of `22`. The new Plan 04 extractors were verified directly via rebuild output and loop queries rather than the transient intermediate count.

## Verification Evidence
- `measurement rebuild --raw` succeeded with `run_id: 11`, `extractor_count: 26`, and the expected new runtime extractors present in the registry.
- `measurement query "agent performance" --raw` includes `thinking_composite`, `marker_density`, and the extended `codex_runtime_metadata`.
- `measurement query "cross session patterns" --raw` includes `clear_invocation`.
- `measurement query "cross runtime comparison" --raw` includes both `claude_compaction_events` and `codex_compaction_events`, while `runtime_source_presence` resolves `symmetric_available` at the runtime-dimension layer.
- `npx vitest run tests/unit/measurement-codex.test.js tests/unit/measurement-thinking-composite.test.js tests/unit/measurement-marker-density.test.js tests/unit/measurement-compaction-events.test.js tests/unit/measurement-clear-invocation.test.js` passed: `5` files, `20` tests.
- `npm test -- measurement` passed: `14` files, `59` tests.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
Plan 06 can now render real agent-performance and cross-runtime-comparison reports from live runtime data, and Plan 07 can later cite compaction and clear-invocation behavior as concrete evidence instead of placeholders.

## Self-Check: PASSED
- FOUND: `get-shit-done/bin/lib/measurement/extractors/runtime.cjs`
- FOUND: `get-shit-done/bin/lib/measurement/extractors/codex.cjs`
- FOUND: `tests/unit/measurement-thinking-composite.test.js`
- FOUND: `tests/unit/measurement-marker-density.test.js`
- FOUND: `tests/unit/measurement-compaction-events.test.js`
- FOUND: `tests/unit/measurement-clear-invocation.test.js`
