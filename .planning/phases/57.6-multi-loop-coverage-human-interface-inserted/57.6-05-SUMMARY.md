---
phase: 57.6-multi-loop-coverage-human-interface-inserted
plan: 05
model: gpt-5-codex
context_used_pct: 58
subsystem: measurement
tags: [measurement, derived-extractors, automation, feature-manifest, vitest]
requires:
  - phase: 57.6-02
    provides: stratification helpers and the canonical automation_skip_reasons manifest enum
provides:
  - Three new DERIVED extractors for facet semantics, write-path provenance, and /insights batch boundaries
  - One new GSDR extractor for manifest-backed skip_reason canonicity
  - Warn-not-fail skip_reason validation in the automation write path
affects: [measurement, automation, signal_quality, cross_session_patterns, pipeline_integrity]
tech-stack:
  added: []
  patterns: [manifest-backed-enum-validation, extractor-level-stratification, warn-not-fail-write-validation]
key-files:
  created:
    - tests/unit/measurement-facets-semantic.test.js
    - tests/unit/measurement-write-path-provenance.test.js
    - tests/unit/measurement-insights-mass-rewrite.test.js
    - tests/unit/measurement-skip-reason-canonical.test.js
    - tests/unit/automation-skip-reason-warn.test.js
    - .planning/phases/57.6-multi-loop-coverage-human-interface-inserted/57.6-05-SUMMARY.md
  modified:
    - get-shit-done/bin/lib/measurement/extractors/derived.cjs
    - get-shit-done/bin/lib/measurement/extractors/gsdr.cjs
    - get-shit-done/bin/lib/automation.cjs
key-decisions:
  - "Facet absence is emitted explicitly as not_emitted data with mandatory stratification on every row."
  - "skip_reason canonicity is validated against feature-manifest.json both retroactively (extractor) and at write time (warn-not-fail automation path)."
patterns-established:
  - "Absence-is-data derived rows: facetless sessions stay queryable instead of disappearing from aggregates."
  - "Single-source vocabulary validation: the same manifest enum governs extractor classification and write-path warnings."
duration: 22min
completed: 2026-04-16
---

# Phase 57.6 Plan 05: Derived Extractors & Skip-Reason Canonical Summary

**Registered Wave 2 derived and GSDR extractors for facet semantics, write-path provenance, /insights batch detection, and manifest-backed skip_reason validation.**

## Performance
- **Duration:** 22min
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments
- Raised the measurement registry from **22 to 26 extractors** by adding `facets_semantic_summary`, `derived_write_path_provenance`, `insights_mass_rewrite_boundary`, and `skip_reason_canonical`.
- Added the `automation.cjs` warn-not-fail integration so non-canonical skip reasons now emit a stderr warning while still persisting the write, matching MEAS-GSDR-03.
- Verified live corpus visibility for the new features: `facets_semantic_summary` returned **143** rows (**61 derived / 82 not_emitted**), `derived_write_path_provenance` returned **204** rows, `insights_mass_rewrite_boundary` returned **3** rows, and `skip_reason_canonical` returned **6** rows in the overview query.
- Confirmed the current corpus exposes the expected `/insights` batches: **2026-03-08** (14 sessions, not stale), **2026-03-15** (15 sessions, stale analysis detected for 1 session), and **2026-04-16** (108 sessions, stale analysis detected for 1 session).
- Confirmed current automation skip-reason canonicity is clean for emitted values: `signal_collection=level-1` and `reflection=disabled` are canonical; the remaining 4 tracked automation features are `not_emitted`.

## Task Commits
1. **Tasks 1-2: derived extractors + skip_reason canonical write-path validation** - `6eeb4a31`

## Files Created/Modified
- `get-shit-done/bin/lib/measurement/extractors/derived.cjs` - Added the three DERIVED extractors plus an empty-path freshness guard needed for facetless rows.
- `get-shit-done/bin/lib/measurement/extractors/gsdr.cjs` - Added `skip_reason_canonical` with manifest-backed classification over `.planning/config.json` automation stats.
- `get-shit-done/bin/lib/automation.cjs` - Added warn-only validation against `feature-manifest.json.automation_skip_reasons` before persisting skip events.
- `tests/unit/measurement-facets-semantic.test.js` - Covers derived vs not_emitted facet rows and the DC-4 stratification invariant.
- `tests/unit/measurement-write-path-provenance.test.js` - Covers bulk, single, and mixed mtime clustering outcomes.
- `tests/unit/measurement-insights-mass-rewrite.test.js` - Covers `/insights` batch detection and staleness detection.
- `tests/unit/measurement-skip-reason-canonical.test.js` - Covers canonical, non-canonical, empty-stats, and missing-manifest extractor behavior.
- `tests/unit/automation-skip-reason-warn.test.js` - Covers canonical, non-canonical, and missing-manifest warn-not-fail behavior.
- `.planning/phases/57.6-multi-loop-coverage-human-interface-inserted/57.6-05-SUMMARY.md` - Execution record for this wave-owned plan slice.

## Decisions & Deviations
- **[Rule 1 - Bug] Empty-path freshness handling in `derived.cjs`.** The new facetless `not_emitted` rows intentionally carry no facet path. `combineFreshness()` previously produced invalid age math for an empty path list, so it now returns an explicit `unknown` freshness record with `no_source_paths`.
- **[Rule 3 - Blocking] Correct manifest path resolution in `extractors/gsdr.cjs`.** The extractor lives under `measurement/extractors/`, so reaching the package root requires four `..` segments, not three. The implementation uses the correct package-root path so `feature-manifest.json` resolves in real execution.
- **Verification blocker:** targeted verification passed, live rebuild/query checks passed, but full `npm test` is currently blocked by a non-owned failure in `tests/unit/measurement-codex.test.js` (`codex_runtime_metadata` expected 1 row and observed 2) while concurrent edits are present in non-owned measurement files: `get-shit-done/bin/lib/measurement/extractors/codex.cjs`, `get-shit-done/bin/lib/measurement/extractors/runtime.cjs`, and `tests/unit/measurement-codex.test.js`.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
Plan 06 can consume the new loop coverage immediately: the registry is at 26 extractors, live queries include the new derived/GSDR rows, and the write path now surfaces skip-reason vocabulary drift at the moment of persistence. The remaining blocker is external to this ownership slice: the unrelated Codex measurement test must be reconciled before the phase can claim a clean full-suite pass.

## Self-Check: PASSED
- `FOUND`: `.planning/phases/57.6-multi-loop-coverage-human-interface-inserted/57.6-05-SUMMARY.md`
- `FOUND`: `6eeb4a31`
