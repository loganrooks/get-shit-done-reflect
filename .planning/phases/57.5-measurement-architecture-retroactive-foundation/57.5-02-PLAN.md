---
phase: 57.5-measurement-architecture-retroactive-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "57.5-01"
files_modified:
  - get-shit-done/bin/lib/measurement/sources/claude.cjs
  - get-shit-done/bin/lib/measurement/extractors/runtime.cjs
  - get-shit-done/bin/lib/measurement/extractors/derived.cjs
  - tests/unit/measurement-runtime-derived.test.js
autonomous: true

must_haves:
  truths:
    - "The runtime/derived extractor families cover the 57.5 Claude-side and /insights-side requirements: runtime session identity, settings-at-start, canonical JSONL token totals, human-turn counts, runtime JSONL coverage, session-meta provenance, and JSONL/session coverage audit"
    - "JSONL remains the canonical source for token totals and human-turn counts; session-meta token and user_message_count fields are annotated derived artifacts, not first-class quantitative ground truth"
    - "Session-meta is explicitly treated as DERIVED with lifecycle and write-path caveats"
    - "Query outputs for these extractors surface provenance, coverage, and semantic statuses (`exposed`, `derived`, `not_available`, `not_applicable`, plus `not_emitted` only where extractor semantics require it)"
    - "Tests cover canonical-source preference, coverage gaps, and the refusal to fall back silently to session-meta totals"
  artifacts:
    - path: "get-shit-done/bin/lib/measurement/sources/claude.cjs"
      provides: "Claude/raw-source loading for session-meta, facets, and parent JSONL linkage"
      contains: "loadClaude"
    - path: "get-shit-done/bin/lib/measurement/extractors/runtime.cjs"
      provides: "runtime-family extractors for session identity, settings, tokens, turns, and coverage"
      contains: "session_tokens_jsonl"
    - path: "get-shit-done/bin/lib/measurement/extractors/derived.cjs"
      provides: "derived-family extractors and /insights/session-meta provenance semantics"
      contains: "session_meta_provenance"
    - path: "tests/unit/measurement-runtime-derived.test.js"
      provides: "fixture-backed runtime/derived extractor tests"
      min_lines: 120
  key_links:
    - from: "get-shit-done/bin/lib/measurement/sources/claude.cjs"
      to: "get-shit-done/bin/lib/telemetry.cjs"
      via: "shared raw-loading/enrichment semantics rather than duplicate logic"
      pattern: "session-meta|facets"
    - from: "get-shit-done/bin/lib/measurement/extractors/runtime.cjs"
      to: "parent JSONL corpus"
      via: "canonical source for tokens and human-turn counts"
      pattern: "jsonl"
    - from: "get-shit-done/bin/lib/measurement/extractors/derived.cjs"
      to: "MEAS-DERIVED provenance rules from 57.5 research/context"
      via: "session-meta treated as derived, not runtime"
      pattern: "DERIVED|write_path|coverage"
---

<objective>
Implement the Claude/runtime and derived extractor families that anchor Phase 57.5's intervention-lifecycle and pipeline-integrity measurement base.

Purpose: land the canonical-source and provenance-heavy extractors first so later GSDR and Codex work composes onto a trustworthy substrate rather than papering over coverage and source-classification mistakes.

Output: Claude raw-source loader plus runtime/derived extractor families with tests.
</objective>

<execution_context>
@./.codex/get-shit-done-reflect/workflows/execute-plan.md
@./.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57.5-measurement-architecture-retroactive-foundation/57.5-CONTEXT.md
@.planning/phases/57.5-measurement-architecture-retroactive-foundation/57.5-RESEARCH.md
@get-shit-done/bin/lib/telemetry.cjs
@tests/unit/telemetry.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Claude-side raw loader with parent JSONL and coverage semantics</name>
  <files>get-shit-done/bin/lib/measurement/sources/claude.cjs</files>
  <action>
Implement a Claude/raw loader that exposes:
- session-meta records
- facets records
- parent JSONL linkage for canonical token/turn extraction
- explicit coverage metadata for matched, unmatched, and unavailable cases

The loader must preserve the distinction between runtime-native artifacts and derived `/insights` outputs. Do not smuggle session-meta token counts back in as runtime ground truth.

Second-order requirement: verify actual path and linking assumptions against the real existing corpus shape rather than relying on the earlier Phase 57 mental model alone.
  </action>
  <verify>
    - loader can enumerate project-filtered Claude corpus entries without error
    - loader exposes explicit coverage states instead of silently dropping unmatched items
    - loader code does not treat session-meta tokens as canonical
  </verify>
  <done>
Claude-side raw loading exists with real coverage semantics and parent JSONL linkage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement the runtime and derived extractor families</name>
  <files>
    get-shit-done/bin/lib/measurement/extractors/runtime.cjs
    get-shit-done/bin/lib/measurement/extractors/derived.cjs
  </files>
  <action>
Implement the Phase 57.5 runtime/derived extractors:

Runtime family:
- `runtime_session_identity`
- `claude_settings_at_start`
- `session_tokens_jsonl`
- `human_turn_count_jsonl`
- `runtime_jsonl_coverage`

Derived family:
- `session_meta_provenance`
- `session_jsonl_coverage_audit`

Use explicit statuses and provenance notes. The runtime family must prefer parent JSONL for token and human-turn quantitative outputs. The derived family must annotate session-meta lifecycle, write-path dependence, and limitations instead of pretending it is runtime truth.
  </action>
  <verify>
    - `rg -n "session_tokens_jsonl|human_turn_count_jsonl|runtime_jsonl_coverage" get-shit-done/bin/lib/measurement/extractors/runtime.cjs`
    - `rg -n "session_meta_provenance|coverage_audit|DERIVED" get-shit-done/bin/lib/measurement/extractors/derived.cjs`
    - no extractor silently falls back from JSONL ground truth to session-meta quantitative fields
  </verify>
  <done>
The runtime/derived extractor families encode the canonical-source and source-classification rules Phase 57.5 depends on.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add runtime/derived tests that prove canonical-source behavior</name>
  <files>tests/unit/measurement-runtime-derived.test.js</files>
  <action>
Create fixture-backed tests that prove:
- JSONL token totals win over session-meta token fields
- human-turn count uses the explicit 4-filter rule
- session-meta outputs are marked derived and caveated
- missing parent JSONL becomes `not_available` or a coverage-state artifact, not a silent null
- coverage audit output records matched / unmatched / missing categories
  </action>
  <verify>
    - `npm test -- tests/unit/measurement-runtime-derived.test.js`
    - review at least one test that would fail if a developer reintroduced session-meta token fallback
  </verify>
  <done>
The core runtime/derived semantics are locked with fixture tests before wider integration lands.
  </done>
</task>

</tasks>

