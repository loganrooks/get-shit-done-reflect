---
phase: 57.5-measurement-architecture-retroactive-foundation
plan: "04"
model: gpt-5-codex
context_used_pct: 78
subsystem: measurement
tags:
  - measurement
  - codex
  - telemetry
  - retroactive-validation
requires:
  - phase: "57.5-01"
    provides: "Measurement CLI/store substrate and bootstrap registry/store/query contract."
  - phase: "57.5-02"
    provides: "Claude runtime and derived extractor families for JSONL/session-meta coverage."
  - phase: "57.5-03"
    provides: "GSDR artifact/freshness extractors and the retroactive artifact trace surface."
provides:
  - "Codex raw adapter over state_5.sqlite plus rollout session_meta corroboration."
  - "codex_runtime_metadata extractor with model, reasoning, sandbox, cwd, rollout, and GSD provenance."
  - "Live mixed-runtime query overlay that keeps availability and symmetry as separate semantics."
  - "Telemetry phase compatibility hook backed by shared measurement query logic."
  - "Hermetic and real-corpus retroactive validation for Claude + Codex + GSDR evidence."
affects:
  - measurement architecture
  - telemetry
  - codex runtime proof
  - retroactive corpus queries
tech-stack:
  added:
    - "node:sqlite (existing built-in runtime dependency, newly used for Codex measurement)"
  patterns:
    - "live registry overlay for query-time feature synthesis"
    - "provenance-rich runtime metadata fields"
    - "telemetry compatibility wrapper over shared measurement logic"
key-files:
  created:
    - get-shit-done/bin/lib/measurement/sources/codex.cjs
    - get-shit-done/bin/lib/measurement/extractors/codex.cjs
    - tests/unit/measurement-codex.test.js
    - tests/integration/measurement-retroactive.test.js
  modified:
    - get-shit-done/bin/lib/measurement/query.cjs
    - get-shit-done/bin/lib/telemetry.cjs
    - tests/unit/telemetry.test.js
key-decisions:
  - "Codex proof stayed at metadata/provenance level: threads table + rollout session_meta, no transcript/content analysis."
  - "query.cjs now runs the full extractor set as a live overlay, using measurement.db as rebuild provenance rather than as the only feature source."
  - "Telemetry compatibility was wired through queryMeasurement instead of keeping a parallel phase-level measurement path."
patterns-established:
  - "Codex runtime proof: verify local schema first, then expose provenance-rich runtime fields with derived GSD context."
  - "Large raw measurement envelopes: callers must handle core @file: indirection when JSON exceeds stdout buffer thresholds."
duration: 16min
completed: 2026-04-16
---

# Phase 57.5 Plan 04: Codex Proof and Retroactive Query Summary

**Codex runtime metadata proof landed and the mixed-runtime query layer now answers retroactive Claude/Codex/GSDR questions without collapsing availability into symmetry.**

## Performance
- **Duration:** 16min
- **Tasks:** 3
- **Files modified:** 7

## Accomplishments
- Added a real Codex loader over `~/.codex/state_5.sqlite` and rollout JSONL `session_meta` headers, with schema verification against the live `threads` table before extractor output is trusted.
- Added `codex_runtime_metadata` so query results now carry explicit model, reasoning effort, sandbox policy, approval mode, cwd, rollout path, runtime identity, and best-available `gsd_version` / `profile` provenance.
- Reworked `query.cjs` into a live extractor overlay across runtime, derived, GSDR, and Codex families, keeping `availability_status` and `symmetry_marker` as separate output dimensions.
- Preserved telemetry CLI behavior while making the phase telemetry path consume shared measurement semantics via `_measurement_compatibility`.
- Locked the behavior with focused unit tests and a mixed-runtime retroactive integration proof.

## Task Commits
1. **Task 1: Implement the Codex raw adapter and metadata/provenance proof extractor** - `dc800f9b`
2. **Task 2: Finish query semantics and refactor telemetry into a compatibility view** - `fb7f8a76`
3. **Task 3: Prove retroactive execution over the real corpus and lock it with tests** - `9642491d`

## Files Created/Modified
- `get-shit-done/bin/lib/measurement/sources/codex.cjs` - Loads Codex thread rows, verifies live schema fields, and corroborates rollout `session_meta` headers.
- `get-shit-done/bin/lib/measurement/extractors/codex.cjs` - Emits provenance-rich `codex_runtime_metadata` feature rows.
- `get-shit-done/bin/lib/measurement/query.cjs` - Runs the full extractor registry as a live query overlay and exposes `runtime_dimension`.
- `get-shit-done/bin/lib/telemetry.cjs` - Adds a shared-query compatibility view for phase telemetry.
- `tests/unit/measurement-codex.test.js` - Verifies schema validation, loader behavior, and Codex extractor provenance.
- `tests/unit/telemetry.test.js` - Verifies phase telemetry still works while exposing shared measurement compatibility.
- `tests/integration/measurement-retroactive.test.js` - Proves mixed-runtime retroactive rebuild/query behavior over a representative corpus.

## Verification
- `npm test -- tests/unit/measurement-codex.test.js tests/unit/telemetry.test.js`
- `npm run test:infra -- measurement-retroactive`

## Real-Corpus Validation
- Ran `measurement query pipeline integrity --raw` against the actual repository corpus and resolved the core `@file:` indirection emitted for large JSON payloads.
- The real query observed runtimes `claude-code`, `codex-cli`, `cross-runtime`, and `project`, with `923` feature rows total and `54` `codex_runtime_metadata` rows.
- The real query used an existing measurement store (`store_present: true`, `rebuilt_at: 2026-04-16T19:54:47.728Z`) and surfaced `195` summaries, `65` verifications, and `254` signals from the current project corpus.
- The real query reported `770` `asymmetric_only` rows and `513` `exposed` rows, which is the concrete evidence that symmetry and availability remain separate axes in the output.
- Ran `telemetry phase 57.5 --raw` against the real repo and confirmed `_measurement_compatibility` is present with the same mixed-runtime dimension (`claude-code`, `codex-cli`, `cross-runtime`, `project`).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Retroactive integration helper needed `@file:` support for large raw query payloads**
- **Found during:** Task 3 verification
- **Issue:** `measurement query --raw` legitimately writes `@file:/tmp/...` when the JSON envelope exceeds the core stdout threshold, which made the new integration helper treat valid output as invalid JSON.
- **Fix:** Updated `tests/integration/measurement-retroactive.test.js` to detect `@file:` results and load the referenced JSON before parsing.
- **Files modified:** `tests/integration/measurement-retroactive.test.js`
- **Verification:** `npm run test:infra -- measurement-retroactive`
- **Commit:** `9642491d`

**2. [Rule 3 - Blocking] Transient `.git/index.lock` collision during Task 3 staging**
- **Found during:** Task 3 commit
- **Issue:** Parallel `git add` commands collided on the repository index and created a transient staging failure.
- **Fix:** Waited for the transient lock to clear, confirmed there was no active git process, and restaged sequentially. KB consulted, no relevant entries.
- **Files modified:** None
- **Verification:** Successful `test(57.5-04): lock codex retroactive measurement proof` commit
- **Commit:** `9642491d`

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact:** No product scope drift; one integration helper hardening change and one transient git recovery.

## Authentication Gates
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 57.6 can extend the live query overlay with broader loop coverage and human-facing report surfaces without first solving Codex runtime provenance again.
- Telemetry now has a clear compatibility bridge into the shared measurement layer, reducing the risk of a second measurement architecture reappearing.

## Self-Check: PASSED
- Verified summary file exists on disk.
- Verified task commits `dc800f9b`, `fb7f8a76`, and `9642491d` exist in git history.
