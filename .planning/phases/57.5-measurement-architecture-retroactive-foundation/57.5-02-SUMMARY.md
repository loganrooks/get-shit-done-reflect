---
phase: 57.5-measurement-architecture-retroactive-foundation
plan: "02"
model: gpt-5.4
context_used_pct: 45
subsystem: measurement
tags: [measurement, claude, runtime, derived, provenance]
requires:
  - phase: 57.5-01
    provides: measurement CLI/store/query substrate and registry contract
provides:
  - Claude raw-source loader with parent JSONL linkage and explicit coverage states
  - Runtime extractors for session identity, settings, canonical JSONL tokens, human turns, coverage, and era boundaries
  - Derived extractors for session-meta provenance and JSONL coverage audit
  - Fixture-backed tests that lock canonical-source preference and era-boundary warnings
affects: [Phase 57.5, telemetry, measurement, runtime-provenance]
tech-stack:
  added: [none]
  patterns: [parent-jsonl canonical source preference, derived-vs-runtime provenance split, explicit era-boundary classification]
key-files:
  created:
    - get-shit-done/bin/lib/measurement/sources/claude.cjs
    - get-shit-done/bin/lib/measurement/extractors/runtime.cjs
    - get-shit-done/bin/lib/measurement/extractors/derived.cjs
    - tests/unit/measurement-runtime-derived.test.js
  modified: []
key-decisions:
  - "JSONL remains canonical for token totals and human-turn counts; session-meta quantitative fields are preserved only as ignored derived artifacts."
  - "Session-meta is modeled as DERIVED `/insights` output with lifecycle and write-path caveats rather than as runtime-native truth."
  - "Runtime identity carries best-available `gsd_version` and `profile` metadata with provenance notes instead of inventing session-native fields."
patterns-established:
  - "Coverage-state-first loading: matched, unmatched, and missing runtime links remain explicit instead of collapsing to null."
  - "Era-aware runtime measurement: session records carry comparability warnings when queries span incompatible Claude eras."
duration: 6min
completed: 2026-04-16
---

# Phase 57.5 Plan 02: Runtime And Derived Extractor Summary

**Claude-side measurement now has a canonical-source loader and runtime/derived extractor families that preserve provenance, coverage, and era-boundary semantics instead of falling back to session-meta guesses.**

## Performance
- **Duration:** 6min
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- Added `loadClaude` to correlate session-meta, facets, and parent JSONL data with explicit matched/unmatched/missing coverage states.
- Implemented runtime extractors for session identity, settings-at-start, canonical JSONL token totals, human-turn counts, JSONL coverage, and era-boundary registry warnings.
- Implemented derived extractors for session-meta provenance and session/JSONL coverage auditing so `/insights` outputs stay clearly marked as derived artifacts.
- Added fixture-backed tests that fail if JSONL stops being canonical, if session-meta token fallback returns, or if era-boundary warnings disappear.

## Task Commits
1. **Task 1: Create the Claude-side raw loader with parent JSONL and coverage semantics** - `6da6444c`
2. **Task 2: Implement the runtime and derived extractor families** - `bcd8bd0e`
3. **Task 3: Add runtime/derived tests that prove canonical-source behavior** - `aded24bf`

## Files Created/Modified
- `get-shit-done/bin/lib/measurement/sources/claude.cjs` - Loads Claude session-meta, facets, settings, and parent JSONL linkage with explicit coverage metadata.
- `get-shit-done/bin/lib/measurement/extractors/runtime.cjs` - Defines runtime-family extractors for identity, settings, canonical tokens, human turns, coverage, and era-boundary registry output.
- `get-shit-done/bin/lib/measurement/extractors/derived.cjs` - Defines derived-family extractors for session-meta provenance and JSONL coverage audits.
- `tests/unit/measurement-runtime-derived.test.js` - Locks canonical-source preference, coverage semantics, and era-boundary warning behavior with fixture-backed tests.

## Decisions & Deviations
None in code: the plan landed in the intended file set and verification passed without requiring out-of-scope fixes.

Execution note: the executor stalled after landing and verifying all three task commits, so this summary was reconstructed by the orchestrator from the committed work and verification output. `.planning/STATE.md` remained orchestrator-owned for the parallel wave.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- The measurement substrate now has Claude-side canonical-source semantics, provenance-rich runtime metadata, and explicit derived-artifact caveats.
- Wave 3 can add the Codex proof and mixed-runtime query semantics on top of runtime + GSDR families without redefining availability or provenance rules.

## Self-Check: PASSED
- Verified created source files exist on disk.
- Verified task commits `6da6444c`, `bcd8bd0e`, and `aded24bf` exist in git history.
- Verified `npm test -- tests/unit/measurement-runtime-derived.test.js` passes.
