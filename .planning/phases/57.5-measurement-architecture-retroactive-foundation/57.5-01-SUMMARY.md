---
phase: 57.5-measurement-architecture-retroactive-foundation
plan: 01
model: gpt-5-codex
context_used_pct: 52
subsystem: measurement-infrastructure
tags: [measurement, sqlite, cli, registry, query-contract]
requires:
  - phase: 56
    provides: rebuildable SQLite cache patterns and Node sqlite usage
  - phase: 57
    provides: telemetry-era runtime source conventions and CLI module patterns
provides:
  - measurement CLI family with `rebuild` and `query` entrypoints
  - sibling measurement SQLite cache at `.planning/measurement/measurement.db`
  - family-composed extractor registry with explicit metadata validation
  - JSON query envelope carrying provenance, coverage, freshness, and symmetry markers
  - core measurement substrate tests
affects: [phase-57.5, telemetry, kb, measurement]
tech-stack:
  added: []
  patterns:
    - family-composed extractor registry
    - rebuildable sibling SQLite cache
    - JSON-first measurement query contract
key-files:
  created:
    - get-shit-done/bin/lib/measurement.cjs
    - get-shit-done/bin/lib/measurement/registry.cjs
    - get-shit-done/bin/lib/measurement/store.cjs
    - get-shit-done/bin/lib/measurement/query.cjs
    - tests/unit/measurement-core.test.js
  modified:
    - get-shit-done/bin/gsd-tools.cjs
    - .gitignore
key-decisions:
  - "Measurement lives in `.planning/measurement/measurement.db` as a sibling rebuildable cache rather than extending `kb.db`."
  - "The extractor registry is composed by explicit source family (`RUNTIME`, `DERIVED`, `GSDR`) and validates required metadata before any rebuild runs."
  - "Feature availability statuses and runtime symmetry markers stay separate enums and are surfaced together in the query contract."
patterns-established:
  - "Thin router, rich substrate: `measurement.cjs` only dispatches; store/registry/query own the contract."
  - "Cross-runtime symmetry as first-class data: query responses expose symmetry markers independently from semantic availability."
  - "Derived-cache hygiene: measurement SQLite artifacts are gitignored like `kb.db` to keep rebuilds out of commits."
duration: 12min
completed: 2026-04-16
---

# Phase 57.5 Plan 01: Measurement Substrate Summary

**Measurement now has a real substrate: a routed CLI family, a sibling rebuildable SQLite cache, a family-aware registry, a JSON query envelope, and core tests that lock the contract before later extractor waves land.**

## Performance
- **Duration:** ~12 minutes
- **Tasks:** 3 of 3 complete
- **Files modified:** 7 source files

## Accomplishments
- Wired `gsd-tools measurement` into the main router and added a thin `measurement.cjs` entrypoint with `rebuild` and `query` surfaces.
- Added `registry.cjs`, `store.cjs`, and `query.cjs` under `get-shit-done/bin/lib/measurement/` to define the substrate contract: explicit source families, metadata-validated extractors, a separate measurement store, and JSON responses with provenance, reliability, coverage, freshness, and symmetry markers.
- Added `tests/unit/measurement-core.test.js` to lock router wiring, store separation from `kb.db`, registry metadata enforcement, and the distinction between availability statuses and symmetry-marker enums.
- Ignored `measurement.db` and its SQLite sidecars so rebuildable cache artifacts do not pollute the git worktree.

## Task Commits
1. **Task 1: Add the measurement command family and module scaffold** - `9168a9d6`
2. **Task 2: Implement the separate store, registry contract, and query envelope** - `8063cfae`
3. **Rule 2 follow-up: ignore derived measurement cache artifacts** - `72b2c05b`
4. **Task 3: Lock the substrate with core tests and assumption checks** - `5e80147b`

## Files Created/Modified
- `get-shit-done/bin/gsd-tools.cjs` - adds the top-level `measurement` router case and command help listing
- `get-shit-done/bin/lib/measurement.cjs` - thin CLI orchestration for `rebuild` and `query`
- `get-shit-done/bin/lib/measurement/registry.cjs` - source-family registry, metadata validation, bootstrap extractors, symmetry helpers
- `get-shit-done/bin/lib/measurement/store.cjs` - separate SQLite store, source discovery, rebuild metadata persistence
- `get-shit-done/bin/lib/measurement/query.cjs` - JSON envelope builder with provenance, reliability, coverage, freshness, and anomaly surfaces
- `tests/unit/measurement-core.test.js` - substrate contract tests
- `.gitignore` - ignores `measurement.db` derived-cache artifacts

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Replaced unsupported sqlite transaction helper and hardened DB open behavior**
- **Found during:** Task 2 verification
- **Issue:** `DatabaseSync` in this runtime does not provide `db.transaction(...)`; the failed rebuild also made the next query open path brittle.
- **Fix:** Switched to explicit `BEGIN/COMMIT/ROLLBACK` transaction handling and made `PRAGMA journal_mode=WAL` best-effort instead of fatal.
- **Files modified:** `get-shit-done/bin/lib/measurement/store.cjs`
- **Commit:** `8063cfae`
- **Knowledge used:** KB consulted, no relevant lessons.

**2. [Rule 2 - Missing Critical] Ignored rebuildable measurement cache artifacts**
- **Found during:** Post-Task 3 worktree check
- **Issue:** The new measurement cache behaved like `kb.db` but was not ignored, so every rebuild left an untracked `.planning/measurement/measurement.db` artifact in the repo.
- **Fix:** Added ignore entries for the DB and SQLite sidecar files.
- **Files modified:** `.gitignore`
- **Commit:** `72b2c05b`
- **Knowledge used:** KB consulted; active signal `sig-2026-04-09-execute-phase-misses-untracked-phase-artifacts` reinforced the fix, but no lesson entry existed.

**3. [Rule 3 - Blocking] Cleared recurring stale git index locks during staging**
- **Found during:** Task 1 and Task 2 commit staging
- **Issue:** orphaned `.git/index.lock` files blocked `git add` despite no active git process in the repository.
- **Fix:** Verified no live git process, retried staging, and removed the stale lock on recurrence.
- **Files modified:** none
- **Commit:** none
- **Knowledge used:** KB consulted, no relevant lessons.

## Authentication Gates
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Later 57.5 plans can add real extractor families without touching the CLI router.
- Query consumers now have a stable contract for availability statuses, runtime symmetry markers, provenance, coverage, and freshness.
- The measurement cache boundary is explicit and no longer competes with `kb.db`.

## Self-Check: PASSED
- Verified created source files exist on disk.
- Verified task/deviation commits `9168a9d6`, `8063cfae`, `72b2c05b`, and `5e80147b` exist in git history.
