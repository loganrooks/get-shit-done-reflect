---
phase: 57.5-measurement-architecture-retroactive-foundation
plan: "04"
type: execute
wave: 3
depends_on:
  - "57.5-01"
  - "57.5-02"
  - "57.5-03"
files_modified:
  - get-shit-done/bin/lib/measurement/sources/codex.cjs
  - get-shit-done/bin/lib/measurement/extractors/codex.cjs
  - get-shit-done/bin/lib/measurement/query.cjs
  - get-shit-done/bin/lib/telemetry.cjs
  - tests/unit/measurement-codex.test.js
  - tests/unit/telemetry.test.js
  - tests/integration/measurement-retroactive.test.js
autonomous: true
resolves_signals:
  - sig-2026-02-18-task-tool-model-enum-no-sonnet-46
  - sig-2026-03-02-config-quality-profile-executor-model-speculative

must_haves:
  truths:
    - "A Codex raw adapter/extractor reads real local Codex artifacts (`state_5.sqlite` and session JSONL metadata) and emits model/runtime/reasoning/sandbox/cwd/rollout provenance"
    - "The query layer can answer mixed-runtime questions with explicit availability, provenance, coverage, and freshness semantics rather than silently averaging away asymmetry"
    - "Existing telemetry surfaces remain usable but now rely on shared loaders/extractor logic where the semantics overlap"
    - "A retroactive rebuild/query run executes against the existing session/artifact corpus without recollection and demonstrates the 57.5 extractor families over real data"
    - "The retroactive demo records model/runtime/reasoning provenance so future execution cannot repeat the current provenance blind spots"
    - "Tests cover Codex metadata extraction, telemetry compatibility, and retroactive mixed-corpus query behavior"
  artifacts:
    - path: "get-shit-done/bin/lib/measurement/sources/codex.cjs"
      provides: "Codex raw-source adapter over state_5.sqlite and session JSONL metadata"
      contains: "loadCodex"
    - path: "get-shit-done/bin/lib/measurement/extractors/codex.cjs"
      provides: "low-risk Codex metadata/provenance proof extractor"
      contains: "codex_runtime_metadata"
    - path: "get-shit-done/bin/lib/measurement/query.cjs"
      provides: "final 57.5 query semantics for intervention-lifecycle/pipeline-integrity questions"
      contains: "runtime_dimension"
    - path: "get-shit-done/bin/lib/telemetry.cjs"
      provides: "compatibility view backed by shared measurement logic where applicable"
      contains: "cmdTelemetry"
    - path: "tests/integration/measurement-retroactive.test.js"
      provides: "retroactive rebuild/query proof over a representative corpus path"
      contains: "retroactive"
  key_links:
    - from: "get-shit-done/bin/lib/measurement/sources/codex.cjs"
      to: "~/.codex/state_5.sqlite"
      via: "thread metadata extraction"
      pattern: "reasoning_effort|sandbox_policy|rollout_path"
    - from: "get-shit-done/bin/lib/measurement/extractors/codex.cjs"
      to: "get-shit-done/bin/lib/measurement/query.cjs"
      via: "Codex metadata feeds runtime-dimension answers"
      pattern: "runtime_dimension"
    - from: "get-shit-done/bin/lib/telemetry.cjs"
      to: "shared measurement loaders/extractors"
      via: "compatibility wrapper rather than parallel architecture"
      pattern: "measurement"
---

<objective>
Land the Phase 57.5 Codex proof, finish the mixed-runtime query semantics, and prove the whole substrate works retroactively over the existing corpus.

Purpose: this is the phase-close plan. It proves the architecture is genuinely cross-runtime, eliminates the telemetry split-brain risk, and demonstrates the substrate on real historical data instead of on isolated fixtures alone.

Output: Codex metadata/provenance extractor, shared-query-backed telemetry compatibility, and a retroactive corpus proof path.
</objective>

<execution_context>
@./.codex/get-shit-done-reflect/workflows/execute-plan.md
@./.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57.5-measurement-architecture-retroactive-foundation/57.5-CONTEXT.md
@.planning/phases/57.5-measurement-architecture-retroactive-foundation/57.5-RESEARCH.md
@get-shit-done/bin/lib/telemetry.cjs
@tests/unit/telemetry.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the Codex raw adapter and metadata/provenance proof extractor</name>
  <files>
    get-shit-done/bin/lib/measurement/sources/codex.cjs
    get-shit-done/bin/lib/measurement/extractors/codex.cjs
  </files>
  <action>
Implement the recommended low-risk Codex proof:
- read `~/.codex/state_5.sqlite` `threads` metadata
- read Codex session JSONL `session_meta` headers where needed for corroboration/path completion
- emit a `codex_runtime_metadata` extractor covering at minimum `model`, `reasoning_effort`, `sandbox_policy`, `cwd`, `rollout_path`, timestamps, and runtime identity

Do not drift into content analysis, token-parity ambition, or deeper Codex behavior modeling here. This plan proves the architecture, not full Codex feature coverage.

Second-order requirement: verify the actual local field names and table shapes against the live files before locking the extractor shape. Do not rely on remembered schema names.
  </action>
  <verify>
    - extractor code references actual `threads` fields observed in the live local store (`model`, `reasoning_effort`, `sandbox_policy`, `cwd`, `rollout_path`)
    - unit/integration tests fail if the adapter silently drops these provenance fields
    - no content-structural or transcript-semantic extractor logic lands in this plan
  </verify>
  <done>
The phase has a real low-risk Codex proof extractor grounded in current local artifacts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Finish query semantics and refactor telemetry into a compatibility view</name>
  <files>
    get-shit-done/bin/lib/measurement/query.cjs
    get-shit-done/bin/lib/telemetry.cjs
  </files>
  <action>
Complete the 57.5 query layer so it can answer intervention-lifecycle and pipeline-integrity questions using the landed extractor families while preserving explicit runtime/provenance/freshness semantics.

Then refactor `telemetry.cjs` so overlapping loader/enrichment logic comes from shared measurement modules or compatibility helpers rather than remaining a separate architecture. Keep the existing telemetry CLI surface usable; do not break current commands for the sake of purity.

Critical semantic constraint: feature availability and cross-runtime symmetry remain separate concepts in query output.
  </action>
  <verify>
    - mixed-runtime queries surface explicit availability/provenance rather than silent null/averaging
    - `telemetry` subcommands still run and use shared measurement logic where semantics overlap
    - no second parallel raw-loading architecture is reintroduced
  </verify>
  <done>
The measurement query layer is phase-complete for 57.5, and telemetry survives as a compatibility surface rather than a forked architecture.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove retroactive execution over the real corpus and lock it with tests</name>
  <files>
    tests/unit/measurement-codex.test.js
    tests/unit/telemetry.test.js
    tests/integration/measurement-retroactive.test.js
  </files>
  <action>
Add tests and validation that prove:
- Codex metadata extraction works from representative local artifact fixtures
- telemetry compatibility remains correct after the shared-loader/query refactor
- a retroactive rebuild/query run can execute over the existing session/artifact corpus without recollection
- the resulting query outputs carry runtime/model/reasoning provenance explicitly

Where a test cannot run hermetically without local corpus access, gate it clearly and record the real-corpus validation evidence in the plan summary/verification output.
  </action>
  <verify>
    - `npm test -- tests/unit/measurement-codex.test.js tests/unit/telemetry.test.js`
    - `npm run test:infra -- measurement-retroactive` or equivalent targeted integration run
    - manual real-corpus rebuild/query run recorded in summary/verification evidence
  </verify>
  <done>
Phase 57.5 ends with a real retroactive mixed-corpus proof, not only with architecture files and fixture tests.
  </done>
</task>

</tasks>
