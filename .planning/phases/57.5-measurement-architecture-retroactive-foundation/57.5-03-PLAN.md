---
phase: 57.5-measurement-architecture-retroactive-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "57.5-01"
files_modified:
  - get-shit-done/bin/lib/measurement/extractors/gsdr.cjs
  - get-shit-done/bin/lib/automation.cjs
  - get-shit-done/bin/kb-rebuild-index.sh
  - get-shit-done/workflows/collect-signals.md
  - get-shit-done/workflows/execute-phase.md
  - tests/unit/measurement-gsdr.test.js
  - tests/integration/kb-infrastructure.test.js
autonomous: true
resolves_signals:
  - sig-2026-03-01-plan-checker-misses-second-order-effects

must_haves:
  truths:
    - "The GSDR extractor family returns automation-health, signal-yield, kb-signal-stats, and kb-freshness outputs suitable for pipeline-integrity questions"
    - "`last_signal_count` is actually written through a supported path rather than remaining a planned-but-unimplemented field"
    - "`kb-rebuild-index.sh` updates both the markdown index and kb.db so freshness is not silently stale"
    - "Current workflow call sites (`collect-signals`, `execute-phase`) use the unified freshness path instead of drifting shell conventions"
    - "Query outputs surface stale-or-unknown kb freshness explicitly rather than trusting kb.db by default"
    - "Integration tests prove the script/update path on a temp corpus, not only unit-level helpers"
  artifacts:
    - path: "get-shit-done/bin/lib/measurement/extractors/gsdr.cjs"
      provides: "GSDR extractor family for automation and KB-backed metrics"
      contains: "automation_health"
    - path: "get-shit-done/bin/lib/automation.cjs"
      provides: "actual write path for `last_signal_count` or equivalent metadata persistence"
      contains: "last_signal_count"
    - path: "get-shit-done/bin/kb-rebuild-index.sh"
      provides: "unified kb freshness script that refreshes both index.md and kb.db"
      contains: "gsd-tools.cjs kb rebuild"
    - path: "tests/integration/kb-infrastructure.test.js"
      provides: "freshness integration proof for unified rebuild behavior"
      contains: "kb-rebuild-index.sh"
  key_links:
    - from: "get-shit-done/bin/lib/measurement/extractors/gsdr.cjs"
      to: ".planning/config.json"
      via: "automation stats and levels"
      pattern: "automation"
    - from: "get-shit-done/bin/kb-rebuild-index.sh"
      to: "get-shit-done/bin/lib/kb.cjs"
      via: "script-triggered db rebuild"
      pattern: "kb rebuild"
    - from: "get-shit-done/workflows/collect-signals.md"
      to: "get-shit-done/bin/kb-rebuild-index.sh"
      via: "call site updated to unified freshness path"
      pattern: "kb-rebuild-index.sh"
---

<objective>
Implement the GSDR measurement family and land the kb freshness repair that blocks trustworthy KB-backed measurement.

Purpose: Phase 57.5 must treat automation health and KB freshness as part of the measurement substrate itself, not as unrelated maintenance work. This plan turns pipeline-integrity surfaces into first-class extractors and fixes the stale-db trap at the same time.

Output: GSDR extractor family, working signal-yield persistence, and unified KB freshness behavior across scripts and workflows.
</objective>

<execution_context>
@./.codex/get-shit-done-reflect/workflows/execute-plan.md
@./.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57.5-measurement-architecture-retroactive-foundation/57.5-CONTEXT.md
@.planning/phases/57.5-measurement-architecture-retroactive-foundation/57.5-RESEARCH.md
@get-shit-done/bin/lib/automation.cjs
@get-shit-done/bin/lib/kb.cjs
@get-shit-done/bin/kb-rebuild-index.sh
@get-shit-done/workflows/collect-signals.md
@get-shit-done/workflows/execute-phase.md
@tests/integration/kb-infrastructure.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the GSDR extractor family and real signal-yield persistence</name>
  <files>
    get-shit-done/bin/lib/measurement/extractors/gsdr.cjs
    get-shit-done/bin/lib/automation.cjs
  </files>
  <action>
Implement GSDR extractors for:
- `automation_health`
- `automation_signal_yield`
- `kb_signal_stats`
- `kb_freshness_status`

Also land the actual write path for `last_signal_count` or an equivalent per-sensor signal-yield metadata field using a supported path in `automation.cjs` and/or the invoking workflows. Prefer the least invasive path that still creates durable, queryable stats.

Second-order requirement: verify existing readers (`sensors.cjs`, health/reporting surfaces) so the new field lands in a compatible shape rather than creating parallel stats keys.
  </action>
  <verify>
    - `rg -n "last_signal_count" get-shit-done/bin/lib/automation.cjs get-shit-done/bin/lib/sensors.cjs get-shit-done/bin/lib/measurement/extractors/gsdr.cjs`
    - extractor output includes both freshness state and signal-yield context
    - no new stat field shape silently breaks current sensor summary readers
  </verify>
  <done>
GSDR extractor outputs exist, and signal-yield is captured through a real persistence path instead of a missing field.
  </done>
</task>

<task type="auto">
  <name>Task 2: Unify the kb freshness path through the rebuild script and workflow call sites</name>
  <files>
    get-shit-done/bin/kb-rebuild-index.sh
    get-shit-done/workflows/collect-signals.md
    get-shit-done/workflows/execute-phase.md
  </files>
  <action>
Fix MEAS-GSDR-05 by making the rebuild script refresh both the markdown index and `kb.db`, then update call sites to rely on that unified behavior instead of a stale shell-script convention.

Prefer the research recommendation:
- `kb-rebuild-index.sh` becomes the single stable call surface
- internally it invokes the actual `gsd-tools.cjs kb rebuild` path after index work

Do not leave a state where workflows appear to rebuild freshness but only update `index.md`.
  </action>
  <verify>
    - `rg -n "gsd-tools.cjs kb rebuild" get-shit-done/bin/kb-rebuild-index.sh`
    - `rg -n "kb-rebuild-index.sh" get-shit-done/workflows/collect-signals.md get-shit-done/workflows/execute-phase.md`
    - manual temp-project integration exercise shows both `index.md` and `kb.db` refreshed
  </verify>
  <done>
KB freshness is unified behind one rebuild path, and the workflow call sites no longer depend on a naming-collision bug.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add tests that prove freshness and pipeline-integrity behavior</name>
  <files>
    tests/unit/measurement-gsdr.test.js
    tests/integration/kb-infrastructure.test.js
  </files>
  <action>
Add tests that prove:
- GSDR extractors interpret automation stats and freshness states correctly
- the rebuild script refreshes `kb.db`, not just markdown index files
- stale/unknown freshness is surfaced to query consumers
- signal-yield persistence can be observed end-to-end from write path to extractor output
  </action>
  <verify>
    - `npm test -- tests/unit/measurement-gsdr.test.js`
    - `npm run test:infra -- kb-infrastructure`
  </verify>
  <done>
Pipeline-integrity and freshness behavior is integration-tested rather than assumed.
  </done>
</task>

</tasks>

