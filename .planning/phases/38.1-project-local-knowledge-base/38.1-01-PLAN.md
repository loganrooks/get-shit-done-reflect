---
phase: 38.1-project-local-knowledge-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-signal-synthesizer.md
  - agents/gsd-phase-researcher.md
  - agents/gsd-reflector.md
  - agents/gsd-executor.md
  - agents/gsd-debugger.md
  - agents/gsd-planner.md
  - agents/gsd-spike-runner.md
  - agents/knowledge-store.md
  - agents/kb-templates/spike-decision.md
  - get-shit-done/workflows/collect-signals.md
  - get-shit-done/workflows/reflect.md
  - get-shit-done/workflows/execute-plan.md
  - get-shit-done/workflows/plan-phase.md
  - get-shit-done/workflows/health-check.md
  - get-shit-done/references/knowledge-surfacing.md
  - get-shit-done/references/signal-detection.md
  - get-shit-done/references/reflection-patterns.md
  - get-shit-done/references/health-check.md
  - get-shit-done/references/spike-execution.md
  - get-shit-done/bin/reconcile-signal-lifecycle.sh
autonomous: true

must_haves:
  truths:
    - "Every agent spec and workflow references .planning/knowledge/ as primary KB location"
    - "Every agent spec and workflow falls back to ~/.gsd/knowledge/ when .planning/knowledge/ does not exist"
    - "Reflector spec no longer instructs writing lesson files"
    - "KB is defined as three types: signals, reflections, spikes (no lessons)"
    - "No file references ~/.gsd/knowledge/ as the sole/primary path without fallback"
  artifacts:
    - path: "agents/gsd-signal-synthesizer.md"
      provides: "Signal write path with project-local primary"
      contains: ".planning/knowledge"
    - path: "agents/gsd-reflector.md"
      provides: "Reflection output with lessons writing removed"
      contains: ".planning/knowledge"
    - path: "agents/knowledge-store.md"
      provides: "KB structure docs showing 3-type layout"
      contains: ".planning/knowledge"
    - path: "get-shit-done/workflows/collect-signals.md"
      provides: "Signal collection with project-local KB"
      contains: ".planning/knowledge"
    - path: "get-shit-done/workflows/reflect.md"
      provides: "Reflection workflow with project-local KB"
      contains: ".planning/knowledge"
    - path: "get-shit-done/references/knowledge-surfacing.md"
      provides: "Knowledge surfacing using project-local KB"
      contains: ".planning/knowledge"
  key_links:
    - from: "agents/gsd-signal-synthesizer.md"
      to: ".planning/knowledge/signals/"
      via: "KB path resolution fallback pattern"
      pattern: 'if \[ -d ".planning/knowledge" \]'
    - from: "get-shit-done/workflows/collect-signals.md"
      to: ".planning/knowledge/"
      via: "KB_DIR variable with fallback"
      pattern: 'KB_DIR=".planning/knowledge"'
    - from: "agents/gsd-reflector.md"
      to: ".planning/knowledge/reflections/"
      via: "Report output path"
      pattern: ".planning/knowledge/reflections"
---

<objective>
Migrate all 20 agent spec and workflow/reference source files from `~/.gsd/knowledge/` to `.planning/knowledge/` as the primary KB location, with graceful fallback to `~/.gsd/knowledge/` when `.planning/knowledge/` does not exist. Also deprecate lessons in the reflector spec and update `knowledge-store.md` to reflect the 3-type KB structure.

Purpose: Enable knowledge base to be version-controlled and accessible in remote execution environments (Codex, CI) where `~/.gsd/` does not exist.
Output: 20 source files updated with fallback-aware KB paths; reflector no longer writes lessons.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.1-project-local-knowledge-base/38.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 9 agent specs with project-local KB fallback pattern and deprecate lessons in reflector</name>
  <files>
    agents/gsd-signal-synthesizer.md
    agents/gsd-phase-researcher.md
    agents/gsd-reflector.md
    agents/gsd-executor.md
    agents/gsd-debugger.md
    agents/gsd-planner.md
    agents/gsd-spike-runner.md
    agents/knowledge-store.md
    agents/kb-templates/spike-decision.md
  </files>
  <action>
For each of the 9 agent spec files, apply the KB path migration:

**Standard fallback pattern for bash code blocks:**
```bash
# KB path resolution -- project-local primary, user-global fallback
if [ -d ".planning/knowledge" ]; then
  KB_DIR=".planning/knowledge"
else
  KB_DIR="$HOME/.gsd/knowledge"
fi
```

**For prose/instruction references**, replace:
- `~/.gsd/knowledge/` with `.planning/knowledge/` (or `~/.gsd/knowledge/` fallback)
- `$HOME/.gsd/knowledge` with the equivalent fallback-aware pattern

**Search for BOTH variants** in each file: `~/.gsd/knowledge` AND `$HOME/.gsd/knowledge` AND bare `.gsd/knowledge`. Missing one variant leaves stale references.

**File-specific notes:**
- `gsd-signal-synthesizer.md` (5 refs): Has KB write path, index read, mkdir commands. Update all to use KB_DIR variable with fallback. The `mkdir -p` for signal directories must use the resolved KB_DIR.
- `gsd-phase-researcher.md` (3 refs): KB index read for knowledge surfacing. Update path references in both bash and prose.
- `gsd-reflector.md` (3 refs): KB path references PLUS lessons deprecation. Remove all instructions to write lesson files (the `~/.gsd/knowledge/lessons/{category}/` writing logic). Keep reflection report writing. Update prose to say KB has 3 types: signals, reflections, spikes. The reflector should still read existing lessons from `~/.gsd/knowledge/lessons/` for backward compatibility but should NOT create new lesson files.
- `gsd-executor.md` (2 refs): KB grep for deviation context.
- `gsd-debugger.md` (3 refs): KB index read and grep.
- `gsd-planner.md` (1 ref): Optional KB index read in knowledge_surfacing section.
- `gsd-spike-runner.md` (1 ref): Spike entry write path.
- `knowledge-store.md` (3 refs): This is the KB structure documentation. Update directory layout to show `.planning/knowledge/` as primary with `signals/`, `reflections/`, `spikes/` (NO `lessons/`). Document the fallback behavior. Note that lessons are deprecated and existing ones remain at `~/.gsd/knowledge/lessons/` as historical artifacts.
- `kb-templates/spike-decision.md` (1 ref): Update spike file path template.

**Critical constraint:** Do NOT modify `get-shit-done/bin/gsd-tools.js` -- it has zero KB references per user decision.
  </action>
  <verify>
Run: `grep -c '~/.gsd/knowledge' agents/*.md agents/kb-templates/*.md | grep -v ':0$'` -- should show references only where they appear as the FALLBACK path (inside an else branch or "fallback" prose). No file should reference `~/.gsd/knowledge/` as the sole/primary path.

Run: `grep -l '.planning/knowledge' agents/*.md agents/kb-templates/*.md` -- should list all 9 agent files.

Run: `grep -c 'lessons' agents/gsd-reflector.md` -- verify lesson-writing instructions are removed (references to reading existing lessons for backward compat are acceptable).

Run: `grep -c 'lessons/' agents/knowledge-store.md` -- verify no `lessons/` in the directory structure documentation (mentions of deprecated lessons in prose are acceptable).
  </verify>
  <done>All 9 agent specs reference `.planning/knowledge/` as primary KB location with `~/.gsd/knowledge/` fallback. Reflector no longer instructs writing lesson files. knowledge-store.md documents 3-type KB structure (signals, reflections, spikes).</done>
</task>

<task type="auto">
  <name>Task 2: Update 11 workflow and reference files with project-local KB fallback pattern</name>
  <files>
    get-shit-done/workflows/collect-signals.md
    get-shit-done/workflows/reflect.md
    get-shit-done/workflows/execute-plan.md
    get-shit-done/workflows/plan-phase.md
    get-shit-done/workflows/health-check.md
    get-shit-done/references/knowledge-surfacing.md
    get-shit-done/references/signal-detection.md
    get-shit-done/references/reflection-patterns.md
    get-shit-done/references/health-check.md
    get-shit-done/references/spike-execution.md
    get-shit-done/bin/reconcile-signal-lifecycle.sh
  </files>
  <action>
Apply the same fallback pattern to all 11 workflow/reference files:

**For bash code blocks:** Use the standard KB_DIR resolution snippet.
**For prose references:** Use `.planning/knowledge/` (or `~/.gsd/knowledge/` fallback) pattern.

**File-specific notes:**
- `collect-signals.md` (4 refs): KB index read, signal write, git add paths. The git add command should add `.planning/knowledge/` files (not `~/.gsd/knowledge/` which is outside the repo).
- `reflect.md` (5 refs): KB_DIR variable, report dir, lesson find, git add. Remove lesson-related `find` commands. Update git add to use `.planning/knowledge/`. The reflect workflow should NOT search for or process lesson files when writing to project-local KB.
- `execute-plan.md` (1 ref): Signal file locate path.
- `plan-phase.md` (3 refs): KB index read, signal read for knowledge surfacing.
- `health-check.md` (1 ref): Signal write path.
- `knowledge-surfacing.md` (10 refs): This file has the most references. Update all `cat ~/.gsd/knowledge/index.md` examples and path references. Use the prose fallback pattern for instruction text and the bash fallback pattern for code examples.
- `signal-detection.md` (2 refs): Signal write path, index read.
- `reflection-patterns.md` (5 refs): KB index, lesson write references (remove lesson write paths), signal read.
- `health-check.md` reference (6 refs): KB validation checks. Update paths used in health check assertions.
- `spike-execution.md` (1 ref): Spike entry write path.
- `reconcile-signal-lifecycle.sh` (1 ref): This is a shell script. Apply Pattern 3 from RESEARCH.md (includes GSD_HOME middle tier). Update both `KB_SIGNALS` path and the `kb-rebuild-index.sh` invocation path. The script should check `.planning/knowledge` first, then `$GSD_HOME/knowledge`, then `$HOME/.gsd/knowledge`.

**For reflect.md and reflection-patterns.md specifically:** Remove references to writing lesson files. Reflection reports remain; only individual lesson file generation instructions are removed. This aligns with the reflector agent spec change in Task 1.
  </action>
  <verify>
Run: `grep -c '~/.gsd/knowledge' get-shit-done/workflows/*.md get-shit-done/references/*.md get-shit-done/bin/*.sh | grep -v ':0$'` -- should show references only as fallback paths.

Run: `grep -l '.planning/knowledge' get-shit-done/workflows/*.md get-shit-done/references/*.md get-shit-done/bin/*.sh` -- should list all 11 files.

Run: `grep 'lessons' get-shit-done/workflows/reflect.md get-shit-done/references/reflection-patterns.md` -- verify lesson-writing instructions are removed from these files.
  </verify>
  <done>All 11 workflow and reference files reference `.planning/knowledge/` as primary KB location with `~/.gsd/knowledge/` fallback. Lesson-writing references removed from reflect workflow and reflection-patterns reference. reconcile-signal-lifecycle.sh uses 3-tier fallback (project-local, GSD_HOME, user-global).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn '~/.gsd/knowledge' agents/ get-shit-done/ | grep -v 'fallback\|fall back\|otherwise\|else\|elif'` -- any remaining primary (non-fallback) references indicate missed updates
2. `grep -rl '.planning/knowledge' agents/ get-shit-done/` -- should list all 20 source files
3. `node bin/install.js --local` -- reinstall to sync source to `.claude/` runtime directory
4. `grep -rl '.planning/knowledge' .claude/agents/ .claude/get-shit-done/` -- verify installed files also have project-local paths
</verification>

<success_criteria>
- All 20 source files (9 agents + 11 workflows/references) reference `.planning/knowledge/` as primary KB location
- All 20 source files include `~/.gsd/knowledge/` as fallback when `.planning/knowledge/` does not exist
- Reflector spec no longer instructs writing lesson files
- knowledge-store.md documents 3-type KB (signals, reflections, spikes)
- reflect.md and reflection-patterns.md have lesson-writing references removed
- `node bin/install.js --local` succeeds and installed files match source
</success_criteria>

<output>
After completion, create `.planning/phases/38.1-project-local-knowledge-base/38.1-01-SUMMARY.md`
</output>
