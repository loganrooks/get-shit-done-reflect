---
phase: 38.1-project-local-knowledge-base
plan: 02
type: execute
wave: 2
depends_on: ["38.1-01"]
files_modified:
  - bin/install.js
  - tests/unit/install.test.js
  - tests/integration/multi-runtime.test.js
  - tests/integration/cross-runtime-kb.test.js
  - tests/integration/kb-infrastructure.test.js
  - tests/smoke/run-smoke.sh
  - .planning/ROADMAP.md
autonomous: true
user_setup:
  - service: local-kb-migration
    why: "One-time migration of existing KB content to project-local directory"
    dashboard_config:
      - task: "Run migration script after plan completes"
        location: "bash bin/migrate-kb.sh from project root"

must_haves:
  truths:
    - "npm test passes with all KB-related test assertions updated for project-local primary"
    - "Installer creates .planning/knowledge/{signals,reflections,spikes} when .planning/ exists"
    - "Installer still creates ~/.gsd/knowledge/ for user-global fallback"
    - "Test suite validates BOTH project-local primary AND user-global fallback paths"
    - "All 5 KB-related test files are updated (install.test.js, multi-runtime.test.js, cross-runtime-kb.test.js, kb-infrastructure.test.js, run-smoke.sh)"
    - "kb-rebuild-index.sh works on .planning/knowledge/ without depending on ~/.gsd/bin/"
    - "Migration script copies signals, reflections, spikes (not lessons) to .planning/knowledge/"
    - "ROADMAP success criteria no longer reference 'lessons' for writes"
  artifacts:
    - path: "bin/install.js"
      provides: "Installer with .planning/knowledge/ creation"
      contains: ".planning/knowledge"
    - path: "tests/unit/install.test.js"
      provides: "Unit tests accepting both KB path variants"
      contains: ".planning/knowledge"
    - path: "tests/integration/multi-runtime.test.js"
      provides: "Integration tests validating project-local KB paths in installed files"
      contains: ".planning/knowledge"
    - path: "tests/integration/cross-runtime-kb.test.js"
      provides: "Cross-runtime tests validating project-local KB paths"
      contains: ".planning/knowledge"
    - path: "tests/integration/kb-infrastructure.test.js"
      provides: "KB infrastructure tests updated for project-local paths and lessons deprecation"
      contains: ".planning/knowledge"
    - path: "tests/smoke/run-smoke.sh"
      provides: "Smoke tests updated with project-local KB path resolution"
      contains: ".planning/knowledge"
    - path: "bin/migrate-kb.sh"
      provides: "One-time migration script from user-global to project-local KB"
    - path: "get-shit-done/bin/kb-rebuild-index.sh"
      provides: "Index rebuilder with project-local KB support"
      contains: ".planning/knowledge"
  key_links:
    - from: "bin/install.js"
      to: ".planning/knowledge/"
      via: "Directory creation when .planning/ exists"
      pattern: "mkdirSync.*planning.*knowledge"
    - from: "tests/integration/multi-runtime.test.js"
      to: "agents/*.md"
      via: "File content assertions for KB paths"
      pattern: ".planning/knowledge"
    - from: "get-shit-done/bin/kb-rebuild-index.sh"
      to: ".planning/knowledge/index.md"
      via: "KB_DIR resolution with project-local primary"
      pattern: 'if \[ -d ".planning/knowledge" \]'
---

<objective>
Update installer, shell scripts, all 5 KB-related test files, and create migration script so the project-local KB infrastructure is complete. The installer creates `.planning/knowledge/` during setup, shell scripts resolve KB path with project-local primary, all 5 test files validate both paths, and a migration script moves existing content. Also fix ROADMAP success criterion #2 to remove stale "lessons" reference.

Purpose: Complete the infrastructure layer so the agent spec changes from Plan 01 are fully supported by tooling and validated by all 5 test files per locked decision.
Output: Updated installer, 5 test files, 2 shell scripts, 1 new migration script, ROADMAP fix. `npm test` passes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.1-project-local-knowledge-base/38.1-RESEARCH.md
@.planning/phases/38.1-project-local-knowledge-base/38.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update installer, shell scripts, and create migration script for project-local KB</name>
  <files>
    bin/install.js
    get-shit-done/bin/kb-rebuild-index.sh
    bin/migrate-kb.sh
  </files>
  <action>
**bin/install.js** -- Add project-local KB directory creation:
- Find where `~/.gsd/knowledge/` directories are created (look for `mkdirSync` with `.gsd/knowledge`)
- Add a check: if `.planning/` directory exists in the current working directory, also create `.planning/knowledge/signals/`, `.planning/knowledge/reflections/`, `.planning/knowledge/spikes/` using `fs.mkdirSync(path, { recursive: true })`. Do NOT create `.planning/knowledge/lessons/` (lessons are deprecated).
- Keep the existing `~/.gsd/knowledge/` creation logic unchanged (it provides fallback)
- Per user decision: `.planning/knowledge/` is created during project setup, AND when `.planning/` already exists during install. This means: check `fs.existsSync('.planning')` and if true, ensure the knowledge subdirs exist.
- Do NOT add `.planning/knowledge/` path conversion to `replacePathsInContent()` -- `.planning/knowledge/` is a relative path that should pass through unchanged (verified in research).

**get-shit-done/bin/kb-rebuild-index.sh** -- This file currently lives at `~/.gsd/bin/kb-rebuild-index.sh`. Copy it to `get-shit-done/bin/kb-rebuild-index.sh` in the project source (so it ships with npm and is available in `.planning/knowledge/` contexts without depending on `~/.gsd/bin/`). Update the copy with:
- Add project-local KB path resolution at the top (Pattern 3 from research):
  ```bash
  if [ -d ".planning/knowledge" ]; then
    KB_DIR=".planning/knowledge"
  elif [ -n "${GSD_HOME:-}" ]; then
    KB_DIR="$GSD_HOME/knowledge"
  else
    KB_DIR="$HOME/.gsd/knowledge"
  fi
  ```
- Replace hardcoded `$HOME/.gsd/knowledge` or `$GSD_HOME/knowledge` references with `$KB_DIR`
- Conditionally skip the `## Lessons` section in index output when `$KB_DIR/lessons/` does not exist (project-local KB has no lessons dir). When `lessons/` exists (user-global fallback), keep the section.
- Keep the atomic write pattern (temp file + mv) unchanged
- Keep the existing `build_signal_rows`, `build_spike_rows`, `build_lesson_rows` functions -- just gate `build_lesson_rows` on directory existence

**bin/migrate-kb.sh** -- Create a new standalone migration script:
```bash
#!/usr/bin/env bash
set -euo pipefail

SRC="$HOME/.gsd/knowledge"
DEST=".planning/knowledge"

if [ ! -d "$SRC" ]; then
  echo "No source KB at $SRC -- nothing to migrate"
  exit 0
fi

if [ ! -d "$DEST" ]; then
  echo "Destination $DEST does not exist. Run installer first."
  exit 1
fi

# Copy signals (preserve directory structure)
if [ -d "$SRC/signals" ] && [ "$(ls -A "$SRC/signals/" 2>/dev/null)" ]; then
  cp -r "$SRC/signals/"* "$DEST/signals/" 2>/dev/null || true
  echo "Migrated signals"
fi

# Copy reflections
if [ -d "$SRC/reflections" ] && [ "$(ls -A "$SRC/reflections/" 2>/dev/null)" ]; then
  cp -r "$SRC/reflections/"* "$DEST/reflections/" 2>/dev/null || true
  echo "Migrated reflections"
fi

# Copy spikes
if [ -d "$SRC/spikes" ] && [ "$(ls -A "$SRC/spikes/" 2>/dev/null)" ]; then
  cp -r "$SRC/spikes/"* "$DEST/spikes/" 2>/dev/null || true
  echo "Migrated spikes"
fi

# Lessons are NOT migrated (deprecated)
echo "Note: Lessons NOT migrated (deprecated). Historical lessons remain at $SRC/lessons/"

echo ""
echo "Migration complete. Run kb-rebuild-index.sh to generate index at $DEST/index.md"
```
Make it executable: `chmod +x bin/migrate-kb.sh`

Do NOT update `~/.gsd/bin/kb-rebuild-index.sh` or `~/.gsd/bin/kb-create-dirs.sh` directly -- those are user-global files outside this repo. The project-local copy at `get-shit-done/bin/kb-rebuild-index.sh` is what ships with npm.
  </action>
  <verify>
Run: `grep '.planning/knowledge' bin/install.js` -- should show the new directory creation logic.
Run: `cat get-shit-done/bin/kb-rebuild-index.sh | head -20` -- should show project-local KB path resolution at top.
Run: `bash -n bin/migrate-kb.sh` -- syntax check passes.
Run: `bash -n get-shit-done/bin/kb-rebuild-index.sh` -- syntax check passes.
Run: `test -x bin/migrate-kb.sh && echo "executable" || echo "not executable"` -- should print "executable".
  </verify>
  <done>Installer creates `.planning/knowledge/{signals,reflections,spikes}` when `.planning/` exists. kb-rebuild-index.sh in project source resolves KB path with project-local primary. Migration script copies signals, reflections, spikes (not lessons) from user-global to project-local.</done>
</task>

<task type="auto">
  <name>Task 2: Update all 5 KB-related test files for project-local KB path validation and fix ROADMAP</name>
  <files>
    tests/unit/install.test.js
    tests/integration/multi-runtime.test.js
    tests/integration/cross-runtime-kb.test.js
    tests/integration/kb-infrastructure.test.js
    tests/smoke/run-smoke.sh
    .planning/ROADMAP.md
  </files>
  <action>
**tests/unit/install.test.js:**
- The `replacePathsInContent` tests currently assert that `~/.gsd/knowledge/` paths pass through unchanged. This is STILL correct -- `replacePathsInContent()` should NOT modify `~/.gsd/knowledge/` paths (they are handled by the fallback pattern in agent specs, not by installer path conversion). Keep these assertions.
- Add new tests:
  - Test that `.planning/knowledge/` paths in source content pass through `replacePathsInContent()` unchanged (no accidental mangling)
  - Test that installer creates `.planning/knowledge/{signals,reflections,spikes}` when `.planning/` directory exists (mock fs.existsSync for `.planning/`)
  - Test that installer does NOT create `.planning/knowledge/lessons/` (lessons deprecated)
- Update any test that checks installed file content to accept `.planning/knowledge/` as primary path alongside `~/.gsd/knowledge/` as fallback

**tests/integration/multi-runtime.test.js:**
- The test "KB paths use ~/.gsd/knowledge/ not old ~/.claude/gsd-knowledge/" (line 109) needs updating. The test scans installed agent files and checks they reference `.gsd/knowledge`. Update to check that files reference `.planning/knowledge` as primary. The fallback `~/.gsd/knowledge/` should also be present (in else/fallback context).
- The violation check (line 136-138) that catches `gsd-knowledge` without `.gsd/knowledge` should also accept `.planning/knowledge` as valid.
- Update the test descriptions to reflect the new primary location.
- The tests at lines 233 and 299 ("KB paths reference shared ~/.gsd/knowledge/") should be updated to check for `.planning/knowledge` as primary.
- Keep tests that verify `~/.gsd/knowledge/` directory creation by installer (this is the fallback location).

**tests/integration/cross-runtime-kb.test.js:**
- Test "installed workflow files reference ~/.gsd/knowledge/" (line 267) should accept `.planning/knowledge` as the primary path.
- The violation check (line 296-301) should accept `.planning/knowledge` alongside `.gsd/knowledge`.
- Update test descriptions to reflect project-local primary.
- Add test: verify installed files contain BOTH `.planning/knowledge` (primary) and `.gsd/knowledge` (fallback) references.

**tests/integration/kb-infrastructure.test.js:**
- Update `kb-create-dirs.sh` tests: the test at line 105 that asserts `signals`, `spikes`, `lessons` directories -- update to verify that `kb-create-dirs.sh` still creates the user-global dirs (this script manages `~/.gsd/knowledge/`). No change needed if the script itself is not modified.
- Update `kb-rebuild-index.sh` tests: these tests run the script with HOME overridden. Since the project-local `get-shit-done/bin/kb-rebuild-index.sh` now has fallback logic, update the `KB_REBUILD_INDEX` constant (line 14) to point to `get-shit-done/bin/kb-rebuild-index.sh` (the new project source copy) instead of `.claude/agents/kb-rebuild-index.sh`. Alternatively, test both versions.
- The "empty KB" test at line 134 expects `## Lessons (0)` -- update to conditionally handle the case where lessons section is skipped when `lessons/` dir doesn't exist (project-local KB). When running against user-global with `lessons/` dir, the section should still appear.
- Update test for "lesson category column populated" (line 293) -- this test remains valid for user-global KB where lessons exist. Ensure the test creates the `lessons/` dir so the conditional in kb-rebuild-index.sh includes the section.
- Add new test: verify that when `lessons/` directory does NOT exist (simulating project-local KB), the index output omits the `## Lessons` section entirely.
- All existing tests that reference `$HOME/.gsd/knowledge` via the `runKbScript` helper still work because HOME is overridden to tmpdir.

**tests/smoke/run-smoke.sh:**
- Line 31: Update `KB_DIR` resolution from `"${GSD_HOME:-$HOME/.gsd}/knowledge"` to use the same project-local-first fallback pattern:
  ```bash
  if [ -d ".planning/knowledge" ]; then
    KB_DIR=".planning/knowledge"
  else
    KB_DIR="${GSD_HOME:-$HOME/.gsd}/knowledge"
  fi
  ```
- Line 68: Update `bash "$REPO_DIR/.claude/agents/kb-create-dirs.sh"` -- keep this for user-global KB setup (fallback), but also ensure project-local dirs exist in setup.
- Line 77-79: Update cleanup to clean both project-local and user-global signal/spike dirs.
- Line 81: Update `kb-rebuild-index.sh` invocation to use the project-local copy: `bash "$REPO_DIR/get-shit-done/bin/kb-rebuild-index.sh"` instead of `bash "$REPO_DIR/.claude/agents/kb-rebuild-index.sh"`.
- Lines 260-261, 293, 329: Update signal/spike/lesson creation paths in `test_manual_signal`, `test_signal_collection`, `test_reflection`, `test_spike` -- the claude prompts instruct writing to `~/.gsd/knowledge/signals/$RUN_ID/`. Update to use `.planning/knowledge/signals/$RUN_ID/` when project-local KB exists, with fallback instructions for `~/.gsd/knowledge/`.
- Line 387-388, 504: Update `test_reflection` and `test_kb_surfacing` KB references similarly.
- Lines 370, 481: Update `kb-rebuild-index.sh` invocation paths.
- Lines 299, 362, 486, 493: Update `find` commands that check KB content to look in project-local KB first.

**ROADMAP.md fix:**
- Find Phase 38.1 success criterion #2 (line 96): `"All agents write signals, lessons, spikes, and reflections to .planning/knowledge/"`
- Update to: `"All agents write signals, spikes, and reflections to .planning/knowledge/ (lessons deprecated)"`
- This aligns with the locked decision that KB has 3 types: signals, reflections, spikes.

After all test updates, run `npm test` to verify the full suite passes.
  </action>
  <verify>
Run: `npm test` -- all tests must pass.
Run: `grep '.planning/knowledge' tests/unit/install.test.js tests/integration/multi-runtime.test.js tests/integration/cross-runtime-kb.test.js tests/integration/kb-infrastructure.test.js tests/smoke/run-smoke.sh` -- should show project-local KB references in all 5 test files.
Run: `grep 'lessons' tests/unit/install.test.js | grep -v 'NOT\|deprecated\|no.*lessons'` -- verify no test expects lessons/ directory creation.
Run: `grep 'lessons.*spikes.*reflections\|signals.*lessons' .planning/ROADMAP.md` -- should NOT match (lessons removed from write criterion).
  </verify>
  <done>All 5 test files updated to validate `.planning/knowledge/` as primary KB location per locked decision. Tests cover both project-local primary and user-global fallback. ROADMAP success criterion #2 corrected to remove stale "lessons" reference. `npm test` passes.</done>
</task>

<task type="auto">
  <name>Task 3: Run migration, rebuild index, and sync installed files</name>
  <files>
    .planning/knowledge/index.md
  </files>
  <action>
Execute the migration and final sync:

1. Create `.planning/knowledge/{signals,reflections,spikes}` directories if they don't exist
2. Run `bash bin/migrate-kb.sh` to copy existing KB content from `~/.gsd/knowledge/` to `.planning/knowledge/`
3. Run `bash get-shit-done/bin/kb-rebuild-index.sh` from project root to generate `.planning/knowledge/index.md`
4. Run `node bin/install.js --local` to sync all source files to `.claude/` runtime directory
5. Verify installed files have project-local KB paths: `grep -rl '.planning/knowledge' .claude/agents/ .claude/get-shit-done/`
6. Run `npm test` one final time to confirm everything works end-to-end
7. Verify the migrated KB content is correct: `ls .planning/knowledge/signals/ .planning/knowledge/reflections/ .planning/knowledge/spikes/`

Do NOT delete `~/.gsd/knowledge/` -- it remains as fallback for non-project contexts.
Do NOT commit `.planning/knowledge/` content in this plan -- that happens as part of normal git workflow.

Note: `.planning/knowledge/index.md` is a generated runtime artifact (output of kb-rebuild-index.sh). It is listed in files because it is a concrete output of this task, but it is regenerated on demand.
  </action>
  <verify>
Run: `test -f .planning/knowledge/index.md && echo "index exists" || echo "no index"` -- should print "index exists".
Run: `ls .planning/knowledge/signals/ | wc -l` -- should show migrated signal directories.
Run: `grep -rl '.planning/knowledge' .claude/agents/ .claude/get-shit-done/ | wc -l` -- should match count of source files updated (20+).
Run: `npm test` -- all tests pass.
  </verify>
  <done>KB content migrated from `~/.gsd/knowledge/` to `.planning/knowledge/`. Index rebuilt at new location. Installed `.claude/` files synced with source. `npm test` passes.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm test` passes (all KB-related test assertions updated across all 5 test files)
2. `.planning/knowledge/index.md` exists and contains signal/reflection/spike entries
3. `grep -rl '.planning/knowledge' .claude/agents/ .claude/get-shit-done/` lists all 20 installed files
4. `grep -rl '.planning/knowledge' agents/ get-shit-done/` lists all 20 source files
5. No file has `~/.gsd/knowledge/` as sole primary path (all have fallback pattern)
6. `bin/migrate-kb.sh` is executable and passes syntax check
7. All 5 test files contain `.planning/knowledge` references: `grep -l '.planning/knowledge' tests/unit/install.test.js tests/integration/multi-runtime.test.js tests/integration/cross-runtime-kb.test.js tests/integration/kb-infrastructure.test.js tests/smoke/run-smoke.sh`
8. ROADMAP success criterion #2 does not reference "lessons" for writes
</verification>

<success_criteria>
- `npm test` passes with project-local KB as primary (all 5 test files updated per locked decision)
- Installer creates `.planning/knowledge/` during setup when `.planning/` exists
- kb-rebuild-index.sh works on `.planning/knowledge/` without depending on `~/.gsd/bin/`
- Migration script copies signals, reflections, spikes (not lessons) to `.planning/knowledge/`
- ROADMAP success criterion #2 corrected (no stale "lessons" reference)
- Installed `.claude/` files match source files after `node bin/install.js --local`
</success_criteria>

<output>
After completion, create `.planning/phases/38.1-project-local-knowledge-base/38.1-02-SUMMARY.md`
</output>
