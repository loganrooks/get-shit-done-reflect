---
phase: 38.1-project-local-knowledge-base
plan: 03
type: execute
wave: 2
depends_on: ["38.1-01"]
files_modified:
  - get-shit-done/workflows/collect-signals.md
  - get-shit-done/references/signal-detection.md
autonomous: true

must_haves:
  truths:
    - "Signal frontmatter templates include source: local field"
    - "Signal frontmatter templates include environment.os, environment.node_version, environment.config_profile fields"
    - "Only new signals get enriched fields -- no backfill of existing signals"
  artifacts:
    - path: "get-shit-done/workflows/collect-signals.md"
      provides: "Signal collection workflow with enriched schema fields in signal template"
      contains: "source: local"
    - path: "get-shit-done/references/signal-detection.md"
      provides: "Signal detection reference with enriched schema fields"
      contains: "environment"
  key_links:
    - from: "get-shit-done/workflows/collect-signals.md"
      to: "agents/kb-templates/signal.md"
      via: "Signal frontmatter template consistency"
      pattern: "source:"
---

<objective>
Add COULD-tier signal schema enrichment fields to signal-producing workflow files. This adds `source: local` and `environment` fields (os, node_version, config_profile) to the signal frontmatter template so new signals carry environment context for future cross-project readiness.

Purpose: Lay groundwork for cross-project signal sharing by enriching signal metadata with environment context. This is optional (COULD tier per user decision) and independent of the core KB migration work.
Output: 2 workflow/reference files updated with enriched signal frontmatter template fields.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.1-project-local-knowledge-base/38.1-CONTEXT.md
@.planning/phases/38.1-project-local-knowledge-base/38.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add signal schema enrichment fields to signal-producing workflows</name>
  <files>
    get-shit-done/workflows/collect-signals.md
    get-shit-done/references/signal-detection.md
  </files>
  <action>
In the signal-producing workflow files (already updated in Plan 01 for KB paths), add environment fields to the signal frontmatter template.

**get-shit-done/workflows/collect-signals.md:**
- Find the signal frontmatter template/example (the YAML block that shows what fields a signal should have)
- Add after the existing fields:
  ```yaml
  source: local
  environment:
    os: "$(uname -s | tr '[:upper:]' '[:lower:]')"
    node_version: "$(node --version 2>/dev/null || echo 'unknown')"
    config_profile: "$(node -e \"try{console.log(JSON.parse(require('fs').readFileSync('.planning/config.json','utf8')).model_profile||'unknown')}catch{console.log('unknown')}\" 2>/dev/null || echo 'unknown')"
  ```
- If the template is in prose rather than a code block, add the fields as documentation: "New signals SHOULD include `source: local` and `environment` fields (os, node_version, config_profile) auto-populated from the runtime."
- Do NOT instruct backfilling existing signals -- only new signals get these fields.

**get-shit-done/references/signal-detection.md:**
- Find the signal schema/template section
- Add the same `source` and `environment` fields to the documented schema
- Add a note: "The `source` field defaults to `local`. Future cross-project signal sharing will use `source: external`."
- Add a note: "The `environment` fields are auto-populated and help diagnose whether a signal is environment-specific or universal."

**What NOT to do:**
- Do NOT modify `agents/kb-templates/signal.md` (that already has `gsd_version` from a prior phase; environment fields are workflow-level, not template-level)
- Do NOT modify any test files (signal schema enrichment is COULD tier; tests should not gate on optional fields)
- Do NOT backfill existing signal files with new fields

After changes, run `node bin/install.js --local` to sync to `.claude/` and verify with `npm test`.
  </action>
  <verify>
Run: `grep 'source: local' get-shit-done/workflows/collect-signals.md get-shit-done/references/signal-detection.md` -- both files should contain the field.
Run: `grep 'environment' get-shit-done/workflows/collect-signals.md get-shit-done/references/signal-detection.md` -- both files should reference environment fields.
Run: `node bin/install.js --local` -- sync succeeds.
Run: `npm test` -- all tests still pass (no test changes, so no regression).
  </verify>
  <done>Signal frontmatter templates enriched with `source: local` and `environment` fields (os, node_version, config_profile) in both signal-producing workflow files. Only new signals will carry these fields. Tests unaffected (COULD tier, no gating assertions).</done>
</task>

</tasks>

<verification>
After task completes:
1. `grep -c 'source: local' get-shit-done/workflows/collect-signals.md get-shit-done/references/signal-detection.md` -- both show at least 1 match
2. `npm test` passes (no regressions from optional schema additions)
3. `grep -rl 'source: local' .claude/get-shit-done/` -- installed files also contain the enrichment after sync
</verification>

<success_criteria>
- Signal schema enrichment fields (source, environment) added to signal templates in both workflow files
- `npm test` passes without regressions
- Installed `.claude/` files contain enriched templates after sync
</success_criteria>

<output>
After completion, create `.planning/phases/38.1-project-local-knowledge-base/38.1-03-SUMMARY.md`
</output>
