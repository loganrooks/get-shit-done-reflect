---
phase: 57.8-signal-provenance-split-artifact-signature-blocks
plan: 01
signature:
  role: executor
  harness: codex-cli
  platform: codex
  vendor: openai
  model: gpt-5.4
  reasoning_effort: xhigh
  profile: quality
  gsd_version: 1.19.4+dev
  generated_at: "2026-04-17T12:09:35.003Z"
  session_id: 019d9aa2-7ff7-7e30-848c-bfe8f70dd8a1
  provenance_status:
    role: derived
    harness: exposed
    platform: derived
    vendor: derived
    model: exposed
    reasoning_effort: exposed
    profile: derived
    gsd_version: derived
    generated_at: exposed
    session_id: exposed
  provenance_source:
    role: artifact_role
    harness: runtime_context
    platform: derived_from_harness
    vendor: derived_from_harness
    model: codex_state_store
    reasoning_effort: codex_state_store
    profile: config
    gsd_version: installed_harness
    generated_at: writer_clock
    session_id: "env:CODEX_THREAD_ID"
subsystem: provenance substrate
tags:
  - provenance
  - frontmatter
  - templates
  - workflow-artifacts
requires:
  - phase: 57.5-measurement-architecture-retroactive-foundation
    provides: runtime/session identity vocabulary and model resolution seams reused by the writer-side helper
provides:
  - shared writer-side provenance helper
  - nested signature validation for plan/summary/verification artifacts
  - summary and verification writers that stamp required signature data
affects:
  - signal provenance migration
  - artifact templates
  - workflow execution
tech-stack:
  added: []
  patterns:
    - shared provenance helper
    - nested signature frontmatter validation
key-files:
  created: [get-shit-done/bin/lib/provenance.cjs]
  modified:
    - get-shit-done/bin/lib/core.cjs
    - get-shit-done/bin/lib/frontmatter.cjs
    - get-shit-done/bin/lib/template.cjs
    - get-shit-done/bin/lib/commands.cjs
    - get-shit-done/templates/phase-prompt.md
    - get-shit-done/templates/summary-standard.md
    - get-shit-done/templates/summary-minimal.md
    - get-shit-done/templates/summary-complex.md
    - get-shit-done/templates/verification-report.md
    - get-shit-done/workflows/execute-plan.md
    - agents/gsd-planner.md
key-decisions:
  - Require full nested signature frontmatter on PLAN, SUMMARY, and VERIFICATION artifacts rather than optional top-level model hints
  - Persist unavailable writer-time facts as not_available with explicit provenance_status and provenance_source markers instead of guessing
patterns-established:
  - Writer-time provenance is assembled through get-shit-done/bin/lib/provenance.cjs and reused by artifact writers and later signal migration code
duration: 39 min
completed: 2026-04-17
---

# Phase 57.8 Plan 01: Provenance Substrate Summary

**Shared writer-side provenance and nested artifact signatures now exist as enforced code paths, not prompt-only conventions.**

## Performance
- **Duration:** 39 min
- **Tasks:** 3 completed
- **Files modified:** 12

## Accomplishments
- Added `get-shit-done/bin/lib/provenance.cjs` plus the core runtime-session seam needed to derive `harness`, `platform`, `vendor`, `model`, `reasoning_effort`, `gsd_version`, and `session_id` with explicit source/status markers.
- Reworked generic frontmatter parsing and serialization so embedded object arrays such as `about_work[]` round-trip cleanly, and `frontmatter validate` now rejects partial or empty `signature` blocks for PLAN, SUMMARY, and VERIFICATION artifacts.
- Aligned artifact writers, templates, planner guidance, and execute-plan workflow text around the same nested `signature` contract and the same `not_available` fallback rule.

## Task Commits
1. **Task 1: Add shared writer-side provenance helper** - `f29b8fda`
2. **Task 2: Upgrade frontmatter round-trip and validation** - `5c1a6de3`
3. **Task 3: Wire signature outputs through writers and templates** - `463f25f2`

## Files Created/Modified
- `get-shit-done/bin/lib/provenance.cjs` - Central provenance/signature builder for artifacts and later split-provenance signal writes.
- `get-shit-done/bin/lib/frontmatter.cjs` - Recursive object-array round-trip logic and nested required-path validation.
- `get-shit-done/bin/lib/template.cjs` and `get-shit-done/bin/lib/commands.cjs` - Artifact writers now stamp required signature blocks directly.
- `agents/gsd-planner.md`, `get-shit-done/templates/*`, `get-shit-done/workflows/execute-plan.md` - Planner and executor guidance now matches the enforced contract.

## Decisions & Deviations
None - plan executed as intended after one isolated-temp validation rerun. I deleted three accidental proof artifacts that were briefly created in the live phase directory when a temp validation command used the wrong cwd.

## Verification
- `buildArtifactSignature()` in the live Codex session resolved `harness: codex-cli`, `model: gpt-5.4`, `reasoning_effort: xhigh`, `gsd_version: 1.19.4+dev`, and `session_id` from `CODEX_THREAD_ID`, with explicit `provenance_status` / `provenance_source`.
- Temp PLAN, SUMMARY, and VERIFICATION artifacts generated via `gsd-tools template fill` all passed `frontmatter validate` under the new required signature schema.
- An empty `signature: {}` PLAN fixture failed validation with the full missing nested-path list.
- A direct round-trip fixture confirmed `about_work[]` stays an array of objects, `detected_by` and `written_by` stay nested objects, and serialization no longer emits `[object Object]`.

## Next Phase Readiness
Plan 02 can now migrate signal docs, workflow prompts, and KB persistence onto the shared provenance vocabulary without adding new runtime-detection or parser work.
