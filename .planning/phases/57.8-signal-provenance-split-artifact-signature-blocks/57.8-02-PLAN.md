---
phase: 57.8-signal-provenance-split-artifact-signature-blocks
plan: 02
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - agents/gsd-artifact-sensor.md
  - agents/gsd-signal-synthesizer.md
  - agents/knowledge-store.md
  - get-shit-done/references/signal-detection.md
  - get-shit-done/workflows/collect-signals.md
  - get-shit-done/bin/lib/kb.cjs
autonomous: true
must_haves:
  truths:
    - "Signal docs, sensor specs, and collect-signals workflow now describe role-split provenance using `about_work[]`, `detected_by`, `written_by`, and `provenance_schema`, not a single flat runtime/model payload."
    - "Sensors stamp detector provenance only; they do not infer `about_work[]` from their own runtime identity."
    - "KB rebuild accepts both legacy flat provenance and the new split provenance without errors, while dual-writing deprecated flat echoes for the one-milestone compatibility window."
    - "Writer-side `gsd_version` persisted in split provenance follows the installed-harness > config > repo-mirror precedence and carries a source marker instead of hiding precedence inside prompts."
  artifacts:
    - path: "get-shit-done/references/signal-detection.md"
      provides: "Canonical split-provenance signal schema and deprecation language for legacy flat fields"
      contains: "about_work"
    - path: "agents/knowledge-store.md"
      provides: "Updated KB/storage contract for split provenance and compatibility window"
      contains: "provenance_schema"
    - path: "get-shit-done/bin/lib/kb.cjs"
      provides: "Additive persistence and rebuild support for split provenance plus legacy echoes"
      contains: "written_by"
    - path: "get-shit-done/workflows/collect-signals.md"
      provides: "Workflow-level signal candidate handling aligned with the split provenance contract"
      contains: "detected_by"
  key_links:
    - from: "get-shit-done/bin/lib/provenance.cjs"
      to: "get-shit-done/bin/lib/kb.cjs"
      via: "shared writer-side provenance shape is persisted without drift between artifact writers and KB rebuild"
      pattern: "about_work|detected_by|written_by"
    - from: "agents/gsd-artifact-sensor.md"
      to: "agents/gsd-signal-synthesizer.md"
      via: "sensor emits detector provenance only and synthesizer adds writer provenance plus compatibility echoes"
      pattern: "detected_by|written_by"
---

<objective>
Migrate the signal pipeline and KB from flat provenance to the 57.8 role-split contract.

Purpose: the artifact substrate from Plan 01 is useless unless signals, workflow prompts, and the KB persist the same structure without collapsing work provenance, detector provenance, and writer provenance back into one blob.

Output: aligned docs/specs/workflows plus additive KB support for split provenance and the one-milestone compatibility bridge.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/57.8-signal-provenance-split-artifact-signature-blocks/57.8-CONTEXT.md
@.planning/phases/57.8-signal-provenance-split-artifact-signature-blocks/57.8-RESEARCH.md
@.planning/phases/57.8-signal-provenance-split-artifact-signature-blocks/57.8-01-PLAN.md

@agents/gsd-artifact-sensor.md
@agents/gsd-signal-synthesizer.md
@agents/knowledge-store.md
@get-shit-done/references/signal-detection.md
@get-shit-done/workflows/collect-signals.md
@get-shit-done/bin/lib/kb.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the signal contract across docs, specs, and workflow prompts</name>
  <files>agents/gsd-artifact-sensor.md, agents/gsd-signal-synthesizer.md, agents/knowledge-store.md, get-shit-done/references/signal-detection.md, get-shit-done/workflows/collect-signals.md</files>
  <action>
Replace the old flat provenance story everywhere the repo teaches or instructs signal writing.

Required outcomes:

- `signal-detection.md` documents `about_work[]`, `detected_by`, `written_by`, `provenance_schema`, and the legacy flat-field deprecation window
- `knowledge-store.md` describes how split provenance is stored and how legacy readers remain supported
- artifact sensor spec explicitly limits itself to detector provenance
- signal synthesizer spec explicitly owns writer provenance and compatibility echoes
- collect-signals workflow text no longer implies that `runtime` / `model` alone identify the work being judged

Do not let the sensor spec smuggle `about_work[]` inference back in through wording like "stamp runtime/model on the candidate." That is the exact collapse 57.8 exists to stop.
  </action>
  <verify>
Use `rg` across the modified files to confirm:

- old flat-only wording is removed or explicitly marked deprecated
- `about_work`, `detected_by`, `written_by`, and `provenance_schema` appear in all relevant instruction surfaces
- no sensor instruction claims detector provenance is sufficient to identify work provenance
  </verify>
  <done>
All human-readable and agent-readable signal contracts agree on the role-split provenance model and the one-milestone compatibility window.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend KB rebuild and row mapping additively for split provenance</name>
  <files>get-shit-done/bin/lib/kb.cjs</files>
  <action>
Implement the storage bridge without breaking existing signal files.

Expected shape:

- keep legacy columns `runtime`, `model`, `gsd_version`
- add split-provenance storage fields/columns such as:
  - `provenance_schema`
  - `about_work_json`
  - `detected_by_json`
  - `written_by_json`
  - `provenance_status`

Row mapping and rebuild must:

- read the new split fields when present
- preserve legacy flat rows
- continue to populate old flat fields for one milestone cycle from the new split structure where justified
- avoid contradictory duplication; detector/writer/work roles must not be merged back into the same meaning

If query output needs to surface both legacy and split values, do it transparently and additively rather than by picking one and hiding the other.
  </action>
  <verify>
Create or use focused signal fixtures to prove:

- a legacy flat signal rebuilds cleanly
- a new split-provenance signal rebuilds cleanly
- the resulting KB row preserves `provenance_schema`
- legacy flat columns remain populated for compatibility where the contract says they should
  </verify>
  <done>
KB rebuild supports old and new provenance formats simultaneously, and the compatibility bridge is explicit rather than accidental.
  </done>
</task>

<task type="auto">
  <name>Task 3: Align workflow persistence with version-precedence and provenance-source markers</name>
  <files>agents/gsd-signal-synthesizer.md, get-shit-done/workflows/collect-signals.md, get-shit-done/bin/lib/kb.cjs</files>
  <action>
Make the live write path tell the truth about where provenance facts came from.

Specifically:

- `gsd_version` persisted in `written_by` and any compatibility echoes must carry a source marker:
  - `installed_harness`
  - `config`
  - `repo_mirror`
  - `not_available`
- the synthesizer should not silently prefer the repo-local `.codex/get-shit-done-reflect/VERSION` in the source repo
- workflow instructions must make clear that `not_available` is the required fallback when runtime/session facts are absent

Second-order effect to watch: if KB dual-write happens before this precedence fix is centralized, the repo will preserve the wrong version with more confidence rather than less.
  </action>
  <verify>
Exercise or fixture the writer-side precedence path so that:

- a stale repo mirror loses to installed-harness/config evidence
- the persisted provenance includes the source marker
- missing evidence produces `not_available` instead of defaulting to the mirror
  </verify>
  <done>
The signal pipeline now persists role-split provenance with explicit source markers and no hidden mirror-first fallback.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Signal docs/specs/workflow all use the same split-provenance vocabulary
- [ ] KB rebuild accepts both legacy and split provenance signal files
- [ ] Compatibility echoes remain readable for one milestone cycle
- [ ] `gsd_version` source precedence is explicit in persisted provenance
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Signal candidates and persisted signals no longer collapse work/detector/writer roles
- Plan 03 can validate and annotate the legacy corpus against the final schema
  </success_criteria>

<output>
After completion, create `.planning/phases/57.8-signal-provenance-split-artifact-signature-blocks/57.8-02-SUMMARY.md`
</output>
