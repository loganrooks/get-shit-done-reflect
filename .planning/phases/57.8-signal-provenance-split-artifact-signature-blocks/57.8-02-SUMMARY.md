---
phase: 57.8-signal-provenance-split-artifact-signature-blocks
plan: 02
signature:
  role: executor
  harness: codex-cli
  platform: codex
  vendor: openai
  model: gpt-5.4
  reasoning_effort: xhigh
  profile: quality
  gsd_version: 1.19.4+dev
  generated_at: "2026-04-17T12:18:00.000Z"
  session_id: 019d9aa2-7ff7-7e30-848c-bfe8f70dd8a1
  provenance_status:
    role: derived
    harness: exposed
    platform: derived
    vendor: derived
    model: exposed
    reasoning_effort: exposed
    profile: derived
    gsd_version: derived
    generated_at: exposed
    session_id: exposed
  provenance_source:
    role: artifact_role
    harness: runtime_context
    platform: derived_from_harness
    vendor: derived_from_harness
    model: codex_state_store
    reasoning_effort: codex_state_store
    profile: config
    gsd_version: installed_harness
    generated_at: writer_clock
    session_id: "env:CODEX_THREAD_ID"
subsystem: signal provenance pipeline
tags:
  - provenance
  - signals
  - knowledge-base
  - compatibility
requires:
  - phase: 57.8-signal-provenance-split-artifact-signature-blocks
    provides: Wave 1 shared provenance helper and nested frontmatter support
provides:
  - split-provenance signal contract across sensors, synthesizer, workflow, and KB docs
  - additive KB storage columns for about_work, detected_by, written_by, provenance_schema, and provenance_status
  - legacy flat echo bridge derived from split provenance during rebuild
affects:
  - signal collection
  - KB rebuild
  - legacy signal compatibility
tech-stack:
  added: []
  patterns:
    - split provenance with additive flat compatibility echoes
    - KB row mapping that derives legacy analytics fields from writer provenance
key-files:
  created: []
  modified:
    - get-shit-done/bin/lib/kb.cjs
    - agents/gsd-artifact-sensor.md
    - agents/gsd-signal-synthesizer.md
    - agents/knowledge-store.md
    - agents/kb-templates/signal.md
    - get-shit-done/references/signal-detection.md
    - get-shit-done/workflows/collect-signals.md
key-decisions:
  - Treat runtime/model/gsd_version as legacy compatibility echoes derived from written_by or detected_by rather than as the canonical provenance payload
  - Keep sensors limited to detector provenance and make the synthesizer own written_by, about_work, schema markers, and legacy echoes
patterns-established:
  - KB rebuild now stores split provenance additively while keeping legacy analytics queries readable through derived flat echoes
duration: 33 min
completed: 2026-04-17
---

# Phase 57.8 Plan 02: Signal Pipeline Summary

**Signal collection and KB rebuild now understand split provenance without collapsing detector, writer, and work identity back into one flat blob.**

## Performance
- **Duration:** 33 min
- **Tasks:** 3 completed
- **Files modified:** 7

## Accomplishments
- Rewrote the signal contract across the artifact sensor, synthesizer, knowledge-store reference, signal-detection reference, workflow prompt, and signal template so `about_work[]`, `detected_by`, `written_by`, and `provenance_schema` are the canonical story.
- Extended `get-shit-done/bin/lib/kb.cjs` additively with split-provenance storage columns and schema stats, while preserving legacy `runtime`, `model`, and `gsd_version` columns for one-milestone compatibility.
- Verified that a split-provenance signal file can omit the flat fields entirely and still rebuild into a row that preserves the structured JSON payload and derives the legacy flat echoes from `written_by`.

## Task Commits
1. **Task 1: Rewrite split-provenance signal contract surfaces** - `bb9de480`
2. **Task 2: Extend KB rebuild for split provenance** - `96305c26`

## Files Created/Modified
- `get-shit-done/bin/lib/kb.cjs` - Additive schema extension and row mapping for split provenance plus flat echo derivation.
- `agents/gsd-artifact-sensor.md` and `agents/gsd-signal-synthesizer.md` - Detector-only sensor provenance and synthesizer-owned writer/work provenance.
- `agents/knowledge-store.md`, `agents/kb-templates/signal.md`, `get-shit-done/references/signal-detection.md`, `get-shit-done/workflows/collect-signals.md` - Canonical signal schema, template, and workflow guidance updated to the new contract.

## Decisions & Deviations
**[Rule 2 - Missing Critical] Include the signal template in the contract rewrite**
- **Found during:** Task 1
- **Issue:** `agents/kb-templates/signal.md` still taught the flat-only provenance shape even after the sensor/synthesizer docs moved to split provenance.
- **Fix:** Updated the template alongside the agent and workflow docs so the live write path does not silently drift from the documented contract.
- **Verification:** The KB rebuild proof fixture used the updated split-provenance shape successfully.

## Verification
- A temp KB with one legacy flat signal and one split-provenance signal rebuilt successfully under schema version `2`.
- The rebuilt legacy row surfaced `provenance_schema: v1_legacy` with empty structured provenance columns and preserved the original flat echoes.
- The rebuilt split row surfaced `provenance_schema: v2_split`, populated `about_work_json`, `detected_by_json`, and `written_by_json`, and derived `runtime: codex-cli`, `model: gpt-5.4`, and `gsd_version: 1.19.4+dev` from the structured provenance without needing those flat fields in the file.
- `kb stats --raw` now reports split vs legacy distribution via `by_provenance_schema`.
- A direct `buildArtifactSignature()` check still resolved `gsd_version_source: installed_harness`, proving the writer-side precedence remains installed-harness first even in the source repo with a stale repo mirror.

## Next Phase Readiness
Plan 03 can now add regression coverage and annotate the audited 57.6 legacy corpus against a live storage bridge instead of a hypothetical one.
