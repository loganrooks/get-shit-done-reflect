---
phase: 57.7-content-analysis-epistemic-deepening-inserted
plan: 09
model: gpt-5-codex
context_used_pct: 27
subsystem: measurement-derived
tags: [measurement, derived, reasoning-quality, facets]
requires:
  - phase: 57.7
    provides: Plan 02 spike 012 chose `facets-substitute` with `grader_independence: self_graded` as the only cleared reasoning-quality mechanism
provides:
  - Registers a single derived `reasoning_quality_proxy` extractor on the Claude facet surface
  - Preserves proxy-only labelling, inferred reliability, and self-graded provenance in every row
  - Adds focused unit coverage for direct facet reads, missing-facet availability, and registry inclusion
affects: [Phase 57.7, MEAS-RUNTIME-11, agent_performance]
tech-stack:
  added: []
  patterns: [direct facet reads, proxy-only labeling, self-graded provenance preservation]
key-files:
  created:
    [
      tests/unit/measurement-reasoning-quality-proxy.test.js
    ]
  modified:
    [
      get-shit-done/bin/lib/measurement/extractors/derived.cjs
    ]
key-decisions:
  - "The shipped mechanism mirrors Spike 012's evaluated formula and reads `session.facets.record` directly instead of chaining through `context.computedFeatures`."
  - "Facet coverage gaps stay explicit as `not_available` rows with `skip_reason: facets_unavailable`; missing coverage is evidence, not something to hide."
patterns-established:
  - "When a spike winner is self-graded, the live extractor must preserve that epistemic qualification in provenance rather than flattening it into a generic success claim."
duration: 7min
completed: 2026-04-17
---

# Phase 57.7 Plan 09 Summary

**Shipped the `reasoning_quality_proxy` surface as a facets-backed derived proxy, keeping the feature explicitly inferred, proxy-labelled, and qualified by Spike 012's `self_graded` verdict.**

## Performance

- **Duration:** 7min
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added `reasoning_quality_proxy` to `DERIVED_EXTRACTORS` in `derived.cjs` with the exact `facets-substitute` formula trialed in Spike 012.
- Kept the extractor on the Claude facet surface and read `session.facets.record` directly, avoiding any dependency on `context.computedFeatures` ordering.
- Forced the feature to remain a proxy by shipping `reliability_tier: inferred`, `proxy_label: reasoning_quality_proxy_only`, and the `grader_independence: self_graded` provenance note on every row.
- Preserved missing facet coverage as `not_available` with `skip_reason: facets_unavailable` instead of silently dropping sessions from the feature surface.
- Added unit coverage for the score shape, direct-facet-read invariant, missing-facet behavior, and registry registration.

## Runtime Outcome

- **Total `reasoning_quality_proxy` rows on current corpus:** `143`
- **Derived rows:** `61`
- **Not-available rows:** `82`
- **Observed skip reason on unavailable rows:** `facets_unavailable`

## Task Commits

1. **Task 1: Add the facets-backed proxy extractor and focused unit coverage** - `1d98821c`

## Files Created/Modified

- `get-shit-done/bin/lib/measurement/extractors/derived.cjs` - Adds the proxy score helpers, the `reasoning_quality_proxy` extractor, and the derived export/registry wiring.
- `tests/unit/measurement-reasoning-quality-proxy.test.js` - Covers score composition, proxy labelling, direct facet reads, missing-facet availability, and registry inclusion.

## Decisions & Deviations

- The plan's conditional `runtime.cjs` path was not needed because Spike 012 ended `CHOSEN: facets-substitute`; no runtime extractor or JSONL heuristic path was added here.
- The proxy formula intentionally mirrors the evaluated spike script, including outcome/helpfulness weights and explicit friction penalties, so the shipped mechanism matches the evidence used to choose it.
- This plan did not convert the proxy into a truth claim. The live surface stays explicitly inferred and self-qualified in provenance, per DC-4 / G-5 and the Spike 012 decision language.

## Verification

- `node -e "require('./get-shit-done/bin/lib/measurement/extractors/derived.cjs')"`
- `npm test -- measurement-reasoning-quality-proxy`
- `npm test -- measurement`
- `node ./get-shit-done/bin/gsd-tools.cjs measurement rebuild`
- `node ./get-shit-done/bin/gsd-tools.cjs measurement query "agent performance" --raw`

## User Setup Required

None.

## Next Phase Readiness

Plan 10 can now demo the agent-performance loop with both late-wave additions present: `reasoning_tokens_reconciler` and `reasoning_quality_proxy`.

## Self-Check: PASSED

- `reasoning_quality_proxy` is present in both `DERIVED_EXTRACTORS` and `buildRegistry()`.
- The extractor reads `session.facets.record` directly and ignores bogus `computedFeatures` input in unit coverage.
- Missing facets produce `not_available` rows with `skip_reason: facets_unavailable`.
- The full `measurement` suite stayed green (`22` files, `109` tests).
- Rebuild succeeded with run id `22`, and raw agent-performance query output includes `reasoning_quality_proxy` rows.
