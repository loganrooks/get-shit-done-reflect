---
phase: 57.7-content-analysis-epistemic-deepening-inserted
plan: 04
model: gpt-5-codex
context_used_pct: 24
subsystem: measurement-runtime
tags: [measurement, runtime, content-analysis, privacy]
requires:
  - phase: 57.7
    provides: Plan 04 depends on the Plan 01 tokenizer verdict, Plan 02 proxy selection, and Plan 03 registry-cleanup groundwork
provides:
  - Ships `tool_invocation_sequence` and `topic_shift_markers` as Claude-only structural extractors
  - Preserves optional `content_contract` metadata on frozen registry entries
  - Adds unit coverage for privacy and registration invariants
affects: [Phase 57.7, MEAS-ARCH-09, Phase 58 GATE-09]
tech-stack:
  added: []
  patterns: [content-derived feature contracts, structural JSONL scanning, registry metadata preservation]
key-files:
  created: [tests/unit/measurement-content-extractors.test.js]
  modified:
    [
      get-shit-done/bin/lib/measurement/registry.cjs,
      get-shit-done/bin/lib/measurement/sources/claude.cjs,
      get-shit-done/bin/lib/measurement/extractors/runtime.cjs
    ]
key-decisions:
  - "content_contract stays optional in the extractor contract, but `defineExtractor` now preserves it as a queryable frozen-entry field."
  - "`tool_invocation_sequence` serves `pipeline_integrity` and `agent_performance`; `topic_shift_markers` serves `pipeline_integrity` and `cross_session_patterns`."
patterns-established:
  - "Content-structural extractors may expose only derived names, counts, and positions; raw tool arguments and message bodies stay out of measurement rows."
duration: 10min
completed: 2026-04-17
---

# Phase 57.7 Plan 04 Summary

**Shipped the first two structural content extractors and made the registry preserve extractor privacy contracts downstream.**

## Performance

- **Duration:** 10min
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Extended the measurement registry so optional `content_contract` values are validated when present and preserved on frozen extractor entries, with `null` retained for older extractors.
- Added three Claude JSONL scan helpers: `scanToolInvocationSequence`, `toolCategoryFor`, and `scanTopicShiftMarkers`.
- Registered `tool_invocation_sequence` and `topic_shift_markers`, appended them to `RUNTIME_EXTRACTORS`, and verified they surface through `measurement query "pipeline integrity" --raw`.
- Added focused unit coverage for happy paths, privacy invariants, `not_available` / `not_emitted` handling, registry preservation, and extractor registration.

## Task Commits

1. **Task 1: Add content-contract validation/preservation and Claude scan helpers** - `242d1c68`
2. **Task 2: Register structural extractors and add unit coverage** - `aaeb4b95`

## Files Created/Modified

- `get-shit-done/bin/lib/measurement/registry.cjs` - Validates optional `content_contract` values and preserves them on frozen extractor entries.
- `get-shit-done/bin/lib/measurement/sources/claude.cjs` - Adds helper scans for tool-use sequencing and structural topic-shift detection.
- `get-shit-done/bin/lib/measurement/extractors/runtime.cjs` - Registers `tool_invocation_sequence` and `topic_shift_markers` and appends both to `RUNTIME_EXTRACTORS`.
- `tests/unit/measurement-content-extractors.test.js` - Covers scan helpers, privacy behavior, registry preservation, and extractor inclusion.

## Decisions & Deviations

- The plan text was internally inconsistent about extra loop mappings. I followed the stronger phase/context guidance: both extractors are `pipeline_integrity`-tagged for GATE-09, while `tool_invocation_sequence` also serves `agent_performance` and `topic_shift_markers` also serves `cross_session_patterns`.
- No schema churn was introduced beyond the additive optional `content_contract` field handling.

## Verification

- `npm test -- measurement-content-extractors`
- `npm test -- measurement`
- `node ./get-shit-done/bin/gsd-tools.cjs measurement rebuild`
- `node ./get-shit-done/bin/gsd-tools.cjs measurement query "pipeline integrity" --raw`

## User Setup Required

None.

## Next Phase Readiness

Plan 05 can now build on the established scan-helper pattern and the frozen-entry `content_contract` fix to calibrate and, if warranted, ship `intervention_points` honestly.

## Self-Check: PASSED

- Both new extractors are present in `RUNTIME_EXTRACTORS` and `buildRegistry()`.
- `measurement rebuild` succeeded over the current corpus with run id `18`.
- Raw query output includes both `tool_invocation_sequence` and `topic_shift_markers`.
