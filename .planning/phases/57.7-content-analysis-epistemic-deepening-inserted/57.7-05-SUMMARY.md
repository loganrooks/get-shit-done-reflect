---
phase: 57.7-content-analysis-epistemic-deepening-inserted
plan: 05
model: gpt-5-codex
context_used_pct: 31
subsystem: measurement-runtime
tags: [measurement, runtime, intervention, calibration]
requires:
  - phase: 57.7
    provides: Plan 04 established the frozen-entry content_contract fix and the runtime scan-helper pattern
provides:
  - Calibrates the intervention heuristic on the live Claude corpus
  - Ships `intervention_points` in live mode with a documented heuristic version
  - Adds unit coverage for redirect markers, interrupt placeholders, and extractor registration
affects: [Phase 57.7, MEAS-ARCH-09, Phase 58 GATE-09]
tech-stack:
  added: []
  patterns: [calibrated structural heuristics, live-or-schema-only gating, derived-features-only measurement rows]
key-files:
  created:
    [
      .planning/phases/57.7-content-analysis-epistemic-deepening-inserted/57.7-05-CALIBRATION.md,
      tests/unit/measurement-intervention-points.test.js
    ]
  modified:
    [
      get-shit-done/bin/lib/measurement/sources/claude.cjs,
      get-shit-done/bin/lib/measurement/extractors/runtime.cjs
    ]
key-decisions:
  - "The seed marker list alone was too weak; 57.7-v1 counts Claude's explicit `[Request interrupted by user]` placeholder as a structural intervention signal."
  - "`intervention_points` ships live because long-session fire rate cleared threshold and the reviewed false-positive rate was zero."
patterns-established:
  - "When a heuristic needs calibration, refine it narrowly against corpus-native structural markers before falling back to schema-only shipping."
duration: 10min
completed: 2026-04-17
---

# Phase 57.7 Plan 05 Summary

**Calibrated the intervention heuristic against the live Claude corpus and shipped `intervention_points` in live mode with an explicit heuristic version.**

## Performance

- **Duration:** 10min
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Added `scanInterventionPoints` to the Claude source helpers, using the existing `isHumanTurnRecord()` guard and a calibrated structural marker set.
- Wrote a calibration report over a deterministic 50-session sample, plus a full-corpus cross-check, showing that the long-session fire-rate threshold is met once the Claude interrupt placeholder is treated as a structural intervention event.
- Registered `intervention_points`, appended it to `RUNTIME_EXTRACTORS`, and kept the stored value privacy-safe by dropping calibration-only `sampled_matches`.
- Added unit coverage for redirect markers, request-interrupted placeholders, tool-result filtering, and registry/export inclusion.

## Calibration outcome

- **Verdict:** `PASS`
- **Live ship mode:** `intervention_points` emits numeric features
- **Sample long-session fire rate:** `19.23%` (`5 / 26`)
- **Reviewed false-positive rate:** `0.00%` (`0 / 13`, reviewing every flagged turn in the matched corpus because fewer than 20 existed)
- **Calibration artifact:** `.planning/phases/57.7-content-analysis-epistemic-deepening-inserted/57.7-05-CALIBRATION.md`

## Task Commits

1. **Task 1: Calibrate heuristic and document verdict** - `818c60e3`
2. **Task 2: Register live extractor and add unit coverage** - `d4f421d2`

## Files Created/Modified

- `get-shit-done/bin/lib/measurement/sources/claude.cjs` - Adds the intervention scan helper and the calibrated interrupt-placeholder path.
- `.planning/phases/57.7-content-analysis-epistemic-deepening-inserted/57.7-05-CALIBRATION.md` - Documents the sample design, bucket stats, manual review, and explicit PASS verdict.
- `get-shit-done/bin/lib/measurement/extractors/runtime.cjs` - Registers `intervention_points` with `heuristic_version: 57.7-v1`.
- `tests/unit/measurement-intervention-points.test.js` - Covers helper behavior and extractor registration.

## Decisions & Deviations

- The original seed-marker list produced zero matches in the 50-session sample. Inspection of the corpus showed that almost every real candidate was surfaced as Claude's `[Request interrupted by user]` placeholder, so the calibrated heuristic counts that exact marker as a structural stop-request event.
- The plan's intended 20-row false-positive sample was impossible because the entire matched corpus only produced 13 flagged turns under the calibrated heuristic. The calibration therefore reviewed all 13 flagged turns instead.

## Verification

- `npm test -- measurement-intervention-points`
- `npm test -- measurement`
- `node ./get-shit-done/bin/gsd-tools.cjs measurement rebuild`
- `node ./get-shit-done/bin/gsd-tools.cjs measurement query "pipeline integrity" --raw`

## User Setup Required

None.

## Next Phase Readiness

Plan 06 can now audit `distinguishes` coverage and write the privacy/intervention sidecar docs against a registry that already contains all three shipped 57.7 runtime content extractors from Wave 2 plus this intervention extractor.

## Self-Check: PASSED

- `intervention_points` is present in `RUNTIME_EXTRACTORS` and `buildRegistry()`.
- `content_contract` remains queryable as `derived_features_only`.
- `measurement rebuild` succeeded with run id `19`, and raw query output includes `intervention_points`.
