---
phase: 57.7-content-analysis-epistemic-deepening-inserted
plan: 01
model: gpt-5-codex
context_used_pct: 36
subsystem: measurement-spike
tags: [spike, tokenizer, claude, reasoning-tokens]
requires:
  - phase: 57.5
    provides: Spike design precedent and phantom-token question framing carried into C3
provides:
  - Commits a reproducible tokenizer-evaluation corpus and runner for Claude sessions
  - Locks the tokenizer verdict to FAIL on the current corpus
  - Locks Plan 08 to schema-only Claude shipping with tokenizer_unavailable
affects: [Phase 57.7, MEAS-RUNTIME-05, Plan 08]
tech-stack:
  added: [js-tiktoken in spike-local devDependencies only]
  patterns: [design-before-decision spike flow, schema-only gating after failed calibration]
key-files:
  created:
    [
      .planning/spikes/011-C3-tokenizer-availability/DECISION.md,
      .planning/spikes/011-C3-tokenizer-availability/experiments/package.json,
      .planning/spikes/011-C3-tokenizer-availability/experiments/run-c3-eval.cjs,
      .planning/spikes/011-C3-tokenizer-availability/supporting-data/tokenizer-evaluation.md
    ]
  modified:
    [
      .planning/spikes/011-C3-tokenizer-availability/DESIGN.md,
      .planning/spikes/011-C3-tokenizer-availability/supporting-data/sample-sessions.json
    ]
key-decisions:
  - "No candidate cleared the 15% median-relative-error threshold, so Claude reasoning-token reconciliation remains schema-only."
  - "production_dependency_decision stays reject_top_level_dependency_schema_only for 57.7."
patterns-established:
  - "Claude tokenizer gate: failed empirical spike means honest not_available shipping, not approximation-based emission."
duration: 20min
completed: 2026-04-17
---

# Phase 57.7 Plan 01 Summary

**Spike C3 answered Q1 with a negative result: neither evaluated tokenizer is accurate enough for Claude production use, so Plan 08 must ship a schema-only Claude branch.**

## Performance

- **Duration:** 20min
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Wrote and committed the spike design plus a 50-record metadata-only evaluation corpus.
- Added a spike-local evaluation runner and committed per-record/tokenizer results for `js-tiktoken(cl100k_base)` and the `charDiv4` baseline.
- Recorded `FAIL: schema-only` with `production_dependency_decision: reject_top_level_dependency_schema_only`, which locks Plan 08 away from a Claude-side computed reconciler.

## Task Commits

1. **Task 1: Write DESIGN.md and bootstrap spike directory with evaluation corpus metadata** - `59580c2d`
2. **Task 2: Run the C3 experiment and commit supporting-data** - `8973a6bc`
3. **Task 3: Write DECISION.md reading the evaluation data and locking the verdict + production_dependency_decision for Plan 08** - `4f820cd5`

## Files Created/Modified

- `.planning/spikes/011-C3-tokenizer-availability/DESIGN.md` - Pre-commits the thresholds, candidates, privacy model, and schema-only fallback.
- `.planning/spikes/011-C3-tokenizer-availability/supporting-data/sample-sessions.json` - Metadata-only 50-session evaluation corpus.
- `.planning/spikes/011-C3-tokenizer-availability/experiments/package.json` - Spike-local dev dependency manifest.
- `.planning/spikes/011-C3-tokenizer-availability/experiments/run-c3-eval.cjs` - Reproducible tokenizer comparison runner.
- `.planning/spikes/011-C3-tokenizer-availability/supporting-data/tokenizer-evaluation.md` - Per-record results and summary statistics.
- `.planning/spikes/011-C3-tokenizer-availability/DECISION.md` - Locks the FAIL/schema-only verdict and Plan 08 downstream behavior.

## Decisions & Deviations

- **Verdict:** `FAIL: schema-only`
- **production_dependency_decision:** `reject_top_level_dependency_schema_only`
- `js-tiktoken(cl100k_base)` achieved a `2.00%` negative-delta rate but missed the accuracy threshold badly (`60.93%` median relative error).
- `charDiv4` achieved a `14.00%` negative-delta rate but still missed the accuracy threshold badly (`51.78%` median relative error).
- Minor deviation: the first Task 2 run crashed on `encoder.free()` even though it had already written the evaluation file. The runner was patched to remove the invalid cleanup call, rerun cleanly, and the resulting numbers were unchanged.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 08 can now read a definitive Claude-side gate: register the extractor schema, but return `not_available` with `skip_reason: tokenizer_unavailable`. No top-level tokenizer dependency should be added in 57.7.

## Self-Check: PASSED

- Summary, DESIGN, evaluation data, and DECISION artifacts exist on disk.
- Commit order preserves DESIGN -> evaluation -> DECISION.
- Top-level `package.json` remains unchanged by this plan.
