---
phase: 57.7-content-analysis-epistemic-deepening-inserted
plan: 08
model: gpt-5-codex
context_used_pct: 31
subsystem: measurement-runtime
tags: [measurement, runtime, reasoning-tokens, codex]
requires:
  - phase: 57.7
    provides: Plan 01 spike 011 fixed the Claude ship mode and production dependency decision for reasoning-token reconciliation
provides:
  - Registers a single cross-runtime `reasoning_tokens_reconciler` extractor
  - Adds Codex reasoning-token source helpers plus absent-field guardrails
  - Extends manifest skip-reason vocabulary and adds focused runtime coverage
affects: [Phase 57.7, MEAS-RUNTIME-05, agent_performance, cross_runtime_comparison]
tech-stack:
  added: []
  patterns: [schema-first extractor seams, file-level runtime guards, unified cross-runtime measurement axis]
key-files:
  created:
    [
      tests/unit/measurement-reasoning-tokens-reconciler.test.js
    ]
  modified:
    [
      get-shit-done/bin/lib/measurement/sources/codex.cjs,
      get-shit-done/bin/lib/measurement/extractors/runtime.cjs,
      get-shit-done/feature-manifest.json
    ]
key-decisions:
  - "Spike 011 remains authoritative: Claude ships schema-only with `tokenizer_unavailable`, and this plan does not add `js-tiktoken` or touch top-level dependencies."
  - "The Codex branch reads only `token_count.reasoning_output_tokens`; absent fields surface as `codex_reasoning_tokens_field_absent` rather than inferred estimates."
patterns-established:
  - "Cross-runtime seams can ship honestly even when one or both runtimes are currently schema-only, as long as the extractor surface and skip-reason vocabulary are explicit."
duration: 8min
completed: 2026-04-17
---

# Phase 57.7 Plan 08 Summary

**Shipped the unified `reasoning_tokens` seam with explicit runtime guards, keeping Claude schema-only per Spike 011 and letting Codex report direct-count absence honestly on the current corpus.**

## Performance

- **Duration:** 8min
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Added `readReasoningOutputTokens()` and `summarizeCodexReasoningTokens()` in `sources/codex.cjs` so Codex rollout parsing is centralized and absent-field handling is deterministic.
- Registered one extractor, `reasoning_tokens_reconciler`, across `claude-code` and `codex-cli`, preserving the unified `reasoning_tokens` axis instead of splitting per runtime.
- Wired the Claude branch to the Spike 011 decision file via canonical frontmatter parsing and kept the current ship mode schema-only because the verdict is `FAIL: schema-only` and `production_dependency_decision` is `reject_top_level_dependency_schema_only`.
- Extended `feature-manifest.json` with `measurement_skip_reasons` for `tokenizer_unavailable` and `codex_reasoning_tokens_field_absent`.
- Added focused unit coverage for the Codex helpers, the Claude live/schema-only matrix, the negative-delta flag, and extractor registration.

## Runtime Outcome

- **Claude branch:** `143` rows `not_available`
- `113` with `skip_reason: tokenizer_unavailable`
- `30` with `skip_reason: parent_jsonl_not_matched`
- **Codex branch:** `73` rows `not_available`
- `73` with `skip_reason: codex_reasoning_tokens_field_absent`
- **Exposed rows on current corpus:** `0`
- **Observed negative-delta flags on current Claude corpus:** `0`

## Task Commits

1. **Task 1: Add source helpers and skip-reason vocabulary** - `16cc9278`
2. **Task 2: Register runtime extractor and add unit coverage** - `e5fa3ea3`

## Files Created/Modified

- `get-shit-done/bin/lib/measurement/sources/codex.cjs` - Adds direct-field reading and rollout summarization helpers for Codex reasoning tokens.
- `get-shit-done/bin/lib/measurement/extractors/runtime.cjs` - Registers `reasoning_tokens_reconciler`, reads the Spike 011 decision file, and emits explicit per-runtime skip reasons.
- `get-shit-done/feature-manifest.json` - Adds measurement-level canonical skip-reason vocabulary for the new extractor.
- `tests/unit/measurement-reasoning-tokens-reconciler.test.js` - Covers Codex helper behavior, Claude live/schema-only branches, negative-delta handling, and registry inclusion.

## Decisions & Deviations

- No package/dependency changes were made. Because Spike 011 ended in `FAIL: schema-only` and `reject_top_level_dependency_schema_only`, the live Claude branch is intentionally unreachable on the real corpus in this plan.
- The current Codex rollout corpus did not expose `token_count.reasoning_output_tokens`, so the direct-count branch landed as a guarded schema seam rather than a live metric. That is the intended Pitfall 4 behavior, not a defect.
- Signal handling stayed documentation-only in this plan. The relevant candidate for phase closeout is `sig-2026-04-17-claude-reasoning-tokens-schema-only-57-7`; no separate KB write was added here.

## Verification

- `node -e "require('./get-shit-done/bin/lib/measurement/sources/codex.cjs'); require('./get-shit-done/bin/lib/measurement/extractors/runtime.cjs')"`
- `node -e "const c=require('./get-shit-done/bin/lib/measurement/sources/codex.cjs'); const rt=require('./get-shit-done/bin/lib/measurement/extractors/runtime.cjs'); console.log(typeof c.readReasoningOutputTokens, typeof c.summarizeCodexReasoningTokens, rt.RUNTIME_EXTRACTORS.some(e => e.name === 'reasoning_tokens_reconciler'))"`
- `rg -n "tokenizer_unavailable|codex_reasoning_tokens_field_absent" get-shit-done/feature-manifest.json`
- `npm test -- measurement-reasoning-tokens-reconciler`
- `npm test -- measurement`
- `node ./get-shit-done/bin/gsd-tools.cjs measurement rebuild`
- `node ./get-shit-done/bin/gsd-tools.cjs measurement query "agent performance" --raw`

## User Setup Required

None.

## Next Phase Readiness

Plan 09 can now add the reasoning-quality proxy beside a stable reasoning-token seam, and Plan 10 can demonstrate that both runtime branches are queryable even when the honest answer is currently schema-only or absent-field guarded.

## Self-Check: PASSED

- `reasoning_tokens_reconciler` is present in both `RUNTIME_EXTRACTORS` and `buildRegistry()`.
- The manifest carries the new measurement skip reasons.
- The full `measurement` suite stayed green (`21` files, `106` tests).
- Rebuild succeeded with run id `21` and raw query output includes `reasoning_tokens` rows.
