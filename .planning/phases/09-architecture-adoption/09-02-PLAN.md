---
phase: 09-architecture-adoption
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - .github/CODEOWNERS
  - .github/FUNDING.yml
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/feature_request.yml
  - .github/workflows/auto-label-issues.yml
  - SECURITY.md
  - bin/install.js
  - commands/gsd/new-project.md.bak
  - get-shit-done/templates/summary-minimal.md
  - get-shit-done/templates/summary-standard.md
  - get-shit-done/templates/summary-complex.md
  - get-shit-done/templates/summary.md
  - .planning/ROADMAP.md
autonomous: true

must_haves:
  truths:
    - "Running grep for upstream-specific references (get-shit-done-cc, glittercowboy, discord.gg, gsd.build) across all source files returns zero hits (excluding get-shit-done-reflect-cc matches)"
    - "All 3 summary templates contain fork-enriched frontmatter (requires, patterns-established fields)"
    - "Standard and complex summary templates contain User Setup Required section"
    - "Complex summary template contains the fork's deviation auto-fix format"
    - "The old standalone summary.md is deleted and all @-references to it are updated to use the 3-tier template system"
    - "ROADMAP.md success criteria for Phase 9 match actual gsd-tools.js capabilities"
    - "new-project.md.bak is deleted"
  artifacts:
    - path: ".github/CODEOWNERS"
      provides: "Fork maintainer ownership"
      contains: "@loganrooks"
    - path: "get-shit-done/templates/summary-complex.md"
      provides: "Enriched complex summary template with fork additions"
      contains: "patterns-established"
    - path: "get-shit-done/templates/summary-standard.md"
      provides: "Enriched standard summary template with fork additions"
      contains: "requires"
    - path: ".planning/ROADMAP.md"
      provides: "Reconciled success criteria for Phase 9"
  key_links:
    - from: "workflows and agents referencing templates/summary.md"
      to: "templates/summary-minimal.md or summary-standard.md or summary-complex.md"
      via: "@-reference updates"
      pattern: "templates/summary-(minimal|standard|complex)\\.md"
---

<objective>
Replace all upstream-specific references with fork equivalents across governance and community files, enrich the three upstream summary templates with fork-specific additions, retire the old standalone summary template, and reconcile the ROADMAP success criteria to match reality. This makes the fork feel like its own product and establishes the modern template system.

Purpose: Fork identity must be complete across all files, and the template system must support the fork's richer summary format.
Output: All governance files updated, templates enriched, old template retired, ROADMAP reconciled.
</objective>

<execution_context>
@/Users/rookslog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rookslog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-architecture-adoption/09-01-SUMMARY.md
@.planning/phases/09-architecture-adoption/09-AUDIT-REPORT.md
@.planning/phases/09-architecture-adoption/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upstream reference cleanup and fork identity replacements</name>
  <files>
    .github/CODEOWNERS
    .github/FUNDING.yml
    .github/ISSUE_TEMPLATE/bug_report.yml
    .github/ISSUE_TEMPLATE/feature_request.yml
    .github/workflows/auto-label-issues.yml
    SECURITY.md
    bin/install.js
    commands/gsd/new-project.md.bak
  </files>
  <action>
Apply all fork identity replacements from the audit report's upstream reference inventory. For each file, replace upstream references with fork equivalents:

**1. `.github/CODEOWNERS`:**
- Replace `@glittercowboy` with `@loganrooks`

**2. `.github/FUNDING.yml`:**
- Replace `github: glittercowboy` with `github: loganrooks`

**3. `.github/ISSUE_TEMPLATE/bug_report.yml`:**
- Replace `get-shit-done-cc` with `get-shit-done-reflect-cc`
- Update any upstream-specific description text to reference the fork

**4. `.github/ISSUE_TEMPLATE/feature_request.yml`:**
- Check for upstream references and replace with fork equivalents (package name, project name)

**5. `.github/workflows/auto-label-issues.yml`:**
- Review labels and update to match fork conventions if needed. If labels are generic (bug, feature, etc.), they may be fine as-is. Only change upstream-specific labels.

**6. `SECURITY.md`:**
- Replace `security@gsd.build` with appropriate fork contact (use `security@` + fork domain, or GitHub Security Advisories link for the fork repo)
- Replace `@glittercowboy` with `@loganrooks`
- Update disclosure process to reference fork repository

**7. `bin/install.js` (line ~1546):**
- Replace `discord.gg/5JJgD5svVS` with fork's community link. If no fork community link exists, replace with the fork's GitHub Discussions URL: `https://github.com/loganrooks/get-shit-done-reflect/discussions`

**8. Delete `commands/gsd/new-project.md.bak`:**
```bash
git rm commands/gsd/new-project.md.bak
```

**9. Address any ADDITIONAL upstream references found during the Plan 01 audit** (check the audit report's summary issue table for any items not listed above).

After all replacements, run the upstream reference sweep to confirm zero remaining hits:
```bash
grep -rn "get-shit-done-cc\|glittercowboy\|discord\.gg\|gsd\.build" \
  --include="*.md" --include="*.js" --include="*.yml" --include="*.json" \
  --exclude-dir=.planning --exclude-dir=node_modules --exclude-dir=.git \
  . | grep -v "get-shit-done-reflect-cc"
```
Expected: zero results.

Also check the audit report for any agent spec fixes needed. If the audit found fork features lost in agent specs during upstream condensation, apply those fixes here (they are small edits to files in `agents/`).
  </action>
  <verify>
1. `grep -rn "glittercowboy" --exclude-dir=.planning --exclude-dir=node_modules --exclude-dir=.git .` returns zero
2. `grep -rn "get-shit-done-cc" --exclude-dir=.planning --exclude-dir=node_modules --exclude-dir=.git . | grep -v "reflect"` returns zero
3. `grep -rn "discord\.gg" --exclude-dir=.planning --exclude-dir=node_modules --exclude-dir=.git .` returns zero
4. `ls commands/gsd/new-project.md.bak` returns "No such file"
5. `cat .github/CODEOWNERS` shows `@loganrooks`
  </verify>
  <done>Zero upstream-specific references remain in source files. All governance/community files reflect fork identity. new-project.md.bak deleted. Any agent spec fixes from audit applied.</done>
</task>

<task type="auto">
  <name>Task 2: Summary template enrichment, retirement, and ROADMAP reconciliation</name>
  <files>
    get-shit-done/templates/summary-minimal.md
    get-shit-done/templates/summary-standard.md
    get-shit-done/templates/summary-complex.md
    get-shit-done/templates/summary.md
    .planning/ROADMAP.md
  </files>
  <action>
**Part A: Enrich summary templates with fork additions**

Read the current fork standalone template (`get-shit-done/templates/summary.md`) to extract the fork-specific additions that need to be ported to the 3-tier system. Then enrich each tier:

**All tiers (minimal, standard, complex):**
Add to the YAML frontmatter section:
```yaml
requires:
  - phase: [prior phase identifier]
    provides: [what that phase established that this plan needed]
patterns-established:
  - "[Pattern name]: [brief description]"
```

**Standard + Complex tiers:**
Add a "User Setup Required" section after the main content:
```markdown
## User Setup Required
<!-- If USER-SETUP.md was generated during execution: -->
**External services require manual configuration.**
See `USER-SETUP.md` in the phase directory for details.
<!-- If no USER-SETUP.md: omit this section -->
```

**Complex tier only:**
Add the fork's detailed deviation auto-fix format after the "Deviations from Plan" section:
```markdown
## Deviations from Plan (Auto-fixed)

**1. [Rule X - Category] Brief description**
- **Found during:** Task [N] ([task name])
- **Issue:** [What was wrong]
- **Fix:** [What was done]
- **Verification:** [How it was verified]
- **Committed in:** [commit hash]
```

Ensure the enrichments integrate cleanly with the existing template structure. Don't break the YAML frontmatter or existing sections. Add fork sections in logical positions.

**Part B: Retire the standalone summary.md**

1. First, find all @-references to `templates/summary.md` across the codebase (use the inventory from the audit report):
```bash
grep -rn "templates/summary\.md" --include="*.md" --exclude-dir=.planning --exclude-dir=node_modules .
```

2. For each reference found:
   - If the reference is in a workflow or agent that uses `gsd-tools.js template select`, update the reference to point to the directory (`templates/`) or remove the specific filename (gsd-tools handles selection).
   - If the reference is a direct `@templates/summary.md` in a plan template or execution context, update to `@templates/summary-standard.md` as the default (the executor will use gsd-tools to select the appropriate tier at runtime).

3. Delete the old template:
```bash
git rm get-shit-done/templates/summary.md
```

**Part C: Reconcile ROADMAP success criteria**

Update the Phase 9 success criteria in `.planning/ROADMAP.md` to match actual gsd-tools.js capabilities. Specifically:

1. Criterion #1: Change from `gsd-tools.js --help shows available commands` to: `Running node get-shit-done/bin/gsd-tools.js with no arguments displays available commands and usage information`

2. Criterion #2: Change from `gsd-tools.js config get .planning/config.json depth` to: `Running node get-shit-done/bin/gsd-tools.js config-set round-trips fork custom fields (health_check, devops, gsd_reflect_version) without data loss, and state load returns upstream config fields correctly`

3. Criterion #3: Keep as-is but clarify: `Fork-specific command logic (DevOps detection, help content, package references) has been migrated from old command files to the workflow layer. The 3 commands converted in Phase 8 (new-project, help, update) plus 3 converted in Phase 9 (signal, upgrade-project, join-discord) all delegate to workflows`

4. Criterion #4: Change from `19 new workflow files` to: `34 total workflow files (28 upstream-origin, 6 fork-only), 4 new reference files, and 3 new summary templates are present and verified`
  </action>
  <verify>
1. `grep -rn "requires:" get-shit-done/templates/summary-minimal.md` returns a match
2. `grep -rn "patterns-established:" get-shit-done/templates/summary-standard.md` returns a match
3. `grep -rn "User Setup Required" get-shit-done/templates/summary-complex.md` returns a match
4. `grep -rn "Deviations from Plan (Auto-fixed)" get-shit-done/templates/summary-complex.md` returns a match
5. `ls get-shit-done/templates/summary.md` returns "No such file" (retired)
6. `grep -rn "templates/summary\.md" --include="*.md" --exclude-dir=.planning --exclude-dir=node_modules . | grep -v "summary-"` returns zero results (all references updated)
7. `grep "config-set round-trips" .planning/ROADMAP.md` confirms reconciled criteria
  </verify>
  <done>All 3 summary templates enriched with fork additions. Old standalone summary.md retired with all references updated. ROADMAP.md success criteria reconciled to match actual gsd-tools.js capabilities.</done>
</task>

</tasks>

<verification>
1. Full upstream reference sweep returns zero hits (excluding .planning/ and node_modules/)
2. All 3 summary templates contain fork-enriched frontmatter fields
3. Old summary.md is deleted, no broken references remain
4. ROADMAP.md Phase 9 success criteria are reconciled with actual capabilities
5. new-project.md.bak is deleted
6. All tests still pass: `npx vitest run` and `node --test get-shit-done/bin/gsd-tools.test.js`
</verification>

<success_criteria>
- The fork feels like its own product: zero upstream-specific references in source files
- The 3-tier summary template system is enriched with fork additions and fully operational
- ROADMAP success criteria are achievable and match reality
- No regressions: all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-architecture-adoption/09-02-SUMMARY.md`
</output>
