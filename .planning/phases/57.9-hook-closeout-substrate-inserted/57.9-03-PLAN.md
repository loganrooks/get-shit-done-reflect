---
phase: 57.9-hook-closeout-substrate-inserted
plan: "03"
signature:
  role: planner
  harness: codex-cli
  platform: codex
  vendor: openai
  model: not_available
  reasoning_effort: not_available
  profile: quality
  gsd_version: 1.19.9+dev
  generated_at: "2026-04-22T00:31:25Z"
  session_id: "019db1be-42d4-7b82-ace9-76fc417dc71e"
  provenance_status:
    role: derived
    harness: exposed
    platform: derived
    vendor: derived
    model: not_available
    reasoning_effort: not_available
    profile: derived
    gsd_version: derived
    generated_at: exposed
    session_id: exposed
  provenance_source:
    role: artifact_role
    harness: runtime_context
    platform: derived_from_harness
    vendor: derived_from_harness
    model: not_available
    reasoning_effort: not_available
    profile: config
    gsd_version: installed_harness
    generated_at: writer_clock
    session_id: env:CODEX_THREAD_ID
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - bin/install.js
  - get-shit-done/bin/lib/config.cjs
  - tests/unit/config-key-validation.test.js
  - tests/unit/install.test.js
  - tests/integration/multi-runtime.test.js
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Claude installs now land a real closeout hook entry using the runtime-supported event name and the shipped postlude script."
    - "Codex installs either write `hooks.json` for the closeout hook when support is present, or write an explicit waiver marker with reason/scope when support is unavailable or ambiguous."
    - "Installer re-runs and mixed-scope configs are regression-tested; the phase no longer relies on blanket 'Codex stays hook-free' assumptions."
  artifacts:
    - path: "bin/install.js"
      provides: "Claude closeout hook registration and Codex install-or-waiver wiring."
      contains: "hooks.json"
    - path: "tests/unit/install.test.js"
      provides: "Unit cases for Claude closeout registration, Codex support detection, and waiver writes."
      contains: "hooks.json"
    - path: "get-shit-done/bin/lib/config.cjs"
      provides: "Config allowlist support for the explicit Codex waiver witness keys written by the installer."
      contains: "codex_hooks_waived"
    - path: "tests/integration/multi-runtime.test.js"
      provides: "Integration coverage for supported-vs-waived Codex installs and the new Claude closeout hook."
      contains: "hooks"
  key_links:
    - from: "bin/install.js"
      to: "hooks/gsd-postlude.js"
      via: "installer copies/registers the bundled postlude hook into the target runtime"
      pattern: "gsdr-postlude"
    - from: "bin/install.js"
      to: ".planning/config.json"
      via: "explicit Codex waiver marker when support is unavailable or ambiguous"
      pattern: "codex_hooks_waived"
    - from: "get-shit-done/bin/lib/config.cjs"
      to: ".planning/config.json"
      via: "waiver marker keys are recognized rather than warned as unknown top-level config"
      pattern: "codex_hooks_waived|codex_hooks_waiver_reason"
    - from: "tests/integration/multi-runtime.test.js"
      to: "bin/install.js"
      via: "integration fixtures prove both supported and waived Codex outcomes, replacing blanket hook-free assertions"
      pattern: "hooks\\.json|codex_hooks_waived"
---

<objective>
Wire the real closeout substrate into the installer: Claude gets the closeout hook, Codex gets install-or-waiver, and tests prove both states honestly.

Purpose: This is the plan that actually lands HOOK-01 and the install-or-waiver half of HOOK-02. It consumes the groundwork helper contract and the shipped postlude script, then turns them into installed runtime behavior with non-silent verification.
Output: `bin/install.js` writes the closeout substrate for Claude and Codex, and test coverage replaces the repo’s old blanket "Codex stays hook-free" assumption.
</objective>

<execution_context>
@./.codex/get-shit-done-reflect/workflows/execute-plan.md
@./.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57.9-hook-closeout-substrate-inserted/57.9-CONTEXT.md
@.planning/phases/57.9-hook-closeout-substrate-inserted/57.9-RESEARCH.md
@.planning/phases/58-structural-enforcement-gates/58-16-gate-06-07-defer-provenance.md
@bin/install.js
@hooks/gsd-postlude.js
@tests/unit/install.test.js
@tests/integration/multi-runtime.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Claude closeout registration and Codex `hooks.json` install using the grounded helper contract</name>
  <files>bin/install.js</files>
  <action>Update the installer to consume the groundwork helper contract and the shipped postlude hook script.

For Claude installs, register the closeout hook on the runtime-supported event name (`Stop`, not literal `SessionStop`) using the existing guarded local/global command patterns and non-duplication logic already used for SessionStart hooks. Preserve current statusline / update / health / context-monitor behavior.

For Codex installs, stop returning early before hook handling. Instead:
- ensure the bundled postlude hook asset is copied into the target Codex runtime tree so `hooks.json` can point at a real file, not just a future path
- write the manifest as today
- when support is present, write the appropriate project/global `hooks.json` entry for the shared closeout hook using the shipped postlude script
- when support is unavailable or ambiguous, skip hook install but write an explicit `.planning/config.json` waiver marker with reason, checked-at timestamp, and scope/evidence fields

Do not promise full Codex tool-hook parity. Limit the wiring to the closeout `Stop` hook and the explicit waiver path.</action>
  <verify>Targeted temp installs prove: Claude local/global installs contain a `Stop` closeout hook entry pointing at the bundled postlude script; Codex supported fixtures get both a real installed postlude hook file and `hooks.json`; unsupported/ambiguous fixtures get no `hooks.json` but do get an explicit waiver marker in `.planning/config.json`.</verify>
  <done>The installer now lands the closeout substrate on Claude and an honest install-or-waiver outcome on Codex.</done>
</task>

<task type="auto">
  <name>Task 2: Replace blanket hook-free assumptions with unit and integration coverage for supported and waived Codex outcomes</name>
  <files>get-shit-done/bin/lib/config.cjs, tests/unit/config-key-validation.test.js, tests/unit/install.test.js, tests/integration/multi-runtime.test.js</files>
  <action>Add the test surface that proves the installer behavior in Task 1 is real and non-silent.

Unit tests should cover:
- Claude closeout hook registration on `Stop`
- non-duplication on installer re-run
- Codex support present -> `hooks.json` written
- Codex support unavailable/ambiguous -> waiver marker written instead of silent skip
- the waiver marker keys (`codex_hooks_waived`, reason/timestamp/scope companions as needed) are accepted by config validation rather than emitted as unknown top-level keys

Integration tests should replace the current blanket "Codex stays hook-free" assertion with at least two explicit scenarios:
- supported Codex fixture -> hook file/config present
- waived Codex fixture -> hook config absent but waiver marker present

Keep the tests mixed-state aware. The current repo’s project/global mismatch (`project_has_flag=no`, `global_has_flag=yes`) is a real edge, not a contrived case.</action>
  <verify>`npx vitest run tests/unit/config-key-validation.test.js tests/unit/install.test.js tests/integration/multi-runtime.test.js` passes. Grep shows the old blanket hook-free Codex integration assertion is replaced by supported/waived scenario coverage, and config validation accepts the waiver keys without warning.</verify>
  <done>The installed closeout substrate is proven by tests on both runtimes, and unsupported Codex states are explicit rather than silent.</done>
</task>

</tasks>

<verification>
 - [ ] `npx vitest run tests/unit/config-key-validation.test.js tests/unit/install.test.js tests/integration/multi-runtime.test.js` passes
- [ ] Claude temp installs contain a `Stop` closeout hook entry
- [ ] Supported Codex temp installs contain `hooks.json`
- [ ] Waived/ambiguous Codex temp installs write an explicit `.planning/config.json` waiver marker
</verification>

<narrowing_decisions>
## Narrowing Decisions

None -- plan covers all CONTEXT.md claims verbatim.
</narrowing_decisions>

<success_criteria>
- HOOK-01 is structurally real in the installer
- HOOK-02 is structurally real as install-or-waiver, not silent skip
- Waiver witness keys are accepted by config validation rather than treated as unknown drift
- Tests prove supported and waived Codex paths separately
- No plan relies on broad Codex tool-hook parity
</success_criteria>

<output>
After completion, create `.planning/phases/57.9-hook-closeout-substrate-inserted/57.9-03-SUMMARY.md`
</output>
