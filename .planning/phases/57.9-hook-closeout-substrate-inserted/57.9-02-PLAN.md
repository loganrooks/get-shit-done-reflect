---
phase: 57.9-hook-closeout-substrate-inserted
plan: "02"
signature:
  role: planner
  harness: codex-cli
  platform: codex
  vendor: openai
  model: not_available
  reasoning_effort: not_available
  profile: quality
  gsd_version: 1.19.9+dev
  generated_at: "2026-04-22T00:31:25Z"
  session_id: "019db1be-42d4-7b82-ace9-76fc417dc71e"
  provenance_status:
    role: derived
    harness: exposed
    platform: derived
    vendor: derived
    model: not_available
    reasoning_effort: not_available
    profile: derived
    gsd_version: derived
    generated_at: exposed
    session_id: exposed
  provenance_source:
    role: artifact_role
    harness: runtime_context
    platform: derived_from_harness
    vendor: derived_from_harness
    model: not_available
    reasoning_effort: not_available
    profile: config
    gsd_version: installed_harness
    generated_at: writer_clock
    session_id: env:CODEX_THREAD_ID
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/gsd-postlude.js
  - hooks/dist/gsd-postlude.js
  - get-shit-done/bin/lib/measurement/sources/session-meta-postlude.cjs
  - tests/unit/measurement-gsdr.test.js
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The repo ships a real postlude hook script from source, not a prose placeholder."
    - "Postlude output is metadata-only and canonical: it records `postlude_fired` plus explicit `not_available` states for unsupported incident markers."
    - "`loadSessionMetaPostlude()` exists and adapts postlude records into the tuple shape `gate_fire_events` already expects."
  artifacts:
    - path: "hooks/gsd-postlude.js"
      provides: "Source closeout hook script that appends canonical postlude records."
      contains: "postlude_fired"
    - path: "get-shit-done/bin/lib/measurement/sources/session-meta-postlude.cjs"
      provides: "Metadata-only raw-source loader for postlude records."
      contains: "loadSessionMetaPostlude"
    - path: "tests/unit/measurement-gsdr.test.js"
      provides: "Regression coverage for postlude-row parsing and gate tuple adaptation."
      contains: "loadSessionMetaPostlude"
  key_links:
    - from: "hooks/gsd-postlude.js"
      to: "get-shit-done/bin/lib/measurement/sources/session-meta-postlude.cjs"
      via: "shared postlude record shape under `.planning/measurement/session-meta-postlude/`"
      pattern: "postlude_fired|not_available"
    - from: "get-shit-done/bin/lib/measurement/sources/session-meta-postlude.cjs"
      to: "get-shit-done/bin/lib/measurement/extractors/gsdr.cjs"
      via: "loader name and tuple shape match the existing optional-require seam"
      pattern: "loadSessionMetaPostlude"
---

<objective>
Ship the canonical metadata-only postlude artifact and source loader before any installer wiring tries to register it.

Purpose: Phase 57.9 cannot honestly claim HOOK-03 unless the postlude substrate exists as a real source artifact. This plan creates the canonical record shape and the loader that Phase 58’s `gate_fire_events` seam is already waiting for.
Output: a real postlude hook script, a real `session-meta-postlude.cjs` source module, and measurement tests proving the contract works.
</objective>

<execution_context>
@./.codex/get-shit-done-reflect/workflows/execute-plan.md
@./.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57.9-hook-closeout-substrate-inserted/57.9-CONTEXT.md
@.planning/phases/57.9-hook-closeout-substrate-inserted/57.9-RESEARCH.md
@.planning/phases/58-structural-enforcement-gates/58-19-SUMMARY.md
@get-shit-done/bin/lib/measurement/extractors/gsdr.cjs
@get-shit-done/bin/lib/measurement/sources/ci-notices.cjs
@tests/unit/measurement-gsdr.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the source postlude hook script and canonical record format</name>
  <files>hooks/gsd-postlude.js, hooks/dist/gsd-postlude.js</files>
  <action>Create the shared closeout hook script that Phase 57.9 installs on Claude/Codex later.

The script must append metadata-only JSONL rows under a dedicated planning-owned location such as `.planning/measurement/session-meta-postlude/`. Each row must include at minimum: timestamp, runtime, phase (when discoverable), `postlude_fired: true`, and explicit structured `not_available` entries for unsupported `error_rate`, `direction_change`, and `destructive_event` markers. The script must tolerate missing `.planning/` roots or missing phase context and exit cleanly rather than failing the runtime hook.

Do not emit raw `gate_fire_events` tuples directly from the hook. The hook owns canonical records; the source loader owns adaptation. Keep the script metadata-only and conservative — no live incident analysis beyond explicit `not_available` unless the information is already available without new runtime coupling. Regenerate `hooks/dist/gsd-postlude.js` through the existing hook build pipeline rather than hand-maintaining source and dist separately if the current build script supports it.</action>
  <verify>Run the hook script against a temp fixture or synthetic environment and confirm it writes one JSONL row under the chosen `.planning/measurement/session-meta-postlude/` path with `postlude_fired: true` plus explicit `not_available` marker objects. Re-run to confirm append-only behavior rather than overwrite. Confirm `hooks/dist/gsd-postlude.js` exists and matches the source build expectation.</verify>
  <done>A real source hook script exists and writes the canonical metadata-only postlude record shape 57.9 needs.</done>
</task>

<task type="auto">
  <name>Task 2: Add `loadSessionMetaPostlude()` and measurement regression coverage</name>
  <files>get-shit-done/bin/lib/measurement/sources/session-meta-postlude.cjs, tests/unit/measurement-gsdr.test.js</files>
  <action>Implement `loadSessionMetaPostlude(cwd, options)` as the loader that `gate_fire_events` already optional-requires.

Mirror the file-reading discipline from `ci-notices.cjs`: tolerate missing directories/files, skip malformed lines conservatively, preserve `source_file`, and honor `options.phase` filtering. Adapt canonical postlude rows into the tuple shape expected by `gate_fire_events` (`{ts, gate, result, phase, source_file, ...}`), mapping explicit `not_available` marker states to honest degraded results instead of silent absence.

Add focused measurement tests that cover:
- missing directory/module-data path -> empty array / graceful behavior
- well-formed row -> adapted tuple(s) with source_file preserved
- explicit `not_available` markers -> adapted degraded/waived tuple(s), not silent drop
- phase filtering -> only matching rows returned</action>
  <verify>`npx vitest run tests/unit/measurement-gsdr.test.js` passes and includes the new postlude loader coverage. A direct Node invocation of `loadSessionMetaPostlude()` against a temp fixture returns the expected tuple array.</verify>
  <done>The postlude substrate exists as a real loader contract consumable by `gate_fire_events`, with regression tests proving graceful and non-silent behavior.</done>
</task>

</tasks>

<verification>
- [ ] `npx vitest run tests/unit/measurement-gsdr.test.js` passes
- [ ] Synthetic hook execution writes canonical JSONL rows under `.planning/measurement/session-meta-postlude/`
- [ ] `loadSessionMetaPostlude()` returns adapted tuples and does not throw on missing data
</verification>

<narrowing_decisions>
## Narrowing Decisions

None -- plan covers all CONTEXT.md claims verbatim.
</narrowing_decisions>

<success_criteria>
- The repo ships a real postlude hook script from source
- The repo ships a real `session-meta-postlude.cjs` loader
- Canonical records are metadata-only and explicit about `not_available`
- Measurement tests prove the contract before installer wiring begins
</success_criteria>

<output>
After completion, create `.planning/phases/57.9-hook-closeout-substrate-inserted/57.9-02-SUMMARY.md`
</output>
