---
phase: 57.9-hook-closeout-substrate-inserted
plan: "03"
signature:
  role: executor
  harness: codex-cli
  platform: codex
  vendor: openai
  model: not_available
  reasoning_effort: xhigh
  profile: quality
  gsd_version: 1.19.9+dev
  generated_at: "2026-04-22T02:09:27Z"
  session_id: not_available
  provenance_status:
    role: derived
    harness: exposed
    platform: derived
    vendor: derived
    model: not_available
    reasoning_effort: derived
    profile: derived
    gsd_version: derived
    generated_at: exposed
    session_id: not_available
  provenance_source:
    role: artifact_role
    harness: runtime_context
    platform: derived_from_harness
    vendor: derived_from_harness
    model: not_available
    reasoning_effort: codex_profile_resolution
    profile: config
    gsd_version: installed_harness
    generated_at: writer_clock
    session_id: not_available
subsystem: installer-hook-wiring
tags: [phase-57.9, closeout-hooks, codex-hooks, claude-hooks, installer, waiver]
requires:
  - phase: 57.9-hook-closeout-substrate-inserted
    provides: "Plan 01 established the closeout/support contract and Plan 02 shipped the postlude asset plus measurement loader that this installer wiring now lands"
provides:
  - "Claude installs now register the shared Stop closeout hook against the shipped postlude script with guarded local commands and timeout metadata"
  - "Codex installs now take an honest install-or-waiver path: supported scopes get hooks.json plus the bundled postlude hook, unsupported or ambiguous scopes write explicit waiver witnesses in .planning/config.json"
  - "Config and integration coverage that replaces blanket Codex hook-free assumptions with supported-vs-waived fixtures"
affects: [HOOK-01, HOOK-02, GATE-06, GATE-07, installer, config-validation]
tech-stack:
  added: []
  patterns:
    - "shared installer helpers reconcile live hook assets, runtime config, and planning waiver witnesses"
    - "mixed project/global Codex evidence is preserved as explicit degraded state rather than collapsed to false"
key-files:
  created:
    - ".planning/phases/57.9-hook-closeout-substrate-inserted/57.9-03-SUMMARY.md"
  modified:
    - "bin/install.js"
    - "get-shit-done/bin/lib/config.cjs"
    - "tests/unit/config-key-validation.test.js"
    - "tests/unit/install.test.js"
    - "tests/integration/multi-runtime.test.js"
    - ".planning/STATE.md"
key-decisions:
  - "Kept Codex limited to the closeout Stop hook; Phase 57.9 does not silently expand into full tool-hook parity."
  - "Treated project/global codex_hooks disagreement as a first-class waiver case with reason, scope, timestamp, and evidence written into planning state."
patterns-established:
  - "Capability-surface changes now need paired installer and mixed-scope integration coverage before the plan can close."
duration: 16min
completed: 2026-04-21
---

# Phase 57.9 Plan 03 Summary

**Landed the real installer substrate for closeout hooks: Claude now wires `Stop`, Codex now installs `hooks.json` only when support is evidenced, and every unsupported path leaves an explicit waiver witness instead of a silent skip.**

## Performance

- **Duration:** 16min
- **Tasks:** 2/2 completed
- **Files modified:** 5

## Accomplishments

- Updated `bin/install.js` so Claude installs register the bundled `gsdr-postlude.js` on `Stop`, remove stale `SessionStop` residue, and preserve non-duplication across re-runs.
- Updated `bin/install.js` so Codex installs copy the bundled closeout hook plus `hooks.json` only when `codex_hooks` support is evidenced for the active scope; unsupported and ambiguous states now write explicit waiver markers to `.planning/config.json`.
- Added the Codex waiver witness keys to `get-shit-done/bin/lib/config.cjs` so config validation accepts the installer-written degraded-state markers.
- Replaced the old blanket "Codex stays hook-free" test assumption with targeted supported-vs-waived fixtures in `tests/unit/install.test.js` and `tests/integration/multi-runtime.test.js`, plus config-key validation coverage in `tests/unit/config-key-validation.test.js`.

## Task Commits

1. **Task 1: Wire Claude closeout registration and Codex `hooks.json` install using the grounded helper contract** - `e93f41c8`
2. **Task 2: Replace blanket hook-free assumptions with unit and integration coverage for supported and waived Codex outcomes** - `3c6c7086`

## Files Created/Modified

- `bin/install.js` - wired Claude `Stop` registration, Codex install-or-waiver behavior, and uninstall/legacy cleanup for the new closeout hook.
- `get-shit-done/bin/lib/config.cjs` - allowlisted the Codex waiver witness keys written by the installer.
- `tests/unit/config-key-validation.test.js` - added acceptance coverage for the waiver witness keys.
- `tests/unit/install.test.js` - added installer assertions for Claude `Stop`, Codex supported `hooks.json`, and Codex waiver writes.
- `tests/integration/multi-runtime.test.js` - replaced the blanket Codex hook-free assertion with explicit Claude/support/waiver scenarios.
- `.planning/phases/57.9-hook-closeout-substrate-inserted/57.9-03-SUMMARY.md` - execution summary for this plan.
- `.planning/STATE.md` - advanced the active phase bookkeeping to the Plan 04 resume point.

## Decisions & Deviations

- **Honest degradation path:** the installer now warns when it cannot record a Codex waiver because no `.planning/config.json` is available. Unsupported state is explicit, not silently ignored.
- **Legacy cleanup hardening:** both Claude and Codex cleanup paths now scrub stale `gsd-postlude` and `gsdr-postlude` residue so partial prior states do not accumulate duplicate closeout registrations.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan `57.9-04` can now align the capability matrix and XRT-01 verifier language to the substrate that is actually shipped in source: shared `Stop`, Claude `SessionEnd` optional, and Codex install-or-waiver conditional.

## Self-Check: PASSED

- `git log --oneline --all | grep -q 'e93f41c8'` -> FOUND
- `git log --oneline --all | grep -q '3c6c7086'` -> FOUND
- `npx vitest run tests/unit/config-key-validation.test.js tests/unit/install.test.js tests/integration/multi-runtime.test.js` -> PASS (269 tests)
- Claude installer fixtures now assert one `Stop` closeout hook with `timeout: 30` and no surviving `SessionStop` entry
- Codex supported fixtures now assert real `hooks.json` + bundled `gsdr-postlude.js`; mixed project/global conflict fixtures assert `codex_hooks_waived` with reason/scope/evidence
