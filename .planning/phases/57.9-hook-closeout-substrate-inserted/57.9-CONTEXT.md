# Phase 57.9: Hook & Closeout Substrate - Context

**Gathered:** 2026-04-21
**Status:** Ready for planning
**Mode:** Exploratory --auto -- preserving uncertainty; auto-progression by type rules

<domain>
## Phase Boundary

[decided:cited] Phase 57.9 closes the missing hook / closeout substrate that Phase 58 explicitly deferred. It ships only the substrate: Claude `SessionStop` wiring, Codex hook-or-waiver behavior, the `session_meta_postlude` source contract and canonical markers needed by GATE-06 / GATE-07 consumers, and the artifact alignment / verification mapping that makes later un-deferral honest. <!-- typed by context-checker -->

[decided:cited] This phase is explicitly **not** Phase 58 consumer registration, Phase 60.1 live incident wiring, broader Codex hook parity, or a generic automation expansion phase. It is the narrow substrate-repair phase that makes the existing defer chain closable without shortcuts. <!-- typed by context-checker -->

</domain>

<working_model>
## Working Model & Assumptions

### Claude closeout hook surface

**Current state:** The current installer writes Claude `SessionStart` hooks and one `PostToolUse` context-monitor hook, but no `SessionStop` closeout hook is configured today.

- [evidenced:cited] The current install path writes Claude `SessionStart` and `PostToolUse` hooks but no `SessionStop` closeout hook.
- [decided:cited] HOOK-01 is satisfied only by source-first installer wiring plus install-time verification against written settings, not by manual runtime patching or workflow prose.
- [decided:reasoned] Phase 57.9 should extend the existing Claude hook registration path rather than invent a second closeout-write mechanism, so the installed runtime stays a single authoritative surface.

### Codex conditional hook / waiver surface

**Current state:** The installer writes Codex manifests and returns before any hook setup, while runtime capability detection already treats `codex_hooks=true` as conditional hook availability.

- [evidenced:cited] Codex install currently exits before settings or hook installation logic, so no installed Codex closeout substrate exists today.
- [assumed:reasoned] A runtime with `codex_hooks=true` is eligible for installer wiring, but the feature's "under development" status means Phase 57.9 still needs an explicit waiver path when the surface is unavailable or unsafe.
- [decided:cited] HOOK-02 is only honestly closed by either installed Codex hook surface or a named waiver / degradation marker; silent skip is forbidden.
- [decided:cited] Codex behavior in this phase must be explicit in XRT-01 terms: `applies-via-installer` when `codex_hooks=true`, otherwise `does-not-apply-with-reason` via a named waiver path.

### `session_meta_postlude` source contract

**Current state:** The `gate_fire_events` extractor already optional-requires `../sources/session-meta-postlude.cjs`; that module does not exist, so the source currently degrades to `not_available`.

- [evidenced:cited] Measurement already has a drop-in seam for `session_meta_postlude`, and it currently reports graceful absence rather than throwing when the module is missing.
- [decided:cited] Phase 57.9 ships a real `get-shit-done/bin/lib/measurement/sources/session-meta-postlude.cjs` source contract instead of leaving permanent graceful absence.
- [decided:reasoned] The source contract must emit canonical records or explicit `not_available` markers for `postlude-fired`, `error-rate`, `direction-change`, and `destructive-event`; downstream consumers must not read ad hoc runtime files directly.

### Defer-chain closure and boundary discipline

**Current state:** Phase 58 explicitly deferred GATE-06 and GATE-07 to 57.9, and later planning artifacts now treat 57.9 as the sole substrate owner while downstream live wiring remains later work.

- [evidenced:cited] GATE-06 and GATE-07 remain `explicitly_deferred` to Phase 57.9 in the authoritative Phase 58 defer and ledger artifacts.
- [decided:cited] Phase 57.9 is a strict substrate-closure phase; it does not absorb Phase 58 consumer registration or Phase 60.1 live incident wiring.
- [governing:reasoned] GATE-06 and GATE-07 can leave defer state only with real HOOK-01 / HOOK-02 / HOOK-03 evidence mapped to the six re-entry conditions already named by Phase 58.

### Claude's Discretion

- Exact hook command names and helper extraction inside `bin/install.js`
- Exact on-disk trace format that `session_meta_postlude` reads, provided it remains metadata-only
- Whether the explicit Codex waiver marker is runtime-local, planning-local, or dual-written, so long as it is auditable and non-silent
- Exact verification command layout across unit vs integration install surfaces

</working_model>

<constraints>
## Derived Constraints

- **DC-1:** [governing:reasoned] The source repo is canonical. `bin/install.js`, `get-shit-done/bin/lib/measurement/`, and planning artifacts are edited first; `.claude/` and `.codex/` are verification outputs, not primary fix surfaces.
- **DC-2:** [decided:cited] Phase 57.9 owns substrate closure only. Phase 58 consumes the substrate, and Phase 60.1 owns downstream live incident wiring.
- **DC-3:** [decided:cited] Codex closure must end in installer-wired hooks when supported or an explicit waiver / degradation artifact when unsupported; silent absence never satisfies HOOK-02.
- **DC-4:** [evidenced:cited] The existing `gate_fire_events` consumer expects `session_meta_postlude` as a drop-in loader returning event rows; Phase 57.9 should match that seam rather than create a second measurement pathway.
- **DC-5:** [decided:cited] Phase 57.9 verification must map HOOK-01 / HOOK-02 / HOOK-03 delivery to the six re-entry conditions in `58-16-gate-06-07-defer-provenance.md`; without that mapping, GATE-06 / GATE-07 remain deferred.

</constraints>

<guardrails>
## Epistemic Guardrails

- **G-1:** [governing:reasoned] No false closure by prose. Workflow text, comments, or roadmap edits alone do not count as HOOK delivery without installer / runtime evidence.
- **G-2:** [governing:reasoned] Codex uncertainty must stay explicit. Do not collapse `codex_hooks` reality into either blanket "Codex has no hooks" or blanket parity claims.
- **G-3:** [governing:reasoned] Phase 57.9 must not swallow later live-sensor or reflection work just because the substrate touches incident markers; later consumers stay later phases.
- **G-4:** [governing:reasoned] The new postlude source must stay metadata-only and compatible with the existing measurement architecture; Phase 57.9 does not introduce a new measurement subsystem.

</guardrails>

<questions>
## Open Questions

### Q1: Under exactly what runtime evidence does Phase 57.9 install Codex hooks versus emit a waiver?
**Research program:** Re-verify the active Codex hook evidence chain in source and installed surfaces: `codex_hooks` detection in `get-shit-done/bin/lib/automation.cjs`, the live hook availability claims in `.planning/research/cross-runtime-parity-research.md`, and installer target semantics for local vs global installs. Determine the minimum evidence that counts as "supported enough to wire" versus "requires explicit waiver."
**Downstream decisions affected:** HOOK-02 implementation shape, verification matrix, capability-matrix wording, and whether waiver evidence is runtime-derived or planning-derived.
**Reversibility:** MEDIUM — the threshold is adjustable, but setting it too low creates false parity and setting it too high preserves a silent gap under a different name.

### Q2: What persisted artifact should `session_meta_postlude` read, and how does it surface explicit `not_available` without depending on later live log-sensor wiring?
**Research program:** Inspect existing hook, session-meta, and bridge-style artifacts already produced by Claude and Codex installs, then compare them against the drop-in loader contract described in Phase 58's gate-fire-events work. Prototype the smallest metadata-only event schema that can represent `postlude-fired`, `error-rate`, `direction-change`, and `destructive-event` plus explicit `not_available` states.
**Downstream decisions affected:** HOOK-03 file format, hook script outputs, loader implementation, and compatibility with the existing measurement registry.
**Reversibility:** MEDIUM — the loader can evolve, but the initial on-disk contract will immediately become a verifier and consumer dependency.

### Q3: Where should the explicit Codex waiver marker live so Phase 58 re-entry verification can audit it cleanly?
**Research program:** Compare three candidate homes for waiver evidence: installer-written runtime artifact, `.planning/config.json`, or dual-write. Test which location survives reinstall, is phase-verifiable, and does not confuse configured preference with observed runtime capability. Reconcile that answer against the current Phase 58 defer artifact language, which names `.planning/config.json` as one candidate witness.
**Downstream decisions affected:** HOOK-02 evidence path, verifier logic, docs wording, and whether waiver state is runtime-scoped or project-scoped.
**Reversibility:** HIGH — evidence location can be refactored later if the marker is explicit and migratable.

</questions>

<dependencies>
## Claim Dependencies

| Claim | Depends On | Vulnerability |
|-------|-----------|---------------|
| HOOK-02 closes with installer-wired Codex hooks or explicit waiver | The support threshold for `codex_hooks=true` being "supported enough" for installer wiring | HIGH — if the support threshold is wrong, the phase could manufacture parity or over-waive real capability |
| `session_meta_postlude` ships as a drop-in loader | The existing optional-require seam in `gate_fire_events` remaining authoritative | LOW — the seam already exists and was designed to accept a future module |
| Phase 57.9 stays substrate-only | DC-2 plus the rewritten ROADMAP / REQUIREMENTS ownership split between 57.9 substrate closure and 60.1 live incident wiring | MEDIUM — if planning ignores the declared ownership split, scope drift reappears immediately |
| GATE-06 / GATE-07 can leave defer state only with HOOK evidence | The six re-entry conditions in the Phase 58 defer artifact remaining authoritative | LOW — these conditions are already the named defer contract and are directly citable |

</dependencies>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase authority and adopted framing
- `.planning/ROADMAP.md` — authoritative Phase 57.9, Phase 58, and Phase 60.1 scope / dependency chain
- `.planning/REQUIREMENTS.md` — HOOK-01, HOOK-02, HOOK-03 and the cross-runtime note that Phase 57.9 owns hook / closeout substrate closure
- `.planning/deliberations/phase-57-9-hook-closeout-substrate-closure.md` — adopted deliberation concluding that 57.9 must stay a strict substrate-closure phase

### Defer and re-entry authority
- `.planning/phases/58-structural-enforcement-gates/58-16-gate-06-07-defer-provenance.md` — authoritative defer witness and six re-entry conditions for GATE-06 / GATE-07
- `.planning/phases/58-structural-enforcement-gates/58-05-codex-behavior-matrix.md` — declared Claude / Codex behavior for GATE-06 and GATE-07
- `.planning/phases/58-structural-enforcement-gates/58-19-SUMMARY.md` — expected drop-in contract for `session_meta_postlude` consumed by `gate_fire_events`
- `.planning/phases/60-sensor-pipeline-codex-parity/60-CONTEXT.md` — carried-forward rule that Phase 60 does not introduce new Codex hook substrate

### Live code and runtime capability surfaces
- `bin/install.js` — current installer behavior, Claude hook registration, and the early Codex return path
- `get-shit-done/bin/lib/automation.cjs` — live Codex `codex_hooks` detection semantics
- `get-shit-done/bin/lib/measurement/extractors/gsdr.cjs` — existing optional `session_meta_postlude` consumer seam
- `get-shit-done/references/capability-matrix.md` — current runtime / hook posture and ownership wording
- `.planning/research/cross-runtime-parity-research.md` — live Codex hook behavior, validation commands, and current conditional hook posture

### Verification surfaces
- `tests/unit/install.test.js` — primary installer regression surface for hook registration behavior
- `tests/integration/multi-runtime.test.js` — primary cross-runtime installer verification surface

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `bin/install.js` `ensureHook()` plus the existing Claude settings / hook write path can be extended for `SessionStop` instead of introducing a second writer
- `get-shit-done/bin/lib/automation.cjs` already resolves Codex hook availability from `.codex/config.toml`
- `get-shit-done/bin/lib/measurement/extractors/gsdr.cjs` already consumes `session_meta_postlude` as an optional raw source
- `tests/unit/install.test.js` and `tests/integration/multi-runtime.test.js` are the main regression surfaces for installer and runtime parity changes

### Established Patterns
- Source-first, installed mirrors second: fix source paths, then verify generated `.claude/` and `.codex/` outputs
- Graceful degradation uses explicit `not_available` or `does-not-apply-with-reason`, not silent missing fields
- Runtime capability claims are expected to be backed by live config / feature evidence rather than blanket assumptions

### Integration Points
- `bin/install.js` for writing Claude and Codex hook surfaces
- `get-shit-done/bin/lib/measurement/sources/` for the new `session-meta-postlude.cjs` source file
- Phase 58 defer / ledger artifacts as the authority for re-entry evidence mapping and un-deferral discipline

</code_context>

<specifics>
## Specific Ideas

- User directive from this discussion: address the underlying 57.9 problem fully and with no shortcuts, even if roadmap or artifact changes are required
- Reuse the existing `session_meta_postlude` seam instead of inventing a parallel gate-fire input path
- Keep 57.9 narrow and substrate-first, like an inserted repair phase rather than a broad feature phase

</specifics>

<deferred>
## Deferred Ideas

- Live incident-detection wiring from logs / sensors remains Phase 60.1 work
- Broader Codex hook parity beyond the closeout substrate remains future work if feature-flag behavior changes
- Phase 58 consumer registration and actual un-deferral follow-up happen after 57.9 evidence exists, not inside 57.9 by prose

</deferred>

---

*Phase: 57.9-hook-closeout-substrate-inserted*
*Context gathered: 2026-04-21*
