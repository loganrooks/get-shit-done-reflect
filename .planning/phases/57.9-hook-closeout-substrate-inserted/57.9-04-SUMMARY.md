---
phase: 57.9-hook-closeout-substrate-inserted
plan: "04"
signature:
  role: executor
  harness: codex-cli
  platform: codex
  vendor: openai
  model: not_available
  reasoning_effort: xhigh
  profile: quality
  gsd_version: 1.19.9+dev
  generated_at: "2026-04-22T02:15:38Z"
  session_id: not_available
  provenance_status:
    role: derived
    harness: exposed
    platform: derived
    vendor: derived
    model: not_available
    reasoning_effort: derived
    profile: derived
    gsd_version: derived
    generated_at: exposed
    session_id: not_available
  provenance_source:
    role: artifact_role
    harness: runtime_context
    platform: derived_from_harness
    vendor: derived_from_harness
    model: not_available
    reasoning_effort: codex_profile_resolution
    profile: config
    gsd_version: installed_harness
    generated_at: writer_clock
    session_id: not_available
subsystem: capability-matrix-verifier-alignment
tags: [phase-57.9, capability-matrix, closeout, xrt-01, verifier]
requires:
  - phase: 57.9-hook-closeout-substrate-inserted
    provides: "Plans 01-03 shipped the substrate itself; this plan aligns the runtime-facing references and closeout verifier with that shipped state"
provides:
  - "capability-matrix wording that names Stop as the shared closeout event, SessionEnd as Claude-only optional surface, and Codex as install-or-waiver conditional"
  - "XRT-01 verifier heuristics that recognize current closeout terminology instead of relying on historical SessionStop wording"
  - "regression coverage proving Stop / SessionEnd / closeout wording still blocks phase closeout if the matrix is left stale"
affects: [XRT-01, HOOK-01, HOOK-02, capability-matrix, verifier]
tech-stack:
  added: []
  patterns:
    - "runtime-facing reference docs and closeout verifier heuristics move in lockstep"
    - "terminology drift is treated as a structural verification risk, not a documentation-only concern"
key-files:
  created:
    - ".planning/phases/57.9-hook-closeout-substrate-inserted/57.9-04-SUMMARY.md"
  modified:
    - "get-shit-done/references/capability-matrix.md"
    - "get-shit-done/bin/lib/verify.cjs"
    - "tests/unit/verify-xrt-01-capability-matrix.test.js"
    - ".planning/STATE.md"
key-decisions:
  - "Kept `SessionStop` only as historical language in docs; the live runtime key is `Stop`, with Claude `SessionEnd` documented separately as optional."
  - "Extended XRT-01 with explicit `Stop`, `SessionEnd`, and `closeout` coverage rather than relying on the generic `hook` keyword alone."
patterns-established:
  - "Capability-surface audits must evolve with current runtime vocabulary or they cease to be structural."
duration: 6min
completed: 2026-04-21
---

# Phase 57.9 Plan 04 Summary

**Closed the vocabulary gap around the shipped substrate: the capability matrix now describes the real `Stop`/`SessionEnd` contract, and XRT-01 now notices current closeout language instead of only the historical `SessionStop` spelling.**

## Performance

- **Duration:** 6min
- **Tasks:** 2/2 completed
- **Files modified:** 3

## Accomplishments

- Updated `get-shit-done/references/capability-matrix.md` so the live runtime authority now matches the shipped 57.9 substrate: shared `Stop`, Claude `SessionEnd` as optional, and Codex as an install-or-waiver conditional path owned by Phase 57.9.
- Updated `get-shit-done/bin/lib/verify.cjs` so XRT-01 explicitly treats `Stop`, `SessionEnd`, and `closeout` terminology as capability-surface touches during phase closeout.
- Updated `tests/unit/verify-xrt-01-capability-matrix.test.js` so stale-matrix blocks are proven with current closeout wording rather than only the historical `SessionStop` term.

## Task Commits

1. **Task 1: Align the capability matrix with the shipped closeout substrate and 57.9 ownership** - `77a16480`
2. **Task 2: Extend XRT-01 closeout verification and tests for current closeout terminology** - `4ff26525`

## Files Created/Modified

- `get-shit-done/references/capability-matrix.md` - now documents the real 57.9 closeout contract and Codex install-or-waiver behavior.
- `get-shit-done/bin/lib/verify.cjs` - XRT-01 closeout heuristics now cover `Stop`, `SessionEnd`, and `closeout`.
- `tests/unit/verify-xrt-01-capability-matrix.test.js` - regression cases for current closeout terminology.
- `.planning/phases/57.9-hook-closeout-substrate-inserted/57.9-04-SUMMARY.md` - execution summary for this plan.
- `.planning/STATE.md` - advanced bookkeeping from “installer wiring” to “phase verification pending”.

## Decisions & Deviations

- **Terminology, not relaxation:** XRT-01 remains a structural block. The change was vocabulary coverage only; the verifier still blocks phase closeout when capability language changes without a matrix refresh.
- **Historical wording retained only as context:** the matrix still acknowledges historical `SessionStop` language, but only to prevent confusion while making `Stop` the live authoritative key.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Phase-level verification can now evaluate 57.9 against the same closeout vocabulary the shipped installer and measurement surfaces use, so the final verification report does not depend on stale `SessionStop` wording.

## Self-Check: PASSED

- `git log --oneline --all | grep -q '77a16480'` -> FOUND
- `git log --oneline --all | grep -q '4ff26525'` -> FOUND
- `npx vitest run tests/unit/verify-xrt-01-capability-matrix.test.js` -> PASS (9 tests)
- `rg -n "Stop|SessionEnd|57\\.9" get-shit-done/references/capability-matrix.md` -> updated closeout wording present
- `npx vitest run tests/unit/config-key-validation.test.js tests/unit/install.test.js tests/integration/multi-runtime.test.js tests/unit/verify-xrt-01-capability-matrix.test.js` -> PASS (278 tests)
