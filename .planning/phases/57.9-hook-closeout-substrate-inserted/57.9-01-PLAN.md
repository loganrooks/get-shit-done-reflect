---
phase: 57.9-hook-closeout-substrate-inserted
plan: "01"
signature:
  role: planner
  harness: codex-cli
  platform: codex
  vendor: openai
  model: not_available
  reasoning_effort: not_available
  profile: quality
  gsd_version: 1.19.9+dev
  generated_at: "2026-04-22T00:31:25Z"
  session_id: "019db1be-42d4-7b82-ace9-76fc417dc71e"
  provenance_status:
    role: derived
    harness: exposed
    platform: derived
    vendor: derived
    model: not_available
    reasoning_effort: not_available
    profile: derived
    gsd_version: derived
    generated_at: exposed
    session_id: exposed
  provenance_source:
    role: artifact_role
    harness: runtime_context
    platform: derived_from_harness
    vendor: derived_from_harness
    model: not_available
    reasoning_effort: not_available
    profile: config
    gsd_version: installed_harness
    generated_at: writer_clock
    session_id: env:CODEX_THREAD_ID
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.js
  - get-shit-done/bin/lib/automation.cjs
  - tests/unit/install.test.js
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Source code has one authoritative closeout-hook vocabulary for this phase: `Stop` is the shared runtime event, Claude `SessionEnd` is optional, and literal `SessionStop` is no longer treated as a runtime config key."
    - "Codex hook capability resolution no longer relies on project `.codex/config.toml` alone; project/global ambiguity is surfaced explicitly instead of silently collapsing to false."
    - "Later installer plans can consume a real capability/result helper rather than re-deriving `codex_hooks` support from stale comments or ad hoc regexes."
  artifacts:
    - path: "bin/install.js"
      provides: "Shared closeout-hook terminology and Codex hook-support helper(s) used by later install wiring."
      contains: "Stop"
    - path: "get-shit-done/bin/lib/automation.cjs"
      provides: "Automation/runtime capability resolution that no longer assumes project-local Codex config is the only evidence source."
      contains: "codex_hooks"
    - path: "tests/unit/install.test.js"
      provides: "Regression coverage for project/global Codex hook evidence and closeout helper semantics."
      contains: "codex_hooks"
  key_links:
    - from: "bin/install.js"
      to: "get-shit-done/bin/lib/automation.cjs"
      via: "shared helper naming and capability semantics"
      pattern: "codex_hooks|Stop|SessionEnd"
    - from: "tests/unit/install.test.js"
      to: "bin/install.js"
      via: "unit cases prove ambiguous project/global evidence becomes explicit, not silent"
      pattern: "project|global|codex_hooks"
---

<objective>
Establish the authoritative closeout-hook and Codex-support contract that every later 57.9 plan builds on.

Purpose: Phase 57.9 cannot safely wire hooks while the source still conflates conceptual `SessionStop` wording with literal runtime event names or silently treats project-local Codex config as the only capability witness. This plan creates the source-of-truth helper semantics first.
Output: installer/runtime helpers and unit tests that settle closeout event naming plus Codex project/global evidence handling.
</objective>

<execution_context>
@./.codex/get-shit-done-reflect/workflows/execute-plan.md
@./.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57.9-hook-closeout-substrate-inserted/57.9-CONTEXT.md
@.planning/phases/57.9-hook-closeout-substrate-inserted/57.9-RESEARCH.md
@.planning/phases/58-structural-enforcement-gates/58-16-gate-06-07-defer-provenance.md
@bin/install.js
@get-shit-done/bin/lib/automation.cjs
@tests/unit/install.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add one authoritative closeout/capability helper contract in source</name>
  <files>bin/install.js, get-shit-done/bin/lib/automation.cjs</files>
  <action>Introduce the smallest shared helper surface needed for the rest of Phase 57.9 to stay honest.

In `bin/install.js`, add a pure helper or helper cluster that answers two questions explicitly:
1. What literal runtime hook key should the closeout substrate use for a given runtime (`Stop` shared, Claude `SessionEnd` optional but not the shared default)?
2. What is the Codex hook-support status for the target install scope, including evidence from project-local and user-global config plus any explicit conflict/ambiguity state?

Do not wire hooks yet. This plan is groundwork only. Replace stale inline comments like "Codex: no settings.json, hooks, or statusline" with wording that reflects conditional Codex hooks and the still-unshipped installer closure. In `automation.cjs`, stop treating project-local `.codex/config.toml` as the only witness when a global config exists. If precedence cannot be proven from current source/docs, return or surface an explicit ambiguous state rather than fabricating a definitive boolean.</action>
  <verify>`npx vitest run tests/unit/install.test.js` passes. Grep confirms a helper/comment surface in `bin/install.js` now references `Stop`/`SessionEnd` rather than literal `SessionStop` as a config key, and `automation.cjs` no longer hard-fails to false when the project config lacks `codex_hooks` but the global config enables it.</verify>
  <done>Later 57.9 plans can consume one authoritative helper contract for closeout event names and Codex support evidence instead of re-deriving them ad hoc.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit regressions for project/global Codex hook evidence and no-write groundwork behavior</name>
  <files>tests/unit/install.test.js</files>
  <action>Add focused unit cases that lock the helper semantics introduced in Task 1.

Cover at minimum:
- project config missing `codex_hooks`, global config enabled -> result is explicit support or explicit ambiguity, never silent "false"
- project config enabled, global config disabled/missing -> project scope remains supportable
- neither config enables hooks -> unsupported/waived path is explicit
- groundwork plan does not yet write any new closeout hook entries or `hooks.json` files by itself

Keep the tests narrowly scoped to helper/result semantics. Do not prematurely test the full installer hook writes here; those belong to later 57.9 plans.</action>
  <verify>`npx vitest run tests/unit/install.test.js -t "codex_hooks"` and the full `npx vitest run tests/unit/install.test.js` both pass. New tests fail if the helper collapses mixed project/global evidence into a silent negative.</verify>
  <done>The capability groundwork is regression-protected, and the repo has an executable witness for the project/global Codex evidence edge that motivated this phase’s research.</done>
</task>

</tasks>

<verification>
- [ ] `npx vitest run tests/unit/install.test.js` passes
- [ ] `rg -n "Stop|SessionEnd|codex_hooks" bin/install.js get-shit-done/bin/lib/automation.cjs` shows the new closeout/capability helper semantics
- [ ] No new Claude/Codex hook writes land yet; this plan is groundwork only
</verification>

<narrowing_decisions>
## Narrowing Decisions

None -- plan covers all CONTEXT.md claims verbatim.
</narrowing_decisions>

<success_criteria>
- One authoritative closeout event vocabulary exists in source
- Codex project/global hook evidence is surfaced honestly
- No hook installation is attempted before the groundwork exists
- Unit tests lock the groundwork behavior
</success_criteria>

<output>
After completion, create `.planning/phases/57.9-hook-closeout-substrate-inserted/57.9-01-SUMMARY.md`
</output>
