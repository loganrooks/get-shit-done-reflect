---
phase: 57.9-hook-closeout-substrate-inserted
plan: "04"
signature:
  role: planner
  harness: codex-cli
  platform: codex
  vendor: openai
  model: not_available
  reasoning_effort: not_available
  profile: quality
  gsd_version: 1.19.9+dev
  generated_at: "2026-04-22T00:31:25Z"
  session_id: "019db1be-42d4-7b82-ace9-76fc417dc71e"
  provenance_status:
    role: derived
    harness: exposed
    platform: derived
    vendor: derived
    model: not_available
    reasoning_effort: not_available
    profile: derived
    gsd_version: derived
    generated_at: exposed
    session_id: exposed
  provenance_source:
    role: artifact_role
    harness: runtime_context
    platform: derived_from_harness
    vendor: derived_from_harness
    model: not_available
    reasoning_effort: not_available
    profile: config
    gsd_version: installed_harness
    generated_at: writer_clock
    session_id: env:CODEX_THREAD_ID
type: execute
wave: 3
depends_on:
  - "03"
files_modified:
  - get-shit-done/references/capability-matrix.md
  - get-shit-done/bin/lib/verify.cjs
  - tests/unit/verify-xrt-01-capability-matrix.test.js
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Runtime-facing reference docs now describe the shipped closeout substrate honestly: `Stop` shared, Claude `SessionEnd` optional, Codex install-or-waiver explicit, 57.9 owner clear."
    - "XRT-01 closeout verification still fires when a phase touches the capability surface using current terms like `Stop`, `SessionEnd`, or closeout hook wording, not only the historical `SessionStop` string."
    - "The capability-matrix review path remains structural: phases touching closeout/capability semantics must update the matrix or fail closeout verification."
  artifacts:
    - path: "get-shit-done/references/capability-matrix.md"
      provides: "Runtime-facing authority for current closeout substrate wording and Codex conditional support."
      contains: "Stop"
    - path: "get-shit-done/bin/lib/verify.cjs"
      provides: "XRT-01 closeout verifier updated for current closeout terminology."
      contains: "SessionEnd"
    - path: "tests/unit/verify-xrt-01-capability-matrix.test.js"
      provides: "Regression coverage for Stop/SessionEnd-based capability touches."
      contains: "Stop"
  key_links:
    - from: "get-shit-done/bin/lib/verify.cjs"
      to: "get-shit-done/references/capability-matrix.md"
      via: "XRT-01 closeout check enforces matrix review for current closeout terminology"
      pattern: "capability-matrix|Stop|SessionEnd"
    - from: "tests/unit/verify-xrt-01-capability-matrix.test.js"
      to: "get-shit-done/bin/lib/verify.cjs"
      via: "regression cases prove current closeout terms still trigger XRT-01 review"
      pattern: "Stop|SessionEnd|postlude"
---

<objective>
Align the runtime-facing references and closeout-time verifier with the substrate that Phase 57.9 actually ships.

Purpose: Even after the installer and postlude source land, the repo would still drift if its capability matrix and XRT-01 closeout checks only recognize the historical `SessionStop` wording. This plan makes the reference and verifier surfaces speak the same runtime language as the shipped substrate.
Output: a capability matrix aligned to the current closeout contract plus XRT-01 closeout tests that recognize `Stop` / `SessionEnd` / closeout-hook terminology.
</objective>

<execution_context>
@./.codex/get-shit-done-reflect/workflows/execute-plan.md
@./.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57.9-hook-closeout-substrate-inserted/57.9-CONTEXT.md
@.planning/phases/57.9-hook-closeout-substrate-inserted/57.9-RESEARCH.md
@get-shit-done/references/capability-matrix.md
@get-shit-done/bin/lib/verify.cjs
@tests/unit/verify-xrt-01-capability-matrix.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align the capability matrix with the shipped closeout substrate and 57.9 ownership</name>
  <files>get-shit-done/references/capability-matrix.md</files>
  <action>Update the capability matrix so the runtime-facing reference matches what 57.9 actually ships.

Replace or annotate any remaining load-bearing wording that treats `SessionStop` as the literal runtime key. The matrix should describe the shared closeout event as `Stop`, note Claude’s additional `SessionEnd` surface where relevant, and state plainly that 57.9 owns the unshipped closeout substrate closure while Codex remains install-or-waiver conditional. Preserve the historical/contextual references where useful, but the live matrix must stop implying a nonexistent literal key or contradictory phase owner.</action>
  <verify>`rg -n "Stop|SessionEnd|57.9" get-shit-done/references/capability-matrix.md` shows current closeout wording and 57.9 ownership. No load-bearing live guidance still relies on literal `SessionStop` as the runtime config key.</verify>
  <done>The runtime-facing capability authority matches the installed substrate and phase ownership contract.</done>
</task>

<task type="auto">
  <name>Task 2: Extend XRT-01 closeout verification and tests for current closeout terminology</name>
  <files>get-shit-done/bin/lib/verify.cjs, tests/unit/verify-xrt-01-capability-matrix.test.js</files>
  <action>Update the XRT-01 closeout capability-matrix diff check so it still catches capability-touch phases when they use current closeout terms.

Expand the capability-touch keyword/heuristic surface to include `Stop`, `SessionEnd`, and equivalent closeout-hook wording alongside the historical `SessionStop` term. Add regression cases proving that a phase summary/context mentioning `Stop` or `SessionEnd` without a matrix update still blocks closeout, and that updating the matrix passes.

Do not weaken the structural rule. The change is terminology coverage, not a relaxation of XRT-01.</action>
  <verify>`npx vitest run tests/unit/verify-xrt-01-capability-matrix.test.js` passes. New test cases fail if `verify.cjs` stops treating `Stop` / `SessionEnd` / closeout-hook wording as capability touches.</verify>
  <done>The closeout verifier remains structurally sensitive to current runtime hook language, so 57.9 cannot ship stale docs/matrix wording by accident.</done>
</task>

</tasks>

<verification>
- [ ] `npx vitest run tests/unit/verify-xrt-01-capability-matrix.test.js` passes
- [ ] `rg -n "Stop|SessionEnd|57.9" get-shit-done/references/capability-matrix.md` reflects the final closeout contract
- [ ] `verify.cjs` capability-touch heuristics cover current closeout terminology, not only historical `SessionStop`
</verification>

<narrowing_decisions>
## Narrowing Decisions

None -- plan covers all CONTEXT.md claims verbatim.
</narrowing_decisions>

<success_criteria>
- Capability matrix wording matches the shipped substrate
- XRT-01 closeout review remains structural under current terminology
- Runtime-facing docs no longer lean on stale literal event names
</success_criteria>

<output>
After completion, create `.planning/phases/57.9-hook-closeout-substrate-inserted/57.9-04-SUMMARY.md`
</output>
