# Phase 57.9: Hook & Closeout Substrate - Discussion Log

> **Justificatory sidecar.** Consumed by `gsdr-context-checker` for claim verification.
> Also serves as a human-readable audit trail of discuss-phase decisions.

**Date:** 2026-04-21
**Phase:** 57.9-hook-closeout-substrate-inserted
**Mode:** exploratory --auto
**Areas discussed:** Claude closeout hook surface, Codex conditional hook / waiver surface, `session_meta_postlude` source contract, defer-chain closure and boundary discipline

***

## Gray Areas (Audit Trail)

### Claude closeout hook surface

| Option | Description | Selected |
|--------|-------------|----------|
| Keep existing `SessionStart` / `PostToolUse` wiring and leave closeout to workflow prose | Avoids installer edits, but does not close HOOK-01 or the Phase 58 defer | |
| Extend the existing installer hook path with `SessionStop` plus install verification | Closes HOOK-01 on the same authoritative path already used for Claude hooks | ✓ |
| Patch installed runtimes manually after install | Bypasses source authority and is not structural | |

**User's choice:** `[auto]` Extend the existing installer hook path with `SessionStop` plus install verification.
**Notes:** Auto-selected because this is the only option that satisfies HOOK-01 without violating source-first authority.

### Codex conditional hook / waiver surface

| Option | Description | Selected |
|--------|-------------|----------|
| Continue silent Codex skip until hooks are stable | Preserves current behavior but leaves HOOK-02 unresolved and unverifiable | |
| Install Codex hooks when `codex_hooks=true`, otherwise emit explicit waiver / degradation evidence | Preserves honest conditionality and gives Phase 58 a real re-entry witness | ✓ |
| Assume full Codex parity regardless of feature-flag state | Over-claims capability and risks fake closure | |

**User's choice:** `[auto]` Install Codex hooks when `codex_hooks=true`, otherwise emit explicit waiver / degradation evidence.
**Notes:** Auto-selected because the rewritten phase scope and REQUIREMENTS.md explicitly forbid silent skip while still allowing conditional reality.

### `session_meta_postlude` source contract

| Option | Description | Selected |
|--------|-------------|----------|
| Leave the current optional-require seam permanently empty | Keeps graceful fallback but never closes HOOK-03 | |
| Ship a canonical loader that returns event records or explicit `not_available` markers | Matches the existing Phase 58 consumer seam and provides real substrate | ✓ |
| Pull live incident log-sensor wiring into 57.9 | Expands beyond substrate closure into later consumer work | |

**User's choice:** `[auto]` Ship a canonical loader that returns event records or explicit `not_available` markers.
**Notes:** Auto-selected because it is the smallest honest closure that satisfies HOOK-03 and reuses existing measurement architecture.

### Defer-chain closure and boundary discipline

| Option | Description | Selected |
|--------|-------------|----------|
| Treat 57.9 as already absorbed by later phases | Shortcut that breaks the explicit Phase 58 defer chain | |
| Keep 57.9 as a strict substrate-closure phase mapped to the Phase 58 re-entry conditions | Preserves traceability and no-shortcuts discipline | ✓ |
| Retire GATE-06 / GATE-07 defers by prose once code lands somewhere nearby | Would manufacture completion without the named evidence chain | |

**User's choice:** `[auto]` Keep 57.9 as a strict substrate-closure phase mapped to the Phase 58 re-entry conditions.
**Notes:** Auto-selected because the user explicitly required that the underlying problem be addressed fully and without shortcuts.

### Claude's Discretion

- Exact hook command naming inside `bin/install.js`
- Exact metadata-only trace format for `session_meta_postlude`
- Exact location of the explicit Codex waiver marker once research resolves the best witness surface
- Exact split between unit and integration verification commands

### Deferred Ideas

- Live incident-detection wiring remains later consumer work
- Broader Codex hook parity beyond closeout substrate remains future work
- Post-57.9 ledger retirement / consumer registration follows once HOOK evidence exists

***

## Claim Justifications

For each typed claim in CONTEXT.md, record the type-specific justification.
Grouped by topic area matching Working Model / Constraints / Guardrails sections.
See `get-shit-done/references/claim-types.md` for justificatory expectations per type.

### Claude closeout hook surface

**[evidenced:cited] The current install path writes Claude `SessionStart` and `PostToolUse` hooks but no `SessionStop` closeout hook.**
- **Citation:** `bin/install.js` Claude settings / hook registration block; current hook writes target `SessionStart` and `PostToolUse` only.
- **Verification:** Read current installer code and confirmed no `SessionStop` registration path exists.

**[decided:cited] HOOK-01 is satisfied only by source-first installer wiring plus install-time verification against written settings, not by manual runtime patching or workflow prose.**
- **Alternatives considered:** manual runtime patching; workflow-only postlude prose.
- **Why rejected:** manual patching violates source authority and would not be structural; prose does not install substrate.
- **User said:** The issue 57.9 addresses must be handled fully, with no shortcuts.

**[decided:reasoned] Phase 57.9 should extend the existing Claude hook registration path rather than invent a second closeout-write mechanism, so the installed runtime stays a single authoritative surface.**
- **Alternatives considered:** second writer for closeout only; post-install mutation script.
- **Why rejected:** both create multiple sources of truth for installed hook state.
- **User said:** No direct statement on mechanism; this is the smallest source-first extension consistent with the repo rules.

### Codex conditional hook / waiver surface

**[evidenced:cited] Codex install currently exits before settings or hook installation logic, so no installed Codex closeout substrate exists today.**
- **Citation:** `bin/install.js` Codex branch returns after manifest / parity work and before Claude settings / hook configuration logic.
- **Verification:** Read the live installer branch and confirmed no Codex `hooks.json` write path currently executes.

**[assumed:reasoned] A runtime with `codex_hooks=true` is eligible for installer wiring, but the feature's "under development" status means Phase 57.9 still needs an explicit waiver path when the surface is unavailable or unsafe.**
- **Challenge protocol:** Re-validate live Codex hook support and determine whether current feature-flag evidence is sufficient for installer-owned writes in the target environment.
- **Evidence checked:** `get-shit-done/bin/lib/automation.cjs`, `.planning/research/cross-runtime-parity-research.md`, and the capability matrix all confirm conditional availability behind `codex_hooks=true`.
- **Why reasonable:** The repo already treats `codex_hooks=true` as meaningful capability evidence, but the conditional / under-development status makes a no-waiver parity claim too strong.

**[decided:cited] HOOK-02 is only honestly closed by either installed Codex hook surface or a named waiver / degradation marker; silent skip is forbidden.**
- **Alternatives considered:** retain silent skip; delay all Codex handling to later phases.
- **Why rejected:** both preserve the unresolved Phase 58 defer without an auditable witness.
- **User said:** The underlying issue must be addressed fully and with no shortcuts.

### `session_meta_postlude` source contract

**[evidenced:cited] Measurement already has a drop-in seam for `session_meta_postlude`, and it currently reports graceful absence rather than throwing when the module is missing.**
- **Citation:** `get-shit-done/bin/lib/measurement/extractors/gsdr.cjs` optional require of `../sources/session-meta-postlude.cjs`.
- **Verification:** Read the extractor and confirmed `MODULE_NOT_FOUND` degrades to `not_available`.

**[decided:cited] Phase 57.9 ships a real `get-shit-done/bin/lib/measurement/sources/session-meta-postlude.cjs` source contract instead of leaving permanent graceful absence.**
- **Alternatives considered:** keep graceful absence indefinitely; bolt markers directly into consumers.
- **Why rejected:** the former never closes HOOK-03, the latter bypasses the existing canonical seam.
- **User said:** No direct file-path preference; the no-shortcuts requirement makes permanent graceful absence insufficient.

**[decided:reasoned] The source contract must emit canonical records or explicit `not_available` markers for `postlude-fired`, `error-rate`, `direction-change`, and `destructive-event`; downstream consumers must not read ad hoc runtime files directly.**
- **Alternatives considered:** free-form hook logs; per-consumer direct reads from runtime-specific files.
- **Why rejected:** both recreate hidden substrate coupling and weaken verifier / measurement reuse.
- **User said:** The issue 57.9 responds to should be addressed fully, not partially around the edges.

### Defer-chain closure and boundary discipline

**[evidenced:cited] GATE-06 and GATE-07 remain `explicitly_deferred` to Phase 57.9 in the authoritative Phase 58 defer and ledger artifacts.**
- **Citation:** `.planning/phases/58-structural-enforcement-gates/58-16-gate-06-07-defer-provenance.md`; `.planning/phases/58-structural-enforcement-gates/58-LEDGER.md`.
- **Verification:** Read both artifacts and confirmed `explicitly_deferred` entries targeting Phase 57.9.

**[decided:cited] Phase 57.9 is a strict substrate-closure phase; it does not absorb Phase 58 consumer registration or Phase 60.1 live incident wiring.**
- **Alternatives considered:** treat 57.9 as a broader consumer-and-substrate catch-up phase; claim later phases already absorbed it.
- **Why rejected:** both weaken the existing defer-provenance chain and reintroduce scope drift.
- **User said:** Make sure the issue 57.9 responds to is addressed fully and without shortcuts, regardless of what it takes.

**[governing:reasoned] GATE-06 and GATE-07 can leave defer state only with real HOOK-01 / HOOK-02 / HOOK-03 evidence mapped to the six re-entry conditions already named by Phase 58.**
- **Source:** `.planning/phases/58-structural-enforcement-gates/58-16-gate-06-07-defer-provenance.md` re-entry section plus the adopted 57.9 closure deliberation.
- **Scope of governance:** Planning, verification, and any later attempt to retire the defer entries.

### Derived Constraints

**[governing:reasoned] DC-1: The source repo is canonical. `bin/install.js`, `get-shit-done/bin/lib/measurement/`, and planning artifacts are edited first; `.claude/` and `.codex/` are verification outputs, not primary fix surfaces.**
- **Source:** Repo AGENTS rules, current project conventions, and prior source-vs-installed drift signals.
- **Scope of governance:** File ownership, implementation order, and verification wording.

**[decided:cited] DC-2: Phase 57.9 owns substrate closure only. Phase 58 consumes the substrate, and Phase 60.1 owns downstream live incident wiring.**
- **Alternatives considered:** absorb consumer work into 57.9; push substrate closure into later phases.
- **Why rejected:** both conflict with the rewritten roadmap and the existing defer witness.
- **User said:** No direct split preference, but the request explicitly rejected shortcuts and ownership ambiguity.

**[decided:cited] DC-3: Codex closure must end in installer-wired hooks when supported or an explicit waiver / degradation artifact when unsupported; silent absence never satisfies HOOK-02.**
- **Alternatives considered:** silent skip; prose note with no persisted waiver evidence.
- **Why rejected:** neither gives Phase 58 a real re-entry witness.
- **User said:** The issue should be addressed fully; no shortcuts.

**[evidenced:cited] DC-4: The existing `gate_fire_events` consumer expects `session_meta_postlude` as a drop-in loader returning event rows; Phase 57.9 should match that seam rather than create a second measurement pathway.**
- **Citation:** `get-shit-done/bin/lib/measurement/extractors/gsdr.cjs`; `.planning/phases/58-structural-enforcement-gates/58-19-SUMMARY.md`.
- **Verification:** Read both artifacts and confirmed the optional loader contract and drop-in expectation.

**[decided:cited] DC-5: Phase 57.9 verification must map HOOK-01 / HOOK-02 / HOOK-03 delivery to the six re-entry conditions in `58-16-gate-06-07-defer-provenance.md`; without that mapping, GATE-06 / GATE-07 remain deferred.**
- **Alternatives considered:** generic verification summary; prose note that the substrate now exists.
- **Why rejected:** the defer chain already defines a stricter closure test and should not be weakened.
- **User said:** The underlying issue must be fully addressed.

### Epistemic Guardrails

**[governing:reasoned] G-1: No false closure by prose. Workflow text, comments, or roadmap edits alone do not count as HOOK delivery without installer / runtime evidence.**
- **Source:** Phase 58 defer-provenance artifact's "non-delivery is the delivery" discipline plus the adopted 57.9 deliberation.
- **Scope of governance:** What counts as completion evidence.

**[governing:reasoned] G-2: Codex uncertainty must stay explicit. Do not collapse `codex_hooks` reality into either blanket "Codex has no hooks" or blanket parity claims.**
- **Source:** Current capability matrix, cross-runtime parity research, and the rewritten REQUIREMENTS cross-runtime note.
- **Scope of governance:** Capability wording, waiver policy, and verification claims.

**[governing:reasoned] G-3: Phase 57.9 must not swallow later live-sensor or reflection work just because the substrate touches incident markers; later consumers stay later phases.**
- **Source:** Rewritten roadmap boundary plus the deliberation's strict substrate-closure recommendation.
- **Scope of governance:** Scope control during research and planning.

**[governing:reasoned] G-4: The new postlude source must stay metadata-only and compatible with the existing measurement architecture; Phase 57.9 does not introduce a new measurement subsystem.**
- **Source:** Existing measurement extractor contract and the repo's phased measurement architecture.
- **Scope of governance:** File format, loader design, and anti-scope-creep discipline.

### Claim Dependencies

| Claim | Depends On | Vulnerability |
|-------|-----------|---------------|
| HOOK-02 closes with installer-wired Codex hooks or explicit waiver | The support threshold for `codex_hooks=true` being "supported enough" for installer wiring | HIGH — if the threshold is wrong, the phase either manufactures parity or waives real capability |
| `session_meta_postlude` ships as a drop-in loader | The existing optional-require seam in `gate_fire_events` remaining authoritative | LOW — the seam already exists and was designed for a future module |
| Phase 57.9 stays substrate-only | The roadmap boundary between 57.9 substrate work and 60.1 live incident wiring remaining stable | MEDIUM — if later work is pulled back into 57.9, scope drift returns immediately |
| GATE-06 / GATE-07 can leave defer state only with HOOK evidence | The six re-entry conditions in the Phase 58 defer artifact remaining authoritative | LOW — those conditions are already the named defer contract and are directly citable |

***

## Context-Checker Verification Log

This section is populated by the `gsdr-context-checker` agent after it runs.
Leave this header and a placeholder line when first writing the file.

*Awaiting context-checker run.*
