---
phase: 55.2-codex-runtime-substrate
plan: 01
model: claude-opus-4-6
context_used_pct: 18
subsystem: automation
tags: [codex-cli, runtime-detection, capability-resolution, brownfield, agent-detection]
requires:
  - phase: 55-sync-01
    provides: "Upstream-synced automation.cjs, init.cjs, core.cjs modules"
provides:
  - "codex-cli as recognized --runtime value with correct capability mapping"
  - "Heuristic fallback detection of Codex via .codex/config.toml"
  - ".codex exclusion from brownfield false positives"
  - ".toml agent file detection in checkAgentsInstalled()"
affects: [automation, init, core, codex-cli-support]
tech-stack:
  added: []
  patterns: [feature-detection-over-runtime-branching, config-toml-regex-parsing]
key-files:
  created: []
  modified:
    - get-shit-done/bin/lib/automation.cjs
    - get-shit-done/bin/lib/init.cjs
    - get-shit-done/bin/lib/core.cjs
key-decisions:
  - "Codex hooks detection uses regex /codex_hooks\\s*=\\s*true/i on config.toml content rather than a TOML parser -- lightweight, no new dependency"
  - "Heuristic fallback placed inside existing try/catch else-branch after .claude/settings.json check -- no new control flow paths"
patterns-established:
  - "Feature-detection style: codex-cli branch sets capability booleans, then existing cap logic handles the rest -- no runtime-name branching outside resolution"
duration: 3min
completed: 2026-04-09
---

# Phase 55.2 Plan 01: Runtime Capability Resolution Summary

**Codex CLI runtime recognition via feature-detection in automation.cjs, brownfield exclusion in init.cjs, and TOML agent detection in core.cjs**

## Performance
- **Duration:** 3min
- **Tasks:** 2 completed
- **Files modified:** 3

## Accomplishments
- `--runtime codex-cli` correctly sets hasTaskTool=true and hasHooks conditional on .codex/config.toml codex_hooks flag
- Heuristic fallback detects .codex/config.toml when .claude/settings.json is absent, enabling automatic Codex capability detection
- Brownfield detection in init.cjs excludes .codex/ directories from false-positive triggers
- checkAgentsInstalled() in core.cjs recognizes .toml agent files alongside .md and .agent.md

## Task Commits
1. **Task 1: Add codex-cli runtime branch and heuristic fallback to automation.cjs** - `a1f1abd8`
2. **Task 2: Add .codex brownfield exclusion and .toml agent detection** - `64e6ed63`

## Files Created/Modified
- `get-shit-done/bin/lib/automation.cjs` - Added codex-cli explicit runtime branch and .codex/config.toml heuristic fallback in cmdAutomationResolveLevel()
- `get-shit-done/bin/lib/init.cjs` - Added .codex to skipDirs Set in brownfield detection
- `get-shit-done/bin/lib/core.cjs` - Added .toml extension check in checkAgentsInstalled() loop and updated JSDoc

## Deviations from Plan

None - plan executed exactly as written.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
Runtime capability resolution now recognizes Codex CLI. Ready for Plan 02 (documentation drift corrections) and Plan 03 (parity smoke test).

## Self-Check: PASSED
