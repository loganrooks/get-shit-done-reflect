---
phase: 41-health-score-automation
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - get-shit-done/bin/gsd-tools.js
  - get-shit-done/references/health-probes/signal-metrics.md
  - get-shit-done/references/health-probes/signal-density.md
  - get-shit-done/references/health-probes/automation-watchdog.md
  - get-shit-done/workflows/health-check.md
  - get-shit-done/references/health-check.md
  - .planning/FORK-DIVERGENCES.md
autonomous: true

must_haves:
  truths:
    - "health-check workflow discovers probe files from references/health-probes/ and executes them in dependency order"
    - "gsd-tools.js health-probe subcommand computes signal-metrics, signal-density, and automation-watchdog and returns structured JSON"
    - "Signal-to-resolution ratio is computed within current regime boundaries (HEALTH-08)"
    - "Signal density trend is computed per-phase within current regime (HEALTH-09)"
    - "Automation watchdog compares last_triggered timestamps against expected cadence (HEALTH-07)"
    - "Existing --full, --focus, --fix, --stale-days flags continue to work unchanged"
    - "Workflow dimension shows UNMEASURED when all workflow probes are skipped due to dependency failures"
    - "--focus flag uses exact category matching, not substring matching"
    - "FORK-DIVERGENCES.md Runtime section documents the health-probe case addition"
  artifacts:
    - path: "get-shit-done/bin/gsd-tools.js"
      provides: "health-probe subcommand with signal-metrics, signal-density, automation-watchdog sub-subcommands"
      contains: "case 'health-probe'"
    - path: "get-shit-done/references/health-probes/signal-metrics.md"
      provides: "Signal-to-resolution ratio probe definition"
      contains: "probe_id: signal-metrics"
    - path: "get-shit-done/references/health-probes/signal-density.md"
      provides: "Signal density trend probe definition"
      contains: "probe_id: signal-density"
    - path: "get-shit-done/references/health-probes/automation-watchdog.md"
      provides: "Automation timestamp watchdog probe definition"
      contains: "probe_id: automation-watchdog"
    - path: "get-shit-done/workflows/health-check.md"
      provides: "Refactored generic probe executor workflow"
      contains: "discover_probes"
    - path: "get-shit-done/references/health-check.md"
      provides: "Reduced reference (output format and repair rules only)"
      contains: "Output Format"
    - path: ".planning/FORK-DIVERGENCES.md"
      provides: "Updated fork divergence tracking for health-probe case"
      contains: "health-probe"
  key_links:
    - from: "get-shit-done/workflows/health-check.md"
      to: "get-shit-done/references/health-probes/*.md"
      via: "probe discovery scanning health-probes/ directory"
      pattern: "health-probes"
    - from: "get-shit-done/references/health-probes/signal-metrics.md"
      to: "get-shit-done/bin/gsd-tools.js"
      via: "execution: subcommand referencing health-probe signal-metrics"
      pattern: "health-probe signal-metrics"
    - from: "get-shit-done/workflows/health-check.md"
      to: "get-shit-done/references/health-scoring.md"
      via: "workflow reads scoring model after all probes execute"
      pattern: "health-scoring"
---

<objective>
Refactor the health check workflow into a generic probe executor and add the health-probe subcommand to gsd-tools.js for complex computation probes (signal metrics, signal density, automation watchdog).

Purpose: Transform the monolithic workflow into a dynamic probe discovery/execution system, and provide the JavaScript computation backend for workflow-dimension probes that require regime-aware KB analysis and config inspection.
Output: Refactored workflow, health-probe subcommand, 3 subcommand probe files, reduced health-check.md reference, updated FORK-DIVERGENCES.md
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-health-score-automation/41-RESEARCH.md
@.planning/phases/41-health-score-automation/41-01-SUMMARY.md
@get-shit-done/references/health-check.md
@get-shit-done/workflows/health-check.md
@get-shit-done/bin/gsd-tools.js
@get-shit-done/references/health-scoring.md
@.planning/FORK-DIVERGENCES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add health-probe subcommand to gsd-tools.js with signal-metrics, signal-density, and automation-watchdog</name>
  <files>
    get-shit-done/bin/gsd-tools.js
    get-shit-done/references/health-probes/signal-metrics.md
    get-shit-done/references/health-probes/signal-density.md
    get-shit-done/references/health-probes/automation-watchdog.md
    .planning/FORK-DIVERGENCES.md
  </files>
  <action>
Add a new top-level `health-probe` case to gsd-tools.js (alongside existing `automation` and `sensors` cases, before the `default` case). This subcommand handles complex health computations that are too involved for inline bash.

**CLI pattern:**
```
gsd-tools.js health-probe {probe-name} [--raw]
```

**Implementation: Add three functions and the case handler.**

**1. `cmdHealthProbeSignalMetrics(cwd, raw)` -- HEALTH-08:**
- Find KB directory: `.planning/knowledge/` primary, `~/.gsd/knowledge/` fallback
- Read `.planning/config.json` for `health_check.resolution_ratio_threshold` (default 5.0)
- Find latest regime_change entry: scan `signals/` dir for files containing `type: regime_change`, sort by created date, use latest as regime start. If none found, use epoch 0 (all history is one regime).
- Count signals within current regime by filtering signal files where created date >= regime start
- Separate detected (lifecycle: detected or triaged) vs resolved (lifecycle: remediated, verified, or closed)
- Compute ratio: detected.length / resolved.length (Infinity if resolved=0 and detected>0, 0 if both 0)
- Status: PASS if ratio <= threshold, WARNING if ratio > threshold
- Output JSON (when --raw):
```json
{
  "probe_id": "signal-metrics",
  "checks": [{
    "id": "SIG-RATIO-01",
    "description": "Signal-to-resolution ratio within current regime",
    "status": "PASS|WARNING",
    "detail": "Ratio N:1 (threshold: M:1)",
    "data": { "detected": N, "resolved": M, "ratio": R, "regime": "regime-id-or-null" }
  }],
  "dimension_contribution": {
    "type": "workflow",
    "signals": { "critical": C, "notable": N, "minor": M }
  }
}
```
- Human-readable output (without --raw): formatted text summary

**2. `cmdHealthProbeSignalDensity(cwd, raw)` -- HEALTH-09:**
- Same KB and regime resolution as signal-metrics
- Group regime signals by phase (parse phase from file path or frontmatter)
- Compute per-phase signal counts (densities array)
- Determine trend: if last 3 phases show increasing counts = "increasing", decreasing = "decreasing", else "stable". If fewer than 3 phases, always "stable".
- Status: PASS if stable or decreasing, WARNING if increasing
- Output JSON format similar to signal-metrics with probe_id "signal-density"

**3. `cmdHealthProbeAutomationWatchdog(cwd, raw)` -- HEALTH-07:**
- Read `.planning/config.json` for automation.stats (contains per-feature last_triggered timestamps and fires counts)
- Read feature-manifest.json for expected features (health_check, signal_collection, etc.)
- For each feature with a configured frequency:
  - Derive expected cadence from frequency: "every-phase" = check if last_triggered was within recent phase activity, "on-resume" = check if last_triggered was within 24h, "milestone-only" = relaxed check
  - If last_triggered is missing or stale beyond 3x expected cadence, flag as WARNING
- Status: PASS if all features healthy, WARNING if any stale
- Output JSON with probe_id "automation-watchdog", dimension_contribution type "infrastructure"

**Case handler (add before default case in main switch):**
```javascript
case 'health-probe': {
  const probeName = args[1];
  if (probeName === 'signal-metrics') {
    cmdHealthProbeSignalMetrics(cwd, raw);
  } else if (probeName === 'signal-density') {
    cmdHealthProbeSignalDensity(cwd, raw);
  } else if (probeName === 'automation-watchdog') {
    cmdHealthProbeAutomationWatchdog(cwd, raw);
  } else {
    error('Unknown health-probe. Available: signal-metrics, signal-density, automation-watchdog');
  }
  break;
}
```

**Also create 3 probe definition files** in `get-shit-done/references/health-probes/`:

1. **signal-metrics.md:**
```yaml
---
probe_id: signal-metrics
category: Signal Resolution Metrics
tier: default
dimension: workflow
execution: subcommand
depends_on: [kb-integrity]
---
```
Body: describe that this probe runs `gsd-tools.js health-probe signal-metrics --raw` and measures signal-to-resolution ratio within the current observation regime (HEALTH-08). Include the subcommand invocation pattern.

2. **signal-density.md:**
```yaml
---
probe_id: signal-density
category: Signal Density Trend
tier: default
dimension: workflow
execution: subcommand
depends_on: [kb-integrity]
---
```
Body: describe that this runs `gsd-tools.js health-probe signal-density --raw` and tracks signal accumulation rate per phase within current regime (HEALTH-09).

3. **automation-watchdog.md:**
```yaml
---
probe_id: automation-watchdog
category: Automation System Health
tier: default
dimension: infrastructure
execution: subcommand
depends_on: [config-validity]
---
```
Body: describe that this runs `gsd-tools.js health-probe automation-watchdog --raw` and verifies automation features are firing at expected cadence by checking last_triggered timestamps (HEALTH-07).

**After adding the health-probe case to gsd-tools.js, update `.planning/FORK-DIVERGENCES.md`:**
In the Runtime section (### Runtime), add a new row to the table for the health-probe case. The existing row documents signal schema enforcement from Phase 31. Add a second row:

| `get-shit-done/bin/gsd-tools.js` | Added `health-probe` top-level case with `signal-metrics`, `signal-density`, `automation-watchdog` sub-subcommands for health check computation (Phase 41) | Health score automation | keep-fork -- upstream has no health-probe subcommand | LOW (additive -- new case block, no upstream overlap) |

Update the existing row's "What Changed" column to mention both divergences (signal schema + health-probe), or add it as a separate row. A separate row is cleaner since these are independent changes. However, since the table tracks *files* not *changes*, append to the existing entry's "What Changed" cell: "; added `health-probe` top-level case with signal-metrics, signal-density, automation-watchdog sub-subcommands (Phase 41)". Keep merge stance as `keep-fork` and conflict risk as `LOW (additive)`.

**Pitfall avoidance (from research):**
- Regime boundary not found: treat as "all history is one regime" (Pitfall 3)
- No regime_change entries: compute across full signal history
- Parse errors in signal files: skip and continue, never crash the whole probe
  </action>
  <verify>
```bash
# Verify subcommand exists
node get-shit-done/bin/gsd-tools.js health-probe --help 2>&1 | head -5
# Verify probe files exist
ls get-shit-done/references/health-probes/signal-metrics.md get-shit-done/references/health-probes/signal-density.md get-shit-done/references/health-probes/automation-watchdog.md
# Test signal-metrics (should not crash even with empty KB)
node get-shit-done/bin/gsd-tools.js health-probe signal-metrics --raw 2>/dev/null || echo "Expected: may fail gracefully without KB"
# Verify probe count
ls get-shit-done/references/health-probes/*.md | wc -l  # Should be 9 (6 from Plan 01 + 3 new)
# Verify FORK-DIVERGENCES.md updated
grep -q "health-probe" .planning/FORK-DIVERGENCES.md && echo PASS || echo FAIL
npm test
```
  </verify>
  <done>gsd-tools.js has health-probe subcommand with signal-metrics (HEALTH-08), signal-density (HEALTH-09), and automation-watchdog (HEALTH-07) sub-subcommands that output structured JSON. 3 corresponding probe definition files exist with correct frontmatter. FORK-DIVERGENCES.md Runtime section updated with health-probe case entry (keep-fork, LOW conflict risk).</done>
</task>

<task type="auto">
  <name>Task 2: Refactor health-check workflow to generic probe executor and reduce health-check.md reference</name>
  <files>
    get-shit-done/workflows/health-check.md
    get-shit-done/references/health-check.md
  </files>
  <action>
**Part A: Refactor `get-shit-done/workflows/health-check.md`** from a monolithic category executor (~240 lines) into a generic probe discovery and execution workflow (~100-120 lines).

Replace the existing workflow content with the following structure:

```xml
<purpose>
Orchestrate workspace health validation via auto-discovered probe files. Discovers probes from references/health-probes/, filters by tier/focus, sorts by dependencies, executes each probe, computes the two-dimensional health score, writes cache, and reports findings.
</purpose>

<core_principle>
The workflow is a generic executor -- it discovers and runs probes without knowing about specific check categories. Adding a new check = adding a probe file, zero workflow edits.
</core_principle>

<required_reading>
Read get-shit-done/references/health-scoring.md for scoring model and composite mapping.
Read get-shit-done/references/health-check.md for output format and repair rules.
Read .planning/config.json for health_check settings.
</required_reading>

<process>
<step name="parse_arguments">
  (Keep existing flag parsing: --full, --focus, --fix, --stale-days N)
  All existing flags remain supported unchanged.
</step>

<step name="load_configuration">
  Read .planning/config.json for mode, stale_threshold, workflow_thresholds, resolution_ratio_threshold.
  Apply --stale-days override if provided.
</step>

<step name="discover_probes">
  Scan get-shit-done/references/health-probes/*.md (use path relative to GSD install dir).
  For each .md file:
  1. Parse YAML frontmatter to extract probe_id, category, tier, dimension, execution, depends_on
  2. Skip files that fail to parse (report as WARNING finding, never crash -- Pitfall 1 from research)
  3. Filter by tier: if --full flag NOT set, exclude probes with tier: full
  4. Filter by --focus using EXACT category matching (not substring):
     - `--focus kb` includes ONLY probes where category is exactly "KB Integrity"
     - `--focus planning` includes ONLY probes where category is exactly "Planning Consistency"
     - This is an explicit mapping, not a fuzzy match. Future probe categories that should respond to a --focus flag must be added to this mapping.
     - Documented mapping (extend as new probes are added):
       | --focus value | Exact category match |
       |---------------|---------------------|
       | kb            | "KB Integrity"      |
       | planning      | "Planning Consistency" |
  5. Build dependency graph from depends_on fields
  6. Topological sort (Kahn's algorithm): probes with no dependencies first, then dependents
  7. If circular dependency detected, report as FAIL finding and skip the cycle (Pitfall 2)
</step>

<step name="execute_probes">
  For each probe in topological order:

  Check if any depends_on probe FAILed -- if so, skip this probe with status "SKIPPED (dependency failed)".

  Execute based on execution type:

  **inline:** Read the probe file body, find bash code blocks under ## Checks section. For each check:
  - Run the bash code block via Bash tool
  - Parse output for PASS, WARNING, or FAIL
  - Handle intra-probe blocks: if a check's **blocks:** list includes subsequent check IDs, and this check FAILed, skip the blocked checks
  - Record result: {id, category, description, status, detail, repairable}

  **subcommand:** Run the declared gsd-tools.js command:
  ```bash
  node ~/.claude/get-shit-done/bin/gsd-tools.js health-probe {probe_id} --raw
  ```
  Parse the returned JSON. Each entry in "checks" array becomes a finding.

  **agent:** (Used by rogue-context.md in Plan 04) Spawn a subagent with the probe file as spec. Parse SENSOR OUTPUT delimiters for structured results. Not needed until Plan 04 adds rogue-context.md.
</step>

<step name="compute_score">
  After all probes execute, compute the two-dimensional health score per health-scoring.md:

  1. Infrastructure: aggregate all findings from dimension: infrastructure probes
     - HEALTHY: zero FAILs, zero WARNINGs
     - DEGRADED: at least one WARNING, zero FAILs
     - UNHEALTHY: any FAIL

  2. Workflow: aggregate findings from dimension: workflow probes
     - **UNMEASURED handling:** If NO workflow-dimension probes produced results (all were skipped
       due to dependency failures, e.g., kb-integrity failed causing signal-metrics and signal-density
       to skip), set workflow level to UNMEASURED. Display in report as "Workflow: unmeasured (dependency failed)".
       For composite score mapping, treat UNMEASURED the same as LOW but display the reason.
       This prevents undefined/NaN scores when the only workflow probes are dependency-gated.
     - Otherwise (at least one workflow probe produced results):
       Use dimension_contribution from subcommand JSON output
       Sum weighted signals (critical*1.0 + notable*0.3 + minor*0.1) after pattern dedup
       Compare against workflow_thresholds (from config or defaults low=2.0, high=5.0)
       LOW / MED / HIGH

  3. Composite: apply 3x3 matrix from health-scoring.md -> GREEN / YELLOW / RED
     - When workflow is UNMEASURED, use the LOW column of the matrix (same scores)
       but annotate the composite with "(workflow unmeasured)"

  4. Write cache to ~/.claude/cache/gsd-health-score.json:
     { infrastructure, workflow, composite, weighted_sum, signal_count, resolution_ratio, density_trend, checked: unix_timestamp, phase, regime_id }
     - When workflow is UNMEASURED, write workflow: "UNMEASURED" in the cache so downstream
       consumers (statusline, hooks) can distinguish from a measured LOW
</step>

<step name="report_findings">
  Display categorized checklist output (same format as before):
  ## Workspace Health Report
  **Project:** {name} | **Health:** {composite traffic light} | **Date:** {date}

  Standing caveat: "Health checks measure known categories. Absence of findings does not mean absence of problems."

  ### {Category} [{PASS|WARNING|FAIL}]
  - [x] Passed check
  - [ ] Failed check (remediation)

  ### Summary
  Infrastructure: {HEALTHY|DEGRADED|UNHEALTHY} | Workflow: {LOW|MED|HIGH|UNMEASURED}
  Composite: {GREEN|YELLOW|RED}
  {N} checks passed | {M} warnings | {K} failures
</step>

<step name="repair_if_requested">
  (Keep existing repair logic from current workflow -- same behavior)
  If --fix flag set, iterate repairable findings and apply repairs.
  YOLO mode: auto-repair. Interactive mode: prompt per repair.
</step>

<step name="signal_integration">
  (Keep existing signal integration from current workflow)
  If findings include WARNING or FAIL, optionally persist health-check signal.
</step>

<step name="cleanup_marker">
  After health check completes (regardless of results), clean up the session-start marker file if it exists.
  This marker (`~/.claude/cache/gsd-health-check-needed`) is written by the SessionStart hook (Plan 03)
  to indicate a health check is needed. Once any health check run completes, the marker's purpose is
  fulfilled and it should be removed so the statusline no longer shows `H?`.

  ```bash
  rm -f ~/.claude/cache/gsd-health-check-needed
  ```
</step>

<step name="final_summary">
  Display final summary with health score and next steps.
</step>
</process>

<error_handling>
(Keep existing error handling, add probe-specific handling)
Probe parse failure: skip probe, report as WARNING
Probe execution timeout: skip probe, report as WARNING
Circular dependency: report as FAIL, skip cycle
Missing cache directory: create ~/.claude/cache/ before writing
</error_handling>
```

**Part B: Reduce `get-shit-done/references/health-check.md`**

The existing 497-line reference contained all check definitions. Now that checks live in probe files, reduce this reference to contain ONLY:
1. Section 1: Overview (existing -- update to reference probe-based architecture)
2. Section 4: Output Format (existing -- keep as-is for report formatting)
3. Section 5: Repair Rules (existing -- keep as-is for --fix behavior)
4. Section 6: Signal Integration (existing -- keep as-is)

Remove Sections 2 and 3 (Check Categories and Shell Patterns) since those now live in individual probe files. Add a note: "Check definitions have been migrated to individual probe files in `references/health-probes/`. See each probe file for check IDs, shell patterns, and blocking rules."

Update Section 1 to reference the new architecture: probe discovery, health-scoring.md, and the probe contract.
  </action>
  <verify>
```bash
# Verify workflow has probe discovery
grep -q "discover_probes" get-shit-done/workflows/health-check.md && echo PASS || echo FAIL
# Verify workflow references health-scoring.md
grep -q "health-scoring" get-shit-done/workflows/health-check.md && echo PASS || echo FAIL
# Verify workflow includes marker cleanup step
grep -q "cleanup_marker" get-shit-done/workflows/health-check.md && echo PASS || echo FAIL
grep -q "gsd-health-check-needed" get-shit-done/workflows/health-check.md && echo PASS || echo FAIL
# Verify UNMEASURED handling documented
grep -q "UNMEASURED" get-shit-done/workflows/health-check.md && echo PASS || echo FAIL
# Verify exact --focus matching documented
grep -q "KB Integrity" get-shit-done/workflows/health-check.md && echo PASS || echo FAIL
grep -q "Planning Consistency" get-shit-done/workflows/health-check.md && echo PASS || echo FAIL
# Verify reference is reduced (should be significantly shorter than 497 lines)
wc -l get-shit-done/references/health-check.md  # Should be ~150-200 lines (down from 497)
# Verify reference mentions probe migration
grep -q "probe files" get-shit-done/references/health-check.md && echo PASS || echo FAIL
# Verify existing flags still documented
grep -q "\-\-full" get-shit-done/workflows/health-check.md && echo PASS || echo FAIL
grep -q "\-\-focus" get-shit-done/workflows/health-check.md && echo PASS || echo FAIL
grep -q "\-\-fix" get-shit-done/workflows/health-check.md && echo PASS || echo FAIL
npm test
```
  </verify>
  <done>health-check.md workflow is a generic probe executor (~100-120 lines) that discovers, filters, sorts, and executes probes from references/health-probes/. --focus uses exact category matching (kb -> "KB Integrity", planning -> "Planning Consistency"). Workflow dimension handles UNMEASURED state when all workflow probes are skipped due to dependency failures. health-check.md reference is reduced to output format and repair rules (~150-200 lines). All existing flags (--full, --focus, --fix, --stale-days) preserved. Marker file cleanup step removes gsd-health-check-needed after any health check run.</done>
</task>

</tasks>

<verification>
1. `gsd-tools.js health-probe signal-metrics --raw` returns valid JSON with probe_id, checks, dimension_contribution
2. `gsd-tools.js health-probe signal-density --raw` returns valid JSON with trend analysis
3. `gsd-tools.js health-probe automation-watchdog --raw` returns valid JSON with timestamp checks
4. health-check.md workflow contains discover_probes step and references health-scoring.md
5. health-check.md workflow contains cleanup_marker step that removes gsd-health-check-needed
6. 9 total probe files exist in references/health-probes/ (6 infra + 3 subcommand)
7. health-check.md reference is reduced and mentions probe migration
8. All existing flags preserved in workflow
9. --focus uses exact category matching (documented mapping table in discover_probes step)
10. Workflow dimension shows UNMEASURED when all workflow probes skipped, composite uses LOW column
11. FORK-DIVERGENCES.md Runtime section includes health-probe case entry
12. `npm test` passes
</verification>

<success_criteria>
- health-probe subcommand handles signal-metrics, signal-density, automation-watchdog with regime-aware computation
- Workflow discovers probes dynamically, sorts by dependency, executes by type (inline/subcommand/agent)
- --focus flag uses exact category matching with documented mapping table (not substring)
- Workflow dimension gracefully handles UNMEASURED state when all probes skipped
- Composite health score computed and cached after probe execution
- Marker file cleaned up after health check completes (statusline transitions from H? to H/H!/H!!)
- Existing command-line interface fully preserved
- FORK-DIVERGENCES.md accurately tracks the gsd-tools.js health-probe divergence
</success_criteria>

<output>
After completion, create `.planning/phases/41-health-score-automation/41-02-SUMMARY.md`
</output>
