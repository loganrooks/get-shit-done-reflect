---
phase: 57.2-discuss-phase-exploratory-mode-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["57.2-01"]
files_modified:
  - get-shit-done/workflows/discuss-phase.md
  - get-shit-done/templates/context.md
autonomous: true

must_haves:
  truths:
    - "Exploratory mode produces CONTEXT.md with Working Model & Assumptions, Derived Constraints, Epistemic Guardrails, Open Questions (generative), and Claim Dependencies sections"
    - "Discuss mode produces unchanged <decisions> / Implementation Decisions output"
    - "Claims use [type:verification] notation with 7 types and 3 verification levels"
    - "Auto-progression uses type-based rules instead of [grounded] gate"
    - "Open Questions use generative format with research program, downstream decisions, reversibility"
    - "Context template examples show typed claims in exploratory mode"
  artifacts:
    - path: "get-shit-done/workflows/discuss-phase.md"
      provides: "Mode-conditional write_context template, type-based auto-progression, context_model update"
      contains: "<working_model>|<constraints>|<guardrails>|<questions>|<dependencies>"
    - path: "get-shit-done/templates/context.md"
      provides: "Updated template with exploratory-mode sections and typed claim examples"
      contains: "Working Model & Assumptions"
  key_links:
    - from: "get-shit-done/workflows/discuss-phase.md"
      to: "get-shit-done/references/claim-types.md"
      via: "reference in context_model and write_context instructions"
      pattern: "references/claim-types.md"
    - from: "get-shit-done/templates/context.md"
      to: "get-shit-done/references/claim-types.md"
      via: "examples using typed claim notation"
      pattern: "\\[decided:cited\\]|\\[assumed:reasoned\\]"
---

<objective>
Restructure the discuss-phase write_context template for exploratory mode and update the context.md template with typed claim examples.

Purpose: This is the core template change that transforms CONTEXT.md from a shallow locked-down spec into a rich research-guiding artifact. Exploratory mode gets structural sections that enforce the kind of epistemic work that produced peak-era quality (Phases 52-54). Discuss mode remains unchanged.

Output: Updated `discuss-phase.md` (write_context, context_model, mode_routing, philosophy sections) and `context.md` template with typed claim examples.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57.2-discuss-phase-exploratory-mode-overhaul/57.2-CONTEXT.md
@.planning/phases/57.2-discuss-phase-exploratory-mode-overhaul/57.2-RESEARCH.md
@.planning/phases/57.2-discuss-phase-exploratory-mode-overhaul/57.2-01-SUMMARY.md
@get-shit-done/references/claim-types.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure discuss-phase.md write_context and supporting sections</name>
  <files>get-shit-done/workflows/discuss-phase.md</files>
  <action>
Modify `get-shit-done/workflows/discuss-phase.md` in four areas. Read the file first to confirm current line numbers (Plan 01 may have shifted them with --chain additions).

**Area 1: context_model block (currently ~lines 65-78)**

Replace the current section list with mode-aware guidance. Keep the existing list but annotate which sections are mode-conditional:

```markdown
<context_model>
Treat CONTEXT.md as a phase steering brief. Section availability depends on discuss mode:

**Common sections (all modes):**
- Phase Boundary
- Canonical References
- Code Context
- Specific Ideas
- Deferred Ideas

**Exploratory mode sections** (replace `<decisions>` when `DISCUSS_MODE` is `exploratory`):
- Working Model & Assumptions (`<working_model>`) -- typed claims using `references/claim-types.md` vocabulary
- Derived Constraints (`<constraints>`)
- Epistemic Guardrails (`<guardrails>`)
- Open Questions (`<questions>`) -- generative format with research program, downstream decisions, reversibility
- Claim Dependencies (`<dependencies>`) -- table recording inferential web

**Discuss mode sections** (standard behavior):
- Implementation Decisions (`<decisions>`)
- Open Questions (table format)

Only include sections that have content. Reference: `@get-shit-done/references/claim-types.md` for type vocabulary.
</context_model>
```

**Area 2: philosophy block exploratory mode additions (currently ~lines 55-63)**

Replace the current "Exploratory mode additions" block with updated type-based guidance. Remove ALL references to `[grounded]` and `[open]` as progression markers. Replace with:

```markdown
**Exploratory mode additions** (active when `DISCUSS_MODE` is `exploratory`):

These additions modify the discussion posture without changing the step structure:

- **Typed claims, not bare markers:** Every claim in CONTEXT.md carries a type and optional verification level using `[type:verification]` notation (see `references/claim-types.md`). The 7 types are: evidenced, decided, assumed, open, projected, stipulated, governing. The 3 verification levels are: cited, reasoned, bare.
- **Working model, not locked decisions:** Frame captured choices as part of a working model. `<working_model>` replaces `<decisions>` in exploratory mode. Language should signal "current understanding" rather than "locked choice."
- **Preserve genuine uncertainty:** If the user says "I'm not sure" or "either could work," record as `[open]` -- a legitimate outcome. Downstream agents (researcher, planner) investigate further.
- **Epistemic guardrails and constraints:** Derive constraints from requirements, prior phases, and codebase. Record guardrails that bound investigation. These are structural sections, not optional.
- **Auto-select by type rules:** With `--auto` in exploratory mode, auto-progression eligibility is determined by claim type (see `references/claim-types.md` Auto-Progression Rules), NOT by a `[grounded]` marker. Specifically:
  - `[evidenced:cited]`, `[decided]`, `[stipulated]`, `[governing]` -> auto-ready
  - `[assumed:reasoned]` or `[assumed:cited]` -> auto-ready (honest rationale provided)
  - `[assumed:bare]` -> NOT auto-ready (needs at minimum a stated rationale)
  - `[open]` -> NEVER auto-ready (requires research)
  - `[projected]` -> auto-ready if the projected phase exists in ROADMAP.md
  For claims that cannot auto-progress, record in the Open Questions section with generative format.
```

**Area 3: write_context step template (currently ~lines 886-1002)**

Replace the template inside the write_context step with a mode-conditional version. The key change: when `DISCUSS_MODE` is `exploratory`, use the new sections. When `discuss`, use existing `<decisions>` unchanged.

The template should be structured as (showing the mode-conditional middle section -- `<domain>`, `<canonical_refs>`, `<code_context>`, `<specifics>`, `<deferred>` sections remain unchanged):

```markdown
# Phase [X]: [Name] - Context

**Gathered:** [date]
**Status:** Ready for planning
**Mode:** [Exploratory|Discuss] [--auto] [--chain] -- [brief mode description]

<domain>
## Phase Boundary

[Clear statement of what this phase delivers -- the scope anchor]

</domain>

<!-- MODE-CONDITIONAL: exploratory mode uses working_model sections, discuss mode uses decisions -->

<!-- IF DISCUSS_MODE == exploratory -->
<working_model>
## Working Model & Assumptions

### [Topic area 1 that was discussed]

**Current state:** [Brief description of what exists now relevant to this topic]

- [type:verification] Claim text
- [type:verification] Another claim

### [Topic area 2 that was discussed]

**Current state:** [Brief description]

- [type:verification] Claim text

### Claude's Discretion
[Areas where Claude has flexibility during planning/implementation]

</working_model>

<constraints>
## Derived Constraints

[Constraints derived from requirements, prior phases, signals, or code reality.
Each constraint cites its derivation source.]

- **DC-1:** [type:verification] Constraint text -- derived from [source]
- **DC-2:** [type:verification] Constraint text -- derived from [source]

</constraints>

<guardrails>
## Epistemic Guardrails

[Bounds on investigation and implementation. What must NOT be assumed.
Verification standards. Quality thresholds.]

- **G-1:** [type:verification] Guardrail text
- **G-2:** [type:verification] Guardrail text

</guardrails>

<questions>
## Open Questions

[Research questions in generative format. NOT binary yes/no.
Each question specifies methodology, not just asks a question.]

### Q1: [Question title]
**Research program:** [How to investigate -- methodology, sources, experiments]
**Downstream decisions affected:** [What depends on the answer]
**Reversibility:** [Cost of getting this wrong -- HIGH/MEDIUM/LOW with explanation]

### Q2: [Question title]
**Research program:** [How to investigate]
**Downstream decisions affected:** [What depends on the answer]
**Reversibility:** [Cost of getting this wrong]

</questions>

<dependencies>
## Claim Dependencies

[Which claims depend on which. A decided claim resting on an assumed claim is a vulnerability.]

| Claim | Depends On | Vulnerability |
|-------|-----------|---------------|
| [ref to claim] | [ref to supporting claim] | [HIGH/MEDIUM/LOW -- why] |

</dependencies>

<!-- ELSE IF DISCUSS_MODE == discuss -->
<decisions>
## Implementation Decisions

### [Category 1 that was discussed]
- **D-01:** [Decision or preference captured]

### [Category 2 that was discussed]
- **D-03:** [Decision or preference captured]

### Claude's Discretion
[Areas where user said "you decide"]

### Folded Todos
[If any todos were folded into scope]

</decisions>
<!-- ENDIF -->
```

IMPORTANT: The `<!-- IF DISCUSS_MODE == exploratory -->` comments are instructions to the AGENT executing the workflow, not literal code comments. The agent reads the mode from the mode_routing step and produces the appropriate template variant. Keep both variants in the workflow spec so the agent knows what to produce in each mode.

Preserve ALL existing shared sections (`<canonical_refs>`, `<code_context>`, `<specifics>`, `<deferred>`) exactly as they are. Only replace the `<decisions>` section block with the mode-conditional block.

Add the `**Mode:**` line to the header (after `**Status:**`).

**Area 4: mode_routing step (currently ~lines 208-250)**

In the exploratory mode routing (case 3, around line 239), update the description to reference typed claims instead of `[grounded]/[open]`:

Change:
```
- Philosophy section gains **working assumptions** framing (see philosophy additions below)
- `--auto` behavior modified: only auto-select when strongly grounded
- Gray area presentation includes epistemic confidence markers
```

To:
```
- Template uses `<working_model>` sections with typed claims (see philosophy additions and write_context template)
- `--auto` behavior modified: auto-progression by type rules per references/claim-types.md
- Gray area presentation includes typed claim markers
```

After all changes, run `node bin/install.js --local` to sync.
  </action>
  <verify>
    - Exploratory sections present: `grep -c '<working_model>\|<constraints>\|<guardrails>\|<questions>\|<dependencies>' get-shit-done/workflows/discuss-phase.md` returns >= 5
    - Discuss mode preserved: `grep '<decisions>' get-shit-done/workflows/discuss-phase.md` still finds the discuss-mode template
    - No stale [grounded] gate: `grep -c '\[grounded\]' get-shit-done/workflows/discuss-phase.md` returns 0 (all replaced with typed claims)
    - Type-based auto rules referenced: `grep 'references/claim-types.md' get-shit-done/workflows/discuss-phase.md`
    - Generative question format: `grep 'Research program\|Downstream decisions\|Reversibility' get-shit-done/workflows/discuss-phase.md`
    - context_model updated: `grep 'Working Model & Assumptions' get-shit-done/workflows/discuss-phase.md`
    - Mode line in header: `grep 'Mode:' get-shit-done/workflows/discuss-phase.md`
    - Installer sync: `node bin/install.js --local` succeeds
  </verify>
  <done>Exploratory mode produces 5 new structural sections with typed claims; discuss mode produces unchanged decisions output; auto-progression uses type-based rules; context_model reflects both variants</done>
</task>

<task type="auto">
  <name>Task 2: Update context.md template with typed claim examples</name>
  <files>get-shit-done/templates/context.md</files>
  <action>
Update `get-shit-done/templates/context.md` with three changes.

**Change 1: Update the File Template section**

The current template (lines 17-70) shows only `<decisions>`. Add a note after the template explaining the mode-conditional behavior:

```markdown
**Note:** The template above shows the discuss-mode variant. In exploratory mode (`workflow.discuss_mode = exploratory`), the `<decisions>` section is replaced with: `<working_model>`, `<constraints>`, `<guardrails>`, `<questions>`, `<dependencies>`. See discuss-phase.md workflow for the full exploratory template. Claims use `[type:verification]` notation per `references/claim-types.md`.
```

**Change 2: Update the Purpose and Key Principle**

Update the top-level description (lines 1-8) to mention exploratory mode:

Change "Template for `.planning/phases/XX-name/{phase}-CONTEXT.md` - captures implementation decisions for a phase." to:
"Template for `.planning/phases/XX-name/{phase}-CONTEXT.md` - captures phase steering context. In discuss mode, captures implementation decisions. In exploratory mode, captures a working model with typed claims, constraints, guardrails, and generative open questions."

Update "Document decisions that downstream agents need" to:
"Document decisions (discuss mode) or working model with typed claims (exploratory mode) that downstream agents need."

**Change 3: Update existing examples and add exploratory example**

Update the 3 existing examples (Post Feed, Backup Command, Photo Organization) to add `[decided:cited]` or `[decided:reasoned]` type markers to key decisions. This shows what discuss-mode output looks like with the new vocabulary. For example, in Example 1 Post Feed:

```markdown
### Layout style
- [decided:reasoned] Card-based layout, not timeline or list
- [decided:reasoned] Each card shows: author avatar, name, timestamp, full post content, reaction counts
- [decided:bare] Cards have subtle shadows, rounded corners -- modern feel
```

Apply similar typing to Example 2 (CLI tool) and Example 3 (Photo library). Use `[decided:reasoned]` for decisions with stated rationale, `[decided:bare]` for preference-only decisions.

Add a 4th example showing exploratory mode output. Place it after Example 3, before `</good_examples>`. Use a domain appropriate for exploration (e.g., a search/indexing feature where the approach is uncertain):

```markdown
**Example 4: Exploratory mode (Search feature)**

# Phase 8: Search & Indexing - Context

**Gathered:** 2025-02-15
**Status:** Ready for planning
**Mode:** Exploratory --auto -- preserving uncertainty for researcher

<domain>
## Phase Boundary

Full-text search across documents with relevance ranking. Users can search by content, metadata, and tags. Advanced query syntax is a separate phase.

</domain>

<working_model>
## Working Model & Assumptions

### Indexing approach
**Current state:** Documents stored as markdown in /data/docs/. No existing search infrastructure.

- [assumed:reasoned] Full-text indexing is preferable to grep-based search -- document corpus is ~10K files, grep would be too slow for interactive use
- [open] Choice of search backend: SQLite FTS5 vs Elasticsearch vs Typesense. Depends on deployment constraints and query complexity needs
- [decided:cited] Documents are UTF-8 markdown -- confirmed by `file --mime data/docs/* | grep -c utf-8` returning 100%

### Relevance ranking
- [assumed:bare] TF-IDF is sufficient for initial ranking -- no user feedback data exists yet for learning-to-rank
- [projected:reasoned] Phase 12 will add user feedback signals for ranking refinement -- current ranking must be extensible
- [stipulated:bare] Top-20 results displayed per query -- chosen as default, not derived from evidence

### Claude's Discretion
- Index update strategy (real-time vs batch)
- Result snippet generation approach
- Cache invalidation policy

</working_model>

<constraints>
## Derived Constraints

- **DC-1:** [evidenced:cited] Must work offline -- deployment target has no internet access (requirements.md line 14)
- **DC-2:** [governing:reasoned] No external SaaS dependencies -- project principle from Phase 1 decisions

</constraints>

<guardrails>
## Epistemic Guardrails

- **G-1:** [governing:reasoned] Do not assume corpus size will stay at 10K -- design for 100K as upper bound
- **G-2:** [stipulated:bare] Acceptable search latency: < 200ms p95 for single-term queries

</guardrails>

<questions>
## Open Questions

### Q1: Which search backend fits deployment constraints?
**Research program:** Compare SQLite FTS5, Elasticsearch, and Typesense on: deployment footprint, query capability, index size for 10K-100K markdown docs. Test FTS5 locally with representative corpus subset.
**Downstream decisions affected:** Infrastructure requirements, dependency footprint, query syntax complexity
**Reversibility:** LOW -- search backend is a foundational choice affecting index format, query API, and all downstream features

### Q2: Is TF-IDF ranking sufficient without user signals?
**Research program:** Index 1K representative docs with FTS5, run 20 test queries, evaluate result quality subjectively. Compare with BM25 if available.
**Downstream decisions affected:** Whether Phase 12 ranking refinement is needed sooner
**Reversibility:** MEDIUM -- ranking algorithm is swappable but affects user-facing quality from day one

</questions>

<dependencies>
## Claim Dependencies

| Claim | Depends On | Vulnerability |
|-------|-----------|---------------|
| [stipulated] Top-20 results | [assumed] TF-IDF sufficient | MEDIUM -- if ranking is poor, showing 20 results amplifies bad ordering |
| [projected] Phase 12 feedback signals | [assumed] TF-IDF sufficient for now | LOW -- if TF-IDF is insufficient, Phase 12 becomes urgent but current work is not wasted |
| [decided] UTF-8 markdown | [evidenced] file --mime check | LOW -- if evidence is current, decision is solid |

</dependencies>
```

Also keep the existing `<canonical_refs>`, `<code_context>`, `<specifics>`, `<deferred>` sections in the example.

**Change 4: Update the guidelines section**

In the `<guidelines>` section (lines 261-283), add guidance about typed claims:

After "Good content (concrete decisions):" add:
```markdown
**Good typed claims (exploratory mode):**
- "[assumed:reasoned] TF-IDF is sufficient -- no user feedback data exists yet"
- "[decided:cited] Card layout -- consistent with existing Card component in src/components/"
- "[open] Choice of search backend -- depends on deployment constraints"
- "[stipulated:bare] 3 retry threshold -- chosen as default"

**Bad typed claims:**
- "[evidenced] It works well" (no citation for evidenced claim)
- "[decided] Modern approach" (too vague, no rationale)
- "[assumed] Standard practice" (no challenge protocol or reasoning)
```

**Change 5: Update the open_questions section**

The `<open_questions>` section (lines 285-314) currently shows a simple table format. Add an exploratory-mode variant showing the generative format:

After the existing table template, add:
```markdown
**Exploratory mode format** (replaces table above when `DISCUSS_MODE` is `exploratory`):

Each question uses generative format specifying how to investigate, not just what to ask:

### QN: [Question title]
**Research program:** [Methodology -- what to read, test, compare]
**Downstream decisions affected:** [What depends on the answer]
**Reversibility:** [HIGH/MEDIUM/LOW -- cost of getting this wrong]
```

After all changes, run `node bin/install.js --local` to sync.
  </action>
  <verify>
    - Exploratory example present: `grep 'Example 4.*Exploratory\|Example 4.*Search' get-shit-done/templates/context.md`
    - Typed claims in examples: `grep -c '\[decided:cited\]\|\[decided:reasoned\]\|\[assumed:reasoned\]' get-shit-done/templates/context.md` returns >= 5
    - Generative question format in example: `grep 'Research program:' get-shit-done/templates/context.md`
    - Dependency table in example: `grep 'Depends On.*Vulnerability' get-shit-done/templates/context.md`
    - Original examples preserved: `grep 'Post Feed\|Backup Command\|Photo Organization' get-shit-done/templates/context.md`
    - Mode note present: `grep 'exploratory mode' get-shit-done/templates/context.md`
    - Installer sync: `node bin/install.js --local` succeeds
  </verify>
  <done>Context template has 4 examples (3 updated with typed claims, 1 new exploratory), guidelines reference typed claims, open questions show both discuss and exploratory formats</done>
</task>

</tasks>

<verification>
1. `discuss-phase.md` write_context produces mode-conditional output: `<working_model>` sections for exploratory, `<decisions>` for discuss
2. `discuss-phase.md` context_model lists both mode variants
3. `discuss-phase.md` philosophy block references typed claims and type-based auto-progression (no `[grounded]` gate)
4. `context.md` template has 4 examples with typed claims
5. `context.md` guidelines and open_questions sections updated for both modes
6. `node bin/install.js --local` succeeds and installed files reflect all changes
7. No regression: discuss mode `<decisions>` template is unchanged from pre-57.2 behavior
</verification>

<success_criteria>
- Exploratory CONTEXT.md will have Working Model, Constraints, Guardrails, Generative Questions, and Dependencies sections
- Discuss CONTEXT.md will have unchanged Implementation Decisions section
- All claims in examples use [type:verification] notation
- Auto-progression decoupled from [grounded] marker -- uses type-based rules
- Template examples are realistic and show the vocabulary in action
</success_criteria>

<output>
After completion, create `.planning/phases/57.2-discuss-phase-exploratory-mode-overhaul/57.2-02-SUMMARY.md`
</output>
