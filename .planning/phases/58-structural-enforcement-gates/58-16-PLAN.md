---
phase: 58-structural-enforcement-gates
plan: 16
type: execute
wave: 4
depends_on: ['04', '05']
files_modified:
  - .planning/phases/58-structural-enforcement-gates/58-16-gate-06-07-defer-provenance.md
autonomous: true
must_haves:
  truths:
    - "Phase 57.9 phase directory does NOT exist at `.planning/phases/57.9-*/` at time of Phase 58 planning (AT-1 Option B branch): GATE-06 and GATE-07 are explicitly deferred to post-57.9 execution."
    - "A named-rationale provenance artifact `58-16-gate-06-07-defer-provenance.md` records: originating CONTEXT claims (DC-6 + AT-1), the dependency on 57.9 HOOK-01/02/03, the Codex degradation path, and two candidate ledger entries Plan 20 consumes with `disposition: explicitly_deferred` and `target_phase_if_deferred: \"Phase 57.9\"` (or the post-57.9 continuation phase Plan 20 names)."
    - "No substrate code is shipped under GATE-06 / GATE-07 in Phase 58 — this is the GATE-09c discipline applied reflexively: narrowing without substrate is legible, not silent."
    - "Per-gate Codex behavior for GATE-06 / GATE-07: `applies-via-installer` on Claude Code if/when 57.9 wires; `applies-via-workflow-step if codex_hooks=true else does-not-apply-with-reason` on Codex — both awaiting 57.9 per `58-05-codex-behavior-matrix.md`."
  artifacts:
    - path: ".planning/phases/58-structural-enforcement-gates/58-16-gate-06-07-defer-provenance.md"
      provides: "GATE-09c narrowing-provenance artifact — two ready-to-consume ledger entries + dependency citation + Codex degradation path"
      contains: "disposition: explicitly_deferred"
  key_links:
    - from: ".planning/phases/58-structural-enforcement-gates/58-16-gate-06-07-defer-provenance.md"
      to: ".planning/phases/58-structural-enforcement-gates/58-20-LEDGER.md"
      via: "Plan 20 consumes the entries when writing Phase 58's own ledger"
      pattern: "disposition: explicitly_deferred"
    - from: ".planning/phases/58-structural-enforcement-gates/58-16-gate-06-07-defer-provenance.md"
      to: ".planning/ROADMAP.md:141-150"
      via: "cites Phase 57.9 entry as the prerequisite phase (HOOK-01/02/03)"
      pattern: "Phase 57.9"
---

<objective>
Explicit-defer GATE-06 (automation postlude structurally fires) and GATE-07 (incident self-signal hook) to post-57.9 with named provenance per GATE-09c. This is AT-1 Option B: Phase 57.9 phase directory does NOT exist at time of planning; rather than ship GATE-06/07 dishonestly against missing substrate, Phase 58 legibly records the defer.

Purpose: CONTEXT DC-6 is explicit — Phase 58 does NOT duplicate 57.9 work. AT-1 requires either 57.9 complete OR explicit defer with GATE-09c provenance. Research Pitfall 4 names this trap: "GATE-06/07 planned as if 57.9 shipped." Verified at planning time via `ls .planning/phases/57.9-*/ 2>/dev/null` = empty: 57.9 has no phase directory and no plans. This plan records the defer properly.

Output: `58-16-gate-06-07-defer-provenance.md` — the single artifact proving the defer is named, not silent. Plan 20 consumes its entries.

Per-gate Codex behavior for GATE-06 / GATE-07: the matrix in Plan 05 covers this — both are `applies-via-installer` on Claude Code once 57.9 wires; `applies-via-workflow-step if codex_hooks=true else does-not-apply-with-reason` on Codex.

Fire-event: this plan emits NO runtime fire-event (no substrate to fire). The defer itself is the legible artifact. When 57.9 ships and GATE-06/07 land, those plans will emit their own markers.

This plan addresses the AT-1 + AT-5 obligations directly: AT-1 (prerequisite sequencing acknowledged with explicit defer) + AT-5 (reflexive GATE-09c applied to Phase 58's own CONTEXT — the GATE-06/07 dependency is an `[open]` claim that must be resolved or deferred with named downstream phase).
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-structural-enforcement-gates/58-CONTEXT.md
@.planning/phases/58-structural-enforcement-gates/58-RESEARCH.md
@.planning/phases/58-structural-enforcement-gates/58-04-ledger-schema.md
@.planning/phases/58-structural-enforcement-gates/58-05-codex-behavior-matrix.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 57.9 status + author the GATE-09c defer-provenance artifact</name>
  <files>.planning/phases/58-structural-enforcement-gates/58-16-gate-06-07-defer-provenance.md</files>
  <action>
First, re-verify that Phase 57.9 has not shipped between planning time and execution time. Then author the provenance artifact.

**Step 1: Verify 57.9 status:**

```bash
ls -d .planning/phases/57.9-*/ 2>/dev/null
ls .planning/phases/57.9-*/*-PLAN.md 2>/dev/null
```

Expected at planning time (2026-04-20): no phase directory, no plans. If (unlikely) 57.9 has shipped by execute time, STOP this plan and re-plan GATE-06/07 as consumer plans per AT-1 Option A. Record the new status in SUMMARY.md.

If 57.9 is still unshipped (expected), proceed to Step 2.

**Step 2: Author the artifact:**

Frontmatter:
```yaml
~~~
phase: 58-structural-enforcement-gates
plan: 16
artifact_type: defer_provenance
gates: [GATE-06, GATE-07]
defer_target_phase: "Phase 57.9"
defer_verified_at: "<ISO8601>"
prerequisite_status: "not_started"
~~~
```

Sections:

- **Section 1: Status verification (AT-1 witness).** Quote the `ls` commands above and their empty results at verification time. Cite CONTEXT DC-6 and `.planning/ROADMAP.md:141-150` (Phase 57.9 entry).

- **Section 2: Per-gate Codex behavior declaration.** For GATE-06: "applies-via-installer on Claude Code once 57.9 wires SessionStop; applies-via-workflow-step if codex_hooks=true else does-not-apply-with-reason on Codex." Same structure for GATE-07. Reference `58-05-codex-behavior-matrix.md` rows for both.

- **Section 3: Ready-to-consume ledger entries (for Plan 20).** Two YAML blocks matching the GATE-09a schema from Plan 04:

  ```yaml
  - context_claim: "GATE-06 automation postlude fires structurally via installed hook substrate"
    disposition: explicitly_deferred
    load_bearing: true
    target_phase_if_deferred: "Phase 57.9"
    narrowing_provenance:
      narrowing_decision: "Phase 58 does not implement GATE-06 substrate; consumes HOOK-01/02/03 output from Phase 57.9"
      originating_claim: "CONTEXT §5 [projected:reasoned] Phase 57.9 delivers installer-wired SessionStop + Codex hook surface + session-level markers"
      rationale: "DC-6 forbids Phase 58 from duplicating 57.9 work; 57.9 phase directory does not exist at Phase 58 planning time (AT-1 Option B); honest structural enforcement requires the substrate that 57.9 owns. Codex degradation path declared per Plan 05 matrix: applies-via-workflow-step if codex_hooks=true else does-not-apply-with-reason."
    role_split_provenance:
      written_by: planner
      written_at: "<ISO8601>"
      session_id: "<current session_id from env or 'planning-session-58-16'>"
  ```

  (Second block structurally identical for GATE-07.)

- **Section 4: Re-entry conditions.** Define when GATE-06 / GATE-07 can be un-deferred and implemented as consumer plans:
  - Phase 57.9 phase directory exists with plans at `ready_to_execute` or `complete`.
  - HOOK-01, HOOK-02, HOOK-03 each verified in the phase's VERIFICATION.md.
  - `bin/install.js` writes `SessionStop` entry to `.claude/settings.json` on install.
  - Codex hook surface either written under `codex_hooks=true` OR `.planning/config.json` has `codex_hooks_waived: true` marker.

- **Section 5: Chain-integrity note.** This plan does not modify any source file beyond creating this artifact. The defer is the scope; Plan 20's ledger inherits the entries. Plan 17 verifier reads this artifact + the ledger to confirm the defer is legible.

**Philosophical note (brief, per G-3 reflexive GATE-09):** Phase 58's CONTEXT §5 claimed GATE-06/07 "depend on 57.9 substrate"; a common failure mode would be shipping "GATE-06 done" via a prose workflow edit that does nothing structurally. This plan rejects that pattern — it explicitly names the non-delivery with a ledger entry, rather than manufacturing false completion.
  </action>
  <verify>
Read the artifact:
```
head -60 .planning/phases/58-structural-enforcement-gates/58-16-gate-06-07-defer-provenance.md
```
Expected: frontmatter + Section 1 witness.

Validate the ledger entries are well-formed per Plan 04's schema. Extract the two entries to a temp file and validate:
```
# Extract Section 3 YAML blocks, wrap in a minimal ledger frontmatter, validate:
cat > /tmp/test-defer-ledger.md <<'EOF'
~~~
phase: 58-structural-enforcement-gates
ledger_schema: v1
generated_at: "2026-04-20T00:00:00Z"
generator_role: planner
entries:
  # (paste the two entries here)
~~~
EOF
node get-shit-done/bin/gsd-tools.cjs frontmatter validate /tmp/test-defer-ledger.md --schema ledger
```
Expected: `{"valid": true, ...}`.

Confirm Section 4 re-entry conditions are explicit (not vague prose).

Confirm 57.9 status verification command output is recorded in Section 1.
  </verify>
  <done>
Artifact exists with AT-1 Option B verified + two valid ledger entries + Codex degradation + re-entry conditions. Plan 20 has the entries to consume.
  </done>
</task>

</tasks>

<verification>
- `58-16-gate-06-07-defer-provenance.md` exists.
- Frontmatter has `defer_target_phase: "Phase 57.9"`.
- Section 3 ledger entries pass `frontmatter validate --schema ledger`.
- Section 4 re-entry conditions are specific and testable.
- No source-code modifications in this plan (defer discipline).
</verification>

<success_criteria>
- AT-1 Option B satisfied (GATE-06/07 legibly deferred).
- AT-5 satisfied for this specific `[projected]` CONTEXT claim.
- Plan 20 has ready-to-consume ledger entries.
- G-3 reflexive GATE-09 applied — no manufactured completion.
</success_criteria>

<output>
After completion, create `.planning/phases/58-structural-enforcement-gates/58-16-SUMMARY.md`
</output>
</content>
</invoke>
