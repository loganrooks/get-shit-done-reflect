---
phase: 58-structural-enforcement-gates
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/58-structural-enforcement-gates/58-03-upstream-delta.md
  - agents/gsdr-assumptions-analyzer.md
autonomous: true
must_haves:
  truths:
    - "Upstream `gsd-assumptions-analyzer.md` is fetched via direct `curl` (authoritative per Research Verified-Ground-Truth) and its 105-line content is preserved verbatim (modulo `replacePathsInContent` path substitutions) at `agents/gsdr-assumptions-analyzer.md`."
    - "`58-03-upstream-delta.md` records the 6-category delta (methodology loading / analyzer agent / mode-aware gating / calibration tier / CONTEXT mandates / confidence badges) with per-category adopt / adopt-narrow / reject-with-rationale decisions; the four categories the fork adopts as-is and the two categories narrowed or rejected each carry explicit named rationale per GATE-09c."
    - "The narrowing decisions for `calibration-tier` (redundant with model_profile) and `CONTEXT.md section mandates` (fork superset is richer) are candidate ledger entries that Plan 20 consumes when writing Phase 58's own ledger."
    - "Per-gate Codex behavior for GATE-08a-e is declared `applies` on both runtimes (workflow file edits apply identically)."
  artifacts:
    - path: ".planning/phases/58-structural-enforcement-gates/58-03-upstream-delta.md"
      provides: "6-category delta analysis + per-category adoption decision + GATE-09c narrowing-provenance entries for the two narrowed categories"
      contains: "Upstream Delta Analysis"
    - path: "agents/gsdr-assumptions-analyzer.md"
      provides: "Fork-named port of upstream's assumptions-analyzer agent (Wave 3 Plan 11 wires it into discuss-phase)"
      contains: "gsdr-assumptions-analyzer"
  key_links:
    - from: "agents/gsdr-assumptions-analyzer.md"
      to: ".claude/agents/gsdr-assumptions-analyzer.md"
      via: "installer copies agents/ to .claude/agents/ via replacePathsInContent; post-install byte-identical-after-transform parity verified by Plan 10 (GATE-15)"
      pattern: "gsdr-assumptions-analyzer"
    - from: ".planning/phases/58-structural-enforcement-gates/58-03-upstream-delta.md"
      to: ".planning/phases/58-structural-enforcement-gates/58-20-LEDGER.md"
      via: "narrowing decisions for categories (d) and (e) become ledger entries with disposition=rejected_with_reason"
      pattern: "narrowing_provenance"
---

<objective>
Fetch upstream's current `discuss-phase-assumptions.md` and its companion `gsd-assumptions-analyzer.md` agent; produce the 6-category delta analysis with per-category adoption decision; port the analyzer agent into the fork as `gsdr-assumptions-analyzer`. This plan satisfies AT-1 for GATE-08a (current-state verification against upstream) and creates the input that Wave 3 Plan 11 consumes for GATE-08b (analyzer agent wiring) and Wave 4 Plan 17 consumes for GATE-08e (narrowing rationale under GATE-09c).

Purpose: CONTEXT Q2 required a live upstream re-fetch — Research R5 confirmed upstream is 671 lines (direct curl) and the analyzer agent exists (HTTP 200, 105 lines). But the categorical decisions (adopt / adopt-narrow / reject-with-rationale) are research's recommendations that must now be committed to a structured artifact so Wave 3 has explicit guidance and Plan 20's ledger has explicit narrowing entries.

Output: a delta-analysis artifact + a ported analyzer agent. Wave 3 Plan 11 consumes both; Wave 4 Plan 17 consumes the narrowing entries.

Per-gate Codex behavior: GATE-08a / 08b / 08c / 08d / 08e all `applies` on both runtimes — workflow file edits and agent files apply identically on Claude Code and Codex.

Fire-event: GATE-08a fires when `58-03-upstream-delta.md` exists with `verification_complete: true` in frontmatter. GATE-08b fires when `agents/gsdr-assumptions-analyzer.md` exists and is invoked by `discuss-phase-assumptions.md` (wiring done in Plan 11). No runtime emission — substrate is a filesystem check in Plan 17's verifier.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-structural-enforcement-gates/58-CONTEXT.md
@.planning/phases/58-structural-enforcement-gates/58-RESEARCH.md

@get-shit-done/workflows/discuss-phase-assumptions.md
@bin/install.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch upstream + produce the 6-category delta analysis artifact</name>
  <files>.planning/phases/58-structural-enforcement-gates/58-03-upstream-delta.md</files>
  <action>
Fetch the upstream files directly via `curl` (WebFetch is unreliable for byte-level comparisons per Research Verified-Ground-Truth). Compute the delta, commit the per-category decision, record narrowing entries.

Steps:

1. Fetch upstream via `curl`:
```
curl -s https://raw.githubusercontent.com/gsd-build/get-shit-done/main/get-shit-done/workflows/discuss-phase-assumptions.md > /tmp/upstream-discuss-phase-assumptions.md
wc -l /tmp/upstream-discuss-phase-assumptions.md
```
Expected: 671 lines (matches Research R5). If the line count differs by >10%, flag it in the artifact as "upstream drift since research" and proceed with the new count — do not block.

2. Fetch upstream agent:
```
curl -s https://raw.githubusercontent.com/gsd-build/get-shit-done/main/agents/gsd-assumptions-analyzer.md > /tmp/upstream-assumptions-analyzer.md
wc -l /tmp/upstream-assumptions-analyzer.md
```
Expected: ~105 lines.

3. Diff against fork:
```
diff /tmp/upstream-discuss-phase-assumptions.md get-shit-done/workflows/discuss-phase-assumptions.md | head -200
```

4. Write `.planning/phases/58-structural-enforcement-gates/58-03-upstream-delta.md` with:

   Frontmatter:
   ```yaml
   ---
   phase: 58-structural-enforcement-gates
   plan: 03
   artifact_type: upstream_delta
   gates: [GATE-08a, GATE-08b, GATE-08c, GATE-08d, GATE-08e]
   upstream_sha: "(git rev-parse HEAD on gsd-build/get-shit-done main at fetch time, via https://api.github.com/repos/gsd-build/get-shit-done/commits/main)"
   upstream_line_count_assumptions: <fetched>
   upstream_line_count_analyzer: <fetched>
   fork_line_count_assumptions: <current fork line count>
   verification_complete: true
   fetched_at: <ISO8601>
   ---
   ```

   Sections:
   - Section 1: Fetch methodology and verification (the `curl` commands used)
   - Section 2: Per-gate Codex behavior declaration (applies on both)
   - Section 3: The 6 categories (from Research R5 table):
     - (a) Methodology loading — ADOPT AS-IS
     - (b) Assumptions-analyzer agent — ADOPT AS-IS (renamed to `gsdr-assumptions-analyzer`, ported in Task 2)
     - (c) Mode-aware gating (`text_mode`) — ADOPT AS-IS
     - (d) Calibration tier — ADOPT NARROWED per GATE-09c (rationale: fork's `model_profile` already calibrates agent effort; user-philosophy tier is redundant surface complexity for solo user repo)
     - (e) CONTEXT.md section mandates — ADOPT NARROWED per GATE-09c (rationale: fork has richer typed-claim vocabulary via DISC-01..10; upstream's 6-section contract is a subset; keep fork superset)
     - (f) Confidence badges (Confident/Likely/Unclear) — ADOPT AS-IS AS MAPPED (map to typed-claim dimensions; document mapping in GATE-08c docs later)
   - Section 4: Ledger entries (ready-to-consume format for Plan 20):
     Each narrowing entry contains: `context_claim`, `disposition: rejected_with_reason`, `narrowing_provenance.originating_claim`, `narrowing_provenance.rationale`, `narrowing_provenance.narrowing_decision`.
   - Section 5: Summary table of 6 decisions (category | decision | rationale if narrowed)
  </action>
  <verify>
Read the artifact back. Confirm:
- Frontmatter has `verification_complete: true` and fetched_at ISO8601.
- Section 3 contains decisions for all 6 categories.
- Categories (d) and (e) carry named rationale.
- Section 4 contains at least 2 ready-to-consume ledger entries for Plan 20.
- `upstream_line_count_assumptions` in frontmatter matches the `wc -l` output.

Run the upstream fetch command a second time and confirm the line count is within 10% of the recorded value (guard against stale research without blocking on a transient upstream bump).
  </verify>
  <done>
Delta artifact exists with all 6 categories decided and the 2 narrowing entries ready for Plan 20's ledger. Wave 3 Plan 11 has explicit guidance on which upstream surface to adopt.
  </done>
</task>

<task type="auto">
  <name>Task 2: Port upstream `gsd-assumptions-analyzer.md` agent as `gsdr-assumptions-analyzer.md`</name>
  <files>agents/gsdr-assumptions-analyzer.md</files>
  <action>
Port upstream's analyzer agent into the fork's `agents/` directory with appropriate fork-branding path substitutions.

Steps:

1. Read `/tmp/upstream-assumptions-analyzer.md` from Task 1 (or re-fetch if Task 1 artifact was cleaned up).

2. Apply fork's `replacePathsInContent` transformation mentally or programmatically:
   - `gsd-build/get-shit-done` → `loganrooks/get-shit-done-reflect` (not usually relevant; repo references are rare in agents)
   - `gsd-assumptions-analyzer` → `gsdr-assumptions-analyzer` (name in `---\nname:\n---` frontmatter and any self-references)
   - `get-shit-done/` → `get-shit-done-reflect/` in any `@path` references (but ONLY in installed copies; source `agents/` keeps `get-shit-done/` per the dual-directory rule documented in CLAUDE.md:15-27)
   - `/gsd:` → `/gsdr:` in any command references
   - `gsd-` agent prefix → `gsdr-` in any agent cross-references

3. Preserve upstream content verbatim otherwise — do NOT rewrite, simplify, or "improve" the prompt. Any non-branding divergence must be recorded in `58-03-upstream-delta.md` Section 3 category (b) with a named rationale per GATE-09c. Default: zero non-branding divergence.

4. Write to `agents/gsdr-assumptions-analyzer.md` (source dir only — installer will copy to `.claude/agents/` on next install).

5. If the upstream file uses tools list, model, or frontmatter that doesn't exist in the fork's agent schema, add a compatibility note at the top of the fork file and keep the upstream content — do NOT drop fields. (Wave 3 Plan 11 wires the agent into `discuss-phase-assumptions.md`; it will surface any incompatibility.)

Note on dual-directory: per `CLAUDE.md:15-27`, edits land in `agents/` (source), and `bin/install.js` copies to `.claude/agents/`. Do NOT manually edit `.claude/agents/gsdr-assumptions-analyzer.md` — that would be overwritten on next install. GATE-15 Plan 10 will verify the installer's byte-identical-after-transform parity in CI.
  </action>
  <verify>
Read `agents/gsdr-assumptions-analyzer.md`. Confirm:
- Frontmatter `name:` field is `gsdr-assumptions-analyzer` (not `gsd-assumptions-analyzer`).
- Line count is within 5 of upstream's line count (small deviation expected from branding substitutions; larger indicates unintentional change).
- No `gsd-build/get-shit-done` path literals remain.
- No `@get-shit-done-reflect/` path references — source uses `@get-shit-done/` per source-dir convention.

Diff against upstream should show only branding / naming substitutions:
```
diff /tmp/upstream-assumptions-analyzer.md agents/gsdr-assumptions-analyzer.md
```
Every diff line should be a rename. If not, that unintended divergence is recorded as a GATE-09c narrowing entry in the delta artifact.
  </verify>
  <done>
`agents/gsdr-assumptions-analyzer.md` exists, is a direct port of upstream with only branding substitutions, and is ready for Wave 3 Plan 11 to wire into the discuss workflow.
  </done>
</task>

</tasks>

<verification>
- `58-03-upstream-delta.md` exists with `verification_complete: true` in frontmatter.
- Line counts recorded in frontmatter match `wc -l` at fetch time.
- All 6 categories decided; 2 have GATE-09c narrowing entries.
- `agents/gsdr-assumptions-analyzer.md` exists, is a near-verbatim port, only branding changes.
- No modifications to `.claude/agents/` — source-dir only (dual-directory rule).
</verification>

<success_criteria>
- Upstream re-fetched directly via `curl` (not WebFetch) — authoritative per Research Verified-Ground-Truth.
- 6-category decisions all committed.
- Analyzer agent ported.
- Ledger entries for (d) and (e) ready for Plan 20.
- Wave 3 Plan 11 has explicit guidance.
</success_criteria>

<output>
After completion, create `.planning/phases/58-structural-enforcement-gates/58-03-SUMMARY.md`
</output>
</content>
</invoke>