---
phase: 58-structural-enforcement-gates
plan: 11
type: execute
wave: 2
depends_on: ['03', '05']
files_modified:
  - get-shit-done/workflows/discuss-phase-assumptions.md
  - docs/workflow-discuss-mode.md
  - get-shit-done/workflows/plan-phase.md
  - get-shit-done/workflows/progress.md
  - agents/gsdr-assumptions-analyzer.md
autonomous: true
resolves_signals:
  - sig-2026-04-03-discuss-mode-adoption-gap-silent-feature-drop
must_haves:
  truths:
    - "`discuss-phase-assumptions.md` fork version is replaced with a port of upstream (671 lines verified 2026-04-20) adapted per `58-03-upstream-delta.md`: methodology loading adopted as-is, analyzer agent wired via `gsdr-assumptions-analyzer`, mode-aware `text_mode` adopted, calibration-tier narrowed per GATE-09c, CONTEXT.md section mandates preserved as fork superset (narrowed upward)."
    - "`gsdr-assumptions-analyzer` is invoked at the three upstream spawn points (initialize / deep_codebase_analysis / present_assumptions) with inline dispatch contract per GATE-13."
    - "`docs/workflow-discuss-mode.md` exists and documents the three discuss modes (standard interactive / `--auto` exploratory / richer-adoption) + confidence-badge to typed-claim mapping (GATE-08c)."
    - "`plan-phase.md` and `progress.md` ship `text_mode` branches that render plain-text numbered lists when `workflow.text_mode: true`, matching upstream mode-aware semantics (GATE-08d)."
    - "Per-gate Codex behavior for GATE-08a / 08b / 08c / 08d / 08e declared `applies` on both runtimes per `58-05-codex-behavior-matrix.md`."
  artifacts:
    - path: "get-shit-done/workflows/discuss-phase-assumptions.md"
      provides: "Upstream-parity discuss-phase-assumptions with methodology loading + analyzer agent + text_mode"
      contains: "gsdr-assumptions-analyzer"
    - path: "docs/workflow-discuss-mode.md"
      provides: "Canonical discuss-mode docs + typed-claim/confidence-badge mapping"
      contains: "text_mode"
    - path: "get-shit-done/workflows/plan-phase.md"
      provides: "plan-phase with text_mode rendering branch"
      contains: "text_mode"
    - path: "get-shit-done/workflows/progress.md"
      provides: "progress with text_mode rendering branch"
      contains: "text_mode"
  key_links:
    - from: "get-shit-done/workflows/discuss-phase-assumptions.md"
      to: "agents/gsdr-assumptions-analyzer.md"
      via: "three inline Task() spawn blocks calling gsdr-assumptions-analyzer with dispatch contract"
      pattern: "gsdr-assumptions-analyzer"
    - from: "get-shit-done/workflows/discuss-phase-assumptions.md"
      to: ".planning/phases/58-structural-enforcement-gates/58-03-upstream-delta.md"
      via: "adoption decisions per category (d) and (e) recorded as narrowing entries Plan 20 consumes"
      pattern: "GATE-09c"
---

<objective>
Adopt upstream's richer `discuss-phase-assumptions.md` per Plan 03's delta analysis; wire the ported `gsdr-assumptions-analyzer` agent; ship `docs/workflow-discuss-mode.md`; add mode-aware gates to `plan-phase.md` and `progress.md`. This is GATE-08a + 08b + 08c + 08d + 08e co-landed on one workflow surface.

Purpose: CONTEXT §6 splits GATE-08 into five enumerated sub-requirements (08a–08e). Plan 03 produced the delta + analyzer port; this plan actually adopts upstream into the workflow surface. `sig-2026-04-03-discuss-mode-adoption-gap-silent-feature-drop` (3 closed issues that masked missing work) is addressed by making the adoption structural: the port is wholesale, narrowings are explicit with GATE-09c provenance, and mode docs exist so future drift is detectable.

Output: upstream-ported workflow file + analyzer spawn points + mode-docs file + mode-aware downstream gates.

Per-gate Codex behavior: all five `applies` on both runtimes per `58-05-codex-behavior-matrix.md` — workflow edits read identically on both.

Fire-events:
- GATE-08a: `discuss-phase-assumptions.md` line count ≥ 600 after edit (proxy for "not a narrowed version").
- GATE-08b: `agents/gsdr-assumptions-analyzer.md` exists AND is referenced by ≥ 3 `Task(` blocks in the workflow.
- GATE-08c: `docs/workflow-discuss-mode.md` exists.
- GATE-08d: grep `text_mode` returns ≥ 1 hit in `plan-phase.md` AND `progress.md`.
- GATE-08e: `58-03-upstream-delta.md` Section 4 ledger entries exist for categories (d) and (e).

Plan 17 verifier asserts all five.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-structural-enforcement-gates/58-CONTEXT.md
@.planning/phases/58-structural-enforcement-gates/58-RESEARCH.md
@.planning/phases/58-structural-enforcement-gates/58-03-upstream-delta.md
@.planning/phases/58-structural-enforcement-gates/58-05-codex-behavior-matrix.md

@get-shit-done/workflows/discuss-phase-assumptions.md
@get-shit-done/workflows/plan-phase.md
@get-shit-done/workflows/progress.md
@agents/gsdr-assumptions-analyzer.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port upstream `discuss-phase-assumptions.md` + wire `gsdr-assumptions-analyzer` agent</name>
  <files>get-shit-done/workflows/discuss-phase-assumptions.md, agents/gsdr-assumptions-analyzer.md</files>
  <action>
Replace the fork's 279-line `discuss-phase-assumptions.md` with an adapted port of upstream's 671-line version per `58-03-upstream-delta.md` Section 3 decisions.

**Fetch:** if `/tmp/upstream-discuss-phase-assumptions.md` from Plan 03 is still available, reuse it; else re-fetch:
```
curl -s https://raw.githubusercontent.com/gsd-build/get-shit-done/main/get-shit-done/workflows/discuss-phase-assumptions.md > /tmp/upstream-discuss-phase-assumptions.md
```

**Apply fork transformations** (per installer's replacePathsInContent + fork conventions):
- `gsd-assumptions-analyzer` → `gsdr-assumptions-analyzer` (all references)
- `gsd-` agent prefix → `gsdr-` where matched
- `/gsd:` → `/gsdr:` (command references)
- `get-shit-done/` paths stay as-is in source (installer transforms to `get-shit-done-reflect/` on install)

**Apply adoption decisions** per Plan 03 delta Section 3:
- (a) Methodology loading: adopt verbatim (with fork branding).
- (b) Analyzer agent: wire the three spawn points. Each spawn block MUST include the GATE-13 inline dispatch contract (see `58-02-gate13-dispatch-contract-design.md` for format) — do NOT rely on `{analyzer_model}` template binding that CI grep would fail on. Resolve model at workflow-expansion time: the analyzer uses the same model class as the discussing agent (typically `{researcher_model}` from the plan-phase profile), so bake that in literally per the restatement pattern. If a literal value isn't known at authoring time, use the allowlist path from Plan 07 Task 2 and add the spawn-site line to the allowlist until resolveModelInternal can expand it.
- (c) Mode-aware `text_mode`: adopt verbatim. Fork already has `--auto`; keep that path and add text_mode rendering.
- (d) Calibration tier: REJECT with named GATE-09c rationale. Add a prose block at the top of the workflow file under a `<!-- NARROWING DECISION -->` HTML comment citing Plan 03 delta category (d). Do NOT silently drop — make the omission legible.
- (e) CONTEXT.md section mandates: PRESERVE FORK SUPERSET. The upstream 6-section contract is a subset of the fork's richer typed-claim contract per DISC-01..10. Add a prose block at the top of the workflow under `<!-- NARROWING DECISION -->` noting this is a narrowing-upward (fork is richer) and citing Plan 03 delta category (e).
- (f) Confidence badges: adopt as-mapped-to-typed-claims per Plan 03. Document mapping in `docs/workflow-discuss-mode.md` (Task 2) and reference it here.

**Analyzer agent** (`agents/gsdr-assumptions-analyzer.md`) was ported by Plan 03 Task 2. Verify it exists before spawning. If Plan 03 didn't complete (shouldn't reach this plan if it didn't), fail fast with a clear message.

**Line-count target:** after port + transformations + narrowing comments, line count should be ≥ 600 (close to upstream 671 minus any removed calibration-tier content). Record the actual count in the commit message.

**Chain to existing fork content:** some fork-specific logic may exist in the current 279-line version (e.g., DISC-01..10 typed-claim integration). Preserve it by merging rather than overwriting where the fork's version is genuinely richer. Use the Plan 03 delta Section 3 (f) decision — fork is richer on typed-claim vocabulary, adopt-as-mapped.
  </action>
  <verify>
```
wc -l get-shit-done/workflows/discuss-phase-assumptions.md
```
Expected: ≥ 600 lines (up from 279; target ~671 minus narrowed calibration-tier).

```
grep -c "gsdr-assumptions-analyzer" get-shit-done/workflows/discuss-phase-assumptions.md
```
Expected: ≥ 3 hits (three spawn points per upstream structure).

```
grep -cn "NARROWING DECISION" get-shit-done/workflows/discuss-phase-assumptions.md
```
Expected: ≥ 2 hits (categories (d) and (e)).

```
grep -c "text_mode" get-shit-done/workflows/discuss-phase-assumptions.md
```
Expected: ≥ 1 hit.

Confirm Task() spawn blocks include inline dispatch contract (per GATE-13 — no `{...}` template at model=):
```
grep -A 3 "subagent_type=.gsdr-assumptions-analyzer" get-shit-done/workflows/discuss-phase-assumptions.md | grep "model="
```
Expected: literal model values (e.g., `model="claude-sonnet-4-5"` or equivalent), not `model="{analyzer_model}"`.
  </verify>
  <done>
Upstream adoption complete with narrowings explicit; analyzer agent wired at three spawn points with GATE-13-conforming dispatch contracts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author `docs/workflow-discuss-mode.md` + add `text_mode` branches to `plan-phase.md` and `progress.md`</name>
  <files>docs/workflow-discuss-mode.md, get-shit-done/workflows/plan-phase.md, get-shit-done/workflows/progress.md</files>
  <action>
**Part A: Create `docs/workflow-discuss-mode.md`**

Frontmatter:
```yaml
~~~
doc_type: workflow_reference
schema_version: v1
applies_to: [discuss-phase, plan-phase, progress]
~~~
```

Sections:
- Section 1: Purpose — explain the three modes (standard interactive, `--auto`, richer-adoption).
- Section 2: Mode matrix (when to use, what user sees, what agent does).
- Section 3: `text_mode` semantics — when `workflow.text_mode: true`, render plain-text numbered lists instead of AskUserQuestion interactive picker; mode-aware presentation swap at specific step boundaries (initialize, present_assumptions, auto_advance per upstream).
- Section 4: Confidence badge → typed-claim mapping (per Plan 03 delta Section 3 (f)):
  - `Confident` ↔ `[evidenced:cited]` / `[evidenced:reasoned]`
  - `Likely` ↔ `[decided:*]` or `[assumed:*]` with reasoned verification
  - `Unclear` ↔ `[open]` or `[assumed:reasoned]` with bare verification
- Section 5: Integration points — which workflows invoke `discuss-phase-assumptions`; where mode-aware gates live (plan-phase, progress).
- Section 6: Cross-reference to `58-03-upstream-delta.md` for the adoption history.

**Part B: Add `text_mode` branches to `plan-phase.md` and `progress.md`**

In each workflow, find the AskUserQuestion invocations (grep for `AskUserQuestion` or `readline` or prompt patterns). At each, add a conditional:

```bash
if [ "${WORKFLOW_TEXT_MODE:-false}" = "true" ]; then
  # Plain-text numbered rendering
  echo "1) Option A"
  echo "2) Option B"
  echo ""
  read -p "Select (1-N): " choice
  # Map choice to option
else
  # Existing AskUserQuestion / interactive path
  ...
fi
```

`WORKFLOW_TEXT_MODE` is read from config (`.planning/config.json` workflow.text_mode field) or from an env var. Keep the existing interactive path as default — this is an additive branch.

If `plan-phase.md` and `progress.md` currently have no AskUserQuestion-style interaction (only agent-driven logic), the text_mode branch may be a no-op for those files — in that case, at the top of each file add a brief note:

```markdown
<!-- text_mode: this workflow is primarily agent-driven; text_mode applies only where
     AskUserQuestion or readline prompts exist. No user-facing interactive points at this time. -->
```

Future edits can add branches as needed; the doc acknowledges the property without forcing a fake branch.

The GATE-08d verification (Plan 17) greps for `text_mode` in both files; the comment-note form satisfies the grep without introducing behaviorally-empty code branches.
  </action>
  <verify>
```
ls -la docs/workflow-discuss-mode.md
```
Expected: file exists.

```
grep -c "text_mode" docs/workflow-discuss-mode.md
```
Expected: ≥ 3 hits.

```
grep -c "text_mode" get-shit-done/workflows/plan-phase.md
```
Expected: ≥ 1 hit (either branch or comment-note).

```
grep -c "text_mode" get-shit-done/workflows/progress.md
```
Expected: ≥ 1 hit.

Confirm confidence-badge mapping is in discuss-mode docs:
```
grep -n "Confident\|Likely\|Unclear" docs/workflow-discuss-mode.md
```
Expected: ≥ 3 hits covering the three badge tiers.
  </verify>
  <done>
Mode docs exist and document the three modes + mapping; plan-phase and progress have text_mode surface (branch or documented no-op).
  </done>
</task>

</tasks>

<verification>
- `discuss-phase-assumptions.md` line count ≥ 600.
- Analyzer agent is invoked at ≥ 3 spawn points with GATE-13-conforming dispatch contracts.
- `docs/workflow-discuss-mode.md` exists with confidence-badge mapping.
- `plan-phase.md` and `progress.md` both contain `text_mode` references.
- Narrowing decisions for categories (d) and (e) legible via `<!-- NARROWING DECISION -->` comments in the workflow file.
</verification>

<success_criteria>
- GATE-08a/b/c/d/e all structurally satisfied on the declared substrates.
- Signal `sig-2026-04-03-discuss-mode-adoption-gap-silent-feature-drop` addressed via structural adoption + explicit narrowing.
- Per-gate Codex behavior matches matrix.
</success_criteria>

<output>
After completion, create `.planning/phases/58-structural-enforcement-gates/58-11-SUMMARY.md`
</output>
</content>
</invoke>
