---
phase: 58-structural-enforcement-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/58-structural-enforcement-gates/58-02-gate05-enumeration.md
  - .planning/phases/58-structural-enforcement-gates/58-02-gate13-dispatch-contract-design.md
autonomous: true
resolves_signals:
  - sig-2026-04-10-researcher-model-override-leak-third-occurrence
  - sig-2026-04-17-codex-auto-compact-prompt-parity-gap
must_haves:
  truths:
    - "Every named delegation site in the repo is enumerated in `58-02-gate05-enumeration.md` with file:line, agent type, current model source, and whether the site currently echoes model to user before spawn."
    - "A macro `echo_delegation` pattern is specified in `58-02-gate13-dispatch-contract-design.md` that every Wave 3 workflow edit will drop into each spawn site; the pattern resolves `{researcher_model}` / `{planner_model}` / etc via `resolveModelInternal` at workflow-expansion time, not at runtime, so auto-compact cannot drop the binding."
    - "The fire-event for GATE-05 is a line appended to `.planning/delegation-log.jsonl` at each spawn; the fire-event for GATE-13 is a grep check in CI that no `Task(` block contains a `{...}` template expression at workflow expansion."
    - "Per-gate Codex behavior for GATE-05 is `applies` on both runtimes (file append works identically); for GATE-13 it is `applies` with explicit note that Codex auto-compact is the motivating runtime-specific failure mode."
  artifacts:
    - path: ".planning/phases/58-structural-enforcement-gates/58-02-gate05-enumeration.md"
      provides: "Every named delegation site with agent type, model source, echo status"
      contains: "GATE-05 Enumeration"
    - path: ".planning/phases/58-structural-enforcement-gates/58-02-gate13-dispatch-contract-design.md"
      provides: "The canonical echo_delegation macro pattern + resolveModelInternal wiring spec"
      contains: "echo_delegation"
  key_links:
    - from: ".planning/phases/58-structural-enforcement-gates/58-02-gate05-enumeration.md"
      to: "get-shit-done/workflows/collect-signals.md:276"
      via: "enumeration names every spawn site Wave 3 Plan 12 edits"
      pattern: "subagent_type"
    - from: ".planning/phases/58-structural-enforcement-gates/58-02-gate13-dispatch-contract-design.md"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "dispatch-contract spec requires resolveModelInternal to be callable from workflow-template expansion"
      pattern: "resolveModelInternal"
---

<objective>
Enumerate every named delegation site + design the canonical echo-block / dispatch-contract pattern that Wave 3 Plan 12 uses to edit all ~22 sites consistently.

Purpose: GATE-05 requires model echo before spawn at every named delegation site (CONTEXT enumerates `collect-signals.md`, researcher / planner / discuss / audit dispatches, and any spawned batch workflows — NOT "all delegation workflows" as prose). GATE-13 requires compaction-resilient dispatch-contract restatement at the same sites. Research R3 found 22 sites across 15 workflow files, none of which currently echo model pre-spawn. This plan produces the enumeration + macro spec so Wave 3 has one reference to follow rather than ad-hoc per-file edits.

Output: two substrate artifacts — a GATE-05 enumeration and a GATE-13 dispatch-contract design doc. No workflow files edited yet; Wave 3 Plan 12 consumes these.

Per-gate Codex behavior:
- GATE-05 `applies` on Claude Code and Codex — `.planning/delegation-log.jsonl` append works identically on both runtimes.
- GATE-13 `applies` on Claude Code and Codex — both suffer dispatch drift under auto-compact; Codex is the motivating runtime-specific failure mode per `sig-2026-04-17-codex-auto-compact-prompt-parity-gap`.

Fire-events:
- GATE-05: one line appended to `.planning/delegation-log.jsonl` per spawn (parseable by new extractor registered in Plan 19).
- GATE-13: CI grep step ensuring no `Task(` block contains `{...}` template expressions at expansion time (ensuring literal values are baked in at workflow expansion, not at runtime).
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-structural-enforcement-gates/58-CONTEXT.md
@.planning/phases/58-structural-enforcement-gates/58-RESEARCH.md

@get-shit-done/workflows/collect-signals.md
@get-shit-done/workflows/research-phase.md
@get-shit-done/workflows/plan-phase.md
@get-shit-done/workflows/discuss-phase.md
@get-shit-done/workflows/quick.md
@get-shit-done/bin/lib/core.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate every named delegation spawn site (GATE-05)</name>
  <files>.planning/phases/58-structural-enforcement-gates/58-02-gate05-enumeration.md</files>
  <action>
Produce an authoritative table of every named delegation site in the repo. Research R3 found 22 sites across 15 workflow files; re-verify before Wave 3 Plan 12 runs.

Scope: grep for `subagent_type\s*=`, `"gsd[r]?-.*"`, `Task\(` across `agents/`, `commands/`, `get-shit-done/workflows/`, `skills/`, `.codex/skills/`. Exclude `.sonnet-run-archive/` and `node_modules/`.

Record for each hit:
- `file:line`
- `agent_type` (e.g. `gsdr-phase-researcher`, `gsdr-planner`, `general-purpose`)
- `model_source` (template variable, e.g. `{researcher_model}`, or literal)
- `reasoning_effort_source`
- `echoes_model_pre_spawn` (yes/no — audit the surrounding 10 lines before the `Task(` or `subagent_type=` block; "yes" only if there is an explicit `echo "Model: ..."` or equivalent print-to-user BEFORE the spawn)
- `pattern_category` (one of: `sensor_dispatch`, `research_dispatch`, `planner_dispatch`, `verifier_dispatch`, `executor_dispatch`, `audit_dispatch`, `advisor_dispatch`, `other`)

This task addresses signal `sig-2026-04-10-researcher-model-override-leak-third-occurrence`: the root cause is that model selection is buried in template variables that are opaque to the user until after spawn, across every delegation site — not just the researcher. The enumeration surfaces all such sites so Wave 3 Plan 12 can add the echo macro uniformly.

Artifact format:
- YAML frontmatter: `phase: 58-structural-enforcement-gates`, `plan: 02`, `artifact_type: gate_enumeration`, `gate: GATE-05`
- Section 1: Scope / methodology (grep commands, exclusions)
- Section 2: Per-gate Codex behavior declaration
- Section 3: Fire-event mechanism (`.planning/delegation-log.jsonl` append at each spawn)
- Section 4: Sites table (file:line | agent_type | model_source | reasoning_effort_source | echoes_model_pre_spawn | pattern_category)
- Section 5: Summary counts (total sites, sites with echo, sites by pattern category)
  </action>
  <verify>
Read the artifact back. Confirm:
- Frontmatter is valid YAML.
- Section 4 has at minimum the 22 sites Research R3 named (collect-signals.md:276-277, collect-signals.md:377-378, research-phase.md:63-64, explore.md:65, quick.md:382-383, plan-phase.md:347-348, plan-phase.md:402-403, plan-phase.md:450-451, execute-phase.md:335-336, execute-phase.md:190-191, verify-work.md:384-385, verify-work.md:430-431, verify-work.md:471-472, commands/gsd/audit.md:260, audit-milestone.md:68-69, discuss-phase.md:666-667, commands/gsd/debug.md:95, map-codebase.md:87, validate-phase.md:101-102, new-project.md:824, new-milestone.md:348, reflect.md:369).
- The `echoes_model_pre_spawn` column is `no` for all sites (Research R3 finding: zero echoes today).
- Section 5 counts match fresh grep.
  </verify>
  <done>
Enumeration artifact lists every named delegation site. Wave 3 Plan 12 has a single reference to follow for GATE-05 edits.
  </done>
</task>

<task type="auto">
  <name>Task 2: Specify the canonical `echo_delegation` + `dispatch_contract_restate` pattern (GATE-13)</name>
  <files>.planning/phases/58-structural-enforcement-gates/58-02-gate13-dispatch-contract-design.md</files>
  <action>
Design the canonical echo + dispatch-contract pattern that Wave 3 Plan 12 will drop into every site the Task 1 enumeration lists.

Required content:

1. **GATE-05 echo macro (shell-native block)** — the literal text Plan 12 inserts before every spawn. Design it as a shell block that:
   - Prints to stdout: `[DELEGATION] agent=${SUBAGENT_TYPE} model=${MODEL} reasoning_effort=${REASONING_EFFORT} isolation=${ISOLATION:-none} session=${SESSION_ID}`
   - Appends one line to `.planning/delegation-log.jsonl` in the form `{"ts":"...","agent":"...","model":"...","reasoning_effort":"...","isolation":"...","session_id":"...","workflow_file":"...","workflow_step":"..."}`
   - Creates `.planning/` if not present but does NOT create sibling dirs.
   - Is idempotent (running before spawn doesn't change spawn semantics if the macro itself fails — use `|| true` on the log append so a filesystem error doesn't block the spawn).

2. **GATE-13 dispatch-contract restatement spec** — the template pattern every `Task(...)` block adopts:

```
# DISPATCH CONTRACT (restated inline per GATE-13 — compaction-resilient)
# Agent: gsdr-phase-researcher
# Model: claude-opus-4-7  [resolved from {researcher_model} at workflow expansion via resolveModelInternal]
# Reasoning effort: high
# Required inputs: <explicit list>
# Output path: <explicit path>
# Codex behavior: applies-via-workflow-step
Task(
  subagent_type="gsdr-phase-researcher",
  model="claude-opus-4-7",
  ...
)
```

The literal model value must be BAKED IN at workflow expansion, not at runtime — this is the resilience property. Under auto-compact the comment block survives; a `{researcher_model}` binding does not.

3. **resolveModelInternal integration** — specify that workflow expansion calls `resolveModelInternal(profile, agent_type)` from `get-shit-done/bin/lib/core.cjs` to produce the literal model string before rendering the `Task()` block. If the runtime cannot resolve at expansion time (which is the case today — workflow files are markdown read at runtime), document the fallback: the comment block MUST still be present even when the value remains a template; the grep check in CI treats `{...}` in the `model=` field as a GATE-13 failure.

4. **CI grep specification for GATE-13** — specify the exact grep Wave 2 Plan 07 will register:

```
grep -rn -A 3 'Task(' agents/ get-shit-done/ commands/ --include='*.md' | grep 'model=' | grep -E 'model="{[^}]+}"'
```

Any hit = FAIL.

5. **Per-gate Codex behavior declaration** — GATE-05 `applies`; GATE-13 `applies` with note that Codex auto-compact is the motivating runtime-specific scenario.

6. **Fire-event summary** — GATE-05: `.planning/delegation-log.jsonl` line-append. GATE-13: grep-result == 0 means "pass"; grep-result != 0 emits `::notice::gate_fired=GATE-13 result=block`.

This task addresses signal `sig-2026-04-17-codex-auto-compact-prompt-parity-gap`: the root cause is that dispatch contracts expressed via template-variable substitution lose semantic content when auto-compact compresses the workflow context. The in-line restated contract is the named substrate that prevents silent drift.

Artifact format:
- YAML frontmatter: `phase: 58-structural-enforcement-gates`, `plan: 02`, `artifact_type: gate_design`, `gate: GATE-13`
- Section 1: Echo macro (exact text)
- Section 2: Dispatch-contract restatement pattern (exact text)
- Section 3: resolveModelInternal integration notes
- Section 4: CI grep spec
- Section 5: Codex behavior and fire-event mechanism
  </action>
  <verify>
Read the artifact back. Confirm:
- Frontmatter valid YAML.
- Sections 1-5 all present.
- Echo macro has the log-append line.
- Dispatch-contract pattern shows literal (not `{...}`) model value in the `Task(...)` block example.
- CI grep example is copy-pastable.
  </verify>
  <done>
Design artifact exists with the exact macro + dispatch-contract text Plan 12 will deploy. Wave 3 has one source of truth for GATE-05 + GATE-13 edits.
  </done>
</task>

</tasks>

<verification>
- Both enumeration / design artifacts exist in `.planning/phases/58-structural-enforcement-gates/`.
- Enumeration Section 4 site count matches fresh grep of repo.
- Design Section 1 echo macro is a copy-pastable shell block.
- Design Section 4 CI grep is copy-pastable.
- Per-gate Codex behavior declared in both artifacts.
- Fire-event mechanism declared in both artifacts.
</verification>

<success_criteria>
- Every named delegation site is enumerated.
- Wave 3 Plan 12 can drop the echo macro into every site without ambiguity.
- Wave 2 Plan 07 has the CI grep spec for GATE-13 ready to adopt.
- Signal resolution claims for GATE-05 and GATE-13 are grounded in the enumeration.
</success_criteria>

<output>
After completion, create `.planning/phases/58-structural-enforcement-gates/58-02-SUMMARY.md`
</output>
</content>
</invoke>