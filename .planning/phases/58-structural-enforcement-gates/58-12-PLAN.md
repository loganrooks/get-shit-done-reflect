---
phase: 58-structural-enforcement-gates
plan: 12
type: execute
wave: 4
depends_on: ['02', '05', '07', '11']
files_modified:
  - get-shit-done/workflows/collect-signals.md
  - get-shit-done/workflows/research-phase.md
  - get-shit-done/workflows/plan-phase.md
  - get-shit-done/workflows/execute-phase.md
  - get-shit-done/workflows/verify-work.md
  - get-shit-done/workflows/audit-milestone.md
  - get-shit-done/workflows/discuss-phase.md
  - get-shit-done/workflows/quick.md
  - get-shit-done/workflows/reflect.md
  - get-shit-done/workflows/explore.md
  - .github/gate-13-allowlist.txt
autonomous: true
resolves_signals:
  - sig-2026-04-10-researcher-model-override-leak-third-occurrence
  - sig-2026-04-17-codex-auto-compact-prompt-parity-gap
must_haves:
  truths:
    - "Every named delegation site enumerated in `58-02-gate05-enumeration.md` has (a) the echo_delegation macro inserted BEFORE the spawn block; (b) the GATE-13 dispatch-contract comment block + literal model value baked in; (c) the spawn line removed from `.github/gate-13-allowlist.txt` because template-variable binding no longer remains."
    - "Running `grep -rn -A 5 'Task(' agents/ get-shit-done/workflows/ commands/ --include='*.md' | grep -E 'model\\s*=\\s*\"\\{[^}]+\\}\"'` returns zero lines (allowlist empty by end of this plan — or the allowlist file is deleted)."
    - "Every spawn adds a line to `.planning/delegation-log.jsonl` per the macro spec — verified by a unit test that spawns one synthetic Task and confirms the append."
    - "Per-gate Codex behavior for GATE-05 declared `applies` on both runtimes; for GATE-13 declared `applies` with Codex auto-compact as motivating scenario, per `58-05-codex-behavior-matrix.md`."
  artifacts:
    - path: "get-shit-done/workflows/collect-signals.md"
      provides: "Sensor + synthesizer spawns with echo macro + inline dispatch contract"
      contains: "DISPATCH CONTRACT"
    - path: ".github/gate-13-allowlist.txt"
      provides: "Empty or deleted after this plan lands"
      contains: ""
  key_links:
    - from: "get-shit-done/workflows/collect-signals.md"
      to: ".planning/delegation-log.jsonl"
      via: "echo_delegation macro appends one line per spawn"
      pattern: "delegation-log.jsonl"
    - from: ".github/gate-13-allowlist.txt"
      to: ".github/workflows/ci.yml"
      via: "GATE-13 CI step consults allowlist; this plan empties it"
      pattern: "gate-13-allowlist"
---

<objective>
Insert the GATE-05 echo macro + GATE-13 inline dispatch contract at every named delegation site from Plan 02's enumeration. Retire the Plan 07 bootstrap allowlist. This is the behavior-change plan that makes GATE-05 and GATE-13 substantively enforced (Plans 01, 02, 07 produced the substrate / enumeration / CI grep; this plan does the workflow-file surgery).

Purpose: 22 sites × 15 files need one uniform transformation. Research R3 confirmed no site currently echoes model pre-spawn and all use template-variable bindings that auto-compact drops. This plan applies the uniform pattern.

Output: echo macro + restated dispatch contract at every named site; allowlist empty; compaction-resilient dispatch everywhere.

Per-gate Codex behavior: GATE-05 `applies` both; GATE-13 `applies` both (Codex auto-compact is the motivating runtime scenario per `58-05-codex-behavior-matrix.md`).

Fire-events:
- GATE-05: `.planning/delegation-log.jsonl` append per spawn (already specified in Plan 02 design).
- GATE-13: CI grep after this plan returns 0 hits → `gate_fired=GATE-13 result=pass`.

Signals addressed: `sig-2026-04-10-researcher-model-override-leak-third-occurrence` (all dispatch sites now echo model pre-spawn); `sig-2026-04-17-codex-auto-compact-prompt-parity-gap` (inline dispatch contracts survive auto-compact).

Note on scope split (post-revision iteration 1): this plan was split from the original 17-file Plan 12 into Plan 12 (10 core workflow files, this plan) + Plan 12a (remaining 6 files: commands/gsd/audit.md, commands/gsd/debug.md, map-codebase.md, new-project.md, new-milestone.md, validate-phase.md + the allowlist retirement). Plan 12a depends on Plan 12. This plan inherits `depends_on: ['02','05','07','11']` — the '11' was added because Plan 11 also edits `plan-phase.md` and Plan 12 must land AFTER Plan 11's upstream-port edits to the same file.

Note on file_ownership: Plan 10 owns `resume-project.md`, Plan 11 owns `discuss-phase-assumptions.md` (already has literal dispatch contracts per its scope), Plan 13 owns `gsd-tools.cjs`, Plan 14 owns agent-dispatch envelope in a distinct set of files. Coordinate with Plan 11 specifically: if Plan 11 landed inline dispatch contracts at the three `gsdr-assumptions-analyzer` spawn points in `discuss-phase-assumptions.md`, this plan SKIPS those sites (records in SUMMARY.md). Because Plan 11 also edits `plan-phase.md`, Plan 12's edits to `plan-phase.md` MUST be additive (echo macro + dispatch contract at spawn sites) and must NOT overwrite Plan 11's `text_mode` branches — re-read `plan-phase.md` fresh before editing.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-structural-enforcement-gates/58-CONTEXT.md
@.planning/phases/58-structural-enforcement-gates/58-RESEARCH.md
@.planning/phases/58-structural-enforcement-gates/58-02-gate05-enumeration.md
@.planning/phases/58-structural-enforcement-gates/58-02-gate13-dispatch-contract-design.md
@.planning/phases/58-structural-enforcement-gates/58-05-codex-behavior-matrix.md

@get-shit-done/workflows/collect-signals.md
@get-shit-done/workflows/research-phase.md
@get-shit-done/workflows/plan-phase.md
@get-shit-done/workflows/execute-phase.md
@get-shit-done/workflows/verify-work.md
@get-shit-done/bin/lib/core.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert echo_delegation macro + inline dispatch contract at the 10 core workflow files (Plan 12 scope — Plan 12a handles the remaining sites)</name>
  <files>get-shit-done/workflows/collect-signals.md, get-shit-done/workflows/research-phase.md, get-shit-done/workflows/plan-phase.md, get-shit-done/workflows/execute-phase.md, get-shit-done/workflows/verify-work.md, get-shit-done/workflows/audit-milestone.md, get-shit-done/workflows/discuss-phase.md, get-shit-done/workflows/quick.md, get-shit-done/workflows/reflect.md, get-shit-done/workflows/explore.md</files>
  <action>
For EACH site enumerated in `58-02-gate05-enumeration.md` Section 4 within these 10 workflow files:

1. **Read the enumeration row** for each site (file:line) to confirm agent_type and current model_source.

2. **Before the Task() / spawn block, insert the echo_delegation macro** (copy-identically from `58-02-gate13-dispatch-contract-design.md` Section 1):

```bash
# GATE-05: echo delegation before spawn
mkdir -p .planning/
echo "[DELEGATION] agent=${SUBAGENT_TYPE} model=${MODEL} reasoning_effort=${REASONING_EFFORT} isolation=${ISOLATION:-none} session=${SESSION_ID}"
echo "{\"ts\":\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\",\"agent\":\"${SUBAGENT_TYPE}\",\"model\":\"${MODEL}\",\"reasoning_effort\":\"${REASONING_EFFORT}\",\"isolation\":\"${ISOLATION:-none}\",\"session_id\":\"${SESSION_ID}\",\"workflow_file\":\"${BASH_SOURCE[0]##*/}\",\"workflow_step\":\"${STEP_NAME}\"}" \
  >> .planning/delegation-log.jsonl || true
```

Adapt variable names to each file's existing bindings (e.g., `{researcher_model}` becomes `${MODEL}` after resolution). If the spawn block is markdown-only (no shell block), the echo block goes into an adjacent shell block that precedes the Task() documentation block.

3. **Insert the GATE-13 dispatch contract comment block** immediately before the `Task(...)` call:

```markdown
# DISPATCH CONTRACT (restated inline per GATE-13 — compaction-resilient)
# Agent: <agent_type>
# Model: <literal_model_value>  [resolved from profile=<profile> via resolveModelInternal]
# Reasoning effort: <literal_value>
# Required inputs: <list>
# Output path: <path>
# Codex behavior: applies-via-workflow-step
Task(
  subagent_type="<agent_type>",
  model="<literal_model_value>",
  ...
)
```

4. **Resolving the literal model value at authoring time:** for each site, determine the model by:
   - Looking up the `agent_type` in `get-shit-done/bin/lib/model-profiles.cjs` to find its profile-to-model mapping.
   - Using the `quality` profile as the authoring-time default (most projects run quality). Under other profiles, `resolveModelInternal` will produce a different literal at expansion time — but the baseline literal still satisfies GATE-13's "no {...} template" requirement.
   - If the profile-to-model mapping is not stable (e.g., it depends on runtime detection), document this in a HEADNOTE at the spawn block and ADD the spawn site back to `.github/gate-13-allowlist.txt` WITH a comment explaining why the template binding must stay.

5. **Remove the spawn site line from `.github/gate-13-allowlist.txt`** (Plan 07 Task 2 populated this) once the literal value is baked in. If ALL sites are resolved, either empty the file (keep it with a comment explaining "allowlist retired in Plan 12") or delete it entirely.

**Idempotency:** running this plan's edit twice should be a no-op. Use the `echo_delegation` marker as a grep sentinel — if the line already exists at a site, skip.

**Do NOT edit** `discuss-phase-assumptions.md` — Plan 11 owns it and already landed the three analyzer spawn sites with inline dispatch contracts per its scope. Record the skip in SUMMARY.md.

This task addresses signals `sig-2026-04-10-researcher-model-override-leak-third-occurrence` and `sig-2026-04-17-codex-auto-compact-prompt-parity-gap`: every spawn site is now visible pre-spawn + compaction-resilient.
  </action>
  <verify>
Grep for macro presence:
```
grep -c "GATE-05: echo delegation before spawn" get-shit-done/workflows/collect-signals.md
```
Expected: ≥ 2 (sensor dispatch + synthesizer per Research R3).

Run the full GATE-13 CI grep locally:
```
grep -rn -A 5 'Task(' agents/ get-shit-done/workflows/ commands/ --include='*.md' 2>/dev/null | grep -E 'model\s*=\s*"\{[^}]+\}"' | grep -v -F -f .github/gate-13-allowlist.txt 2>/dev/null
```
Expected: zero lines (allowlist covers any retained template bindings with documented rationale; ideally no retained bindings = empty allowlist).

Grep allowlist:
```
cat .github/gate-13-allowlist.txt | grep -v '^#' | grep -v '^$' | wc -l
```
Expected: 0 (or very small count with documented retained bindings).
  </verify>
  <done>
First batch of 10 workflow files have echo macro + inline dispatch contracts; allowlist shrunk significantly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Partial allowlist update + scope-boundary verification (remaining sites shipped by Plan 12a)</name>
  <files>.github/gate-13-allowlist.txt</files>
  <action>
After Task 1 has applied the echo macro + inline dispatch contract to the 10 workflow files in this plan's scope, PARTIALLY update `.github/gate-13-allowlist.txt`.

**Plan 12 scope (this plan):** 10 core workflow files — Task 1 sites only.
**Plan 12a scope (follow-on plan):** `commands/gsd/audit.md`, `commands/gsd/debug.md`, `map-codebase.md`, `new-project.md`, `new-milestone.md`, `validate-phase.md` — the remaining enumerated sites.

**Actions for this plan:**

1. Remove allowlist entries corresponding ONLY to sites Task 1 addressed in this plan's 10 workflow files (collect-signals, research-phase, plan-phase, execute-phase, verify-work, audit-milestone, discuss-phase, quick, reflect, explore). Entries for command-file sites (audit.md, debug.md) and workflow sites shipped by Plan 12a (map-codebase, new-project, new-milestone, validate-phase) MUST remain in the allowlist — Plan 12a retires them.

2. Run the partial-sweep GATE-13 grep restricted to this plan's scope:

```bash
PLAN_12_FILES="get-shit-done/workflows/collect-signals.md get-shit-done/workflows/research-phase.md get-shit-done/workflows/plan-phase.md get-shit-done/workflows/execute-phase.md get-shit-done/workflows/verify-work.md get-shit-done/workflows/audit-milestone.md get-shit-done/workflows/discuss-phase.md get-shit-done/workflows/quick.md get-shit-done/workflows/reflect.md get-shit-done/workflows/explore.md"

MATCHES=$(grep -n -A 5 'Task(' $PLAN_12_FILES 2>/dev/null \
    | grep -E 'model\s*=\s*"\{[^}]+\}"' \
    | grep -v -F -f .github/gate-13-allowlist.txt 2>/dev/null \
    || true)
if [ -n "$MATCHES" ]; then
  echo "Remaining violations in Plan 12 scope:"; echo "$MATCHES"
  exit 1
fi
echo "OK: GATE-13 grep clean within Plan 12 scope (Plan 12a will retire remaining allowlist entries)"
```

3. Add a HEADNOTE to `.github/gate-13-allowlist.txt` documenting the split:

```
# Phase 58 Plan 12 scope: 10 core workflow files retired (collect-signals, research-phase,
# plan-phase, execute-phase, verify-work, audit-milestone, discuss-phase, quick, reflect, explore).
# Remaining entries below are Plan 12a's scope (commands/gsd/audit.md, commands/gsd/debug.md,
# map-codebase.md, new-project.md, new-milestone.md, validate-phase.md) — Plan 12a empties
# this file or replaces with a retirement comment after its landing.
```

Plan 12a will complete the allowlist retirement and run the full-sweep GATE-13 grep.
  </action>
  <verify>
```bash
PLAN_12_FILES="get-shit-done/workflows/collect-signals.md get-shit-done/workflows/research-phase.md get-shit-done/workflows/plan-phase.md get-shit-done/workflows/execute-phase.md get-shit-done/workflows/verify-work.md get-shit-done/workflows/audit-milestone.md get-shit-done/workflows/discuss-phase.md get-shit-done/workflows/quick.md get-shit-done/workflows/reflect.md get-shit-done/workflows/explore.md"

grep -n -A 5 'Task(' $PLAN_12_FILES 2>/dev/null \
  | grep -E 'model\s*=\s*"\{[^}]+\}"' \
  | grep -v -F -f .github/gate-13-allowlist.txt 2>/dev/null \
  | tee /tmp/gate-13-plan12-remaining.txt
wc -l /tmp/gate-13-plan12-remaining.txt
```
Expected: 0 lines (Plan 12 scope clean; Plan 12a scope still covered by remaining allowlist entries).

```
grep -c "Plan 12 scope" .github/gate-13-allowlist.txt
```
Expected: at least 1 hit (the HEADNOTE documenting the split).

Echo-macro presence across Plan 12's 10 files:
```
grep -rln "GATE-05: echo delegation before spawn" get-shit-done/workflows/collect-signals.md get-shit-done/workflows/research-phase.md get-shit-done/workflows/plan-phase.md get-shit-done/workflows/execute-phase.md get-shit-done/workflows/verify-work.md get-shit-done/workflows/audit-milestone.md get-shit-done/workflows/discuss-phase.md get-shit-done/workflows/quick.md get-shit-done/workflows/reflect.md get-shit-done/workflows/explore.md | wc -l
```
Expected: at least 8-10 files carry the macro (matches Plan 02 enumeration count for these files).
  </verify>
  <done>
Plan 12 scope macro-inserted; allowlist partially reduced with HEADNOTE documenting split; Plan 12a picks up the remaining 6 files.
  </done>
</task>

</tasks>

<verification>
- Every enumerated delegation site has echo macro + inline dispatch contract.
- `.github/gate-13-allowlist.txt` empty or only has documented retained exceptions.
- GATE-13 CI grep returns 0 lines.
- `discuss-phase-assumptions.md` is NOT edited by this plan (Plan 11 owns it).
- Running this plan twice is idempotent.
</verification>

<success_criteria>
- GATE-05 structurally enforced at every named site.
- GATE-13 structurally enforced — compaction-resilient across all spawn sites.
- Bootstrap allowlist retired.
- Signals resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/58-structural-enforcement-gates/58-12-SUMMARY.md`
</output>
</content>
</invoke>