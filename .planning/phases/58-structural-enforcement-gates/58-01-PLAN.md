---
phase: 58-structural-enforcement-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/complete-milestone.md
  - .planning/STATE.md
autonomous: true
resolves_signals:
  - sig-2026-03-28-squash-merge-destroys-commit-history
must_haves:
  truths:
    - "`complete-milestone.md` no longer defaults the AskUserQuestion merge choice to `Squash merge (Recommended)`; `Merge with history` is the structural default for both phase-strategy and milestone-strategy paths."
    - "Neither line 613 nor line 623 of `complete-milestone.md` contains `git merge --squash`; both paths invoke `git merge --no-ff --no-commit` so individual commit history is preserved."
    - "A single GATE-02 enumeration artifact exists at `.planning/phases/58-structural-enforcement-gates/58-01-gate02-enumeration.md` listing every `gh pr merge`, `git merge --squash`, and `git merge --no-ff` site in the source tree with conforming / non-conforming classification and the per-gate Codex behavior marker for each site."
    - "Per-gate Codex behavior for GATE-02 is declared `applies` on both Claude Code and Codex — CI grep is runtime-neutral."
    - "Running the squash-default fix a second time is a no-op (idempotent); running the enumeration rescan produces the same file set."
    - "AT-6: `.planning/STATE.md` is reconciled with Phase 57.8's merge (commit c8a15d95, 2026-04-17) before any other Phase 58 plan executes; `stopped_at` and narrative reference Phase 57.8 as closed rather than Phase 57.7."
  artifacts:
    - path: "get-shit-done/workflows/complete-milestone.md"
      provides: "Milestone-close workflow; phase-strategy and milestone-strategy merge paths now default to merge-with-history"
      contains: "git merge --no-ff --no-commit"
    - path: ".planning/phases/58-structural-enforcement-gates/58-01-gate02-enumeration.md"
      provides: "Authoritative enumeration of every merge-surface site with conforming status + Codex marker"
      contains: "GATE-02 Enumeration"
  key_links:
    - from: "get-shit-done/workflows/complete-milestone.md"
      to: "get-shit-done/workflows/execute-phase.md:793"
      via: "both milestone-close and phase-close paths now use --merge / --no-ff consistently"
      pattern: "git merge --no-ff|gh pr merge.*--merge"
    - from: ".planning/phases/58-structural-enforcement-gates/58-01-gate02-enumeration.md"
      to: ".github/workflows/ci.yml"
      via: "Wave 2 Plan 07 reads this enumeration to author the CI grep check"
      pattern: "GATE-02 Enumeration"
---

<objective>
Eliminate the `git merge --squash` default in `complete-milestone.md` (Research R1 critical finding — audit and CONTEXT missed it) and produce the authoritative GATE-02 enumeration that Wave 2 Plan 07 consumes when writing the CI grep check.

Purpose: GATE-02 requires `--merge` as the structural default for PR merges in every workflow / command / agent surface that invokes `gh pr merge`, AND CI must verify `--squash` is absent. Research R1 found a critical contradiction between CONTEXT.md ("No --squash anywhere") and reality: `complete-milestone.md:603,613,623` defaults to `Squash merge (Recommended)` and the phase-strategy and milestone-strategy paths literally invoke `git merge --squash`. The `gh pr merge --squash` surface is clean but the `git merge --squash` surface is not. This plan removes the default and enumerates all sites so the CI plan can grep correctly.

Output: fixed `complete-milestone.md` + `.planning/phases/58-structural-enforcement-gates/58-01-gate02-enumeration.md` enumeration artifact.

Per-gate Codex behavior: GATE-02 applies on Claude Code AND Codex — CI grep is runtime-neutral (fires on every PR that touches these paths regardless of which runtime authored the commit).

Fire-event: Wave 2 Plan 07 will register the CI grep step whose failure emits `::notice::gate_fired=GATE-02 result=block` on the workflow step (the emission point). This plan lays the substrate; Plan 07 wires the emission.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-structural-enforcement-gates/58-CONTEXT.md
@.planning/phases/58-structural-enforcement-gates/58-RESEARCH.md

@get-shit-done/workflows/complete-milestone.md
@get-shit-done/workflows/execute-phase.md
@get-shit-done/references/planning-config.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Reconcile STATE.md with Phase 57.8's merge — close AT-6 at plan-phase entry</name>
  <files>.planning/STATE.md</files>
  <action>
AT-6 requires STATE.md to be reconciled with Phase 57.8's merge (commit c8a15d95, merged 2026-04-17) BEFORE Phase 58 plan-phase is considered entered. STATE.md currently reflects Phase 57.7 as the active focus; Phase 57.8 (Signal Provenance Role-Aware Backfill) completed and merged but its merge was never reflected in STATE.md's `stopped_at` or narrative. CONTEXT DC-5 explicitly flags this as live evidence for GATE-10.

This task closes AT-6 manually at plan-phase entry time. Plan 13's `phase reconcile` primitive will later automate this for future phases, but AT-6 is an ENTRY condition and cannot wait for Plan 13's wave-3 execution.

**Required changes to `.planning/STATE.md`:**

1. Update frontmatter field `stopped_at`:
   - Current: `stopped_at: Phase 58 context gathered` (ambiguous — doesn't reflect 57.8 closure)
   - New: `stopped_at: Phase 57.8 merged (commit c8a15d95, 2026-04-17); Phase 58 plan-phase in progress`

2. Update frontmatter field `status` to reference 57.8 closure explicitly:
   - Incorporate a single-line summary like: "Phase 57.8 (Signal Provenance Role-Aware Backfill) merged 2026-04-17 via c8a15d95; Phase 58 plan-phase entered under AT-6 reconciliation."

3. Update the `## Current Position` section:
   - Current: "Phase: 57.7 of 64 (Content Analysis & Epistemic Deepening) — Complete"
   - New: "Phase: 57.8 of 64 — Complete (merged 2026-04-17, commit c8a15d95). Phase 58 (Structural Enforcement Gates) plan-phase in progress."
   - Update the Plan/Status line to reflect 57.8's plan count (read the 57.8 phase directory to get actual count) and preserve the honesty of 57.7's demo/regression narrative in the historical record (move 57.7 status into a "Recent Phases" sub-section if the template doesn't already have one).

4. Update `last_activity` and `last_updated`:
   - `last_updated` to current ISO8601 timestamp
   - `last_activity` to reference Phase 58 plan-phase entry

5. If there's a narrative section ("Current focus"), update the current focus line to: "v1.20 Phase 58 (Structural Enforcement Gates) plan-phase in progress; Phase 57.8 closed."

**Verification of 57.8 merge:**

Run `git log --oneline --merges | grep -i "57.8\|c8a15d95"` to confirm the merge commit. If the SHA doesn't match c8a15d95 exactly (CONTEXT DC-5 cites it), use the actual merge commit SHA found via `git log --oneline --grep "57.8" --merges`. Update the narrative with whatever the actual SHA is.

**Idempotency:** running this task twice is a no-op — the STATE.md reflects the same reconciled state. Future runs of Plan 13's `phase reconcile` on Phase 58 close will overwrite cleanly.

**Commit:** after editing STATE.md, stage it and commit separately from the Task 1 / Task 2 outputs with a message like:

```
docs(58-01): reconcile STATE.md with Phase 57.8 merge (AT-6 entry condition)

Phase 57.8 (Signal Provenance Role-Aware Backfill) merged 2026-04-17 via
commit c8a15d95 but STATE.md was never reconciled. Closes AT-6 at Phase 58
plan-phase entry. Future phase closeouts will go through Plan 13's
`gsd-tools phase reconcile` primitive.
```

This task does NOT depend on Plan 13's reconcile tooling — it's a one-shot manual reconciliation at AT-6 time. Plan 13 delivers the structural primitive for subsequent phases.
  </action>
  <verify>
```
grep -E "57\.8|c8a15d95|stopped_at" .planning/STATE.md | head -10
```
Expected: STATE.md contains `c8a15d95` (or the verified actual merge SHA) and references Phase 57.8 as closed, Phase 58 as in-progress.

```
head -15 .planning/STATE.md
```
Expected: frontmatter `stopped_at` field explicitly references Phase 57.8 closure with the merge commit.

```
git log -1 --format=%s -- .planning/STATE.md
```
Expected: most recent commit on STATE.md matches the `docs(58-01): reconcile STATE.md ...` message pattern.
  </verify>
  <done>
STATE.md reflects Phase 57.8 as closed and Phase 58 as in-progress; AT-6 is satisfied at plan-phase entry rather than retroactively at phase close. Committed as a separate docs commit to create a clean audit trail.
  </done>
</task>

<task type="auto">
  <name>Task 1: Enumerate every merge-surface site and classify each as conforming / non-conforming under GATE-02</name>
  <files>.planning/phases/58-structural-enforcement-gates/58-01-gate02-enumeration.md</files>
  <action>
Produce an authoritative enumeration of every merge-surface site in the source tree. This artifact feeds Wave 2 Plan 07 when it writes the CI grep check.

Required scope (from Research R1 + R10):

1. Grep `"gh pr merge"` across `agents/`, `get-shit-done/`, `commands/`, `.claude/`, `skills/`, `.codex/skills/`, `.github/workflows/`. Exclude `.sonnet-run-archive/` and any node_modules. Record every hit as `file:line | invocation-text | conforming(yes|no) | reason`.

2. Grep `"git merge --squash"` across the same paths. Every hit is non-conforming unless in a code comment explicitly marked `# historical`. Record same way.

3. Grep `"git merge --no-ff"` for completeness (these are conforming). Record same way.

4. For every non-conforming site, specify the fix Wave 2 Plan 07 / this plan will apply.

5. Add a "Per-gate Codex behavior" section at the top declaring GATE-02 `applies` on both runtimes (CI grep is runtime-neutral).

6. Add a "Fire-event mechanism" section: `::notice::gate_fired=GATE-02` emitted by the CI grep step on failure.

The artifact format is a markdown file with:
- YAML frontmatter: `phase: 58-structural-enforcement-gates`, `plan: 01`, `artifact_type: gate_enumeration`, `gate: GATE-02`
- Section 1: Scope and methodology
- Section 2: Per-gate Codex behavior (applies / does-not-apply / applies-via-workflow-step)
- Section 3: Fire-event mechanism
- Section 4: Table of all sites (file:line, invocation, conforming, fix-if-non-conforming)
- Section 5: Summary count (conforming / non-conforming / pending-fix-in-this-plan)

Do NOT re-audit files outside the repo. Do NOT invent sites. Record only what grep returns.
  </action>
  <verify>
The artifact exists, has valid YAML frontmatter, and contains at minimum these confirmed sites from Research R1:
- `get-shit-done/workflows/execute-phase.md:793` (conforming, `gh pr merge ... --merge`)
- `get-shit-done/workflows/complete-milestone.md:613` (non-conforming, `git merge --squash`)
- `get-shit-done/workflows/complete-milestone.md:623` (non-conforming, `git merge --squash`)
- `get-shit-done/workflows/complete-milestone.md:642` (conforming, `git merge --no-ff`)
- `get-shit-done/workflows/complete-milestone.md:652` (conforming, `git merge --no-ff`)

Run the grep commands used in the artifact and confirm the counts match the enumeration. Read the file back and confirm Codex-behavior section + fire-event section are both present.
  </verify>
  <done>
`58-01-gate02-enumeration.md` exists, is machine-parseable by grep for "GATE-02 Enumeration", and names every merge-surface site in the repo at the time of authoring. Section 4's count matches a fresh grep of the repo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix `complete-milestone.md` squash default in both phase-strategy and milestone-strategy paths</name>
  <files>get-shit-done/workflows/complete-milestone.md</files>
  <action>
Fix the AskUserQuestion default and the two `git merge --squash` invocations that Research R1 identified.

Required changes (from Research R1 critical finding, lines 603, 613, 623):

1. Line ~603 AskUserQuestion options: change "Squash merge (Recommended)" to "Merge with history (Recommended)" AND promote it to be the FIRST option so it is the default pressed-enter choice. Squash becomes an explicit opt-in option, not the default.

2. Line ~613 (phase-strategy): replace `git merge --squash "$branch"` with `git merge --no-ff --no-commit "$branch"` so the phase-branch history is preserved in a merge commit rather than flattened.

3. Line ~623 (milestone-strategy): replace `git merge --squash "$MILESTONE_BRANCH"` with `git merge --no-ff --no-commit "$MILESTONE_BRANCH"`.

4. Any prose text that says "squash is recommended" or "squash preserves clean history" — rewrite to reflect that merge-with-history is now the default and explain the rationale: "preserves individual commit history per `sig-2026-03-28-squash-merge-destroys-commit-history` and `feedback_no_squash_merge` user preference."

5. If there is an existing "Merge with history" branch of the AskUserQuestion (Research R1 line 642, 652) that invokes `git merge --no-ff --no-commit`, consolidate so both the default and the explicit opt-in reach the same non-squash invocation. Do NOT duplicate work across branches — one `git merge --no-ff --no-commit` call site per strategy.

6. Document the fix at the top of the file's "History" / changelog section if present, referencing Phase 58 Plan 01 and the motivating signal.

This task addresses signal `sig-2026-03-28-squash-merge-destroys-commit-history`: the root cause was squash being the default path in complete-milestone.md; eliminating the default closes that cause.

The fix must be idempotent — running this edit twice is a no-op.
  </action>
  <verify>
After the edit, run:

```
grep -n "git merge --squash" get-shit-done/workflows/complete-milestone.md
```

Expected: zero hits (or only hits inside a clearly marked `# historical` comment block).

Run:

```
grep -n "Squash merge (Recommended)" get-shit-done/workflows/complete-milestone.md
```

Expected: zero hits.

Run:

```
grep -cn "git merge --no-ff --no-commit" get-shit-done/workflows/complete-milestone.md
```

Expected: at least 2 hits (one per merge strategy).

Load the workflow file and confirm the AskUserQuestion options list `Merge with history (Recommended)` as the first option.
  </verify>
  <done>
`complete-milestone.md` contains zero `git merge --squash` invocations outside historical comments, `Merge with history (Recommended)` is the default AskUserQuestion choice, and both the phase-strategy and milestone-strategy paths invoke `git merge --no-ff --no-commit`. The fix matches the enumeration artifact from Task 1.
  </done>
</task>

</tasks>

<verification>
- `grep -rn "git merge --squash" get-shit-done/` returns zero non-historical hits.
- `58-01-gate02-enumeration.md` lists every site grep finds and matches the counts.
- Running Task 2 again is a no-op (idempotent).
- `Merge with history (Recommended)` is the first option in the AskUserQuestion step at `complete-milestone.md:~603`.
</verification>

<success_criteria>
- Squash default removed from both paths in `complete-milestone.md`.
- GATE-02 enumeration artifact produced with conforming / non-conforming counts.
- Wave 2 Plan 07 has the input it needs to author the CI grep check.
- Per-gate Codex behavior and fire-event mechanism declared in the enumeration.
</success_criteria>

<output>
After completion, create `.planning/phases/58-structural-enforcement-gates/58-01-SUMMARY.md`
</output>
</content>
</invoke>