---
phase: 58-structural-enforcement-gates
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/58-structural-enforcement-gates/58-05-codex-behavior-matrix.md
autonomous: true
must_haves:
  truths:
    - "Every GATE-01..15 + XRT-01 has an explicit per-gate Codex behavior declaration in a single authoritative table: `applies` / `applies-via-workflow-step` / `applies-via-installer` / `does-not-apply-with-reason` (no blanket 'Codex has no hooks')."
    - "Every row in the matrix carries the motivating runtime substrate citation (e.g., `codex_hooks` feature flag per cross-runtime-parity-research.md; installer Codex skip at bin/install.js:2846-2856; CI runtime-neutrality)."
    - "Each gate's Codex behavior cites the Phase 57.9 dependency where relevant (GATE-06 / GATE-07 degrade to `applies-via-workflow-step if codex_hooks=true else does-not-apply-with-reason`)."
    - "Running the matrix through `gsd-tools frontmatter validate --schema codex_behavior_matrix` (if schema exists) or a sanity check (every gate has a declaration and a rationale) passes."
  artifacts:
    - path: ".planning/phases/58-structural-enforcement-gates/58-05-codex-behavior-matrix.md"
      provides: "Authoritative per-gate Codex behavior declarations — AT-3 compliance artifact"
      contains: "per-gate Codex behavior"
  key_links:
    - from: ".planning/phases/58-structural-enforcement-gates/58-05-codex-behavior-matrix.md"
      to: "get-shit-done/references/capability-matrix.md"
      via: "matrix entries inform capability-matrix.md updates when gates ship (XRT-01 per Plan 18)"
      pattern: "capability-matrix"
    - from: ".planning/phases/58-structural-enforcement-gates/58-05-codex-behavior-matrix.md"
      to: "Every Wave 2 / Wave 3 / Wave 4 plan"
      via: "each plan's per-gate Codex behavior marker cites this matrix as the authoritative source"
      pattern: "per-gate Codex behavior"
---

<objective>
Author the authoritative per-gate Codex behavior matrix that every Phase 58 plan cites. This is AT-3 compliance: every GATE-01..15 + XRT-01 carries an explicit `applies` / `does-not-apply-with-reason` / `applies-via-workflow-step` marker, with motivating runtime substrate citation.

Purpose: CONTEXT DC-4 and G-4 reject the blanket "Codex has no hooks" framing. Every gate must declare its Codex behavior explicitly. Research R1-R4 recommended the table skeleton in the "Per-gate Codex behavior table (skeleton)" section; this plan finalizes it as the single source of truth. Downstream Wave 2 / 3 / 4 plans' per-gate Codex declarations reference this matrix by file path rather than re-deriving.

Output: `58-05-codex-behavior-matrix.md` — a structured, machine-readable table plus rationale per row.

Per-gate Codex behavior for THIS plan (meta): `applies` — the matrix artifact itself is a filesystem artifact, runtime-neutral.

Fire-event: matrix existence + completeness (every 25 requirements has a row) is verified by Plan 17 verifier. No runtime emission.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-structural-enforcement-gates/58-CONTEXT.md
@.planning/phases/58-structural-enforcement-gates/58-RESEARCH.md

@get-shit-done/references/capability-matrix.md
@.planning/research/cross-runtime-parity-research.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author `58-05-codex-behavior-matrix.md` as the authoritative AT-3 artifact</name>
  <files>.planning/phases/58-structural-enforcement-gates/58-05-codex-behavior-matrix.md</files>
  <action>
Produce the authoritative per-gate Codex behavior matrix. This artifact satisfies AT-3 and is cited by every downstream plan's per-gate Codex behavior declaration.

Frontmatter:
```yaml
~~~
phase: 58-structural-enforcement-gates
plan: 05
artifact_type: codex_behavior_matrix
scope: phase_58_gates
authored_at: <ISO8601>
~~~
```

Structure:

**Section 1: Purpose and authority.** Cite CONTEXT DC-4 ("Every GATE must carry a per-gate Codex behavior declaration; blanket 'Codex has no hooks' is rejected") and G-4 ("No prose-only Codex framing"). Cite AT-3 ("Per-gate Codex behavior table authored as part of planning scope").

**Section 2: Methodology.** Declare the four allowed values:
- `applies` — the gate fires identically on both Claude Code and Codex via a runtime-neutral mechanism (CI grep, filesystem append, branch protection).
- `applies-via-workflow-step` — the gate fires on Codex via a workflow-file edit that Codex reads exactly as Claude Code does (markdown-based enforcement).
- `applies-via-installer` — the gate relies on installer-wired hooks; installer writes the surface on the appropriate runtime; consumer plan registers the extractor.
- `does-not-apply-with-reason` — the gate has no named Codex substrate; reason MUST cite the missing substrate and the downstream phase that would resolve.

**Section 3: Runtime substrate references.** One-paragraph summary of the Codex runtime surface relevant to Phase 58:
- `codex_hooks=true` feature flag per `.planning/research/cross-runtime-parity-research.md:70-80` — hook types SessionStart, SessionStop, UserPromptSubmit, PreToolUse, PostToolUse available under feature-flag gating; flag status "under development."
- `bin/install.js:2846-2856` — Codex is unconditionally excluded from hook installation today; Phase 57.9 changes this.
- CI (`.github/workflows/ci.yml`) is runtime-neutral — does not care which runtime authored the commit.
- Branch protection is runtime-neutral.
- Workflow markdown files are read identically by both runtimes.
- Agent files and command files are read identically.

**Section 4: The matrix.** One row per requirement. Each row has columns:
- `gate_id` (e.g., `GATE-01`)
- `requirement_text_summary` (1-line summary)
- `claude_code_behavior`
- `codex_behavior`
- `rationale`
- `depends_on_phase_57_9` (true/false)
- `substrate_citation` (one file:line or artifact reference)

Use Research R1-R4's recommendations as starting point (skeleton from Research "Per-gate Codex behavior table"). Finalize every row. Specifically:

| gate_id | claude_code_behavior | codex_behavior | rationale |
|---|---|---|---|
| GATE-01 | applies-via-workflow-step | applies-via-workflow-step | CI + branch protection runtime-neutral |
| GATE-02 | applies | applies | CI grep runtime-neutral |
| GATE-03 | applies-via-workflow-step | applies-via-workflow-step | CI diff + manifest check; workflow-step in quick.md |
| GATE-04a | applies | applies-via-workflow-step | Workflow-side archive; runs identically on both runtimes |
| GATE-04b | applies | applies-via-workflow-step | Workflow-side hard-stop staleness predicate |
| GATE-04c | applies | applies-via-workflow-step | Workflow-side severity prompt |
| GATE-05 | applies | applies | delegation-log.jsonl append file-based, runtime-neutral |
| GATE-06 | applies-via-installer | applies-via-workflow-step if codex_hooks=true else does-not-apply-with-reason | Phase 57.9 wires; Codex flag gated |
| GATE-07 | applies-via-installer | applies-via-workflow-step if codex_hooks=true else does-not-apply-with-reason | Same |
| GATE-08a | applies | applies | Fetch artifact; runtime-neutral |
| GATE-08b | applies | applies | Agent file; read identically |
| GATE-08c | applies | applies | Docs; runtime-neutral |
| GATE-08d | applies | applies | Workflow edits |
| GATE-08e | applies | applies | Narrowing rationale in ledger |
| GATE-09a | applies | applies | Filesystem ledger |
| GATE-09b | applies | applies | Planning-gate check |
| GATE-09c | applies | applies | Ledger frontmatter |
| GATE-09d | applies | applies | Verifier reads filesystem |
| GATE-10 | applies | applies | CLI subcommand runtime-neutral |
| GATE-11 | applies | applies | CLI subcommand runtime-neutral |
| GATE-12 | applies | applies | Workflow envelope |
| GATE-13 | applies | applies | Inline dispatch contract; especially useful under Codex auto-compact |
| GATE-14 | applies | applies | Branch protection runtime-neutral |
| GATE-15 | applies | applies | CI runtime-neutral |
| XRT-01 | applies | applies | Plan-phase assertion; capability-matrix diff |

**Section 5: Codex-specific open risks.**
- `signal-detection.md:67-76` opus/sonnet class heuristic is Claude-only and stale per Research R10 — flagged OUT OF SCOPE for Phase 58 per `<deferred>`, but recorded here as a risk that would undermine GATE-05 / GATE-13 effectiveness measurement on Codex sessions.
- GATE-06 / GATE-07 Codex degradation to `does-not-apply-with-reason` is the EXPECTED path if Phase 57.9 has not delivered Codex hook wiring by Phase 58 execution time. Plan 16 in Wave 4 explicitly records this as a ledger entry per GATE-09c.

**Section 6: Ledger entry format for Codex waivers.** Specify the ledger-entry shape for any `does-not-apply-with-reason` declaration: `disposition: explicitly_deferred`, `target_phase_if_deferred: "Phase 57.9"` (or 60.1 per deferred item), `narrowing_provenance.rationale: "Codex <hook/substrate> unavailable per <citation>"`.
  </action>
  <verify>
Read the artifact. Confirm:
- Frontmatter valid YAML with `artifact_type: codex_behavior_matrix`.
- Section 4 has exactly 25 rows (GATE-01, 02, 03, 04a, 04b, 04c, 05, 06, 07, 08a, 08b, 08c, 08d, 08e, 09a, 09b, 09c, 09d, 10, 11, 12, 13, 14, 15, XRT-01).
- No row has `blanket N/A` or `Codex has no hooks` without a named substrate citation.
- Every `does-not-apply-with-reason` row has a `target_phase` in the rationale.
- Every `applies-via-installer` row names Phase 57.9 as prerequisite.

Grep the matrix for all 25 requirement IDs to ensure completeness:
```
for gate in GATE-01 GATE-02 GATE-03 GATE-04a GATE-04b GATE-04c GATE-05 GATE-06 GATE-07 GATE-08a GATE-08b GATE-08c GATE-08d GATE-08e GATE-09a GATE-09b GATE-09c GATE-09d GATE-10 GATE-11 GATE-12 GATE-13 GATE-14 GATE-15 XRT-01; do
  grep -q "$gate" .planning/phases/58-structural-enforcement-gates/58-05-codex-behavior-matrix.md || echo "MISSING: $gate"
done
```

Expected output: no "MISSING" lines.
  </verify>
  <done>
Matrix artifact exists with all 25 rows + rationale + substrate citations. Downstream Wave 2 / 3 / 4 plans cite this matrix when declaring their per-gate Codex behavior.
  </done>
</task>

</tasks>

<verification>
- `58-05-codex-behavior-matrix.md` exists with 25 rows (all requirements).
- No row uses "blanket" or "N/A" framing without substrate citation.
- Codex-specific open risks section names signal-detection.md:67-76 + GATE-06/07 57.9 dependency.
- Ledger-entry format for waivers documented for Plan 20 consumption.
</verification>

<success_criteria>
- AT-3 satisfied — per-gate Codex behavior declared for every requirement.
- Downstream plans have single source of truth to cite.
- Plan 20's ledger has the format for recording any Codex waivers.
- Plan 16 (GATE-06/07 defer) has the rationale template ready.
</success_criteria>

<output>
After completion, create `.planning/phases/58-structural-enforcement-gates/58-05-SUMMARY.md`
</output>
</content>
</invoke>
