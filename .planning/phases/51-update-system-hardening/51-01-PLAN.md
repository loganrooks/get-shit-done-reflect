---
phase: 51-update-system-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/migrations/v1.18.0.json
  - bin/install.js
autonomous: true

must_haves:
  truths:
    - "Running installer on a v1.17 installation generates MIGRATION-GUIDE.md with per-version sections"
    - "Fresh install produces no MIGRATION-GUIDE.md"
    - "Migration spec for v1.18.0 exists and is machine-readable JSON"
    - "Stale gsd-tools.js is removed during upgrade"
  artifacts:
    - path: "get-shit-done/migrations/v1.18.0.json"
      provides: "Per-release migration spec for v1.18.0"
      contains: "sections"
    - path: "bin/install.js"
      provides: "Migration guide generation, stale file cleanup, fresh-vs-upgrade detection"
      contains: "generateMigrationGuide"
  key_links:
    - from: "bin/install.js"
      to: "get-shit-done/migrations/v1.18.0.json"
      via: "fs.readdirSync on migrations/ directory"
      pattern: "migrations.*\\.json"
    - from: "bin/install.js"
      to: "get-shit-done-reflect/VERSION"
      via: "previousVersion detection for fresh vs upgrade"
      pattern: "previousVersion"
---

<objective>
Add migration spec infrastructure and installer-driven MIGRATION-GUIDE.md generation with fresh-vs-upgrade detection and stale gsd-tools.js cleanup.

Purpose: Users upgrading from v1.17 (or earlier) get actionable per-version documentation of what changed, while fresh installs remain clean. Stale pre-modularization files are automatically removed.
Output: v1.18.0 migration spec JSON, generateMigrationGuide() function in installer, stale file cleanup entry, VERSION-based upgrade detection
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-update-system-hardening/51-RESEARCH.md
@bin/install.js
@get-shit-done/feature-manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v1.18.0 migration spec and migration guide generation infrastructure</name>
  <files>get-shit-done/migrations/v1.18.0.json, bin/install.js</files>
  <action>
1. Create directory `get-shit-done/migrations/` and file `get-shit-done/migrations/v1.18.0.json` with this structure:
```json
{
  "version": "1.18.0",
  "title": "Modularization & Cross-Runtime Hardening",
  "date": "2026-03-26",
  "sections": [
    {
      "category": "breaking",
      "heading": "Runtime Entry Point Renamed",
      "body": "gsd-tools.js has been renamed to gsd-tools.cjs to match the CommonJS module system. The installer automatically removes the old file during upgrade.",
      "action": "automatic"
    },
    {
      "category": "feature",
      "heading": "Modular CLI Architecture",
      "body": "The monolithic CLI has been split into 16 focused modules in lib/. The command interface is unchanged -- all existing commands work identically.",
      "action": "none"
    },
    {
      "category": "config",
      "heading": "Config Field Rename: depth to granularity",
      "body": "The 'depth' config field has been renamed to 'granularity'. Run /gsdr:upgrade-project to migrate your project config automatically.",
      "action": "run-upgrade-project"
    },
    {
      "category": "config",
      "heading": "Manifest Version 2 with Migrations Array",
      "body": "manifest_version has been bumped from 1 to 2 to support declarative config migrations. Run /gsdr:upgrade-project to update your project config.",
      "action": "run-upgrade-project"
    },
    {
      "category": "feature",
      "heading": "Hook Field Validation",
      "body": "Invalid hook entries in settings.json are now validated and stripped to prevent Claude Code's schema validator from silently discarding the entire settings file.",
      "action": "automatic"
    },
    {
      "category": "feature",
      "heading": "Cross-Platform Code Detection",
      "body": "Project initialization now detects language and build files using a cross-platform approach (no Unix find dependency), supporting 16+ build/package file types.",
      "action": "none"
    }
  ]
}
```

2. In `bin/install.js`, add two helper functions BEFORE the `cleanupOrphanedFiles` function (around line 1548):

a) `isVersionInRange(version, fromVersion, toVersion)` -- Returns true if `version` is strictly greater than `fromVersion` AND less than or equal to `toVersion`. Use the existing dot-split numeric comparison pattern (split on '.', compare each segment numerically). Do NOT add a semver dependency. Strip any `+dev` suffix before comparison.

b) `compareVersions(a, b)` -- Returns -1, 0, or 1 for sorting. Same dot-split numeric approach.

c) `generateMigrationGuide(targetDir, previousVersion, currentVersion)` -- Reads all JSON files from `get-shit-done/migrations/` (resolved relative to `__dirname + '/../get-shit-done/migrations'`), filters to specs where `isVersionInRange(spec.version, previousVersion, currentVersion)` is true, sorts by `compareVersions`, and assembles a Markdown guide with this layout:
   - Header: `# Migration Guide: {previousVersion} -> {currentVersion}`
   - Metadata: Generated timestamp, previous version, current version
   - For each spec in version order: `## Version {spec.version}: {spec.title}` followed by each section with category badge prefix (BREAKING/Config/Feature) and action callout (automatic or run-upgrade-project)
   - Footer with link to `/gsdr:upgrade-project`
   - Writes to `path.join(targetDir, 'MIGRATION-GUIDE.md')`
   - Prints stdout message: `  + Generated MIGRATION-GUIDE.md ({previousVersion} -> {currentVersion})`
   - If no applicable specs found, returns without writing (no empty guide)

3. In the `install()` function, add fresh-vs-upgrade detection. Per locked decision: detect via VERSION file presence. BEFORE the existing `cleanupOrphanedFiles(targetDir)` call at line 2400, add:

```javascript
// Fresh vs upgrade detection (UPD-04)
const reflectVersionPath = path.join(targetDir, 'get-shit-done-reflect', 'VERSION');
let previousVersion = null;
if (fs.existsSync(reflectVersionPath)) {
  try {
    previousVersion = fs.readFileSync(reflectVersionPath, 'utf8').trim()
      .replace(/\+dev$/, '');
  } catch { /* treat as fresh install */ }
}
const currentVersionClean = versionString.replace(/\+dev$/, '');
const isUpgrade = previousVersion && previousVersion !== currentVersionClean;
```

4. AFTER the VERSION file write (after line 2595), add the migration guide generation call:
```javascript
// Generate migration guide for upgrades only (UPD-01, UPD-04)
if (isUpgrade) {
  generateMigrationGuide(targetDir, previousVersion, currentVersionClean);
}
```

5. Add `get-shit-done-reflect/bin/gsd-tools.js` to the `cleanupOrphanedFiles` array (per locked decision). This goes in the orphanedFiles array at line 1552, after the existing 7 entries:
```javascript
// Pre-modularization stale artifacts (v1.17 -> v1.18)
'get-shit-done-reflect/bin/gsd-tools.js',  // Renamed to gsd-tools.cjs in Phase 45
```

6. Export `generateMigrationGuide`, `isVersionInRange`, `compareVersions`, and `cleanupOrphanedFiles` from the module.exports at line 3027 so tests can unit-test them directly.
  </action>
  <verify>
Run `npm test` -- all 376 existing tests must pass (no regressions). Then verify the new functions are exported: `node -e "const m = require('./bin/install.js'); console.log(typeof m.generateMigrationGuide, typeof m.isVersionInRange, typeof m.compareVersions)"` should print `function function function`. Verify the migration spec is valid JSON: `node -e "const s = require('./get-shit-done/migrations/v1.18.0.json'); console.log(s.version, s.sections.length)"` should print `1.18.0 6`.
  </verify>
  <done>
Migration spec v1.18.0.json exists with 6 sections. generateMigrationGuide() is callable and produces Markdown output. isVersionInRange() and compareVersions() handle dot-split version comparison. cleanupOrphanedFiles includes gsd-tools.js entry. Fresh-vs-upgrade detection reads VERSION file before install proceeds. All existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for migration guide generation, version comparison, and fresh-vs-upgrade detection</name>
  <files>tests/unit/install.test.js</files>
  <action>
Add a new `describe('Phase 51: Migration Guide and Version Detection')` block to `tests/unit/install.test.js` with these test cases:

1. **Version comparison tests** (unit, no tmpdir needed):
   - `compareVersions('1.17.0', '1.18.0')` returns -1
   - `compareVersions('1.18.0', '1.18.0')` returns 0
   - `compareVersions('1.19.0', '1.18.0')` returns 1
   - `compareVersions('1.18.0', '1.18.1')` returns -1 (patch level)

2. **Version range tests** (unit):
   - `isVersionInRange('1.18.0', '1.17.0', '1.18.0')` is true (equal to upper bound)
   - `isVersionInRange('1.17.0', '1.17.0', '1.18.0')` is false (equal to lower bound = excluded)
   - `isVersionInRange('1.16.0', '1.17.0', '1.18.0')` is false (below range)
   - `isVersionInRange('1.19.0', '1.17.0', '1.18.0')` is false (above range)

3. **Migration guide generation** (tmpdirTest):
   - Create a tmpdir with a mock migrations directory containing a test spec JSON
   - Call `generateMigrationGuide(tmpdir, '1.17.0', '1.18.0')` (will need to temporarily point the function at the test migrations dir -- OR test it indirectly by setting up a v1.17 VERSION + running install, which is better left to 51-03)
   - Alternative approach: Test generateMigrationGuide directly by passing targetDir and verifying the output file content. Since the function reads from `__dirname/../get-shit-done/migrations/`, test with the real v1.18.0.json spec to verify it produces expected Markdown structure. Create a tmpdir, call `generateMigrationGuide(tmpdir, '1.17.0', '1.18.0')`, then assert:
     - `MIGRATION-GUIDE.md` exists at `path.join(tmpdir, 'MIGRATION-GUIDE.md')`
     - Content contains `# Migration Guide: 1.17.0 -> 1.18.0`
     - Content contains `## Version 1.18.0: Modularization`
     - Content contains `**BREAKING:**`
     - Content contains `run-upgrade-project` or the action callout text
   - Test that calling with same version (no upgrade) produces no file: `generateMigrationGuide(tmpdir, '1.18.0', '1.18.0')` should NOT create MIGRATION-GUIDE.md

4. **Stale file cleanup** (tmpdirTest):
   - Create tmpdir with `get-shit-done-reflect/bin/gsd-tools.js` file present
   - Call `cleanupOrphanedFiles(tmpdir)` (import from install.js -- it is not currently exported, so either add it to exports OR test indirectly via the full install path in 51-03)
   - Since cleanupOrphanedFiles is not exported, verify the stale file entry exists by checking the install() behavior in the full upgrade test (51-03). In THIS plan, verify the orphanedFiles array includes the new entry by reading install.js source and asserting the string is present (a structural test similar to TST-01 namespace scan pattern).
   - Better: add `cleanupOrphanedFiles` to module.exports in Task 1 so it can be directly tested. Then: create tmpdir structure with the stale file, call cleanupOrphanedFiles, assert file is gone.

Ensure all tests follow existing patterns: use `it()` for unit tests, `tmpdirTest()` for filesystem tests. Import the new exports from `../../bin/install.js`.
  </action>
  <verify>
Run `npm test` -- all tests including new ones must pass. Verify new test count is at least 384 (376 + 8 new minimum).
  </verify>
  <done>
At least 8 new tests covering version comparison, version range, migration guide content generation, same-version no-op, and stale file cleanup. All tests pass. Test names clearly map to UPD-01, UPD-04, UPD-06 requirements.
  </done>
</task>

</tasks>

<verification>
1. `npm test` passes with zero failures
2. `node -e "const m = require('./bin/install.js'); console.log(Object.keys(m).filter(k => ['generateMigrationGuide','isVersionInRange','compareVersions'].includes(k)))"` prints all three function names
3. `cat get-shit-done/migrations/v1.18.0.json | node -e "process.stdin.on('data',d=>{const j=JSON.parse(d);console.log(j.version,j.sections.length)})"` prints `1.18.0 6`
</verification>

<success_criteria>
- Migration spec infrastructure exists (migrations/ directory + v1.18.0.json)
- Installer generates MIGRATION-GUIDE.md for upgrades, skips for fresh installs
- Stale gsd-tools.js is in the cleanup list
- Version comparison helpers work correctly
- 8+ new tests verify the behavior
- All 376+ existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/51-update-system-hardening/51-01-SUMMARY.md`
</output>
