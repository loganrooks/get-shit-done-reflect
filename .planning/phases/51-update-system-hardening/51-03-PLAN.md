---
phase: 51-update-system-hardening
plan: 03
type: execute
wave: 3
depends_on: ["51-01", "51-02"]
files_modified:
  - tests/unit/install.test.js
autonomous: true

must_haves:
  truths:
    - "v1.17 installation upgrades to v1.18 with .planning/ artifacts intact and migration guidance available in MIGRATION-GUIDE.md"
    - "Stale gsd-tools.js is removed after upgrade"
    - "MIGRATION-GUIDE.md is generated during upgrade with v1.18.0 content"
    - "Fresh install produces no MIGRATION-GUIDE.md"
    - "Hook registration reflects current version state after upgrade"
    - "lib/*.cjs modules are in place after upgrade"
  artifacts:
    - path: "tests/unit/install.test.js"
      provides: "End-to-end upgrade tests (UPD-05) and fresh install verification (UPD-04)"
      contains: "UPD-05"
  key_links:
    - from: "tests/unit/install.test.js"
      to: "bin/install.js"
      via: "execSync running installer with HOME override"
      pattern: "execSync.*install\\.js"
    - from: "tests/unit/install.test.js"
      to: "get-shit-done/migrations/v1.18.0.json"
      via: "installer reads migration specs during upgrade"
      pattern: "MIGRATION-GUIDE"
---

<objective>
End-to-end upgrade test from v1.17 to v1.18 verifying the full upgrade path: stale file cleanup, .planning/ preservation, config migration, MIGRATION-GUIDE.md generation, hook registration, and fresh install distinction.

Purpose: UPD-05 requires testing the upgrade PATH, not just the final state (G3 guardrail). This plan exercises the complete installer upgrade flow from a simulated v1.17 installation to v1.18.
Output: End-to-end upgrade tests proving all UPD requirements work together
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-update-system-hardening/51-RESEARCH.md
@.planning/phases/51-update-system-hardening/51-01-SUMMARY.md
@.planning/phases/51-update-system-hardening/51-02-SUMMARY.md
@tests/unit/install.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end v1.17-to-v1.18 upgrade test (UPD-05)</name>
  <files>tests/unit/install.test.js</files>
  <action>
Add a new `describe('Phase 51: End-to-End Upgrade Path (UPD-05)')` block using tmpdirTest:

**Test: v1.17 to v1.18 upgrade preserves .planning artifacts and generates migration guide**

Use the `tmpdirTest` helper with HOME override (same pattern as Phase 50's TST-03 idempotency tests). Steps:

1. **Simulate v1.17 installation state:**
   - Create `{tmpdir}/.claude/get-shit-done-reflect/` directory structure
   - Write VERSION file containing `1.17.5`
   - Create `{tmpdir}/.claude/get-shit-done-reflect/bin/gsd-tools.js` with dummy content (stale pre-modularization artifact)
   - Create `{tmpdir}/.claude/settings.json` with a basic hooks structure containing one valid hook entry
   - Create `{tmpdir}/.planning/` directory with:
     - `STATE.md` containing `# State\nPhase: 1\nStatus: active`
     - `config.json` containing `{"mode":"yolo","gsd_reflect_version":"1.17.5","manifest_version":1,"depth":"standard"}`
     - `phases/01-init/` empty directory (artifact preservation check)

2. **Run v1.18 installer:**
   ```javascript
   const installScript = path.resolve(process.cwd(), 'bin/install.js');
   execSync(`node "${installScript}" --claude --global`, {
     env: { ...process.env, HOME: tmpdir },
     cwd: tmpdir,
     stdio: ['pipe', 'pipe', 'pipe'],
     timeout: 30000
   });
   ```

3. **Assert upgrade outcomes:**

   a. **Stale file removed (UPD-02):**
   - `{tmpdir}/.claude/get-shit-done-reflect/bin/gsd-tools.js` does NOT exist
   - `{tmpdir}/.claude/get-shit-done-reflect/bin/gsd-tools.cjs` DOES exist

   b. **lib/*.cjs modules present (UPD-02):**
   - `{tmpdir}/.claude/get-shit-done-reflect/bin/lib/` directory exists
   - At least 10 `.cjs` files in the lib directory (16 expected: core, init, frontmatter, manifest, etc.)

   c. **VERSION updated:**
   - VERSION file content matches current package version (with or without +dev suffix)

   d. **.planning/ artifacts preserved (UPD-05):**
   - `{tmpdir}/.planning/STATE.md` exists and contains `Phase: 1`
   - `{tmpdir}/.planning/config.json` exists
   - `{tmpdir}/.planning/phases/01-init/` directory exists

   e. **MIGRATION-GUIDE.md generated (UPD-01):**
   - `{tmpdir}/.claude/MIGRATION-GUIDE.md` exists
   - Content contains `Migration Guide`
   - Content contains `1.17.5` (previous version reference)
   - Content contains `Modularization` or version 1.18 reference
   - Content contains `BREAKING` (from the gsd-tools.js rename section)

   f. **Hook registration present (UPD-03):**
   - Read `{tmpdir}/.claude/settings.json`
   - Parse as JSON
   - settings.hooks.SessionStart exists and is an array
   - Array contains entries with `gsdr-check-update` and `gsdr-health-check` substrings

**Test: Fresh install produces no migration guide (UPD-04)**

Use tmpdirTest with HOME override:

1. Do NOT create any pre-existing .claude/ directory or VERSION file
2. Run installer: `node "${installScript}" --claude --global` with HOME override
3. Assert:
   - `{tmpdir}/.claude/MIGRATION-GUIDE.md` does NOT exist
   - `{tmpdir}/.claude/get-shit-done-reflect/VERSION` DOES exist (version file written for future upgrade detection)
   - `{tmpdir}/.claude/get-shit-done-reflect/bin/gsd-tools.cjs` DOES exist (fresh install works)

**Test: Same-version reinstall produces no migration guide**

Use tmpdirTest:
1. Create VERSION file with current package version (read from package.json)
2. Run installer
3. Assert MIGRATION-GUIDE.md does NOT exist (same version = not an upgrade)

Each test should have a 30-second timeout given the full installer execution.
  </action>
  <verify>
Run `npm test` -- all tests pass. The new describe block should contain at least 3 tmpdirTests. Total test count should be ~395+ (376 baseline + 8 from 51-01 + 12 from 51-02 + 3 from 51-03).
  </verify>
  <done>
3 end-to-end tests verify: (1) v1.17-to-v1.18 upgrade removes stale files, preserves .planning/, generates MIGRATION-GUIDE.md, installs lib/*.cjs modules, registers hooks; (2) fresh install produces no MIGRATION-GUIDE.md; (3) same-version reinstall produces no MIGRATION-GUIDE.md. All tests pass. The upgrade PATH is tested, not just the final state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify complete test suite and requirement coverage</name>
  <files>tests/unit/install.test.js</files>
  <action>
Final verification sweep across all Phase 51 work:

1. Run the full test suite (`npm test`) and record the total test count.

2. Verify requirement coverage by checking test names/descriptions map to requirements:
   - UPD-01: Migration guide generation test exists (from 51-01 Task 2 + 51-03 Task 1)
   - UPD-02: Stale file cleanup test exists (from 51-01 Task 2), lib/*.cjs presence asserted (51-03 Task 1)
   - UPD-03: Hook registration asserted in upgrade test (51-03 Task 1)
   - UPD-04: Fresh install no-guide test exists (51-03 Task 1)
   - UPD-05: End-to-end upgrade test exists (51-03 Task 1)
   - UPD-06: Migration spec v1.18.0.json exists and is read by installer (structural test from 51-01)

3. If any requirement has no corresponding test, add a minimal test. For example, if UPD-03 hook registration is only tested indirectly in the upgrade test, that counts. But if it is entirely missing, add a focused test.

4. Verify no test file has syntax errors or accidental skips (no `.skip` or `.only` left in committed code).

5. Run `npm test` one final time to confirm green across the board.
  </action>
  <verify>
`npm test` passes with zero failures. Total test count is documented. Each UPD requirement has at least one test exercising it.
  </verify>
  <done>
Full test suite passes. All 6 UPD requirements have test coverage. No accidental skips or syntax errors. Total test count recorded for phase summary.
  </done>
</task>

</tasks>

<verification>
1. `npm test` passes with zero failures
2. Total test count increased by ~23+ from the 376 baseline
3. Each UPD-01 through UPD-06 has at least one test
4. The v1.17->v1.18 upgrade test exercises the full path: VERSION detection, stale cleanup, file installation, guide generation, hook registration
5. Fresh install test proves no migration noise for new users
</verification>

<success_criteria>
- End-to-end v1.17-to-v1.18 upgrade test passes, verifying stale cleanup + .planning preservation + guide generation + hook registration + lib modules
- Fresh install test confirms no MIGRATION-GUIDE.md generated
- Same-version reinstall confirms no spurious guide
- All UPD-01 through UPD-06 requirements have test coverage
- Total test count: 395+ (376 baseline + ~23 new from Phase 51)
</success_criteria>

<output>
After completion, create `.planning/phases/51-update-system-hardening/51-03-SUMMARY.md`
</output>
