# Phase 55.1: Upstream Bug Patches - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-09
**Phase:** 55.1-upstream-bug-patches
**Mode:** Exploratory --auto (grounded-only auto-selection)
**Areas discussed:** Patch sourcing, #2005 fix approach, #1972 extension scope, #1981 worktree fix

---

## Patch Sourcing Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Implement ourselves | All 3 issues OPEN upstream, no fix code merged | [auto] [grounded] |
| Cherry-pick from upstream | Wait for upstream fix branches | Not available — no fix commits exist |
| Wait for upstream merge | Delay until upstream ships fixes | Rejected — bugs are critical, no ETA |

**Auto-selected:** Implement ourselves [grounded: all 3 issues OPEN, upstream fix/2005 branch has zero fix commits]
**Notes:** Consistent with Phase 55 pattern. Fixes can be offered upstream as PRs later.

---

## Bug #2005: details-wrapped ROADMAP Corruption

| Option | Description | Selected |
|--------|-------------|----------|
| Smarter `<details>` parsing | Only strip blocks containing SHIPPED milestones | [auto] [grounded] |
| Remove `<details>` usage | Stop using HTML details in ROADMAP.md | Rejected — useful for collapsed milestones |
| Guard in phase-complete | Add pre-check before writing | Partial — doesn't fix root cause |

**Auto-selected:** Smarter `<details>` parsing [grounded: root cause identified at core.cjs:1103, `extractCurrentMilestone` positive lookup at L1123 is the correct pattern but fallback strips everything]
**Notes:** Our ROADMAP.md uses `<details>` for 7 shipped milestones. Bug is latent but will trigger when milestone completion wraps current section.

---

## Bug #1972: atomicWriteFileSync Extension

| Option | Description | Selected |
|--------|-------------|----------|
| Extend to 3 named modules | milestone.cjs (7), phase.cjs (7), frontmatter.cjs (2) | [auto] [grounded] |
| Full codebase audit | Find ALL writeFileSync calls everywhere | Deferred — overkill for this phase |
| Skip .gitkeep writes | Only convert data-critical writes | Claude's discretion |

**Auto-selected:** Extend to 3 named modules [grounded: all 3 modules already require core.cjs, 16 write sites identified]
**Notes:** .gitkeep writes debatable but included for consistency.

---

## Bug #1981: Worktree reset --soft Data Loss

| Option | Description | Selected |
|--------|-------------|----------|
| Use `reset --hard` | Correct working tree completely | Needs research — may lose intended changes |
| Use `checkout HEAD -- .` | Restore working tree from HEAD without moving pointer | Needs research |
| Fix branch creation | Prevent wrong-branch situation entirely | Needs research — architectural change |
| Keep `reset --soft` + add file correction | Minimal change to existing flow | Needs research |

**Auto-selected:** Open question — no grounded default. All options need researcher investigation of EnterWorktree flow.
**Notes:** This is the highest-severity bug (silent data loss) but also the one requiring the most investigation.

---

## Claude's Discretion

- Exact regex pattern for smarter `<details>` handling
- Whether `.gitkeep` writes should use atomicWriteFileSync
- Merge ordering between the three fixes

## Deferred Ideas

- Upstream PR contributions for the three fixes
- Full `writeFileSync` audit across entire codebase
- EnterWorktree architectural improvements beyond #1981 fix
