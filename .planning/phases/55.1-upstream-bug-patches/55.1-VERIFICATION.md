---
phase: 55.1-upstream-bug-patches
verified: 2026-04-08T00:00:00Z
status: passed
score: 8/8 must-haves verified
re_verification: false
---

# Phase 55.1: Upstream Bug Patches Verification Report

**Phase Goal:** Patch three upstream-reported bugs that affect ROADMAP integrity (#2005 details-wrapped corruption), file write safety (#1972 incomplete atomicWriteFileSync), and worktree data loss (#1981 reset --soft)
**Verified:** 2026-04-08
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | replaceInCurrentMilestone correctly scopes replacements to current milestone when shipped milestones are wrapped in `<details>` | VERIFIED | `findCurrentMilestoneRange` helper implemented in core.cjs:1153; positive range lookup confirmed; replaceInCurrentMilestone delegates to it when cwd provided (core.cjs:1274-1297) |
| 2 | extractCurrentMilestone matches `<summary>`-tagged milestone layouts | VERIFIED | `summaryPattern` regex at core.cjs:1182 matches `<summary>[^<\n]*${escapedVersion}[^<\n]*</summary>`; `<details>` boundary detection at lines 1191-1200 |
| 3 | replaceInCurrentMilestone without cwd falls back to original lastIndexOf heuristic | VERIFIED | Fallback path at core.cjs:1288-1296 preserves original `lastIndexOf('</details>')` logic exactly; Test 4 in gsd-tools.test.js confirms backward compatibility |
| 4 | Phase completion updates ROADMAP.md checkboxes even when shipped milestones use `<details>` wrapping | VERIFIED | phase.cjs lines 715 and 744 both pass `cwd` to `replaceInCurrentMilestone`; roadmap.cjs lines 321 and 329 both pass `cwd` |
| 5 | All file writes in milestone.cjs, phase.cjs, and frontmatter.cjs use atomicWriteFileSync | VERIFIED | Zero `fs.writeFileSync` remaining in all 3 modules; all 7+7+2=16 sites converted; structural test in test suite enforces this (test suite: 211/211 pass) |
| 6 | All 4 workflow files contain worktree_branch_check blocks with reset --hard | VERIFIED | All 4 npm source files confirmed; all 4 local installed files (.claude/get-shit-done-reflect/workflows/) confirmed; each has exactly 1 `reset --hard`, zero `reset --soft` |
| 7 | No workflow file uses reset --soft for worktree branch correction | VERIFIED | grep confirms zero `reset --soft` matches in all 4 source and installed workflow files |
| 8 | All 3 test suites pass after both sets of changes | VERIFIED | upstream: 211/211 pass (7 new structural tests + 4 regression tests for #2005 included); fork and vitest pass counts documented in SUMMARYs (18/18, 467/467) |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `get-shit-done/bin/lib/core.cjs` | Fixed extractCurrentMilestone and replaceInCurrentMilestone | VERIFIED | Contains `summary.*escapedVersion` at line 1183; `findCurrentMilestoneRange` at 1153; `resolveCurrentMilestoneVersion` at 1115 |
| `get-shit-done/bin/gsd-tools.test.js` | Regression tests for bug #2005 | VERIFIED | Lines 5287-5431: 4 regression tests across 2 describe blocks covering both sub-bugs and backward compat |
| `get-shit-done/bin/lib/milestone.cjs` | Atomic file writes for milestone operations | VERIFIED | Contains `atomicWriteFileSync` imported from core.cjs; 7 converted sites; zero `fs.writeFileSync` |
| `get-shit-done/bin/lib/phase.cjs` | Atomic file writes for phase operations | VERIFIED | Contains `atomicWriteFileSync` imported from core.cjs; 7 converted sites; zero `fs.writeFileSync` |
| `get-shit-done/bin/lib/frontmatter.cjs` | Atomic file writes for frontmatter operations | VERIFIED | Contains `atomicWriteFileSync` imported from core.cjs; 2 converted sites; zero `fs.writeFileSync` |
| `get-shit-done/workflows/execute-phase.md` | Worktree branch check with correct reset mode | VERIFIED | `worktree_branch_check` block at lines 13-39 with `reset --hard`; placed after `<required_reading>`, before `<process>` |
| `get-shit-done/workflows/quick.md` | Worktree branch check with correct reset mode | VERIFIED | `worktree_branch_check` block present; `reset --hard` confirmed; no `reset --soft` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| phase.cjs | replaceInCurrentMilestone | function call with cwd argument | VERIFIED | Lines 715 and 744 confirmed: `replaceInCurrentMilestone(roadmapContent, ..., cwd)` |
| roadmap.cjs | replaceInCurrentMilestone | function call with cwd argument | VERIFIED | Lines 321 and 329 confirmed: `replaceInCurrentMilestone(roadmapContent, ..., cwd)` |
| milestone.cjs | core.cjs | destructured import of atomicWriteFileSync | VERIFIED | Line 7: `const { ..., atomicWriteFileSync } = require('./core.cjs')` |
| phase.cjs | core.cjs | destructured import of atomicWriteFileSync | VERIFIED | Line 7: `const { ..., atomicWriteFileSync } = require('./core.cjs')` |
| frontmatter.cjs | core.cjs | destructured import of atomicWriteFileSync | VERIFIED | Line 7: `const { safeReadFile, normalizeMd, output, error, atomicWriteFileSync } = require('./core.cjs')` |

### Requirements Coverage

Not mapped to REQUIREMENTS.md — these are upstream bug patches, not user-facing requirements.

### Anti-Patterns Found

No blockers or warnings found.

Notes:
- `~/.claude/get-shit-done-reflect/workflows/` (global install) does NOT contain `worktree_branch_check`. This is correct and expected: `node bin/install.js --local` installs to the project-local `.claude/`, not the global config dir. The project-local `.claude/get-shit-done-reflect/workflows/` (the active runtime for this development session) was correctly updated. The global install is the user's production install and is not affected by a `--local` install run.
- The plan's CLAUDE.md explicitly documents this dual-directory architecture and specifies that `.claude/` in the project directory is the runtime install target.

### Human Verification Required

None. All claims are fully verifiable programmatically.

### Gaps Summary

No gaps. All 8 observable truths are verified at all three levels (exists, substantive, wired). All commits exist (7443afa0, 4a9cf3b1, 1ab79c1c, d0e868a0). The upstream test suite at 211/211 with structural regression tests provides ongoing protection against regression.

---

_Verified: 2026-04-08_
_Verifier: Claude (gsdr-verifier)_
