---
phase: 55.1-upstream-bug-patches
plan: 01
model: claude-opus-4-6
context_used_pct: 25
subsystem: gsd-tools-core
tags: [roadmap-parsing, milestone-scoping, details-tag, bug-fix]
requires:
  - phase: 55-upstream-mini-sync
    provides: "Synced core.cjs, phase.cjs, roadmap.cjs from upstream v1.34.2"
provides:
  - "Fixed extractCurrentMilestone to match <summary>-tagged milestone layouts"
  - "Fixed replaceInCurrentMilestone to use positive range lookup via findCurrentMilestoneRange helper"
  - "4 regression tests for bug #2005"
affects: [roadmap-update, phase-complete, milestone-scoping]
tech-stack:
  added: []
  patterns: [positive-range-lookup, shared-helper-extraction]
key-files:
  created: []
  modified:
    - get-shit-done/bin/lib/core.cjs
    - get-shit-done/bin/lib/phase.cjs
    - get-shit-done/bin/lib/roadmap.cjs
    - get-shit-done/bin/gsd-tools.test.js
key-decisions:
  - "Extracted findCurrentMilestoneRange as internal helper (not exported) shared by extractCurrentMilestone and replaceInCurrentMilestone"
  - "replaceInCurrentMilestone falls back to lastIndexOf heuristic when cwd omitted -- backward compatibility preserved"
  - "resolveCurrentMilestoneVersion extracted as separate helper to avoid duplicating STATE.md reading logic"
patterns-established:
  - "Positive range lookup: findCurrentMilestoneRange returns {start, end} offsets for heading, <summary>, and bullet layouts"
duration: 4min
completed: 2026-04-09
---

# Phase 55.1 Plan 01: Fix ROADMAP Corruption with Details-Wrapped Milestones Summary

**Patched replaceInCurrentMilestone to use positive range lookup via shared findCurrentMilestoneRange helper, fixing silent no-op when shipped milestones use `<details>` wrapping (#2005)**

## Performance
- **Duration:** 4min
- **Tasks:** 2/2 completed
- **Files modified:** 4

## Accomplishments
- Extracted `findCurrentMilestoneRange(content, cwd)` helper that supports three ROADMAP layouts: markdown headings, `<summary>` tags inside `<details>`, and bullet entries
- Extracted `resolveCurrentMilestoneVersion(content, cwd)` to share STATE.md version resolution between the range finder and extractCurrentMilestone
- Rewrote `replaceInCurrentMilestone` to accept optional `cwd` parameter and delegate to `findCurrentMilestoneRange` for precise scoping
- Updated all 4 callers (2 in phase.cjs, 2 in roadmap.cjs) to pass `cwd`
- Added 4 regression tests covering both sub-bugs and backward compatibility
- All 3 test suites pass: upstream 204/204, fork 18/18, vitest 467/467

## Task Commits
1. **Task 1: Fix extractCurrentMilestone and replaceInCurrentMilestone in core.cjs** - `7443afa0`
2. **Task 2: Add regression tests for bug #2005** - `4a9cf3b1`

## Files Created/Modified
- `get-shit-done/bin/lib/core.cjs` - Added findCurrentMilestoneRange helper, resolveCurrentMilestoneVersion helper; refactored extractCurrentMilestone to delegate; rewrote replaceInCurrentMilestone with cwd parameter
- `get-shit-done/bin/lib/phase.cjs` - Lines 715, 744: pass cwd to replaceInCurrentMilestone
- `get-shit-done/bin/lib/roadmap.cjs` - Lines 321, 329: pass cwd to replaceInCurrentMilestone
- `get-shit-done/bin/gsd-tools.test.js` - 4 new regression tests for bug #2005

## Deviations from Plan

### Auto-fixed Issues

None - plan executed exactly as written.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
Plan 55.1-02 (atomicWriteFileSync adoption or worktree reset fix) can proceed immediately. The core.cjs changes are self-contained and do not affect the other two bug patches.

## Self-Check: PASSED
