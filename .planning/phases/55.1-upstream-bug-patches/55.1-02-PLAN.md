---
phase: 55.1-upstream-bug-patches
plan: 02
type: execute
wave: 2
depends_on: ["55.1-01"]
files_modified:
  - get-shit-done/bin/lib/milestone.cjs
  - get-shit-done/bin/lib/phase.cjs
  - get-shit-done/bin/lib/frontmatter.cjs
  - get-shit-done/workflows/execute-phase.md
  - get-shit-done/workflows/execute-plan.md
  - get-shit-done/workflows/quick.md
  - get-shit-done/workflows/diagnose-issues.md
  - get-shit-done/bin/gsd-tools.test.js
autonomous: true

must_haves:
  truths:
    - "All file writes in milestone.cjs, phase.cjs, and frontmatter.cjs use atomicWriteFileSync instead of fs.writeFileSync"
    - "All 4 workflow files (execute-phase.md, execute-plan.md, quick.md, diagnose-issues.md) contain worktree_branch_check blocks with reset --hard"
    - "No workflow file uses reset --soft for worktree branch correction"
    - "All 3 test suites pass after both sets of changes"
  artifacts:
    - path: "get-shit-done/bin/lib/milestone.cjs"
      provides: "Atomic file writes for milestone operations"
      contains: "atomicWriteFileSync"
    - path: "get-shit-done/bin/lib/phase.cjs"
      provides: "Atomic file writes for phase operations"
      contains: "atomicWriteFileSync"
    - path: "get-shit-done/bin/lib/frontmatter.cjs"
      provides: "Atomic file writes for frontmatter operations"
      contains: "atomicWriteFileSync"
    - path: "get-shit-done/workflows/execute-phase.md"
      provides: "Worktree branch check with correct reset mode"
      contains: "worktree_branch_check"
    - path: "get-shit-done/workflows/quick.md"
      provides: "Worktree branch check with correct reset mode"
      contains: "worktree_branch_check"
  key_links:
    - from: "get-shit-done/bin/lib/milestone.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "destructured import of atomicWriteFileSync"
      pattern: "atomicWriteFileSync.*require\\('./core\\.cjs'\\)"
    - from: "get-shit-done/bin/lib/phase.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "destructured import of atomicWriteFileSync"
      pattern: "atomicWriteFileSync.*require\\('./core\\.cjs'\\)"
    - from: "get-shit-done/bin/lib/frontmatter.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "destructured import of atomicWriteFileSync"
      pattern: "atomicWriteFileSync.*require\\('./core\\.cjs'\\)"
---

<objective>
Fix upstream bugs #1972 (incomplete atomicWriteFileSync adoption) and #1981 (worktree reset --soft data loss).

Purpose: Bug #1972 -- 16 `writeFileSync` calls in milestone.cjs, phase.cjs, and frontmatter.cjs are crash-unsafe; they should use the existing `atomicWriteFileSync` helper. Bug #1981 -- the `worktree_branch_check` mitigation for EnterWorktree creating branches from wrong base is entirely absent from our fork's workflow files; adding the corrected version with `reset --hard` (not the upstream `reset --soft` which only moves the commit pointer without correcting the file tree).

Output: 16 writeFileSync sites converted, 4 workflow files patched, structural regression tests for both.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55.1-upstream-bug-patches/55.1-CONTEXT.md
@.planning/phases/55.1-upstream-bug-patches/55.1-RESEARCH.md
@.planning/phases/55.1-upstream-bug-patches/55.1-01-SUMMARY.md
@get-shit-done/bin/lib/milestone.cjs
@get-shit-done/bin/lib/phase.cjs
@get-shit-done/bin/lib/frontmatter.cjs
@get-shit-done/bin/gsd-tools.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert writeFileSync to atomicWriteFileSync in 3 modules (#1972)</name>
  <files>get-shit-done/bin/lib/milestone.cjs, get-shit-done/bin/lib/phase.cjs, get-shit-done/bin/lib/frontmatter.cjs</files>
  <action>
Mechanical replacement: change `fs.writeFileSync` to `atomicWriteFileSync` at all 16 sites. The function signature is identical (`filePath, content, encoding`), so this is a drop-in replacement.

**milestone.cjs (7 sites):**
1. Add `atomicWriteFileSync` to the destructured require on line 7:
   `const { escapeRegex, getMilestonePhaseFilter, extractOneLinerFromBody, normalizeMd, planningPaths, output, error, atomicWriteFileSync } = require('./core.cjs');`
2. Replace `fs.writeFileSync` at lines 77, 157, 164, 181, 188, 191, 195 with `atomicWriteFileSync`.
3. The `fs` import can remain (it's still needed for `fs.existsSync`, `fs.readFileSync`, `fs.mkdirSync`, etc.).

**phase.cjs (7 sites):**
1. Add `atomicWriteFileSync` to the destructured require on line 7:
   Add `atomicWriteFileSync` to the existing destructure: `const { escapeRegex, loadConfig, normalizePhaseName, ..., atomicWriteFileSync } = require('./core.cjs');`
2. Replace `fs.writeFileSync` at lines 363, 378, 455, 479, 593, 762, 795 with `atomicWriteFileSync`.
3. Include the `.gitkeep` writes (lines 363, 455) for consistency -- atomicWriteFileSync overhead for empty files is negligible.

**frontmatter.cjs (2 sites):**
1. Add `atomicWriteFileSync` to the destructured require on line 7:
   `const { safeReadFile, normalizeMd, output, error, atomicWriteFileSync } = require('./core.cjs');`
2. Replace `fs.writeFileSync` at lines 364, 378 with `atomicWriteFileSync`.

**Verification after each module:** After changing each module, do a quick grep to confirm zero remaining `fs.writeFileSync` calls in that file. If any remain that are NOT in the 16 enumerated sites (e.g., writing to non-`.planning/` paths), investigate before converting -- but the research confirms all writes in these modules are `.planning/`-path writes.

Commit: `fix(55.1-02): extend atomicWriteFileSync to milestone/phase/frontmatter (#1972)`
  </action>
  <verify>
Run `npm run test:upstream && npm run test:upstream:fork && npm test` -- all tests pass. Then run:
```bash
grep -n 'fs\.writeFileSync' get-shit-done/bin/lib/milestone.cjs get-shit-done/bin/lib/phase.cjs get-shit-done/bin/lib/frontmatter.cjs
```
Expected: zero matches (all converted).
  </verify>
  <done>
All 16 `fs.writeFileSync` calls converted to `atomicWriteFileSync`. Zero `fs.writeFileSync` calls remain in milestone.cjs, phase.cjs, and frontmatter.cjs. All 3 test suites pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add worktree_branch_check to 4 workflow files (#1981) and structural tests</name>
  <files>get-shit-done/workflows/execute-phase.md, get-shit-done/workflows/execute-plan.md, get-shit-done/workflows/quick.md, get-shit-done/workflows/diagnose-issues.md, get-shit-done/bin/gsd-tools.test.js</files>
  <action>
**CRITICAL: Edit the npm source directory (`get-shit-done/workflows/`), NOT `.claude/get-shit-done/workflows/`.** Per dual-directory architecture, the installer copies source to runtime. After this plan completes, run `node bin/install.js --local` to sync.

**Add worktree_branch_check block to all 4 workflow files.**

The block should be added inside the executor's prompt context, BEFORE any `<step>` or `<execution_context>` element, as the FIRST check the executor performs. Place it after the `<required_reading>` or `<core_principle>` section but before `<process>` or the first `<step>`.

Insert this block in each file (adapt indentation to match the file's style):

```markdown
<worktree_branch_check>
**FIRST ACTION** before any other work: verify this worktree's branch is based on the correct commit.

```bash
# Check if worktree branch base matches expected
EXPECTED_BASE=$(git log --format=%H -1 main 2>/dev/null || echo "unknown")
ACTUAL_BASE=$(git merge-base HEAD main 2>/dev/null || echo "none")
CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "unknown")
```

If `CURRENT_BRANCH` is `main` or `ACTUAL_BASE` does not match the expected feature branch base (i.e., the worktree was created from `main` instead of the feature branch):

```bash
# Safe to hard reset -- this check runs before any agent work has occurred
git reset --hard {EXPECTED_BASE}

# Verify correction succeeded
if [ "$(git rev-parse HEAD)" != "{EXPECTED_BASE}" ]; then
  echo "ERROR: Could not correct worktree base -- aborting to prevent data loss"
  exit 1
fi
```

If the branch base is correct: proceed immediately with no action needed.

This check mitigates an issue where EnterWorktree creates branches from main instead of the current feature branch HEAD. Affects all platforms (not just Windows).
</worktree_branch_check>
```

**File-specific placement notes:**
- `execute-phase.md`: After `<required_reading>` block, before `<process>`
- `execute-plan.md`: After any preamble/purpose, before the first `<step>` or execution context
- `quick.md`: After `<required_reading>` or intro section, before the task execution steps
- `diagnose-issues.md`: After intro/purpose, before diagnostic steps

Also update documentation reference: the block says "Affects all platforms" (not "known issue on Windows" as upstream incorrectly states).

**Add structural tests in gsd-tools.test.js:**

Add a new describe block after the existing correctness regression tests (after line ~5283):

```javascript
describe('Bug fix structural verification — #1972 atomicWriteFileSync, #1981 worktree_branch_check', () => {
```

**Test for #1972 (structural):** Read source of milestone.cjs, phase.cjs, frontmatter.cjs. Assert each does NOT contain `fs.writeFileSync` (pattern: `/fs\.writeFileSync/`). Assert each DOES contain `atomicWriteFileSync` in its require destructure.

**Test for #1981 (structural):** Read source of the 4 workflow files from `get-shit-done/workflows/`. Assert each contains `worktree_branch_check`. Assert each contains `reset --hard` (not `reset --soft`). Assert NONE contain `reset --soft`.

Follow the established structural test pattern from `acquireStateLock` tests (line 5236): read source with `fs.readFileSync`, check for pattern presence/absence with `assert.ok`.

Define paths at the top of the describe block:
```javascript
const MILESTONE_PATH = path.join(__dirname, 'lib', 'milestone.cjs');
const PHASE_PATH = path.join(__dirname, 'lib', 'phase.cjs');
const FRONTMATTER_SRC_PATH = path.join(__dirname, 'lib', 'frontmatter.cjs');
const WORKFLOWS_DIR = path.join(__dirname, '..', 'workflows');
```

Commit: `fix(55.1-02): add worktree_branch_check with reset --hard to workflows (#1981) and structural tests`

After both commits, run `node bin/install.js --local` to sync the workflow changes to `.claude/`.
  </action>
  <verify>
Run `npm run test:upstream` -- all tests pass including new structural tests. Then verify:
```bash
grep -l 'worktree_branch_check' get-shit-done/workflows/execute-phase.md get-shit-done/workflows/execute-plan.md get-shit-done/workflows/quick.md get-shit-done/workflows/diagnose-issues.md
```
Expected: all 4 files listed.

```bash
grep 'reset --soft' get-shit-done/workflows/execute-phase.md get-shit-done/workflows/execute-plan.md get-shit-done/workflows/quick.md get-shit-done/workflows/diagnose-issues.md
```
Expected: zero matches.

```bash
node bin/install.js --local
```
Expected: success, workflow files synced to `.claude/`.
  </verify>
  <done>
All 4 workflow files contain `worktree_branch_check` with `reset --hard`. Zero `reset --soft` in any workflow file. Zero `fs.writeFileSync` in milestone.cjs/phase.cjs/frontmatter.cjs. Structural tests pass. Installer syncs changes to `.claude/`. All 3 test suites pass.
  </done>
</task>

</tasks>

<verification>
1. `npm run test:upstream` passes (all tests including new structural tests)
2. `npm run test:upstream:fork` passes
3. `npm test` passes
4. `grep -c 'fs\.writeFileSync' get-shit-done/bin/lib/milestone.cjs get-shit-done/bin/lib/phase.cjs get-shit-done/bin/lib/frontmatter.cjs` returns 0 for all files
5. `grep -l 'worktree_branch_check' get-shit-done/workflows/*.md` returns all 4 target files
6. `grep -c 'reset --soft' get-shit-done/workflows/execute-phase.md get-shit-done/workflows/execute-plan.md get-shit-done/workflows/quick.md get-shit-done/workflows/diagnose-issues.md` returns 0 for all files
7. `node bin/install.js --local` succeeds
</verification>

<success_criteria>
- All 16 writeFileSync sites converted to atomicWriteFileSync across 3 modules
- All 4 workflow files have worktree_branch_check blocks with reset --hard
- Structural tests verify both fixes prevent regression
- All 3 test suites pass with zero regressions
- Installer syncs workflow changes to .claude/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/55.1-upstream-bug-patches/55.1-02-SUMMARY.md`
</output>
