---
phase: 55-upstream-mini-sync
plan: 03
type: execute
wave: 3
depends_on: ["55-02"]
requirements: [SYNC-01]
files_modified:
  - get-shit-done/bin/lib/phase.cjs
  - get-shit-done/bin/lib/roadmap.cjs
  - get-shit-done/workflows/complete-milestone.md
  - bin/install.js
autonomous: true

must_haves:
  truths:
    - "phase.cjs has upstream v1.34.2 content with fork's trivial adjustments re-applied"
    - "roadmap.cjs has upstream v1.34.2 content with fork's trivial adjustments re-applied"
    - "complete-milestone.md has upstream data-loss prevention and requirements check"
    - "bin/install.js has all 11 upstream reliability fixes with fork's replacePathsInContent and dual-directory logic preserved"
    - "Installer marker strings use the fork's get-shit-done-reflect branding"
  artifacts:
    - path: "get-shit-done/bin/lib/phase.cjs"
      provides: "Phase operations with fork adjustments"
      contains: "module.exports"
    - path: "get-shit-done/bin/lib/roadmap.cjs"
      provides: "Roadmap operations with fork adjustments"
      contains: "module.exports"
    - path: "get-shit-done/workflows/complete-milestone.md"
      provides: "Complete milestone workflow with data-loss safety"
      contains: "milestone"
    - path: "bin/install.js"
      provides: "Installer with upstream reliability fixes and fork extensions"
      contains: "replacePathsInContent"
  key_links:
    - from: "get-shit-done/bin/lib/phase.cjs"
      to: "get-shit-done/bin/lib/state.cjs"
      via: "require('./state.cjs')"
      pattern: "require\\('./state\\.cjs'\\)"
    - from: "get-shit-done/bin/lib/roadmap.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "require('./core.cjs')"
      pattern: "require\\('./core\\.cjs'\\)"
    - from: "bin/install.js"
      to: "get-shit-done/bin/lib/"
      via: "copies source modules to .claude/ runtime"
      pattern: "replacePathsInContent"
---

<objective>
Replace the 2 mostly-upstream modules (phase.cjs, roadmap.cjs) with fork re-adjustments, adopt the complete-milestone.md workflow, and hybrid-merge the installer with all 11 upstream reliability fixes.

Purpose: Completes the module-level sync. phase.cjs and roadmap.cjs have trivial fork adjustments (+17/-1 and +15/-1). The installer is medium-risk: 11 upstream fixes need careful integration while preserving the fork's dual-directory architecture and branding.

Output: 4 files updated. All source-level modules are synced to v1.34.2.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-upstream-mini-sync/55-CONTEXT.md
@.planning/phases/55-upstream-mini-sync/55-RESEARCH.md
@.planning/phases/55-upstream-mini-sync/55-02-SUMMARY.md
@.planning/FORK-DIVERGENCES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace phase.cjs and roadmap.cjs with fork re-adjustments, adopt complete-milestone.md</name>
  <files>
    get-shit-done/bin/lib/phase.cjs
    get-shit-done/bin/lib/roadmap.cjs
    get-shit-done/workflows/complete-milestone.md
  </files>
  <action>
    **phase.cjs (mostly upstream, +17/-1 fork adjustment):**

    1. Save the fork's current adjustments by reading current phase.cjs and identifying fork-specific lines. The fork has only +17/-1 lines of adjustment on the upstream base (per FORK-DIVERGENCES.md). These are minor adjustments to phase handling.
    2. Wholesale replace: `git show f7549d43:get-shit-done/bin/lib/phase.cjs > get-shit-done/bin/lib/phase.cjs`
    3. Read upstream v1.34.2 phase.cjs and re-apply the fork adjustments. Since the adjustment is small (+17/-1), manually inspect the old fork diff and apply the equivalent changes to the new upstream base. The fork adjustments are likely in areas like phase naming or path handling.
    4. If the upstream code has changed the region where fork adjustments applied, adapt the adjustment to fit the new code structure.

    **Area 3 performance fix inclusion:** Upstream v1.34.2 phase.cjs already includes the `readdirSync` hoist optimization. No separate action needed.

    **roadmap.cjs (mostly upstream, +15/-1 fork adjustment):**

    Same approach as phase.cjs:
    1. Identify fork's +15/-1 adjustment in current roadmap.cjs
    2. Wholesale replace: `git show f7549d43:get-shit-done/bin/lib/roadmap.cjs > get-shit-done/bin/lib/roadmap.cjs`
    3. Re-apply fork adjustments to new upstream base

    **Area 3 performance fix inclusion:** Upstream v1.34.2 roadmap.cjs already includes the `readdirSync` hoist optimization. No separate action needed.

    **complete-milestone.md (workflow file):**

    Adopt the upstream v1.34.2 version:
    ```bash
    git show f7549d43:get-shit-done/workflows/complete-milestone.md > get-shit-done/workflows/complete-milestone.md
    ```

    Then verify the fork's shell robustness guards (from commit 5e04128e) are still present. The upstream version at v1.34.2 (799 lines) includes data-loss prevention, requirements check, and UI artifact archival. If the fork's C2 shell robustness guards are NOT in the upstream version, re-apply them manually. These guards typically involve error handling and shell safety patterns.

    **Commit strategy (per user decision -- one commit per merge category):**
    After this task completes, create ONE commit for the 3 module replacements:
    `feat(55-03): replace phase.cjs, roadmap.cjs with fork re-adjustments and adopt complete-milestone.md`
  </action>
  <verify>
    ```bash
    # Verify modules load
    node -e "require('./get-shit-done/bin/lib/phase.cjs'); console.log('phase.cjs OK')"
    node -e "require('./get-shit-done/bin/lib/roadmap.cjs'); console.log('roadmap.cjs OK')"
    # Verify line counts are in expected range
    wc -l get-shit-done/bin/lib/phase.cjs get-shit-done/bin/lib/roadmap.cjs get-shit-done/workflows/complete-milestone.md
    # Verify complete-milestone has data-loss safety
    grep -c "backlog\|999\|preserve\|safety" get-shit-done/workflows/complete-milestone.md || echo "Check manually"
    ```
  </verify>
  <done>phase.cjs and roadmap.cjs replaced with v1.34.2 content plus fork adjustments re-applied. complete-milestone.md adopted with data-loss prevention logic. All files load without error. Committed as one module-replacement commit.</done>
</task>

<task type="auto">
  <name>Task 2: Hybrid-merge bin/install.js with 11 upstream reliability fixes</name>
  <files>bin/install.js</files>
  <action>
    This is a MEDIUM-risk hybrid merge. The installer is 3,294 lines with significant fork extensions (replacePathsInContent, dual-directory architecture, fork branding). Upstream has 11 reliability fixes that need integration.

    **Step 1: Read both versions**
    - Fork's current install.js (3,294 lines)
    - Upstream v1.34.2: `git show f7549d43:bin/install.js`

    **Step 2: Identify upstream reliability fixes to adopt**
    From the drift survey Area 4 (11 commits):
    - Hook path anchoring (absolute paths for hook scripts)
    - Uninstall safety (prevent removing files that weren't installed by GSD)
    - Fresh install guards (handle first-time installs gracefully)
    - Manifest cleanup (proper temp file cleanup on failure)
    - Hook script deployment improvements
    - Config dir detection robustness
    - fs.existsSync guard before settings.json writes
    - Various bug fixes (1736, 1754, 1817, 1818, 1823, 1834, 1891, 1906, 1908, 1924)

    **Step 3: Merge strategy**
    Since both versions are large, the approach is:
    1. Diff fork install.js against upstream v1.34.2 install.js to see all differences
    2. For each difference: classify as "fork extension" (keep) or "missing upstream fix" (adopt)
    3. Apply missing upstream fixes to the fork's install.js

    **Fork extensions to PRESERVE (do not lose these):**
    - `replacePathsInContent()` -- converts `~/.claude/get-shit-done/` to `./.claude/get-shit-done/` paths
    - Dual-directory copy logic (source -> .claude/ runtime)
    - Fork branding: `get-shit-done-reflect` in marker strings (Pitfall 5 from RESEARCH.md)
    - Codex marker strings with `-reflect` suffix
    - Fork-specific file lists and directory structures
    - Any `--local` flag handling

    **CRITICAL: Marker string preservation**
    The fork uses `get-shit-done-reflect` in GSD_CODEX_MARKER and similar strings. Upstream uses `get-shit-done`. Keep the fork's marker strings -- they only affect this fork's install/uninstall cycle.

    **Step 4: After merge, verify**
    - replacePathsInContent still exists and works
    - Fork marker strings preserved
    - Upstream reliability fixes present (search for the fix patterns)

    **Commit strategy (per user decision -- one commit per merge category):**
    After this task completes, create a SEPARATE commit for the installer (distinct merge category from module replacements):
    `feat(55-03): hybrid-merge installer with 11 upstream reliability fixes`
  </action>
  <verify>
    ```bash
    # Verify installer loads without syntax errors
    node -e "require('./bin/install.js')" 2>&1 || echo "Note: install.js may not be require-able directly -- check syntax only"
    node -c bin/install.js && echo "Syntax OK"
    # Verify fork extensions preserved
    grep -c "replacePathsInContent" bin/install.js
    grep -c "reflect" bin/install.js | head -1
    # Verify upstream reliability fixes present (spot-check)
    grep -c "existsSync\|realpathSync\|manifest" bin/install.js
    # Verify line count is reasonable (should be >= 3294 with additions)
    wc -l bin/install.js
    ```
  </verify>
  <done>bin/install.js has all 11 upstream reliability fixes integrated. Fork extensions preserved: replacePathsInContent, dual-directory logic, fork marker strings. File has valid syntax. Committed as separate installer commit. Ready for test validation in Plan 04.</done>
</task>

</tasks>

<verification>
- phase.cjs loads with fork adjustments on upstream v1.34.2 base
- roadmap.cjs loads with fork adjustments on upstream v1.34.2 base
- complete-milestone.md has upstream data-loss prevention
- bin/install.js has valid syntax with both upstream fixes and fork extensions
- Fork marker strings preserved in installer (grep for "reflect")
- replacePathsInContent exists in installer
</verification>

<success_criteria>
All 4 files updated. Mostly-upstream modules have fork adjustments re-applied cleanly. Installer passes syntax check and preserves fork's dual-directory architecture. Two commits created: one for module replacements, one for installer. All source-level module syncing is complete -- only router updates, test adoption, and validation remain.
</success_criteria>

<output>
After completion, create `.planning/phases/55-upstream-mini-sync/55-03-SUMMARY.md`
</output>
