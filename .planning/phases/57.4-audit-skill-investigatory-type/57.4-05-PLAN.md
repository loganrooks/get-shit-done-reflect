---
phase: 57.4-audit-skill-investigatory-type
plan: 05
type: execute
wave: 3
depends_on:
  - 01
  - 02
  - 03
  - 04
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true

must_haves:
  truths:
    - "WF-01 rewritten to reflect absorption into /gsdr:audit as cross_model delegation mode (no longer a standalone /gsdr:cross-model-review command)"
    - "AUDIT-01 and AUDIT-02 (forward compliance for Phase 57.3 audit infrastructure) remain intact — this plan does not touch them"
    - "New requirements added for the 3-axis taxonomy (subject × orientation × delegation), the obligations paradigm, Rule 5 frame-reflexivity, I1-I4 investigatory ground rules, and the three new obligations (chain integrity, dispatch hygiene, framework invisibility)"
    - "New requirements reference deliberations as authority, not the rewritten reference files (the rewrites exist as implementation, but the authority traces back to the deliberations)"
    - "Each new requirement has a motivation citation per REQUIREMENTS.md convention"
    - "Requirement IDs follow the existing prefix convention (AUDIT-XX, WF-XX) without collision"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirements for Phase 57.4: WF-01 rewritten, new AUDIT requirements for 3-axis model/obligations/Rule 5/I1-I4/cross-cutting obligations"
      contains: "audit_subject"
  key_links:
    - from: "WF-01 (rewritten)"
      to: "commands/gsd/audit.md"
      via: "requirement text references the command absorbing cross-model delegation"
      pattern: "/gsdr:audit"
    - from: "New AUDIT-* requirements for 3-axis taxonomy"
      to: ".planning/deliberations/audit-taxonomy-three-axis-obligations.md"
      via: "motivation citation"
      pattern: "audit-taxonomy-three-axis-obligations"
---

<objective>
Rewrite REQUIREMENTS.md WF-01 at line 167 to reflect its absorption into `/gsdr:audit` as a delegation mode (rather than a standalone `/gsdr:cross-model-review` command), and add new AUDIT-* requirements for the 3-axis taxonomy, obligations paradigm, Rule 5 frame-reflexivity, I1-I4 investigatory ground rules, and the three new cross-cutting obligations (chain integrity, dispatch hygiene, framework invisibility).

Purpose: REQUIREMENTS.md is the authoritative scope ledger for the project. Phase 57.4 ships changes that must be captured as formal requirements so that the milestone audit trail is complete. The v1 AUDIT-04 placeholder for a flat-taxonomy audit skill is superseded by this phase's work; the new requirements name what actually shipped.

Per CONTEXT.md `<files_affected>` section: "WF-01 requirement text (line 167) still names a standalone `/gsdr:cross-model-review` command and may need revision to reflect absorption into `/gsdr:audit` — flagged in ROADMAP.md Phase 62 note but not yet rewritten."

Output: An updated REQUIREMENTS.md with the rewritten WF-01 and N new AUDIT requirements under the existing Audit Infrastructure section. No other sections modified.

**Why this is Wave 3 not Wave 2:** The rewritten WF-01 text must reference the command name and behavior that Plans 03 (command) and 04 (agent) actually shipped. If the command were named differently or if the cross-model path were deferred entirely, WF-01 would need different text. Waiting until after Wave 2 ensures naming and behavior accuracy.

**Why this is NOT the last plan:** Plan 06 (banner removal + install sync + verification) is the last plan. REQUIREMENTS.md updates are an intermediate deliverable — the banners are removed only after all primary deliverables (including these requirement updates) have landed.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57.4-audit-skill-investigatory-type/57.4-CONTEXT.md
@.planning/phases/57.4-audit-skill-investigatory-type/57.4-RESEARCH.md

**File being modified:**
@.planning/REQUIREMENTS.md

**Dependencies (already landed when this plan runs):**
@get-shit-done/references/audit-conventions.md
@get-shit-done/references/audit-ground-rules.md
@commands/gsd/audit.md
@agents/gsdr-auditor.md

**Primary authority for new requirements:**
@.planning/deliberations/audit-taxonomy-three-axis-obligations.md
@.planning/deliberations/audit-taxonomy-retrospective-analysis.md
@.planning/phases/57.4-audit-skill-investigatory-type/pre-phase-archive/REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite WF-01 at REQUIREMENTS.md line ~167 to reflect absorption into /gsdr:audit</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Locate WF-01 in REQUIREMENTS.md. Per RESEARCH.md §"Open Questions (Resolved)", the current text is at line 167 and reads (approximately):

> - [ ] **WF-01**: `/gsdr:cross-model-review` command launches background cross-model review with: (a) committed audit spec documenting what is being reviewed and by whom (traceability), (b) structured response template (accept/accept-with-nuance/dispute-with-evidence). Command is opt-in, model choice configurable not prescribed, review round count user-determined. The review protocol must remain flexible -- the command provides infrastructure, not a rigid procedure
>   - *Motivation:* `pattern: strongest positive pattern across audit (P01-P08, 4 projects, 6+ sessions)` | `research: PITFALLS.md C6 -- over-formalization risk`

**Rewrite WF-01 as follows:**

> - [ ] **WF-01**: Cross-model audit delegation is supported as an `audit_delegation: cross_model:{model_id}` mode within the `/gsdr:audit` command (Phase 57.4), rather than a standalone `/gsdr:cross-model-review` command. The command composes a fully-formed task spec (obligations, ground rules, fit assessment) and dispatches to the target model's CLI (e.g., `codex exec` for GPT). The cross-model path ships as **experimental** in 57.4 with a `--trust-cross-model` opt-in requirement for `--auto` mode, a user warning about known fragility (environment setup, early exit, instruction degradation), stdout teeing to a log file, and explicit output file verification. A post-implementation spike (Q2 candidate) will measure dispatch reliability and determine whether the experimental flag can be removed. Command is opt-in, model choice configurable not prescribed; audit round count is user-determined via `--continue`. The delegation must remain flexible — the command provides infrastructure, not a rigid procedure. See audit-conventions.md Section 3.3 (Axis 3: Delegation) and audit-ground-rules.md Section 3.3 (dispatch hygiene obligation).
>   - *Motivation:* `pattern: strongest positive pattern across audit (P01-P08, 4 projects, 6+ sessions)` | `research: PITFALLS.md C6 -- over-formalization risk` | `deliberation: audit-taxonomy-three-axis-obligations.md -- WF-01 absorbed as cross_model delegation mode per 57.4 scope`

**Mark WF-01 as not-yet-done** — it is not complete until the post-implementation spike (Q2) validates reliability. Keep the `[ ]` checkbox. In the SUMMARY.md for this plan, note that WF-01 is partially delivered (experimental path shipped) with a Q2 spike remaining for full sign-off.

**Prohibitions:**
- Do NOT delete WF-01 — rewrite it.
- Do NOT change the WF-01 ID (some other requirement may reference it).
- Do NOT change any other WF requirement (WF-02, WF-03, WF-04, WF-05a/b are out of scope).
- Do NOT change AUDIT-01 or AUDIT-02 — those are Phase 57.3 forward compliance requirements that survive.

**Second-order checks:**
- `grep -n "WF-01" .planning/REQUIREMENTS.md` — must match exactly once (the rewritten WF-01 entry)
- `grep -n "/gsdr:cross-model-review" .planning/REQUIREMENTS.md` — must return ZERO results (the standalone command name is replaced)
- `grep -n "cross_model" .planning/REQUIREMENTS.md` — must match in the new WF-01 text
- Verify line numbers shifted correctly: if WF-01 was at line 167 and the rewrite is longer, subsequent line numbers shift. No fixed line number dependency in the rest of REQUIREMENTS.md — verify no code references line 167 of REQUIREMENTS.md specifically (unlikely but check): `grep -rn "REQUIREMENTS.md:167" .planning/ .claude/ get-shit-done/ 2>/dev/null`. If there is such a reference, update it.
  </action>
  <verify>
- `grep -n "WF-01" .planning/REQUIREMENTS.md` — exactly one match
- `grep -n "/gsdr:cross-model-review" .planning/REQUIREMENTS.md` — zero matches
- `grep -n "audit_delegation" .planning/REQUIREMENTS.md` — at least one match in the new WF-01
- `grep -n "experimental" .planning/REQUIREMENTS.md | grep -A 2 "WF-01"` — "experimental" language present in WF-01
- `grep -n "--trust-cross-model" .planning/REQUIREMENTS.md` — opt-in flag mentioned in WF-01
- Read the new WF-01 entry: does it flow coherently and reference the actual 57.4 deliverables (command + agent + experimental cross-model path)?
- Second-order: `grep -rn "REQUIREMENTS.md:167" .planning/ .claude/ get-shit-done/ 2>/dev/null` — no stale references to the old line number (or if present, they are updated in the SUMMARY.md as follow-up)
- Second-order: `grep -n "WF-02\|WF-03\|WF-04\|WF-05" .planning/REQUIREMENTS.md` — untouched (same text as before)
- Second-order: `grep -n "AUDIT-01\|AUDIT-02" .planning/REQUIREMENTS.md` — untouched
  </verify>
  <done>
WF-01 rewritten to describe cross-model audit delegation as a mode within `/gsdr:audit` (not a standalone command), explicitly naming the experimental status, --trust-cross-model opt-in, Q2 spike follow-up, and the dispatch hygiene obligation. Motivation citations updated. WF-02 through WF-05, AUDIT-01, AUDIT-02 untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add new AUDIT requirements for the 3-axis taxonomy, obligations paradigm, Rule 5, I1-I4, and cross-cutting obligations</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Find the Audit Infrastructure section in REQUIREMENTS.md (search for AUDIT-01 to locate it). Add new AUDIT requirements after the existing AUDIT-* entries. Number them sequentially starting from the next available number (e.g., if existing AUDITs are AUDIT-01, AUDIT-02, AUDIT-03, start the new ones at AUDIT-04 — verify the current numbering by grepping before writing).

**Precondition:**

```bash
grep -nE "^- \[.\] \*\*AUDIT-" .planning/REQUIREMENTS.md
```

Record the highest existing AUDIT number. Start new requirements at N+1.

**New requirements to add (adjust numbers per precondition):**

**AUDIT-NN (3-axis taxonomy):**

> - [x] **AUDIT-NN**: Audit taxonomy is a three-orthogonal-axis model — subject (what is being audited: 9 subjects including `process_review` and `artifact_analysis`), orientation (from what stance: `standard` / `investigatory` / `exploratory`), delegation (who does it: `self` / `cross_model:{model_id}`). The axes compose orthogonally. Frontmatter uses three fields (`audit_subject`, `audit_orientation`, `audit_delegation`) replacing the v1 `audit_type` enum; `audit_type` is retained as an optional legacy field for backward compatibility. Subject is optional for `investigatory` and `exploratory` orientations.
>   - *Motivation:* `deliberation: audit-taxonomy-three-axis-obligations.md — 3-axis decomposition replaces flat 8-type taxonomy, validated against 13 audit sessions by retrospective` | `context: 57.4-CONTEXT.md DC-4 — frontmatter schema v2`

**AUDIT-NN+1 (obligations paradigm):**

> - [x] **AUDIT-NN+1**: Non-standard orientation audits use an obligations paradigm (obligations are things to engage with, not sections to write) instead of per-type body templates. Obligations compose across axes into a flat list: core (Rules 1-5) + orientation (standard / investigatory I1-I4 / exploratory) + subject (if named) + cross-cutting (conditional on chain or cross-model dispatch). The hermeneutic composition principle governs tension between obligations: name the tension, name what about the situation creates it, show how you navigated it — responsive to both demands, not cleanly picking one side. Standard routine audits may use suggested scaffolds (templates-as-scaffolding, not templates-as-mandate).
>   - *Motivation:* `deliberation: audit-taxonomy-three-axis-obligations.md Composition Principle` | `retrospective: audit-taxonomy-retrospective-analysis.md — validates three populations (template/obligations/multi-agent)`

**AUDIT-NN+2 (Rule 5 frame-reflexivity):**

> - [x] **AUDIT-NN+2**: Audit core rules include Rule 5 (frame-reflexivity): "Did the framing shape what you found?" Distinct from Rule 4 (within-frame escape): Rule 5 catches whether the frame itself was appropriate. Lightweight closing step for standard orientation; full section for investigatory. Grounded in specific questions (not generic prompts) to prevent compliance theater: "If this audit had been classified differently, what would it have looked for that you didn't?" Empty Rule 5 answers are a signal that the frame is invisible to the auditor, not a signal of neutrality.
>   - *Motivation:* `review: REVIEW.md Part 4 lines 103-116 — frame-reflexivity Rule 5 argument` | `guardrail: G-3 — rules must not become compliance theater`

**AUDIT-NN+3 (I1-I4 investigatory ground rules):**

> - [x] **AUDIT-NN+3**: Investigatory orientation carries four additional ground rules (I1-I4): (I1) start from the discrepancy, not a theory — the choice of comparison point is already an interpretive act; (I2) let the investigation guide artifact selection — the artifact chain is a finding, not an input; (I3) present competing explanations — at least two interpretations per finding, do not collapse to one; (I4) name the position of the investigation — every investigation is conducted from somewhere. Plus: show what remains unknown; show how you navigated any tensions between obligations.
>   - *Motivation:* `review: REVIEW.md Part 3 lines 70-74 — I1-I4 investigatory ground rules`

**AUDIT-NN+4 (three new cross-cutting obligations):**

> - [x] **AUDIT-NN+4**: Three cross-cutting audit obligations apply conditionally: **chain integrity** (for audits that use predecessor audit findings as evidence, re-verify each predecessor claim independently before incorporating it); **dispatch hygiene** (for `cross_model` delegation, verify the delegation prompt does not contain framing that systematically biases findings — comparative framing, target counts, desired conclusions); **framework invisibility** (for investigatory and exploratory audits, name a concrete finding that would not appear no matter how rigorously conducted because of how the audit's scope was framed).
>   - *Motivation:* `retrospective: audit-taxonomy-retrospective-analysis.md Gaps 4, 5, 7 — chain failures, dispatch contamination, framework limits`

**AUDIT-NN+5 (invocable audit skill):**

> - [x] **AUDIT-NN+5**: An invocable audit skill exists as `/gsdr:audit` (orchestrator command at `commands/gsd/audit.md`) + `gsdr-auditor` executor agent (at `agents/gsdr-auditor.md`). The command reads conversation context, infers a 3-axis classification, composes obligations, creates a session directory at `.planning/audits/YYYY-MM-DD-{slug}/`, writes a fully-formed task spec with all ground rules and obligations copied inline per DC-2, and dispatches the agent (self) or `codex exec` (cross_model, experimental). The agent spec embodies the hermeneutic epistemic stance from the deliberations, grounds Rule 5 and framework invisibility in specific questions, and writes audit output to disk per the task spec. Closes the Phase 57.3 deferred audit skill gap.
>   - *Motivation:* `signal: sig-2026-04-09-phase-573-deferred-audit-skill-no-command — capability gap from Phase 57.3` | `signal: sig-2026-04-09-agent-audit-outputs-ephemeral-no-artifact — session directory convention addresses traceability`

Mark all new AUDIT requirements as `[x]` (completed in this phase). WF-01 stays `[ ]` (partially delivered pending spike).

**Prohibitions:**
- Do NOT delete or modify existing AUDIT-01, AUDIT-02, or any other existing AUDIT entry.
- Do NOT add requirements for deferred features (gsd-tools audit subcommand, mid-audit orientation switching, multi-agent obligation enforcement, frame-reflexivity for non-audit workflows) — those are in CONTEXT.md `<deferred>` and out of scope.
- Do NOT invent requirement IDs that collide with existing ones.

**Second-order checks:**
- Count existing AUDIT requirements before adding. Verify the new numbers do not collide.
- Verify each new requirement has a motivation citation (per REQUIREMENTS.md convention — every requirement has a `*Motivation:*` line).
- Verify the `[x]` vs `[ ]` checkbox state is consistent with actual delivery (new AUDITs are `[x]` because Plans 01-04 have landed when this runs; WF-01 is `[ ]` because the spike is pending).
- Cross-check the requirement text against CONTEXT.md `<domain>` and `<working_model>` — requirements should not contradict CONTEXT.md decisions.
  </action>
  <verify>
- `grep -cE "^- \[.\] \*\*AUDIT-" .planning/REQUIREMENTS.md` — count is (old count + 6)
- `grep -n "AUDIT-" .planning/REQUIREMENTS.md | tail -10` — new AUDITs listed sequentially
- `grep -n "3-axis\|three-orthogonal-axis" .planning/REQUIREMENTS.md` — at least one match in new AUDIT requirement
- `grep -n "Rule 5" .planning/REQUIREMENTS.md` — match in new AUDIT requirement
- `grep -nE "I1|I2|I3|I4" .planning/REQUIREMENTS.md` — match in I1-I4 AUDIT requirement
- `grep -n "chain integrity\|dispatch hygiene\|framework invisibility" .planning/REQUIREMENTS.md` — match in cross-cutting obligations AUDIT requirement
- `grep -n "/gsdr:audit\b" .planning/REQUIREMENTS.md` — match in both WF-01 (Task 1) and new AUDIT-NN+5 (Task 2)
- `grep -n "gsdr-auditor" .planning/REQUIREMENTS.md` — match in new AUDIT-NN+5 (agent spec reference)
- Read each new AUDIT requirement: does it have a motivation citation on the next line with backtick-wrapped reference?
- Second-order: verify no requirement ID collision by listing all AUDIT IDs: `grep -oE "\*\*AUDIT-[0-9]+\*\*" .planning/REQUIREMENTS.md | sort -u`
- Second-order: verify the file is still valid Markdown (headers and list structure intact): read the section around the new requirements and the AUDIT section header
  </verify>
  <done>
6 new AUDIT requirements added to REQUIREMENTS.md (3-axis taxonomy, obligations paradigm, Rule 5, I1-I4, three cross-cutting obligations, invocable audit skill). Each carries a motivation citation. All marked `[x]` (completed). Existing AUDIT-01/02 and WF-02 through WF-05 untouched. WF-01 rewritten in Task 1. No ID collisions.
  </done>
</task>

</tasks>

<verification>
**End-of-plan verification (run as a single pass):**

1. `grep -c "^- \[.\] \*\*AUDIT-" .planning/REQUIREMENTS.md` — count is (original count + 6)
2. `grep -c "^- \[.\] \*\*WF-" .planning/REQUIREMENTS.md` — same count as before this plan (WF-01 rewritten not deleted; WF-02 through WF-05 untouched)
3. `grep -n "/gsdr:cross-model-review" .planning/REQUIREMENTS.md` — ZERO results
4. `grep -n "cross_model:" .planning/REQUIREMENTS.md` — at least one match (new WF-01)
5. `grep -n "3-axis" .planning/REQUIREMENTS.md` — at least one match (new AUDIT)
6. `grep -nE "\bRule 5\b" .planning/REQUIREMENTS.md` — at least one match (new AUDIT)
7. `grep -nE "\bI1\b|\bI2\b|\bI3\b|\bI4\b" .planning/REQUIREMENTS.md` — at least 4 matches across new AUDIT requirements
8. `grep -n "chain integrity" .planning/REQUIREMENTS.md` — at least one match
9. `grep -n "dispatch hygiene" .planning/REQUIREMENTS.md` — at least one match
10. `grep -n "framework invisibility" .planning/REQUIREMENTS.md` — at least one match
11. `grep -n "obligations paradigm" .planning/REQUIREMENTS.md` — at least one match
12. `grep -oE "\*\*AUDIT-[0-9]+\*\*" .planning/REQUIREMENTS.md | sort -u | wc -l` — count matches total AUDIT count (no duplicates)
13. Read the Workflow Gaps section: WF-01 rewritten, WF-02 through WF-05 unchanged
14. Read the Audit Infrastructure section: existing AUDITs unchanged, 6 new AUDITs appended
15. **Gsd-tools check:** No gsd-tools subcommands invoked in this plan. Pure markdown edit. Safe.
16. **Dual-directory check:** REQUIREMENTS.md is under `.planning/`, not `commands/` or `agents/`. Installer does not touch it. No sync concern.
</verification>

<success_criteria>
- WF-01 rewritten to describe cross-model audit delegation as a mode within `/gsdr:audit` with experimental status and --trust-cross-model opt-in
- 6 new AUDIT requirements added: 3-axis taxonomy, obligations paradigm, Rule 5 frame-reflexivity, I1-I4 investigatory ground rules, three cross-cutting obligations, invocable audit skill
- Each new AUDIT has a motivation citation
- All new AUDITs marked `[x]` (completed in this phase); WF-01 remains `[ ]` (pending Q2 spike)
- Existing AUDIT-01, AUDIT-02, WF-02 through WF-05, and all other requirements untouched
- No requirement ID collisions
- File is valid Markdown
</success_criteria>

<output>
After completion, create `.planning/phases/57.4-audit-skill-investigatory-type/57.4-05-SUMMARY.md` with:
- WF-01 rewrite: before and after text (or a diff excerpt)
- 6 new AUDIT requirement IDs assigned (record the actual numbers since they depend on existing AUDIT numbering)
- Motivation citation for each new requirement
- Partial-delivery note for WF-01 (experimental path shipped, Q2 spike pending)
- Verification that existing requirements are untouched (grep counts before and after for AUDIT-01, AUDIT-02, WF-02 through WF-05)
- Any REQUIREMENTS.md line number shift caused by the rewrite/additions (for downstream reference-tracking)
</output>
