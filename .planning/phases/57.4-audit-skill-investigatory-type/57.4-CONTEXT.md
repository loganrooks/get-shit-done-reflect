# Phase 57.4: Audit Skill & Investigatory Type - Context

**Gathered:** 2026-04-09
**Status:** Ready for planning
**Mode:** Exploratory --auto -- preserving uncertainty, auto-progression by type rules
**Iteration:** v2 (framing-corrected 2026-04-10) — v1 archived in `pre-phase-archive/57.4-CONTEXT-v1.md`. This version builds on, corrects, and responds to the failures of the prior iteration: the flat taxonomy conflated three orthogonal concerns (subject, orientation, delegation), the template model couldn't compose obligations across axes, and the design lacked a mechanism for cross-model dispatch. A deliberation (`audit-taxonomy-three-axis-obligations.md`, open — fed forward, not concluded) reconstructed the design. A retrospective (`audit-taxonomy-retrospective-analysis.md`) validated the reconstruction against all 13 audit sessions.

**Framing correction (2026-04-10):** After the context-checker ran on v2, a review identified that v2 CONTEXT.md had been treating `audit-conventions.md` and `audit-ground-rules.md` as authoritative base to be extended, when the 3-axis deliberation had actually **superseded** their core design commitments. See DISCUSSION-LOG.md §"Framing Correction (2026-04-10, post-context-checker)" and `sig-2026-04-10-discuss-phase-authority-weighting-gap.md` for the pattern. The `<domain>`, `<constraints>` (DC-1 through DC-5), and `<canonical_refs>` sections have been rewritten to flow authority from the deliberations. The working model prose below retains its original structure but "Current state:" framings for superseded content have been relabeled as "Superseded Phase 57.3 state". **Authority direction: deliberations → this CONTEXT.md → plan. NOT reference files → CONTEXT.md.**

<domain>
## Phase Boundary

**This phase radically rethinks the formalization of the auditing workflow.** Phase 57.3 produced `audit-conventions.md` and `audit-ground-rules.md` as the first formal audit infrastructure — directory conventions, frontmatter schema, a flat 8-type taxonomy, body templates per type, ground rules 1-4, and type-family rule extensions. After 57.3 shipped, the work of actually running audits (13 sessions plus 6 exploratory audits of audit quality) revealed that the flat taxonomy **does not express the complexities of the auditing situation**. What looked like "types" conflated three orthogonal concerns — *what* is being audited (subject), *from what stance* (orientation), *who does it* (delegation) — under one enum. The template paradigm could not compose obligations across those axes. Frame-reflexivity was absent. Chain integrity, dispatch hygiene, and framework invisibility were not enforced by any rule.

**The 3-axis deliberation is the reconstructive response, not an extension.** `.planning/deliberations/audit-taxonomy-three-axis-obligations.md` (open — fed forward, not concluded) restructures the ontology: 3 orthogonal axes, 9 subjects, 3 orientations, 2 delegation modes, an obligations-based output paradigm for non-standard orientations, a hermeneutic composition principle ("name the tension, navigate situationally"), and explicit acknowledgement of the aporia of epistemic humility. `.planning/deliberations/audit-taxonomy-retrospective-analysis.md` validates the restructuring against all 13 audit sessions and surfaces three new obligations (chain integrity, dispatch hygiene, framework invisibility) from gaps templates could not catch. `pre-phase-archive/REVIEW.md` contributes frame-reflexivity Rule 5 and the I1-I4 investigatory ground rules. **The deliberations are primary authority for this phase's scope. The reference files are prior artifacts that this phase will rewrite.**

The phase ships the reconstructed formalization along three deliverable vehicles:

1. **`/gsdr:audit` command + `gsdr-auditor` agent** — invocable skill that dispatches audits under the 3-axis model, composes obligations per situation, handles session directory creation, copies ground rules into task specs, and supports cross-model delegation.
2. **Rewritten `audit-conventions.md` and `audit-ground-rules.md`** — replacing the flat taxonomy, per-type templates, and type-family rule sets with the 3-axis taxonomy, obligations paradigm, Rule 5, I1-I4, and the composition principle. Meta-rules that survive (directory naming, rule-copying protocol, escape-hatch principle) are preserved.
3. **Formalized cross-model delegation (WF-01, pulled forward from Phase 62)** — cross-model dispatch as an `audit_delegation` mode rather than a standalone `/gsdr:cross-model-review` command. The audit command is the natural home since audits are the primary cross-model use case.

**Two concerns held together without collapsing one into the other:**

1. **Epistemic reconstruction (primary)** — the 3-axis model, obligations paradigm, Rule 5, I1-I4, and the composition principle are not decoration added to a mostly-sound design. They replace core design commitments of Phase 57.3. The reference files before the rewrite are a **prior iteration**, not a **base being extended**. This matters for how the plan is written: the rewrite is the deliverable, not a side effect.

2. **Operational delivery (secondary, instrumental)** — the `/gsdr:audit` command and `gsdr-auditor` agent make the reconstructed formalization invocable. Phase 57.3 deferred the invocable skill; this phase ships it alongside the rethinking. The skill is the vehicle by which the reconstruction enters practice. Without the skill, the rethinking is unused prose. Without the rethinking, the skill ships a formalization the retrospective already showed to be insufficient.

Does NOT include: gsd-tools audit subcommand (deferred per trial-before-formalize), changes to existing agent implementations (gsd-verifier, gsd-integration-checker, etc.), workflow rewrites for audit-milestone or validate-phase, the adaptive harness design from backlog 999.x, mid-audit orientation switching, frame-reflexivity as a practice for non-audit workflows.

</domain>

<working_model>
## Working Model & Assumptions

### Three-axis taxonomy (the central reconstruction)

**Superseded Phase 57.3 state:** The flat 8-type taxonomy in audit-conventions.md Section 3 conflates three orthogonal concerns: what you're auditing (subject), from what stance (orientation), and who does it (delegation). `cross_model_review` is a delegation strategy, not a subject type. `exploratory` is an orientation, not a subject type. The user's observation during discuss-phase v1 triggered the reconstruction. This section of audit-conventions.md carries a supersession banner pointing at `audit-taxonomy-three-axis-obligations.md` as primary authority pending the rewrite this phase will deliver.

**Axis 1: Subject** (what are you auditing?)

- [decided:reasoned] 9 subject types replacing the original 8: the 7 genuine subjects from the old taxonomy (`phase_verification`, `milestone`, `codebase_forensics`, `requirements_review`, `comparative_quality`, `claim_integrity`, `adoption_compliance`) plus 2 new subjects discovered by testing the decomposition against real audit cases: `process_review` (methodology soundness) and `artifact_analysis` (patterns in a corpus). `cross_model_review` exits the subject enum — it was always a delegation strategy.
- [decided:reasoned] Subject is optional for investigatory and exploratory orientations. When you don't know what you're investigating yet (the Phase 57 case), the investigation discovers its subject.
- [decided:reasoned] Each subject carries an epistemic profile: `oriented_toward`, `assumes`, `might_miss`. The orchestrator reads these when matching situation to subject. If the situation has features that every subject's "might miss" column would miss, that's a signal for investigatory-without-subject. Profiles documented in the deliberation's Axis 1 table.

**Axis 2: Orientation** (from what stance?)

- [decided:reasoned] Three orientations: `standard` (default — close on findings), `investigatory` (something went wrong; hold diagnosis open, competing explanations), `exploratory` (a question beckons; follow it, don't force closure). Orientation is orthogonal to subject — you can investigate, explore, or verify any domain.
- [decided:reasoned] The distinction between investigatory and exploratory is one of occasion: investigatory is occasioned by breakdown (expectations violated), exploratory by possibility (a question opens). Both are hermeneutically circular. The body structures differ because they start from different places (discrepancy vs. question).

**Axis 3: Delegation** (who does it?)

- [decided:reasoned] Two delegation modes: `self` (default — local gsdr-auditor agent) and `cross_model` (dispatch to another model's CLI). Delegation is orthogonal to both subject and orientation.

- [decided:reasoned] The `exploratory` escape hatch is preserved for the *subject* axis only — `audit_subject: exploratory` for audits that don't match any named subject. The orientation axis does not need an escape hatch because its 3 values (standard, investigatory, exploratory) already span the epistemic posture space.

### Obligations model

**Superseded Phase 57.3 state:** audit-conventions.md Section 4 has body templates per type. The deliberation revealed that templates can't compose across axes — "which template wins?" is the wrong question when two axes both contribute structure. Section 4 carries a supersession banner; `audit-taxonomy-three-axis-obligations.md` is primary authority for the obligations paradigm that replaces it.

- [decided:reasoned] Replace templates with composable obligation sets for non-standard orientations. Each axis contributes obligations — things the audit must *address*, not sections it must *write*. The difference: "write a Competing Explanations section" (template) vs. "you must present competing explanations" (obligation). The second lets the auditor weave competing explanations into the narrative wherever they emerge naturally.
- [decided:reasoned] Three populations confirmed by retrospective: (1) Standard routine audits use templates (VERIFICATION.md, MILESTONE-AUDIT.md — consistency matters), (2) Complex/investigatory/exploratory use obligations (flexibility matters), (3) Multi-agent sessions need additional mechanisms (not fully designed — see open questions).

**Core obligations (every audit):**

- [decided:reasoned] Ground rules 1-3 (cite evidence, test disconfirmation, distinguish measure from measured) — carried forward from audit-ground-rules.md (survive the rewrite per the supersession banner; these are among the meta-rules explicitly preserved).
- [decided:reasoned] Rule 4: What exceeded these obligations? (escape hatch — carried forward from audit-ground-rules.md; survives the rewrite).
- [decided:reasoned] Rule 5: Did the framing shape what you found? (frame-reflexivity — NEW in v2, added by this phase per REVIEW.md Part 4; authoritative source is the deliberation). Lightweight for standard orientation, full section for investigatory. This is distinct from Rule 4: Rule 4 catches excess within the frame; Rule 5 catches whether the frame was the right one.

**Orientation obligations:**

- [decided:reasoned] Standard: close on findings with evidence, produce a clear verdict, address all items in scope.
- [decided:reasoned] Investigatory (I1-I4, confirmed from REVIEW.md Part 3): (I1) start from the discrepancy, not a theory; (I2) let the investigation guide artifact selection; (I3) present competing explanations for each major finding; (I4) name the position of the investigation. Plus: show what remains unknown, show how you navigated any tensions between obligations.
- [decided:reasoned] Exploratory: state the initiating question/curiosity; follow the question wherever it leads; name what you found that you weren't looking for; name what the exploration opened (new questions, possibilities); name what you didn't look at (acknowledged partiality, not failure); "I don't know yet" is a valid conclusion.

**Subject obligations:**

- [decided:reasoned] Each subject type contributes its own obligation set. These compose with orientation obligations into a flat list. See deliberation Section "Subject obligations" for the full table.

**Three new obligations from retrospective:**

- [decided:reasoned] Chain integrity: when audit B uses audit A's finding as evidence, and audit A had a quality failure, audit B inherits the failure invisibly. Obligation: for each finding that depends on a predecessor audit's claim, re-verify that claim independently before incorporating it.
- [decided:reasoned] Dispatch hygiene: when running parallel agents, verify that prompts don't contain framing that could systematically bias findings (comparative framing, target counts, desired conclusions).
- [decided:reasoned] Framework invisibility: name what your audit framework cannot see — not what you chose not to look at, but what the structure of the audit makes invisible.

**Composition principle:**

- [decided:reasoned] When obligations from different axes tension against each other, the auditor must: (1) name the tension, (2) name what about the situation creates it, (3) show how they navigated it — responsive to both demands. This is a hermeneutic principle, not an algorithmic one: "If you find yourself cleanly ignoring one obligation in favor of another, you've likely stopped engaging with the tension."
- [assumed:reasoned] The composition principle is written but untested. First real investigatory audits will reveal whether agents can actually engage with it or whether they collapse to ignoring one side.

**Template excess section:**

- [decided:reasoned] All audit output includes "What the Obligations Didn't Capture" — a named section for findings that don't fit any obligation. Consistently substantial content in this section for a particular kind of audit is evidence for new subject types or new obligations.

**Suggested structures:**

- [decided:reasoned] For agents that need scaffolding, suggested output structures remain available per orientation. But they are suggestions, not mandates. The obligations are what's checked. Templates are genuinely useful for standard orientation (consistency, scannability). The orchestrator's complexity assessment (subject well-defined? scope bounded? established precedent?) determines whether to offer template or obligations mode.

### Command architecture

**Current state:** No `/gsdr:audit` command exists. The audit-milestone command is the closest pattern.

- [decided:reasoned] Two-file architecture: `commands/gsd/audit.md` (orchestrator) + `agents/gsdr-auditor.md` (executor). The pattern is followed by commands that orchestrate long-running work (audit-milestone, validate-phase, execute-phase). Lightweight commands (help, signal) operate inline.
- [decided:reasoned] Orchestrator writes full task spec, agent executes — spike-runner pattern. The command does discovery (reads conversation context, infers type/scope), creates session directory, selects obligations and ground rules, writes task spec, dispatches agent. The agent receives a fully-formed spec and runs the audit.
- [decided:reasoned] Type inference from context, not forced selection. The command reads conversation history for context clues and proposes an audit classification (subject × orientation × delegation). If context is sufficient, proceeds directly. If ambiguous, 0-2 clarifying questions.
- [decided:reasoned] Free-text topic as primary invocation, following deliberate.md pattern: `/gsdr:audit "something went wrong with Phase 57"`. Context inference proven in deliberate.md.
- [decided:reasoned] Session directory follows the date-first convention from audit-conventions.md Section 1 (survives the rewrite): `.planning/audits/YYYY-MM-DD-{slug}/` with task spec + output files. Frontmatter uses the base Section 2 fields (`date`, `scope`, Recommended, Optional — all surviving) plus the three new axis fields (`audit_subject`, `audit_orientation`, `audit_delegation`) that replace the legacy `audit_type` enum per DC-4 and the deliberation.
- [decided:reasoned] Ground rules and obligations auto-composed by the orchestrator based on 3-axis classification, copied into task spec (not referenced by path). Per audit-ground-rules.md Section 4 (the rule-copying meta-rule, which survives the rewrite). The *content* of rules being copied comes from the deliberation + surviving core rules 1-4, not from the stale type-family sections.
- [decided:reasoned] Qualified type assignment: for situations that don't fit neatly, the orchestrator writes a fit assessment in the task spec explaining why this classification was chosen and where the auditor should expand.
- [assumed:reasoned] The `--auto` flag controls interactivity consistent with other GSD commands. Structural standard audits can run fully autonomously. Investigatory audits may benefit from conversational mode. The flag and classification together determine execution mode.

### Cross-model delegation (WF-01, pulled from Phase 62)

**Current state:** [evidenced:reasoned] Cross-model dispatch works but is fragile. Known problems: environment setup issues, agents finishing early, instructions not properly conveyed, lots of hand-holding. <!-- typed by context-checker: sourced from audit-taxonomy-three-axis-obligations.md line 100 "currently fragile"; anchors WF-01 pull-forward justification in dependency table -->

- [decided:reasoned] WF-01 pulled from Phase 62 into 57.4. The audit command is the natural home for cross-model dispatch since audits are the primary cross-model use case.
- [decided:reasoned] The command generates the task spec (ground rules, obligations, scope, fit assessment), then dispatches to the target model's CLI with proper environment setup and instruction framing.
- [open] Specific cross-model dispatch mechanics: robust CLI invocation patterns, environment detection, instruction framing, output collection. Failure modes (agents finishing early, env misconfig, instruction degradation) need investigation during research.

### Agent design

**Current state:** No `gsdr-auditor` agent exists. Closest agents: `gsdr-context-checker` (explicit ground rules), `gsd-verifier` (structural verification), `gsdr-spike-runner` (receives fully-formed spec).

- [decided:reasoned] Tool access: Read, Bash, Grep, Glob, Write. Same as verifier/checker agents.
- [decided:reasoned] Agent spec embodies the epistemic stance from the deliberations (primary authority) and the surviving core rules 1-4 of audit-ground-rules.md — not just lists the rules but explains why they matter. This means the spec must carry the 3-axis model's hermeneutic character (composition principle, aporia of epistemic humility, obligations-as-things-to-engage-with) into the agent's operating frame, not just the mechanical rule list.
- [decided:reasoned] The agent handles Rule 4 (escape hatch) and Rule 5 (frame-reflexivity) structurally — they appear as named sections in the output, not optional afterthoughts.
- [decided:reasoned] gsdr-auditor model: sonnet in frontmatter (consistent with gsdr- agent pattern). Orchestrator escalates for investigatory/epistemic audits when higher reasoning is warranted. This matches the gsdr-context-checker pattern.
- [assumed:reasoned] The "What the Obligations Didn't Capture" section and the frame-reflexivity closing step must be genuinely engaged with, not perfunctory. The agent spec should ground these in specific questions rather than generic prompts.

### Frontmatter schema changes

**Superseded Phase 57.3 state:** audit-conventions.md Section 2 has `audit_type` as a single required field drawn from the flat 8-type enum. The `audit_type` field itself (as a semantic carrier) is superseded — it collapses three orthogonal concerns into one slot. The Section 2 base fields (`date`, `scope`, Recommended, Optional) survive the rewrite per the supersession banner.

- [decided:reasoned] Three new fields replacing the semantic content of `audit_type`: `audit_subject` (optional for investigatory/exploratory), `audit_orientation` (standard | investigatory | exploratory), `audit_delegation` (self | cross_model:{model_id}).
- [decided:reasoned] Backward compatibility: existing artifacts have `audit_type`. Migration adds the new 3 fields; `audit_type` maps to `audit_subject` with `audit_orientation: standard` and `audit_delegation: self` as defaults. Legacy field preserved.
- [assumed:reasoned] The frontmatter field count stays within the ~12-field budget established in Phase 57.3 (3 required + 5 recommended + 4 optional). The 3 new fields replace `audit_type` semantically (1 becomes 3), but the migration keeps `audit_type` for backward compat, which adds 2 net fields. This is acceptable at 14 total but should be monitored.

### Claude's Discretion

- Exact argument parsing format (flexible, not rigid)
- How many clarifying questions to ask (0-2 based on context sufficiency)
- Whether to present audit overview for user approval before executing
- Task spec wording beyond the obligations and template structure
- Complexity classifier implementation (how to assess template vs. obligations mode)
- How deeply to integrate orientation escalation (session starts standard, shifts to investigatory when findings warrant it)

</working_model>

<constraints>
## Derived Constraints

**Authority note (added 2026-04-10 post-context-checker):** DC-1 through DC-5 originally cited `audit-conventions.md` and `audit-ground-rules.md` as if they were stable authority. Those reference files are **partially superseded** by the 3-axis deliberation (see `<domain>` above and the supersession banners at the top of each file). The constraints below have been rewritten to flow authority from the deliberations where the reference files have been superseded, and to cite the reference files only for the meta-rules that survive the rewrite. See `sig-2026-04-10-discuss-phase-authority-weighting-gap.md` for the pattern this correction addresses.

- **DC-1:** [evidenced:cited] The command must follow the "command IS the orchestrator" pattern established by `commands/gsd/audit-milestone.md` line 16 — the command does discovery and dispatch, the agent does execution. *(This citation is unaffected by the supersession — audit-milestone.md is a command file, not a taxonomy/rule-set authority. The pattern is a meta-rule about command architecture.)*
- **DC-2:** [evidenced:cited] Ground rules and obligations must be copied into task specs, not referenced by path — per `audit-ground-rules.md` Section 4 line 114: "the agent needs the rules in its context window, not a pointer to them". *(Section 4 is one of the meta-rules that survives the rewrite per the supersession banner. The copying protocol is orthogonal to which rules get copied, and applies equally to the obligations-based paradigm.)*
- **DC-3:** [evidenced:cited] Session directory uses date-first convention per `audit-conventions.md` Section 1: `.planning/audits/YYYY-MM-DD-{slug}/`. *(Section 1 survives the rewrite per the supersession banner — directory naming is unaffected by the 3-axis restructuring.)*
- **DC-4:** [decided:reasoned] Frontmatter uses a v2 schema reconstructing the single `audit_type` enum from `audit-conventions.md` Section 2 into three axis-specific fields: `audit_subject` (optional for investigatory/exploratory orientations), `audit_orientation` (`standard` | `investigatory` | `exploratory`), and `audit_delegation` (`self` | `cross_model:{model_id}`). Base fields from Section 2 that survive unchanged: `date`, `scope`, and the Recommended and Optional field sets (`auditor_model`, `triggered_by`, `task_spec`, `ground_rules`, `tags`, `output_files`, `predecessor_audits`, `migrated_from`, `migrated_date`). Backward compatibility: `audit_type` is retained as a legacy field for existing artifacts; migration maps it to `audit_subject` with `audit_orientation: standard` and `audit_delegation: self` as defaults. Authoritative source for the decomposition: `audit-taxonomy-three-axis-obligations.md` Section "Axis 1/2/3". <!-- retyped from [evidenced:cited] to [decided:reasoned] 2026-04-10: the extension is a planned deliverable, not a currently-citable artifact. -->
- **DC-5:** [governing:reasoned] The taxonomy must remain open to subjects, orientations, or delegation modes that do not yet exist. The 3-axis model itself is provisional and may gain a fourth axis if the aporetic character of audit situations makes the current decomposition insufficient. Authoritative source: `audit-taxonomy-three-axis-obligations.md` (Section "Open Questions" and "Resolved Questions"), `forms-excess-and-framework-becoming.md` (governing constraint on how formal systems handle what exceeds their categories). The legacy formulation in `audit-conventions.md` Section 3 line 160 ("enum with escape hatch") is compatible with this but scoped to the flat taxonomy; the v2 formulation extends openness to the axes themselves, not only to subject values. <!-- retyped from [governing:cited] to [governing:reasoned] 2026-04-10: primary authority is the deliberation, not the superseded reference file. -->
- **DC-6:** [decided:reasoned] gsd-tools integration deferred per trial-before-formalize convention — command reads reference docs directly for now. TODO in STATE.md tracks future formalization.
- **DC-7:** [evidenced:cited] The dual-directory architecture requires editing `commands/gsd/audit.md` and `agents/gsdr-auditor.md` (npm source), not `.claude/` — per CLAUDE.md project instructions.

</constraints>

<guardrails>
## Epistemic Guardrails

- **G-1:** [governing:reasoned] The REVIEW.md (pre-phase-archive) and the deliberation must be taken seriously — both subject their proposals to the same critique they apply to the existing taxonomy. The deliberation's self-reflexive warnings (especially about performative humility) are design constraints, not decoration.
- **G-2:** [governing:bare] "Don't just naively add a type — investigate the situation in which the audit is demanded" (user direction during v1 discuss-phase). The 3-axis restructuring is the response to this directive — it investigates the taxonomic assumptions, not just the missing type.
- **G-3:** [governing:reasoned] Frame-reflexivity (Rule 5) and the "What the Obligations Didn't Capture" section must be genuine, not performative. If the agent writes "I considered my biases" without actually doing so, the rule has become compliance theater. The agent spec should ground these in specific questions.
- **G-4:** [governing:reasoned] The hermeneutic composition principle is a practice, not an algorithm. If agents consistently resolve obligation tensions by cleanly picking one side, the principle has failed. Monitor the first investigatory audits for this collapse pattern.
- **G-5:** [governing:reasoned] The relationship between investigatory and exploratory orientations is deliberately left as a design choice, not ontologically settled. They may merge, stay distinct, or evolve differently. Implementation should not foreclose this evolution.
- **G-6:** [governing:reasoned] The aporia of epistemic humility is a condition of the framework's existence, not a problem to solve. The framework creates structural openings (Rule 4, Rule 5, framework invisibility obligation, excess section) where limits are encountered in practice. Whether these function as genuine openings or performative gestures depends on the practice of the agents who use them — which is not something the framework can guarantee. No "Known Limits" section.

</guardrails>

<questions>
## Open Questions

### Q1: Can agents engage with the hermeneutic composition principle?
**Research program:** No research can answer this prior to implementation — it requires running real investigatory audits with composed obligation sets and examining whether the agent names tensions and navigates them or collapses to ignoring one side. The implementation should include explicit instruction in the agent spec and the first 2-3 investigatory audits should be reviewed for composition quality.
**Downstream decisions affected:** Whether the composition principle needs algorithmic support (e.g., explicit tension-detection prompting), or whether agent instruction is sufficient.
**Reversibility:** MEDIUM — if agents consistently collapse, adding algorithmic support is additive work, not a rewrite. But the philosophical character of the framework depends on hermeneutic resolution, not algorithmic resolution.

### Q2: Cross-model delegation mechanics
**Research program:** Investigate robust CLI invocation patterns for dispatching to Codex CLI / other model CLIs from within a Claude Code session. Document specific failure modes (agents finishing early, env misconfig, instruction degradation). Look at existing cross-model dispatch patterns in the codebase (the codex-drift-audit, the discuss-phase cross-model review).
**Downstream decisions affected:** Whether the audit command can reliably dispatch cross-model in --auto mode, or whether cross-model always requires interactive oversight. Whether the task spec format needs modification for cross-model consumption.
**Reversibility:** HIGH — cross-model delegation is an additive axis. If it doesn't work reliably, `audit_delegation: self` remains the default and cross-model becomes a documented manual workflow.

### Q3: Multi-agent obligation enforcement
**Research program:** The retrospective identified that when multiple agents run in parallel (6+ agents in the session-log audit), obligation enforcement at the individual agent level is lost — obligations are checked only at synthesis. Investigate whether each sub-agent should receive orientation obligations in its task spec, and whether the synthesizer should have its own obligations that include checking sub-agent compliance.
**Downstream decisions affected:** Whether the orchestrator generates per-agent task specs with obligation subsets, or a single task spec for a single auditor agent.
**Reversibility:** MEDIUM — adding per-agent obligation distribution later requires restructuring the orchestrator's task spec generation, but doesn't invalidate the single-agent path.

</questions>

<dependencies>
## Claim Dependencies

| Claim | Depends On | Vulnerability |
|-------|-----------|---------------|
| 3-axis model (decided) | User observation re: cross_model_review as delegation (evidenced) + deliberation analysis | LOW — the observation is correct and the retrospective validates the decomposition against all 13 sessions |
| Obligations model (decided) | Retrospective validation of 3 populations (evidenced) + deliberation design | LOW — empirically validated; the discuss-phase chain shows obligations would have caught failures templates missed |
| I1-I4 ground rules (decided) | REVIEW.md analysis (evidenced) | LOW — ground rules directly drawn from real investigation; discuss-phase chain validates I2/I3 specifically |
| Frame-reflexivity Rule 5 (decided) | REVIEW.md Part 4 argument (evidenced) + deliberation confirmation | MEDIUM — adding a rule to ALL types is a broad commitment from a narrow evidence base (one deliberation + one retrospective). If it becomes performative, it needs revision |
| Composition principle (assumed) | Deliberation hermeneutic argument (governing) | HIGH — untested. The first investigatory audits are the test. If agents can't engage with tension-naming, the principle needs algorithmic support or revision |
| Cross-model delegation (decided:scope) | WF-01 pull-forward (decided) + current fragile state (evidenced) | MEDIUM — scope is decided but mechanics are open. If cross-model dispatch can't be made reliable, it falls back to documented manual workflow |
| Chain integrity obligation (decided) | Retrospective Gap 4 analysis (evidenced) | LOW — the discuss-phase chain is direct evidence for this failure mode |
| Framework invisibility obligation (decided) | Retrospective Gap 7 + cross-project adoption audit evidence | LOW — the cross-project adoption audit independently demonstrated this need |
| gsdr-auditor model: sonnet (decided) | gsdr-context-checker precedent (evidenced) + cost/quality reasoning | LOW — trivially changeable in model-profiles.cjs |

</dependencies>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Primary authority — deliberations and REVIEW (v2 design source)

**These are the authoritative sources for the v2 audit formalization. Downstream agents should treat the deliberations as the primary ground truth for the 3-axis model, obligations paradigm, Rule 5, I1-I4, and the composition principle — NOT the reference files, which are partially superseded and will be rewritten by this phase.**

- `.planning/deliberations/audit-taxonomy-three-axis-obligations.md` — the main deliberation: 3-axis decomposition (subject × orientation × delegation), obligation sets per axis, composition principle, concrete examples, resolved/open questions, the aporia of epistemic humility. **Open — fed forward, not concluded.** Primary authority for taxonomy, obligations, and composition.
- `.planning/deliberations/audit-taxonomy-retrospective-analysis.md` — sonnet agent's retrospective applying the 3-axis model to 13 audit sessions. Validates the model empirically; identifies 7 numbered gaps. Of those 7: Gaps 4, 5, 7 become the three new obligations (chain integrity, dispatch hygiene, framework invisibility); Gap 6 is data hygiene; Gaps 1, 2, 3 are deferred or open. Confirms 3 populations: template (standard routine) / obligations (investigatory/exploratory) / multi-agent (not fully designed — see Q3). **Read the retrospective directly for gap accounting — do not rely on any summary count outside the retrospective itself.** <!-- corrected by context-checker 2026-04-10: prior summary "6 dissolve with formalization" was the discuss-phase agent's interpretation, not retrospective language, and contained an arithmetic inconsistency -->
- `.planning/phases/57.4-audit-skill-investigatory-type/pre-phase-archive/REVIEW.md` — philosophical deconstruction of audit taxonomy, investigatory type proposal, I1-I4 ground rules, frame-reflexivity (Rule 5) argument. Subjects its own proposals to the same critique it applies to the existing taxonomy. **Primary authority for Rule 5 and I1-I4.**
- `.planning/deliberations/forms-excess-and-framework-becoming.md` — governing constraint on how formal systems handle excess. **Authority for DC-5 (taxonomy openness) and G-6 (aporia of epistemic humility).** Also now carries the claim-type vocabulary concern surfaced during v2 discuss-phase (see "Later encounter" section).
- `.planning/deliberations/phase-scope-translation-loss-audit-capability-gap.md` — triggering deliberation: identified the operational gap (audit skill deferred) and the epistemic gap (investigatory stance missing) as two concerns held together.

### Reference files — partially superseded, to be rewritten by this phase

**These files are what the phase is rewriting. They are NOT authority for the v2 design. They contain surviving meta-rules (directory naming, rule-copying protocol, escape-hatch principle, core rules 1-4) that this phase preserves. Everything else in them has been superseded by the deliberations above.** Each file carries a supersession banner at the top pointing at the authoritative sources.

- `get-shit-done/references/audit-conventions.md` — **SUPERSEDED in part.** Section 1 (directory structure) survives. Base frontmatter fields in Section 2 (`date`, `scope`, Recommended and Optional) survive. Section 3 (flat 8-type taxonomy) is superseded by the 3-axis model. Section 4 (body templates) is superseded by the obligations paradigm for non-standard orientations. This phase will rewrite the superseded sections.
- `get-shit-done/references/audit-ground-rules.md` — **SUPERSEDED in part.** Core Rules 1-3 and Rule 4 (escape hatch) survive. Section 4 (meta-rule on task spec embedding) survives. The type-family rule sets (S1-S2, E1-E3, C1) and per-type selection logic are superseded by the obligations paradigm. This phase will add Rule 5 (frame-reflexivity), I1-I4 (investigatory ground rules), and the obligation composition logic, and will rewrite the type-family sections.
- `get-shit-done/references/claim-types.md` — **UNAFFECTED by this phase.** Typed claim vocabulary used in audit specs and agent outputs. Note: a concern about this vocabulary's adequacy for composition-principle claims was surfaced during v2 discuss-phase and captured in `forms-excess-and-framework-becoming.md` as a "Later encounter" section; no change is made here.

### Pre-phase archive (derivation chain)
- `.planning/phases/57.4-audit-skill-investigatory-type/pre-phase-archive/57.4-CONTEXT-v1.md` — archived v1 CONTEXT.md (pre-deliberation flat-type design). Shows what was decided before the restructuring.
- `.planning/phases/57.4-audit-skill-investigatory-type/pre-phase-archive/57.4-RESEARCH-v1.md` — archived v1 research. Command patterns and model tier analysis still valid; taxonomy sections stale.
- `.planning/phases/57.4-audit-skill-investigatory-type/pre-phase-archive/260409-pz6-CONTEXT.md` — operational design decisions from the quick task that was upgraded to this phase.

### Command patterns to follow
- `commands/gsd/audit-milestone.md` — "This command IS the orchestrator" pattern, spawns integration-checker agent
- `commands/gsd/deliberate.md` — context inference, mode detection, free-text arguments, --continue flag
- `commands/gsd/signal.md` — lightweight skill with argument parsing and KB integration
- `commands/gsd/validate-phase.md` — phase-targeting skill with state reading

### Agent patterns to follow
- `agents/gsdr-context-checker.md` — agent with explicit ground rules, reads reference docs, structured verification output
- `agents/gsdr-spike-runner.md` — receives fully-formed spec (DESIGN.md), executes phases, produces DECISION.md

### Additional evidence
- `.planning/phases/57.3-audit-workflow-infrastructure/57.3-CONTEXT.md` — full Phase 57.3 audit infrastructure design, failure patterns, and the rationale that the retrospective later found insufficient. **Read as historical context for what was superseded, not as current authority.**
- `.planning/audits/2026-04-09-discuss-phase-exploration-quality/` — gold-standard audit session (model for what good audit output looks like)
- `.planning/knowledge/signals/sig-2026-04-10-discuss-phase-authority-weighting-gap.md` — the signal documenting why this canonical_refs section was reordered post-context-checker

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `commands/gsd/audit-milestone.md` — command structure, frontmatter schema (name, description, argument-hint, allowed-tools), objective/execution_context/context/process sections
- `agents/gsdr-context-checker.md` — agent spec with embedded epistemic ground rules, structured tool access declaration
- `get-shit-done/bin/lib/model-profiles.cjs` — agent-to-model mapping; `gsdr-auditor` needs an entry

### Established Patterns
- Command frontmatter: `name: gsd:audit`, `description`, `argument-hint`, `allowed-tools` list
- Agent dispatch: command uses `Agent()` or `Task()` to spawn agent with prompt containing task spec
- Session state: `gsd-tools.cjs init phase-op` for phase context, `gsd-tools.cjs commit` for atomic commits
- Config reading: `gsd-tools.cjs config-get` for feature flags and settings

### Integration Points
- `get-shit-done/bin/lib/model-profiles.cjs` — needs `gsdr-auditor` entry
- `bin/install.js` — installer will pick up new files from `commands/gsd/` and `agents/` automatically
- `get-shit-done/references/audit-conventions.md` — needs full rewrite of Section 3 (taxonomy) and Section 4 (templates); base schema in Section 2 modified to replace `audit_type` with three axis fields
- `get-shit-done/references/audit-ground-rules.md` — needs Rule 5, I1-I4, three new obligations, composition principle, and rewrite of type-family sections

</code_context>

<files_affected>
## Files Affected by This Phase

Consolidated manifest the planner can work from. Scatters elsewhere in `<domain>`, `<canonical_refs>`, and `<code_context>` but gathered here to prevent omission.

### Files to CREATE (new)
- `commands/gsd/audit.md` — orchestrator command for `/gsdr:audit`. Frontmatter (`name: gsd:audit`, description, argument-hint, allowed-tools), argument parsing, context inference (read conversation history for classification clues), 3-axis classification (subject × orientation × delegation), obligation composition per classification, ground-rule set selection and copying into task spec, session directory creation (per audit-conventions.md Section 1 — survives), task spec writing, agent dispatch (self or cross-model per `audit_delegation`). Pattern: command-as-orchestrator per audit-milestone.md + spike-runner dispatch model.
- `agents/gsdr-auditor.md` — executor agent spec. Frontmatter (`model: sonnet`, color, tools: Read/Bash/Grep/Glob/Write), embedded epistemic stance (not just rule lists — the *why* of each rule, the hermeneutic character of the composition principle, the aporetic space of epistemic humility), structural handling of Rule 4 (escape hatch) and Rule 5 (frame-reflexivity) as named output sections, obligations-as-engagement (not sections-to-write) framing, the "What the Obligations Didn't Capture" section as non-optional, orchestrator-level escalation path for investigatory/epistemic audits warranting higher reasoning.
- `.claude/commands/gsdr/audit.md` + `.claude/agents/gsdr-auditor.md` — synced automatically by `bin/install.js` from the npm source; per CLAUDE.md dual-directory rule, edit the source files only.

### Files to REWRITE (partially superseded; banners currently in place)
- `get-shit-done/references/audit-conventions.md`:
  - **Rewrite:** Section 3 (flat 8-type taxonomy → 3-axis model with 9 subjects, 3 orientations, 2 delegation modes, epistemic profiles per subject), Section 4 (per-type body templates → obligations paradigm for non-standard orientations + suggested structures mode for standard), the `audit_type` frontmatter field in Section 2 (→ `audit_subject` + `audit_orientation` + `audit_delegation`, with `audit_type` retained as legacy field for backward compat + migration default).
  - **Preserve:** Section 1 (date-first directory convention), Section 2 base required fields (`date`, `scope`), Section 2 Recommended and Optional field sets, the escape-hatch principle (extended to the axes, not just subject values).
  - **Remove post-implementation:** the supersession banner currently at the top.
- `get-shit-done/references/audit-ground-rules.md`:
  - **Rewrite:** type-family extensions (S1-S2, E1-E3, C1) and per-type rule selection logic → obligation composition rules (subject contributes, orientation contributes, they compose into a flat list). Add Rule 5 (frame-reflexivity) as a core rule joining 1-4. Add I1-I4 (investigatory ground rules from REVIEW.md Part 3). Add the three new obligations from retrospective (chain integrity, dispatch hygiene, framework invisibility). Add the composition principle as a dedicated section with the hermeneutic character preserved (not algorithmic).
  - **Preserve:** Core Rules 1-3 (cite evidence, test disconfirmation, distinguish measure from measured), Rule 4 (escape hatch), Section 4 (rule-copying meta-rule: "the agent needs the rules in its context window, not a pointer to them").
  - **Remove post-implementation:** the supersession banner currently at the top.

### Files to MODIFY (targeted, not rewrites)
- `get-shit-done/bin/lib/model-profiles.cjs` — add `gsdr-auditor` entry with `model: sonnet` per gsdr-agent convention. Ghost entries for `gsd-ui-auditor` and `gsd-doc-verifier` (no agent specs) may need cleanup as part of this work — deferred to a separate decision per STATE.md todos, but if the gsdr-auditor entry sits next to them in the profiles file, consider at minimum adding a comment noting the ghost state.
- `.planning/REQUIREMENTS.md` — finalize new requirements for the 3-axis taxonomy, obligations paradigm, Rule 5, I1-I4, and the three new obligations. These were TBD in Phase 57.4's roadmap entry ("supersedes the v1 AUDIT-04 placeholder"). WF-01 requirement text (line 167) still names a standalone `/gsdr:cross-model-review` command and may need revision to reflect absorption into `/gsdr:audit` — flagged in ROADMAP.md Phase 62 note but not yet rewritten.
- `.planning/STATE.md` — standard per-phase execution updates during plan/execute (decision log entries, plan completion tracking, session continuity).
- `.planning/ROADMAP.md` — mark Phase 57.4 progress row complete after verification; remove the "Discussed" status value.

### Files NOT touched by this phase
- `get-shit-done/references/claim-types.md` — typed claim vocabulary; unaffected. A concern about its adequacy for composition-principle claims was surfaced during v2 discuss-phase and captured in `forms-excess-and-framework-becoming.md` as a "Later encounter" section; deferred.
- `commands/gsd/audit-milestone.md` — closest existing command pattern; referenced for structure but not modified. Future adoption of the 3-axis model by audit-milestone is deferred.
- Existing agents (`gsdr-context-checker`, `gsd-verifier`, `gsd-integration-checker`, `gsdr-spike-runner`, etc.) — not modified. Adoption of the new audit paradigm or frame-reflexivity by these agents is a separate concern (see `<deferred>`: "Frame-reflexivity for non-audit workflows").
- The audit session archive under `.planning/audits/` — existing artifacts are not migrated. Migration would backfill the three new axis fields from the legacy `audit_type` per the backward-compat mapping; that is a separate migration concern, not a 57.4 deliverable.

### Post-implementation cleanup (MUST include in plan)
- Remove the supersession banner from `get-shit-done/references/audit-conventions.md` after the rewrite lands
- Remove the supersession banner from `get-shit-done/references/audit-ground-rules.md` after the rewrite lands
- Run `node bin/install.js --local` to sync the banner-removed copies to `.claude/`
- Verify that `commands/gsd/audit.md` and `agents/gsdr-auditor.md` are synced into `.claude/` on first install after the source files land
- Update this `<files_affected>` section's "to CREATE" / "to REWRITE" entries to "DONE" markers post-verification (or archive the section into SUMMARY.md)

### Dual-directory reminder
Per `CLAUDE.md`: **edit the npm source directories** (`commands/`, `agents/`, `get-shit-done/`), **never** `.claude/` directly. The installer's `replacePathsInContent()` handles `~/.claude/get-shit-done/` → `./.claude/get-shit-done/` path translation during install. Run `node bin/install.js --local` after source edits to sync the runtime copies. v1.15 Phase 22 shipped without agent-protocol.md because someone edited `.claude/` directly; don't repeat it.

</files_affected>

<specifics>
## Specific Ideas

- [assumed:reasoned] The user typically invokes `/gsdr:audit` mid-situation after encountering something unexpected — the skill should read conversation history for context clues before asking questions <!-- typed by context-checker: behavioral assumption shaping type-inference design; no empirical source cited; reasonable from gold-standard audit session behavior but not tested -->
- The gold-standard audit session (`.planning/audits/2026-04-09-discuss-phase-exploration-quality/`) is the model for what good output looks like
- The deliberation's concrete examples section (8 examples spanning all 3 axis combinations) should inform the agent spec's understanding of obligation composition
- The retrospective's "three populations" finding should drive the orchestrator's complexity assessment: is this template territory (Population 1), obligations territory (Population 2), or multi-agent territory (Population 3)?
- User's key epistemic directions (verbatim): "cross_model_review doesn't seem like a type, that's more of a delegation"; "name the tension, name the demand of the situation"; "does our framework enable for a better response to whatever the situation was"; "whether our harness is capable of recognizing limits, practicing epistemic humility while at the same time not being performatively humble, which is an aporetic space to dwell in"
- The REVIEW.md note to "mark the remainder" — leave visible in the design the awareness that the design is partial — should inform the agent spec's tone and the excess section's framing

</specifics>

<deferred>
## Deferred Ideas

- **gsd-tools audit subcommand**: encode audit type taxonomy + ground rule sets as structured data in lib/audit.cjs so the command can call `gsd-tools init audit` instead of parsing reference docs directly. Deferred per trial-before-formalize — ship command+agent first, formalize tooling once usage patterns emerge. TODO in STATE.md.
- **Mid-audit orientation switching**: the deliberation notes that orientation is better understood as a trajectory (standard → investigatory as findings warrant). Complex and dependent on agent capability — defer to post-usage evaluation.
- **Frame-reflexivity for non-audit workflows**: REVIEW.md Part 5 suggests verifier, signal synthesizer, planner all have similar framing concerns. Design principle, not a Phase 57.4 deliverable.
- **Multi-agent obligation enforcement mechanism**: retrospective Gap 3 identifies the need but the mechanism (per-agent task specs, synthesizer obligation checking) requires the single-agent path to be proven first. Record as Q3 for post-implementation investigation.
- **Orientation escalation in frontmatter**: retrospective Gap 2 suggests `audit_orientation` should be the starting orientation, with a trajectory note in the output when it changed. Not frontmatter-level — handle in suggested output structures.
- **Pipeline enrichment step**: deferred from 57.2 to backlog 999.1. Would auto-classify audit artifacts during the discuss → plan pipeline.

</deferred>

---

*Phase: 57.4-audit-skill-investigatory-type*
*Context gathered: 2026-04-09*
*Iteration: v2 (post-deliberation)*
