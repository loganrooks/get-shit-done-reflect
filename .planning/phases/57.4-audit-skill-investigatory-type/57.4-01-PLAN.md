---
phase: 57.4-audit-skill-investigatory-type
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/references/audit-conventions.md
autonomous: true
resolves_signals:
  - sig-2026-04-10-discuss-phase-authority-weighting-gap

must_haves:
  truths:
    - "Section 3 presents the 3-axis taxonomy (9 subjects × 3 orientations × 2 delegation modes), not the flat 8-type enum"
    - "Each of the 9 subjects carries an epistemic profile (oriented_toward, assumes, might_miss)"
    - "Section 4 presents the obligations paradigm for non-standard orientations with suggested structures retained only for standard orientation"
    - "Section 2 required fields replace the single audit_type enum with audit_subject (optional for investigatory/exploratory), audit_orientation, audit_delegation"
    - "audit_type is retained as an Optional legacy field for backward compatibility only"
    - "Section 1 (directory structure), Section 2 base fields (date, scope, Recommended, Optional), Section 5 three-layer model (with obligations wording), and Section 6 survive semantically unchanged"
    - "Every new authority claim cites deliberations (audit-taxonomy-three-axis-obligations.md, audit-taxonomy-retrospective-analysis.md, REVIEW.md, forms-excess-and-framework-becoming.md) — NOT pre-rewrite Section 3 or Section 4 of this file"
    - "The supersession banner remains at the top of the file at the end of this plan (removal is Plan 06)"
  artifacts:
    - path: "get-shit-done/references/audit-conventions.md"
      provides: "v2 audit conventions reference — 3-axis taxonomy, obligations paradigm, v2 frontmatter schema, surviving meta-rules preserved"
      contains: "audit_subject"
  key_links:
    - from: "Section 3 (3-axis taxonomy)"
      to: ".planning/deliberations/audit-taxonomy-three-axis-obligations.md"
      via: "citation as primary authority"
      pattern: "audit-taxonomy-three-axis-obligations"
    - from: "Section 2 frontmatter schema"
      to: "DC-4 three-axis field definitions from CONTEXT.md and deliberation Axis 1/2/3 sections"
      via: "field definitions match DC-4 verbatim and the deliberation axis tables"
      pattern: "audit_subject|audit_orientation|audit_delegation"
    - from: "Section 3.4 escape hatch (axis-level openness)"
      to: ".planning/deliberations/forms-excess-and-framework-becoming.md"
      via: "governing citation for taxonomy openness (DC-5, G-6)"
      pattern: "forms-excess-and-framework-becoming"
---

<objective>
Rewrite `get-shit-done/references/audit-conventions.md` to encode the v2 3-axis taxonomy (subject × orientation × delegation), the obligations paradigm, and the v2 frontmatter schema. Preserve the meta-rules named as surviving by the supersession banner: directory convention, base frontmatter fields, escape-hatch principle, three-layer model, inter-location relationships.

This is a **primary deliverable** of Phase 57.4 — not polishing, not cleanup. The reference file is being reconstructed to match what the deliberations already decided. The superseded sections will be replaced in full; the surviving sections will be touched only where the new obligations wording requires a minor edit (Section 5 Layer 2 prose).

Purpose: The v1 flat 8-type taxonomy conflated three orthogonal concerns (what is being audited, from what stance, who does it) under one enum. The template paradigm in Section 4 cannot compose obligations across those axes. The 3-axis deliberation restructured the ontology and replaced templates with composable obligations for non-standard orientations. The retrospective validated this against all 13 audit sessions. This plan translates that reconstruction into the reference artifact that consuming workflows and agents read.

Output: A rewritten `audit-conventions.md` that the `/gsdr:audit` command (Plan 03), the `gsdr-auditor` agent spec (Plan 04), and any future workflow adoption can cite as v2 authority. The supersession banner STAYS IN PLACE at the end of this plan — Plan 06 removes it only after the rewrites have been verified end-to-end.

**Authority direction (NON-NEGOTIABLE):** Deliberations → this rewrite. NOT pre-rewrite Section 3 / Section 4 / audit_type enum → this rewrite. Every claim in the new Section 3 and Section 4 must trace back to a deliberation, to REVIEW.md, or to the retrospective — never to a section of this file that is itself being rewritten. This is the exact failure mode `sig-2026-04-10-discuss-phase-authority-weighting-gap.md` documents.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57.4-audit-skill-investigatory-type/57.4-CONTEXT.md
@.planning/phases/57.4-audit-skill-investigatory-type/57.4-RESEARCH.md

**Primary authority for the rewrite — read these before writing:**
@.planning/deliberations/audit-taxonomy-three-axis-obligations.md
@.planning/deliberations/audit-taxonomy-retrospective-analysis.md
@.planning/phases/57.4-audit-skill-investigatory-type/pre-phase-archive/REVIEW.md
@.planning/deliberations/forms-excess-and-framework-becoming.md

**File being rewritten (read ONLY the sections named as surviving by the supersession banner at the top — Section 1, Section 2 base/Recommended/Optional fields, Section 5 three-layer frame, Section 6):**
@get-shit-done/references/audit-conventions.md

**Canonical excerpts — prefer lifting from RESEARCH.md §"Canonical Excerpts from the Deliberations"** rather than re-reading the deliberations for axis tables and obligation text. The excerpts are verbatim, line-sourced, and the research was unusually thorough on this point.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Section 3 as "Audit Taxonomy: Three Orthogonal Axes"</name>
  <files>get-shit-done/references/audit-conventions.md</files>
  <action>
Replace the current Section 3 (flat 8-type taxonomy enum at lines ~162-198) with a new Section 3 titled "Audit Taxonomy: Three Orthogonal Axes". The new section must encode:

**Preamble (2-4 short paragraphs):**
- Name what the v1 flat taxonomy got wrong: it conflated three orthogonal concerns (what is being audited, from what stance, who does it) under a single enum. Cite the deliberation (`audit-taxonomy-three-axis-obligations.md`), NOT the pre-rewrite Section 3 of this file.
- Name the 3-axis decomposition as the reconstructive response: subject × orientation × delegation, with obligations composing across the axes.
- State that subject is optional when orientation is `investigatory` or `exploratory` — the investigation discovers its subject. Cite the Phase 57 investigatory case from the deliberation.
- State that the axes themselves are provisional per DC-5 / G-6: the framework may need a fourth axis. Cite `forms-excess-and-framework-becoming.md` as governing authority.

**Subsection 3.1: Axis 1 — Subject (what are you auditing?)**
- Lift the 9-subject table from RESEARCH.md §"Axis 1: Subject — 9 subject types with epistemic profiles" (sourced to deliberation lines 49-82). Columns: Subject | Family | What it examines. Rows: `phase_verification`, `milestone`, `codebase_forensics`, `requirements_review`, `comparative_quality`, `claim_integrity`, `adoption_compliance`, `process_review` (NEW), `artifact_analysis` (NEW).
- Immediately below, lift the epistemic profile table (deliberation lines 69-79) verbatim: Subject | Oriented toward | Assumes | Might miss. All 9 rows present.
- Note that `process_review` and `artifact_analysis` are NEW subjects discovered by testing the decomposition against real audit cases.
- Note that `cross_model_review` has EXITED the subject enum because it was always a delegation strategy — cite the user's observation verbatim where possible ("cross_model_review doesn't seem like a type, that's more of a delegation") if you can source it from CONTEXT.md `<specifics>` section; otherwise paraphrase.
- State: "Subject is optional for `investigatory` and `exploratory` orientations."

**Subsection 3.2: Axis 2 — Orientation (from what stance?)**
- Lift the 3-row orientation table from RESEARCH.md §"Axis 2: Orientation" (deliberation lines 83-92). Columns: Orientation | When | Key characteristic. Rows: `standard` (default — close on findings), `investigatory` (something went wrong — hold diagnosis open, competing explanations), `exploratory` (a question beckons — follow it, don't force closure).
- State: orientation is orthogonal to subject — any orientation can run against any subject (or no subject).
- State (per CONTEXT.md G-5 and the deliberation): `investigatory` is occasioned by breakdown (expectations violated); `exploratory` is occasioned by possibility (a question opens). Both are hermeneutically circular. The distinction is one of occasion, not ontology — their body structures differ because they start from different places (discrepancy vs. question).
- Mention that the relationship between investigatory and exploratory is deliberately left as a design choice, not ontologically settled (G-5). They may merge, stay distinct, or evolve differently.

**Subsection 3.3: Axis 3 — Delegation (who does it?)**
- Lift the 2-row delegation table from RESEARCH.md §"Axis 3: Delegation" (deliberation lines 93-100). Columns: Delegation | What it means. Rows: `self` (default — local gsdr-auditor agent), `cross_model:{model_id}` (dispatch to another model's CLI).
- State: delegation is orthogonal to both subject and orientation.
- State: cross-model dispatch is currently fragile (per the deliberation, line 100 — "environment setup issues, agents finishing early, instructions not properly conveyed, lots of hand-holding"). Do not hide this — it is factual and relevant for auditors and orchestrators reading this reference.

**Subsection 3.4: The escape hatch (preserved and extended)**
- `audit_subject: exploratory` remains the legitimate escape hatch on the subject axis for audits that don't match any named subject (note: this phrasing is a slight shift from v1 since `exploratory` is now primarily an orientation — the escape hatch on the subject axis is a separate concept that the new framing must handle cleanly; a possible cleaner framing is to say "subjects can be omitted altogether when no named subject fits, especially under investigatory or exploratory orientation" — use your judgment but keep the openness concrete).
- Per DC-5, the escape-hatch principle is now extended to the axes themselves — not only to subject values. A fourth axis may be needed if the aporetic character of audit situations makes the current decomposition insufficient.
- Cite `forms-excess-and-framework-becoming.md` as governing authority for openness: "a formal system encounters something that exceeds its categories, and the system's response to the excess reveals something about the system itself." The 3-axis model itself is subject to this.

**Tone directive:** Write as prose that flows, not bullet lists alone. The 3-axis model is a hermeneutic reconstruction, not a schema. A reader of the new Section 3 should feel that it is describing a stance toward auditing, not just categorizing audits.

**Prohibitions:**
- Do NOT cite the pre-rewrite Section 3 or Section 4 of this file as authority for any v2 claim.
- Do NOT retain the "8 named types" language.
- Do NOT retain the `cross_model_review` subject row (it moved to Axis 3 as a delegation mode).
- Do NOT retain `structural/epistemic/compliance` family groupings as authoritative classifications — you may mention the family column (`Family` in the 9-subject table) but only as a label attached to each subject for historical continuity, not as a typology.
- Do NOT remove the supersession banner at the top of the file — Plan 06 removes it.

**Second-order checks (per sig-2026-03-01-plan-checker-misses-second-order-effects):**
- Verify no new cross-references from Section 3 point at the pre-rewrite Section 4 (templates) as authority. Section 4 is being rewritten in Task 2.
- Verify that the `audit_subject` name used here matches exactly what will appear in the Section 2 frontmatter schema (Task 3 makes Section 2 consistent).
  </action>
  <verify>
- `grep -n "audit-taxonomy-three-axis-obligations" get-shit-done/references/audit-conventions.md` — must have ≥2 matches in the new Section 3
- `grep -n "process_review" get-shit-done/references/audit-conventions.md` — must match in the new Axis 1 table as a NEW subject
- `grep -n "artifact_analysis" get-shit-done/references/audit-conventions.md` — must match in the new Axis 1 table as a NEW subject
- `grep -n "cross_model_review" get-shit-done/references/audit-conventions.md` — if it appears, must be flagged as "removed from subject enum" or historical context, NOT as a current subject row
- `grep -c "^### Axis" get-shit-done/references/audit-conventions.md` — must be ≥3 (Axis 1, 2, 3 subsection headers)
- Read the new Section 3 top-to-bottom as a standalone section: does it read as a stance toward auditing, not a classification schema?
- Second-order: `grep -nE "per audit-conventions.md Section (3|4)" get-shit-done/references/audit-conventions.md` — must return ZERO results (self-citation of superseded sections is the exact failure mode sig-2026-04-10 documents)
- Second-order: verify the Section 3 frontmatter field names (`audit_subject`, `audit_orientation`, `audit_delegation`) will exactly match what Task 3 puts in Section 2 — if Task 2 is already done, cross-check; otherwise note for Task 3
  </verify>
  <done>
Section 3 of `audit-conventions.md` contains: a reconstructive preamble; Axis 1 (9 subjects with family column + epistemic profile table); Axis 2 (3 orientations with the investigatory/exploratory occasion distinction); Axis 3 (2 delegation modes with fragility noted); Section 3.4 the axis-level escape hatch citing forms-excess-and-framework-becoming.md. Pre-rewrite Section 3 (flat 8-type enum) is replaced. Supersession banner still at top of file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Section 4 as "Output Paradigm: Obligations and Templates"</name>
  <files>get-shit-done/references/audit-conventions.md</files>
  <action>
Replace the current Section 4 (type-aware body templates, lines ~201-318) with a new Section 4 titled "Output Paradigm: Obligations and Templates".

**Subsection 4.1: Two populations (one page of prose)**
- Cite the retrospective (`audit-taxonomy-retrospective-analysis.md`) as authority: it confirmed three populations, but only two produce artifacts addressed at this reference-file level (Population 3, multi-agent, is deferred per CONTEXT.md Q3).
- Population 1 — Standard routine audits. Templates remain as scaffolding because consistency and scannability matter. VERIFICATION.md and MILESTONE-AUDIT.md are the canonical examples.
- Population 2 — Investigatory, exploratory, and complex audits. Obligations replace templates because flexibility matters and templates cannot compose across axes. The deliberation's concrete examples (8 examples in `audit-taxonomy-three-axis-obligations.md` lines 197-265 — summarized in RESEARCH.md §"The 8 concrete examples") show what composition looks like.
- The complexity assessment (template territory vs obligations territory) is performed at dispatch time by the orchestrator (`/gsdr:audit` command, Plan 03), not here. This reference file describes the paradigm; the command chooses for each situation.

**Subsection 4.2: Obligations vs. templates — the difference that matters**
- Write this as a hermeneutic distinction, not a mechanical one. Follow the deliberation's framing (lines 111-125):
  - Templates say: "Write a Competing Explanations section." The auditor fills the section.
  - Obligations say: "You must present competing explanations." The auditor weaves competing explanations into the narrative wherever they emerge naturally.
- State: task specs under the obligations paradigm say "address these obligations" — not "fill these sections."
- State: this is the distinction that lets obligations compose across axes. Two templates cannot merge; two obligation sets can.
- State: the "What the Obligations Didn't Capture" section is **mandatory in every audit output** under both paradigms — a named section where the auditor records findings that don't fit any obligation. If this section is consistently substantial for a particular kind of audit, that is evidence for a new subject type or new obligations. Cite deliberation lines 174-176.

**Subsection 4.3: Suggested scaffolding for standard-orientation audits**
- Subject-specific scaffolds remain available as suggestions (NOT mandates) for standard-orientation audits only. Preserve the intent of the v1 body templates but reframe them as suggestions.
- Lift the three v1 scaffolds (structural / epistemic / compliance) from v1 Section 4.1-4.3 and reattribute them by subject (not family). Mark each clearly as: "Suggested structure for standard-orientation audits only. Investigatory and exploratory orientations use obligations, not templates."
- Map v1 family scaffolds to subjects:
  - v1 structural (phase_verification, milestone, codebase_forensics) → keep as suggested scaffold for those three subjects
  - v1 epistemic (requirements_review, comparative_quality, claim_integrity — and NOT cross_model_review which exited) → keep as suggested scaffold for those three subjects
  - v1 compliance (adoption_compliance) → keep as suggested scaffold
- For the two NEW subjects (`process_review` and `artifact_analysis`): leave scaffolding unspecified with a note that "scaffolds will emerge from practice" — per Claude's Discretion in CONTEXT.md. Do NOT invent scaffolds for NEW subjects absent any usage data.
- The v1 exploratory template (Section 4.4) does NOT survive as a template, because `exploratory` is now an orientation that uses obligations. Delete it. If a note is useful, say: "The v1 exploratory body template has been absorbed into the exploratory orientation obligations (see `audit-ground-rules.md` — rewritten by Phase 57.4 Plan 02)."

**Subsection 4.4: Obligation sets live in `audit-ground-rules.md`**
- Point at `audit-ground-rules.md` (rewritten by Plan 02) as the home for the actual obligation text: core obligations (Rules 1-5), orientation obligations (standard, investigatory I1-I4, exploratory), subject obligations (9-row table), and the three cross-cutting obligations (chain integrity, dispatch hygiene, framework invisibility).
- The composition principle also lives in `audit-ground-rules.md`, not here. This file describes *that* obligations compose; it does not describe *how*.

**Tone directive:** Populations 1 and 2 are a descriptive framing, not a dispatch rule. If an audit demands a mix, say so — the orchestrator composes obligations and the auditor navigates the composition. Avoid language that makes the population framing sound like a gate.

**Prohibitions:**
- Do NOT retain the `core+structural`, `core+epistemic`, `core+compliance` selection logic from v1 Section 4 — that is a rule-set selection idiom tied to the flat taxonomy and is superseded.
- Do NOT write pseudocode for "when to use templates vs obligations" — the orchestrator decides, not this reference.
- Do NOT remove the supersession banner at the top of the file.

**Second-order checks:**
- Cross-reference consistency: `grep -n "audit-ground-rules.md"` — new Section 4 must point at it (Plan 02 is rewriting that file in parallel; the cross-reference will land correctly).
- Verify the v1 exploratory body template is removed (not just reframed): `grep -n "Question or Motivation"` in the file — this heading from v1 Section 4.4 should NOT appear in the new Section 4.
  </action>
  <verify>
- `grep -n "What the Obligations Didn't Capture" get-shit-done/references/audit-conventions.md` — must appear at least once in the new Section 4
- `grep -n "Population 1" get-shit-done/references/audit-conventions.md` — must appear in Section 4.1
- `grep -n "Population 2" get-shit-done/references/audit-conventions.md` — must appear in Section 4.1
- `grep -n "audit-ground-rules.md" get-shit-done/references/audit-conventions.md` — must appear as cross-reference from the new Section 4
- `grep -n "Question or Motivation" get-shit-done/references/audit-conventions.md` — must NOT appear (v1 exploratory template heading removed)
- `grep -nE "core\+(structural|epistemic|compliance)" get-shit-done/references/audit-conventions.md` — if any match, it must be in a "legacy v1 label — no longer current" context only, NOT as a current ground_rules value
- Read Section 4 top-to-bottom: does the obligations-vs-templates distinction land as hermeneutic, not mechanical?
- Second-order: verify Section 4 does not cite pre-rewrite Section 4 of this file as authority
  </verify>
  <done>
Section 4 now presents two populations (template scaffolding for standard; obligations for everything else), the hermeneutic distinction between obligations and templates, suggested scaffolds retained as optional for the 7 v1 subjects that map to standard orientation, NEW subjects left without scaffolds, and a cross-reference to `audit-ground-rules.md` for actual obligation text. V1 exploratory template is removed. Supersession banner still at top.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Section 2 frontmatter schema + touch Section 5 for obligations wording</name>
  <files>get-shit-done/references/audit-conventions.md</files>
  <action>
**Part A — Update Section 2 (Frontmatter Schema) to encode the v2 3-axis fields.**

Rewrite the Required fields table to replace `audit_type` with three axis fields per DC-4:

| Field | Type | Description |
|-------|------|-------------|
| `date` | YYYY-MM-DD | Date the audit was conducted (NOT the migration date) |
| `audit_subject` | enum \| omitted | One of 9 subjects per Section 3.1. **Optional** for `investigatory` / `exploratory` orientations when the investigation has not yet identified a subject. |
| `audit_orientation` | enum | `standard` (default) \| `investigatory` \| `exploratory` — per Section 3.2 |
| `audit_delegation` | enum | `self` (default) \| `cross_model:{model_id}` — per Section 3.3 |
| `scope` | string | What was audited — concise description of the target |

Keep the Recommended and Optional field tables semantically unchanged (these are the surviving Section 2 content per the banner). Update only the `ground_rules` field description: old values `core+structural` / `core+epistemic` / `core+compliance` are NO LONGER accurate under the obligations paradigm. Reframe `ground_rules` as a free-form label naming the composition (e.g., `core+investigatory+process_review`, `core+standard+phase_verification`, `core+exploratory` when subject is omitted). Note: this is a semantic change to a surviving field; flag it clearly in the rewrite ("the field survives; its value grammar changes under the obligations paradigm"). If the value grammar change feels like a larger rewrite than the banner permits, document that in the summary — do not silently expand scope.

Add a new Optional field `audit_type` marked **Legacy (v1 compatibility)**:

| Field | Type | Description |
|-------|------|-------------|
| `audit_type` | enum | **Legacy (v1)** — retained for backward compatibility with pre-57.4 artifacts. Migration rule: maps to `audit_subject` with `audit_orientation: standard` and `audit_delegation: self` as defaults. New audits SHOULD populate the three v2 fields; they MAY also populate `audit_type` for compatibility. |

Update the "Field count rationale" paragraph: total = 5 required (was 3) + 5 recommended + 5 optional (was 4; `audit_type` joined) = **15 fields** (was 12). This bumps the count above the signal KB schema's ~15 — per RESEARCH.md Pitfall 6, flag this in the rationale with the exact sentence: "This phase ships at the signal KB schema budget. Any further frontmatter additions require deliberation." If migration eventually removes `audit_type`, the count drops back to 14. Document this trajectory in the rationale.

Update the three Example frontmatters:
- **Minimal example:** show a bare v2 audit — `date`, `audit_subject`, `audit_orientation`, `audit_delegation`, `scope`.
- **Full session example:** show a composed obligations label in `ground_rules`, all three v2 axis fields populated, subject-orientation mix (e.g., `process_review × investigatory × self`).
- **Migrated example:** show both the legacy `audit_type` and the new axis fields coexisting for a migrated artifact (demonstrates backward compat).

**Part B — Touch Section 5 (Three-Layer Model) for obligations wording.**

Section 5 survives semantically per the banner — the three layers still hold. Minor edit only: in Layer 2 prose, replace "type-aware body templates" with "obligations (non-standard) + suggested scaffolds (standard)" or similar language that reflects the new paradigm. Do NOT rewrite Section 5 structurally — it is a surviving section. This is a one-word-level edit, not a rewrite. If the minimal edit doesn't flow, write a single new sentence clarifying that Layer 2 is now populated by obligations for non-standard orientations and scaffolds for standard.

**Do NOT touch:** Section 1 (directory structure), Section 2 Recommended and Optional field tables (other than the `ground_rules` semantic note and the new `audit_type` Legacy row), Section 6 (Relationship to Other Audit Locations), the design authority list at the bottom of the file.

**Do NOT remove the supersession banner.**

**Second-order checks:**
- The `audit_subject` / `audit_orientation` / `audit_delegation` field names must match exactly what Task 1 used in Section 3 prose. Verify with grep.
- The `ground_rules` field value grammar change is the most fragile touch in this task. If in doubt, leave the field definition as-is and add a "(value grammar under revision — see Section 4)" marker instead of attempting a full reframe. The goal is to not introduce a silent contradiction between this field and the actual usage the `/gsdr:audit` command (Plan 03) will emit.
- Verify no tool or script in the codebase depends on `audit_type` being required. Run: `grep -rn "audit_type" get-shit-done/bin/ 2>/dev/null | head -20` — report any matches in the summary. If none, the migration path is clean.
  </action>
  <verify>
- `grep -c "audit_subject" get-shit-done/references/audit-conventions.md` — must be ≥4 (definition in Section 2, mention in Section 3, minimal example, full example)
- `grep -c "audit_orientation" get-shit-done/references/audit-conventions.md` — must be ≥4
- `grep -c "audit_delegation" get-shit-done/references/audit-conventions.md` — must be ≥4
- `grep -n "Legacy (v1)" get-shit-done/references/audit-conventions.md` — must match the new `audit_type` Optional row
- `grep -n "audit_type" get-shit-done/references/audit-conventions.md` — must appear ONLY in (a) the Optional Legacy row, (b) the migrated example, (c) explicit "v1 legacy" historical mentions. Must NOT appear as a Required field.
- Read Section 1 (Directory Structure) — must be byte-for-byte unchanged (no accidental edits during the rewrite)
- Read Section 5 — Layer 2 prose must reference obligations/scaffolds rather than type-aware templates
- Second-order: run `grep -rn "audit_type" get-shit-done/bin/ 2>/dev/null` and confirm no tooling depends on audit_type as a required field. Record findings in the SUMMARY.md.
- `grep -n "^## " get-shit-done/references/audit-conventions.md` — must still have Section 1, 2, 3, 4, 5, 6 headers (structure intact)
- Supersession banner still at top of file: `head -20 get-shit-done/references/audit-conventions.md` — banner text present
  </verify>
  <done>
Section 2 required fields are `date`, `audit_subject` (optional for investigatory/exploratory), `audit_orientation`, `audit_delegation`, `scope`. `audit_type` is Optional Legacy. Field count rationale updated to 15 with pitfall-6 ceiling noted. Three examples updated. Section 5 Layer 2 wording updated minimally. Sections 1, 6 untouched. Supersession banner preserved.
  </done>
</task>

</tasks>

<verification>
**End-of-plan verification (run as a single pass after all 3 tasks):**

1. `grep -c "audit-taxonomy-three-axis-obligations" get-shit-done/references/audit-conventions.md` — must be ≥3 (at least once per rewritten section: preamble 3, prose 3.1-3.3, Section 4 cross-reference)
2. `grep -c "audit_subject\|audit_orientation\|audit_delegation" get-shit-done/references/audit-conventions.md` — must be ≥12 (3 fields × ≥4 mentions each: definition, axis table, minimal example, full example)
3. `grep -c "^## " get-shit-done/references/audit-conventions.md` — must be ≥6 (Sections 1-6 all still present)
4. `grep -n "SUPERSEDED" get-shit-done/references/audit-conventions.md | head -3` — supersession banner still present at lines 1-20
5. Read Section 1 (Directory Structure) — byte-for-byte unchanged except whitespace/lint
6. Read Section 5 — Layer 2 prose uses obligations/scaffolds wording
7. `grep -nE "per audit-conventions.md Section (3|4)" get-shit-done/references/audit-conventions.md` — must return ZERO results (self-citation failure mode)
8. `grep -nE "\bcross_model_review\b" get-shit-done/references/audit-conventions.md` — if any match, must be historical context only
9. Second-order: run `grep -rn "audit_type" get-shit-done/bin/ 2>/dev/null | head -20` — record any tooling dependencies on audit_type in SUMMARY.md
10. Second-order: run `grep -rn "audit_type" tests/ 2>/dev/null | head -20` — record any test dependencies on audit_type in SUMMARY.md
11. **Gsd-tools check (per sig-2026-03-01-plan-checker-misses-tool-api-assumptions):** No gsd-tools subcommands are invoked in this plan's tasks — this is pure markdown rewrite. Confirmed safe.
12. The file should still parse as valid Markdown — verify by reading first 20 and last 20 lines: no orphan code blocks, no unclosed YAML, no truncated headers.
</verification>

<success_criteria>
- `audit-conventions.md` Section 3 encodes the 3-axis taxonomy (9 subjects with epistemic profile table, 3 orientations, 2 delegation modes, escape hatch extended to axes per DC-5)
- Section 4 presents the obligations paradigm with Population 1 / Population 2 framing and the hermeneutic obligations-vs-templates distinction
- Section 2 required fields are `date`, `audit_subject` (optional for investigatory/exploratory), `audit_orientation`, `audit_delegation`, `scope`; `audit_type` is Optional Legacy
- All claims in new Section 3 and Section 4 cite deliberations/REVIEW.md/retrospective/forms-excess, NOT the pre-rewrite file sections
- Section 1 (directory structure), Section 5 (three-layer model — with Layer 2 obligations wording), Section 6 (inter-location relationships) survive
- Supersession banner still at top of file (Plan 06 removes it)
- Any downstream tooling dependencies on `audit_type` flagged in SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/57.4-audit-skill-investigatory-type/57.4-01-SUMMARY.md` with:
- Which sections were rewritten (3, 4, 2 frontmatter, 5 Layer 2 wording only)
- Which sections were preserved (1, 2 Recommended/Optional, 5 structure, 6)
- Authority trace: each major new claim → which deliberation and line number
- Line count before/after (the file should remain roughly the same length — baseline ~365 lines)
- Confirmation that the supersession banner is still at the top of the file
- Any tooling dependencies on `audit_type` discovered during Task 3's second-order check
- Any scope-ambiguity encounters where the rewrite had to choose between minimal edit and larger change (especially in Section 2 `ground_rules` value grammar and Section 5 Layer 2 wording)
</output>
