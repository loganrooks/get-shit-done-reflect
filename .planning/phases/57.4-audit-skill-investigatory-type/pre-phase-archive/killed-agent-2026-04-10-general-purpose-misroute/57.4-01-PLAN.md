---
phase: 57.4-audit-skill-investigatory-type
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/references/audit-conventions.md
autonomous: true
resolves_signals:
  - sig-2026-04-10-discuss-phase-authority-weighting-gap

must_haves:
  truths:
    - "Section 3 presents the 3-axis taxonomy (9 subjects x 3 orientations x 2 delegation modes), not the flat 8-type enum"
    - "Section 4 presents the obligations paradigm for non-standard orientations and templates-as-scaffolding for standard"
    - "Section 2 required fields replace the single audit_type enum with audit_subject, audit_orientation, audit_delegation (legacy audit_type retained)"
    - "Section 1 (directory structure), Section 2 base fields (date, scope, Recommended, Optional), and the escape-hatch principle survive semantically unchanged"
    - "Authority citations in the new sections point at the deliberations, not at other parts of this reference file"
    - "The supersession banner remains at the top of the file at the end of this plan (removal is scheduled for Plan 07 after the rewrite has been verified)"
  artifacts:
    - path: "get-shit-done/references/audit-conventions.md"
      provides: "Phase 57.4 v2 audit conventions reference (3-axis taxonomy, obligations paradigm, v2 frontmatter)"
      contains: "audit_subject"
  key_links:
    - from: "Section 3 (3-axis taxonomy)"
      to: ".planning/deliberations/audit-taxonomy-three-axis-obligations.md"
      via: "citation as primary authority"
      pattern: "audit-taxonomy-three-axis-obligations"
    - from: "Section 2 frontmatter schema"
      to: "DC-4 three-axis field definitions from CONTEXT.md"
      via: "field definitions match DC-4 verbatim"
      pattern: "audit_subject|audit_orientation|audit_delegation"
---

<objective>
Rewrite `get-shit-done/references/audit-conventions.md` to encode the v2 3-axis taxonomy, the obligations paradigm, and the v2 frontmatter schema. Preserve the meta-rules named as surviving by the supersession banner (directory convention, base fields, escape-hatch principle, three-layer model).

This is a **primary deliverable** of Phase 57.4, not polishing. The reference file is being reconstructed to match what the deliberations already decided.

Purpose: The v1 flat taxonomy and per-type templates cannot express the complexities of the auditing situation. The 3-axis deliberation restructured the ontology (subject x orientation x delegation) and replaced templates with composable obligations for non-standard orientations. This plan translates that reconstruction into the reference artifact that consuming workflows and agents read.

Output: A rewritten `audit-conventions.md` that any downstream workflow (`/gsdr:audit`, gsdr-auditor agent, future audit-milestone adoption) can cite as v2 authority. The supersession banner stays in place at end of this plan — it will be removed by Plan 07 after the rewrite has been verified end-to-end.

**Authority direction (NON-NEGOTIABLE):** Deliberations -> this rewrite. NOT the v1 sections of this file -> this rewrite. Every claim in the new Section 3 and Section 4 must trace back to the deliberations, not to Section 3 or Section 4 of the pre-rewrite file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57.4-audit-skill-investigatory-type/57.4-CONTEXT.md
@.planning/phases/57.4-audit-skill-investigatory-type/57.4-RESEARCH.md

**Primary authority for the rewrite (deliberations — read these verbatim before writing):**
@.planning/deliberations/audit-taxonomy-three-axis-obligations.md
@.planning/deliberations/audit-taxonomy-retrospective-analysis.md
@.planning/phases/57.4-audit-skill-investigatory-type/pre-phase-archive/REVIEW.md
@.planning/deliberations/forms-excess-and-framework-becoming.md

**File being rewritten (read ONLY the sections named as surviving by the banner):**
@get-shit-done/references/audit-conventions.md

**Canonical excerpts to lift directly** — prefer lifting from RESEARCH.md §"Canonical Excerpts from the Deliberations" rather than re-reading the deliberations for tables. The excerpts are verbatim and line-sourced.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Section 3 (Audit Type Taxonomy) as the 3-axis taxonomy</name>
  <files>get-shit-done/references/audit-conventions.md</files>
  <action>
Replace the current Section 3 (flat 8-type taxonomy enum, lines ~162–198) with a rewritten Section 3 titled "Audit Taxonomy: Three Orthogonal Axes". The new section must encode:

**Preamble** (short, 2–4 paragraphs):
- Name what the v1 flat taxonomy got wrong (conflated three orthogonal concerns: what, from-what-stance, who) — cite the deliberation, not the v1 Section 3
- Name the 3-axis decomposition as the reconstructive response (subject x orientation x delegation)
- State that subject is optional when orientation is `investigatory` or `exploratory` — the investigation discovers its subject
- State that the axes themselves are provisional per DC-5 / G-6 (open to a fourth axis) — cite `forms-excess-and-framework-becoming.md`

**Subsection 3.1: Axis 1 — Subject (what are you auditing?)**
- Lift the 9-subject table from RESEARCH.md §"Axis 1: Subject — 9 subject types with epistemic profiles" (which is sourced to deliberation lines 49–82). The table columns: Subject | Family | What it examines
- Immediately below, lift the epistemic profile table (deliberation lines 69–79) verbatim: Subject | Oriented toward | Assumes | Might miss
- Include: `process_review` and `artifact_analysis` are NEW subjects; `cross_model_review` has EXITED the subject enum because it was always a delegation strategy
- State "Subject is optional for `investigatory` and `exploratory` orientations"

**Subsection 3.2: Axis 2 — Orientation (from what stance?)**
- Lift the 3-row orientation table from RESEARCH.md §"Axis 2: Orientation" (deliberation lines 83–92): Orientation | When | Key characteristic — with rows `standard`, `investigatory`, `exploratory`
- State: orientation is orthogonal to subject — any orientation can run against any subject (or no subject)
- State: `investigatory` is occasioned by breakdown; `exploratory` is occasioned by possibility; both are hermeneutically circular; the distinction is one of occasion, not ontology (per CONTEXT.md G-5 and the deliberation)

**Subsection 3.3: Axis 3 — Delegation (who does it?)**
- Lift the 2-row delegation table from RESEARCH.md §"Axis 3: Delegation" (deliberation lines 93–100): Delegation | What it means — rows `self` and `cross_model:{model_id}`
- State: delegation is orthogonal to both subject and orientation
- State: cross-model dispatch is currently fragile (per the deliberation, line 100) — this is factual; do not hide it

**Subsection 3.4: The escape hatch (preserved and extended)**
- `audit_subject: exploratory` remains the legitimate escape hatch on the subject axis (per surviving Section 3 text: "enum with escape hatch")
- Per DC-5, the escape-hatch principle is now extended to the axes themselves — not only to subject values. A fourth axis may be needed if the aporetic character of audit situations makes the current decomposition insufficient
- Cite `forms-excess-and-framework-becoming.md` as governing authority for openness

Do NOT: cite the v1 Section 3 of this file as authority for any v2 claim. Do NOT retain the "8 named types" language, the structural/epistemic/compliance family groupings as authoritative (they are mentioned historically at most), or the `cross_model_review` subject row.

Verification mentions: Keep the old Section 3 header intact for consistency; write prose that flows from a reader reading top-to-bottom. Keep line numbers and cross-references minimal — the authority is the deliberation, not line numbers in this file.

Note: Do NOT remove the supersession banner at the top of the file in this task. Plan 07 removes it after the full rewrite has been verified.
  </action>
  <verify>
- Grep `get-shit-done/references/audit-conventions.md` for `audit-taxonomy-three-axis-obligations` — must find at least one citation as authority in the new Section 3
- Grep the same file for `9 subject` — must match (the new Axis 1 table has 9 rows)
- Grep for `cross_model_review` — if it appears, must be flagged as "removed from subject enum" historical context, NOT as a current subject row
- Grep for `process_review` and `artifact_analysis` — both must appear as new subjects
- Grep for `investigatory` and `exploratory` — both must appear as orientations in the Axis 2 table
- Second-order: verify the rewrite cites the deliberation, not Section 3 or Section 4 of this file, as authority (`grep -E "per audit-conventions.md Section (3|4)"` in the new Section 3 — must return ZERO results; self-citation of superseded sections is the exact failure mode sig-2026-04-10 documents)
- Read the resulting Section 3 top-to-bottom as a standalone section — does it make sense to a reader who has NOT read the deliberation? The goal is that the deliberation is cited for depth, but Section 3 stands on its own for operational use
  </verify>
  <done>
Section 3 of `audit-conventions.md` contains the 3-axis taxonomy (9 subjects with epistemic profile table, 3 orientations, 2 delegation modes), the escape hatch extended to the axes per DC-5, and citations to the deliberations as authority. The pre-rewrite Section 3 (flat 8-type enum table) is no longer present. Supersession banner remains at the top of the file pending Plan 07.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Section 4 (Type-Aware Body Templates) as the Obligations Paradigm + Section 2 frontmatter schema update</name>
  <files>get-shit-done/references/audit-conventions.md</files>
  <action>
**Part A — Rewrite Section 4** as "Output Paradigm: Obligations and Templates".

Replace the current Section 4 (type-aware body templates per family, lines ~201–318) with:

**4.1 The Two Populations** (~1 page of prose)
- Cite the retrospective as authority: it confirmed three populations, but only two produce artifacts at the reference-file level (Population 3 — multi-agent — is deferred per Q3)
- Population 1 (standard routine audits): templates remain as scaffolding — consistency and scannability matter. VERIFICATION.md, MILESTONE-AUDIT.md are the canonical examples
- Population 2 (investigatory, exploratory, and complex audits): obligations replace templates — flexibility matters, and templates cannot compose across axes
- The complexity assessment ("template territory vs obligations territory") is performed by the orchestrator (`/gsdr:audit` command) based on the classification and the situation. This reference file does not prescribe the assessment; the command does.

**4.2 Obligations vs. Templates — the difference that matters**
- Write this as a hermeneutic distinction, not a mechanical one, following the deliberation:
  - Templates say: "Write a Competing Explanations section." The auditor fills the section.
  - Obligations say: "You must present competing explanations." The auditor weaves competing explanations into the narrative wherever they emerge naturally.
- State: task specs under the obligations paradigm say "address these obligations" — not "fill these sections".
- State: the "What the Obligations Didn't Capture" section is mandatory in every audit output under both paradigms — a named section where the auditor records findings that don't fit any obligation.

**4.3 Suggested scaffolding for standard-orientation audits**
- Subject-specific scaffolds remain available as guides (NOT mandates) for standard-orientation audits. Preserve the intent of the v1 body templates but reframe them as suggestions, not prescriptions.
- Lift scaffolds directly from the v1 Section 4 content for the `phase_verification`, `requirements_review`, `claim_integrity`, `comparative_quality`, `adoption_compliance`, `codebase_forensics`, `milestone` subjects (these map to v1 structural/epistemic/compliance families). Mark them clearly as "suggested structure, not required" and as "available for standard orientation only — investigatory and exploratory orientations use obligations, not templates".
- For the two NEW subjects (`process_review` and `artifact_analysis`), either (a) leave scaffolding unspecified with a note that scaffolds will emerge from practice, or (b) provide minimal skeletal scaffolds — your call per Claude's Discretion.

**4.4 Obligation sets live in `audit-ground-rules.md`**
- Point at `audit-ground-rules.md` (which Plan 02 rewrites) as the home for the actual obligation text (core, orientation, subject, plus chain integrity / dispatch hygiene / framework invisibility).
- The composition principle also lives in `audit-ground-rules.md`, not here — this file describes *that* obligations compose; it does not describe *how* to compose them.

**Part B — Update Section 2 (Frontmatter Schema)** to encode the v2 3-axis fields.

Rewrite the Required fields table to replace `audit_type` with three fields:

| Field | Type | Description |
|-------|------|-------------|
| `date` | YYYY-MM-DD | Date the audit was conducted (NOT the migration date) |
| `audit_subject` | enum \| omitted | One of 9 subjects per Section 3.1; **omit** for `investigatory` / `exploratory` orientations when no subject is known at start |
| `audit_orientation` | enum | `standard` (default) \| `investigatory` \| `exploratory` — per Section 3.2 |
| `audit_delegation` | enum | `self` (default) \| `cross_model:{model_id}` — per Section 3.3 |
| `scope` | string | What was audited — concise description of the target |

Keep the Recommended and Optional field tables semantically unchanged (these are the surviving Section 2 content per the banner). Update the `ground_rules` field description to reflect the obligations paradigm — old values like `core+structural` are NO LONGER accurate; the new value format is composition-descriptive, e.g., `core+investigatory+process_review` or `core+standard+phase_verification`. Frame as a free-form label naming the composition, not a closed enum.

Add a new Optional field `audit_type` marked explicitly as **Legacy (v1 compatibility)**:

| Field | Type | Description |
|-------|------|-------------|
| `audit_type` | enum | **Legacy (v1)** — retained for backward compatibility with pre-57.4 artifacts. Migration rule: maps to `audit_subject` with `audit_orientation: standard` and `audit_delegation: self` as defaults. New audits SHOULD populate the three v2 fields; they MAY also populate `audit_type` for compatibility. |

Update the "Field count rationale" paragraph: total = 3 required + 5 recommended + 5 optional = **13 fields** (was 12; the 1 legacy `audit_type` joined the Optional set). If the count drifts above 15, that's a signal per the Pitfall 6 in RESEARCH.md — add this sentence to the rationale paragraph.

Update the three Example frontmatters (minimal, full session, migrated artifact) to use the v2 schema. The minimal example should show a bare v2 audit; the full session should show a composed obligations label in `ground_rules`; the migrated example should show both the legacy `audit_type` and the new axis fields coexisting for a migrated artifact.

Do NOT: delete or modify Section 1 (directory structure). Do NOT modify Section 5 (Three-Layer Model) semantically — the three layers still hold; update the prose if needed so it references "obligations" not "type-aware templates" in Layer 2. Do NOT modify Section 6 (Relationship to Other Audit Locations).

Do NOT remove the supersession banner at the top of the file — Plan 07 removes it.
  </action>
  <verify>
- Grep the file for `audit_type` — must appear only (a) in the new Section 2 Optional field row marked Legacy, (b) in the migrated example frontmatter, and (c) wherever the rewrite historically references the v1 enum. Must NOT appear as a Required field
- Grep for `audit_subject`, `audit_orientation`, `audit_delegation` — each must appear at least 3 times (definition in Section 2, description in Section 3, example frontmatter)
- Grep for `"What the Obligations Didn't Capture"` — must appear at least once in Section 4
- Grep for `core+structural|core+epistemic|core+compliance` — must NOT appear as a current `ground_rules` value (these were v1 family groupings). If still present, it must be marked as "v1 legacy label format"
- Grep for "Population 1" and "Population 2" — both should appear in Section 4.1 with the deliberation-sourced semantics (template vs obligations)
- Second-order: run `node bin/install.js --local` is NOT needed here (Plan 07 handles the sync); confirm the file still parses as Markdown by reading the first 20 lines and last 20 lines (no YAML frontmatter issues, no orphan code blocks)
- Second-order: verify the new Section 4 points at `audit-ground-rules.md` for obligation text (grep for `audit-ground-rules.md`) — Plan 02 is rewriting that file, so this cross-reference will land correctly
- Read Section 2 top-to-bottom: do the 5 required fields now include the 3 axis fields? Does the Optional section mention `audit_type` as legacy?
  </verify>
  <done>
Section 4 now presents the obligations paradigm (not templates-per-family) and points at `audit-ground-rules.md` for obligation text. Section 2 required fields include `audit_subject`, `audit_orientation`, `audit_delegation`. `audit_type` is marked Legacy in Optional. Examples updated to v2 schema. Sections 1, 5, 6 semantically preserved (Section 5 prose updated for "obligations" wording).
  </done>
</task>

</tasks>

<verification>
**End-of-plan verification (run as a single pass):**

1. `grep -n "audit-taxonomy-three-axis-obligations" get-shit-done/references/audit-conventions.md` — must have ≥2 matches (one per rewritten section at minimum)
2. `grep -n "audit_subject\|audit_orientation\|audit_delegation" get-shit-done/references/audit-conventions.md | wc -l` — must be ≥12 (3 fields x ≥4 mentions each: definition, table, example min, example full)
3. `grep -c "^## " get-shit-done/references/audit-conventions.md` — must be ≥6 (Sections 1-6 headers all present)
4. `grep -n "SUPERSEDED" get-shit-done/references/audit-conventions.md | head -3` — supersession banner must still be at the top of the file (lines 1-20). Plan 07 removes it
5. Read Section 1 (Directory Structure) — must be byte-for-byte unchanged except for whitespace/Markdown linting
6. Read Section 5 (Three-Layer Model) — Layer 2 prose should reference "obligations" not "type-aware templates" (one-word-level edit, not rewrite)
7. `npm test -- wiring-validation` — auto-covers the file for @-reference integrity (no broken references)
</verification>

<success_criteria>
- `audit-conventions.md` encodes the 3-axis taxonomy (9 subjects, 3 orientations, 2 delegation modes) in Section 3
- Section 4 presents the obligations paradigm with Population 1 / Population 2 framing
- Section 2 Required fields include the three v2 axis fields; `audit_type` is Optional Legacy
- All claims in new Section 3 and Section 4 cite the deliberations (not the pre-rewrite file itself) as authority
- Section 1, Section 5 (semantic), Section 6 survive unchanged
- Supersession banner still at top of file (Plan 07 removes it)
</success_criteria>

<output>
After completion, create `.planning/phases/57.4-audit-skill-investigatory-type/57.4-01-SUMMARY.md` with:
- Which sections were rewritten (Section 3, Section 4, Section 2 frontmatter)
- Which sections were preserved (Section 1, Section 5 semantic, Section 6)
- Where authority flows from (deliberations -> rewrite; list the two deliberations + REVIEW.md + forms-excess)
- Line count before/after (the file should remain roughly the same length — ~350-400 lines)
- Confirmation that the supersession banner is still in place
- Any rewording decisions in the suggested scaffolding for `process_review` and `artifact_analysis` (per Claude's Discretion)
</output>
