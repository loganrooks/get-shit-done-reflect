---
phase: 58.1-codex-update-distribution-parity
plan: "02"
signature:
  role: executor
  harness: codex-cli
  platform: codex
  vendor: openai
  model: gpt-5.4
  reasoning_effort: xhigh
  profile: quality
  gsd_version: 1.19.6+dev
  generated_at: "2026-04-20T22:15:37Z"
  session_id: "019dac5e-dbfe-7031-9f8b-0f64c4c57edc"
  provenance_status:
    role: derived
    harness: exposed
    platform: derived
    vendor: derived
    model: exposed
    reasoning_effort: exposed
    profile: derived
    gsd_version: derived
    generated_at: exposed
    session_id: exposed
  provenance_source:
    role: artifact_role
    harness: runtime_context
    platform: derived_from_harness
    vendor: derived_from_harness
    model: codex_state_store
    reasoning_effort: codex_state_store
    profile: config
    gsd_version: repo_mirror
    generated_at: writer_clock
    session_id: env:CODEX_THREAD_ID
subsystem: codex-update-workflow
tags: [codex, update, workflow, integration, install-chain]
requires:
  - phase: 58.1-01
    provides: shared Codex update-target resolver, CLI bridge, and installer path rewrite support
provides:
  - Runtime-aware update command/workflow wiring that consumes the Codex resolver
  - Published-package update callsite for Codex and Claude update execution
  - Integration regressions covering global-vs-mirror selection, custom config-dir propagation, local `.codex` routing, and installed skill/workflow chain integrity
affects: [gsdr-update, Codex distribution parity, installed workflow routing]
tech-stack:
  added: []
  patterns:
    - "Thin command, thin workflow, shared resolver policy"
    - "Installed-surface assertions via temp-runtime installs instead of live npm mutation"
key-files:
  created:
    - ".planning/phases/58.1-codex-update-distribution-parity/58.1-02-SUMMARY.md"
  modified:
    - "commands/gsd/update.md"
    - "get-shit-done/bin/update-target.cjs"
    - "get-shit-done/workflows/update.md"
    - "tests/integration/multi-runtime.test.js"
key-decisions:
  - "The update workflow now detects runtime first, then routes Codex through the shared resolver while keeping Claude on the simpler local/global branch."
  - "Update execution always uses `get-shit-done-reflect-cc@latest` so running update from the source repo cannot accidentally resolve the local checkout."
patterns-established:
  - "Installed Codex skills and workflows are verified by temp installs that inspect the installed chain directly: skill -> workflow -> resolver bridge."
duration: 15min
completed: 2026-04-20
---

# Phase 58.1 Plan 02 Summary

**Wave 2 rewired the live update surface to the shared Codex resolver, forced published-package installs, fixed runtime-specific wording, and added integration coverage proving the installed Codex update chain no longer hides stale global installs behind repo-local mirrors.**

## Performance

- **Duration:** ~15 min
- **Tasks:** 2 completed
- **Files modified:** 4 planned files plus 1 blocking bridge fix

## Accomplishments

- Rewrote `get-shit-done/workflows/update.md` into a runtime-aware flow that detects Codex first, shells to `update-target.cjs`, surfaces `remaining_divergent_scope`, preserves Codex `does_not_apply_reason` cache semantics, and points the update callsite at `get-shit-done-reflect-cc@latest`.
- Kept `commands/gsd/update.md` thin while updating its intent text to reflect runtime-aware detection and published-package execution.
- Added integration regressions in `tests/integration/multi-runtime.test.js` for:
  - stale global Codex install vs newer repo mirror
  - both scopes stale with one selected target plus the other surfaced
  - custom `--config-dir` propagation
  - local `.codex` skill/workflow/resolver routing
  - installed Codex `gsdr-update` entrypoint integrity after path rewriting
- Preserved the installed update chain contract: temp-installed Codex skills now prove the chain from installed skill to installed workflow to installed resolver bridge, with the workflow text carrying explicit `--codex --global`, `--codex --local`, and published-package examples.

## Task Commits

1. **Task 1: Keep the update command/workflow thin and runtime-correct** - `38fa33ac`
2. **Task 2: Add integration regressions for Codex update parity** - `a7904470`

## Files Created/Modified

- `commands/gsd/update.md` - thin entrypoint text updated for runtime-aware update execution.
- `get-shit-done/workflows/update.md` - runtime-aware update workflow that consumes the shared Codex resolver and uses published npm execution.
- `tests/integration/multi-runtime.test.js` - Codex update parity fixtures for global/local/custom-dir installs and installed entrypoint integrity.
- `get-shit-done/bin/update-target.cjs` - tiny bridge extension so the workflow can inject the published npm latest version into the shared resolver.

## Verification

- `rg -n "update-target\\.cjs|--codex|config-dir|get-shit-done-reflect-cc@latest|remaining_divergent_scope|Restart Codex CLI" commands/gsd/update.md get-shit-done/workflows/update.md get-shit-done/bin/update-target.cjs`
- `npx vitest run tests/integration/multi-runtime.test.js`
- `npx vitest run tests/unit/install.test.js`

## Decisions & Deviations

- Blocking deviation: added `--latest-version` support to `get-shit-done/bin/update-target.cjs` even though that bridge file was not listed in the original plan-02 write set. The workflow needed a latest-aware resolver call for installed surfaces; without that tiny seam, it would have had to duplicate target-selection policy in markdown.
- Process deviation: the assigned `gsdr-executor` for wave 2 never produced output or branch movement, so I took over locally after closing it. The implementation still followed the approved plan and preserved atomic task commits.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase `58.1` is ready for verifier review.
- The verifier can validate both summary artifacts plus the branch state against the runtime-aware workflow and regression coverage now on disk.

## Self-Check: PASSED

- Wave-2 commits exist on the phase branch: `38fa33ac`, `a7904470`
- Integration suite passed locally (28/28)
- Unit installer suite passed locally (251/251)
