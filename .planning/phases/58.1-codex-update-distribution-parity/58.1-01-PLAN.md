---
phase: 58.1-codex-update-distribution-parity
plan: "01"
signature:
  role: planner
  harness: codex-cli
  platform: codex
  vendor: openai
  model: gpt-5.4
  reasoning_effort: xhigh
  profile: quality
  gsd_version: 1.19.4+dev
  generated_at: "2026-04-20T21:17:17.349Z"
  session_id: "019dacbe-274b-7c63-ae1d-29c7bf73af24"
  provenance_status:
    role: derived
    harness: exposed
    platform: derived
    vendor: derived
    model: exposed
    reasoning_effort: exposed
    profile: derived
    gsd_version: derived
    generated_at: exposed
    session_id: exposed
  provenance_source:
    role: artifact_role
    harness: runtime_context
    platform: derived_from_harness
    vendor: derived_from_harness
    model: codex_state_store
    reasoning_effort: codex_state_store
    profile: config
    gsd_version: installed_harness
    generated_at: writer_clock
    session_id: env:CODEX_THREAD_ID
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/update-target.cjs
  - get-shit-done/bin/update-target.cjs
  - bin/install.js
  - tests/unit/install.test.js
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Codex update targeting enumerates both repo-local and resolved global scopes and selects exactly one stale target with an explicit reason instead of local-first masking."
    - "When both local and global Codex scopes are stale or divergent, one invocation still selects exactly one target and surfaces the remaining divergent scope instead of silently collapsing or ignoring it."
    - "Selected Codex install arguments always include `--codex` plus the correct scope and resolved `--config-dir` when the active global directory is non-default."
    - "Codex cache handling for update checks is explicit `does-not-apply-with-reason` metadata, not a fabricated `.codex/cache/gsd-update-check.json` path."
  artifacts:
    - path: "get-shit-done/bin/lib/update-target.cjs"
      provides: "Pure Codex update-target resolver and target-selection contract"
      contains: "cache_to_clear"
    - path: "get-shit-done/bin/update-target.cjs"
      provides: "CLI JSON bridge for workflows and installed skills"
      contains: "JSON.stringify"
    - path: "bin/install.js"
      provides: "Shared pure helper consumption for Codex global-dir/path precedence"
      contains: "CODEX_CONFIG_DIR"
    - path: "tests/unit/install.test.js"
      provides: "Unit regressions for Codex target selection, both-scopes-stale surfacing, and relative path rewriting"
      contains: "Codex update target"
  key_links:
    - from: "get-shit-done/bin/update-target.cjs"
      to: "get-shit-done/bin/lib/update-target.cjs"
      via: "CLI wrapper loads resolver and prints machine-readable JSON"
      pattern: "JSON\\.stringify"
    - from: "get-shit-done/bin/lib/update-target.cjs"
      to: "bin/install.js"
      via: "Shared pure Codex directory precedence/helper, not direct install.js import"
      pattern: "CODEX_CONFIG_DIR|config-dir"
    - from: "tests/unit/install.test.js"
      to: "get-shit-done/bin/lib/update-target.cjs"
      via: "Unit cases assert stale-target selection, both-scopes-stale remaining-scope surfacing, config-dir propagation, and no-op cache semantics"
      pattern: "cache_to_clear|remaining_divergent_scope|--codex"
---

<objective>
Create the single Codex update-target authority seam that Phase 58.1 will build on, with focused unit regressions around scope selection and Codex-specific path handling.

Purpose: Remove the local-first, Claude-default update-policy drift at the runtime layer before touching the user-facing update workflow.
Output: A pure Codex update-target resolver, a thin CLI bridge for workflows, and unit coverage proving stale-target selection/config-dir propagation.
</objective>

<execution_context>
@./.codex/get-shit-done-reflect/workflows/execute-plan.md
@./.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58.1-codex-update-distribution-parity/58.1-CONTEXT.md
@.planning/phases/58.1-codex-update-distribution-parity/58.1-DISCUSSION-LOG.md
@.planning/phases/58.1-codex-update-distribution-parity/58.1-RESEARCH.md
@AGENTS.md
@bin/install.js
@get-shit-done/bin/lib/provenance.cjs
@tests/unit/install.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a pure Codex update-target resolver and CLI JSON bridge</name>
  <files>get-shit-done/bin/lib/update-target.cjs, get-shit-done/bin/update-target.cjs</files>
  <action>Create one shared resolver under `get-shit-done/bin/lib/` that owns Codex update-target policy. It must inspect repo-local and resolved global Codex installs, read both VERSION files when present, compare versions, and choose exactly one target scope per invocation. Follow the research-locked authority rule: installed global Codex scope outranks repo-local mirror as evidence, but when both scopes exist and diverge the resolver must surface both and pick the older stale target rather than stopping on first match. When both scopes are stale, return one `selected_target` only and include explicit metadata for the unselected divergent scope so the workflow can report it instead of hiding it. Return structured JSON containing at minimum: `runtime`, `local`, `global`, `selected_target`, `install_args`, `config_dir`, `reason`, `cache_to_clear`, `does_not_apply_reason`, and `remaining_divergent_scope` when one stale scope remains after selection.

Keep the policy seam pure and side-effect free. Do not import `bin/install.js` directly from the resolver. If you need installer facts, extract or share pure helper logic instead of coupling to the CLI hotspot. The CLI wrapper in `get-shit-done/bin/update-target.cjs` should accept the minimal inputs the workflow needs (`cwd`, runtime fixed to Codex, optional explicit `--config-dir`) and print JSON only, so the workflow can stay thin.</action>
  <verify>`node get-shit-done/bin/update-target.cjs --runtime codex --cwd "$PWD"` prints valid JSON with `selected_target`, `install_args`, `reason`, and `cache_to_clear` fields when run from the repo root and from temp Codex fixtures, including a both-scopes-stale fixture where the JSON contains one selected target plus `remaining_divergent_scope` for the other stale scope.</verify>
  <done>The repo has one authoritative Codex update-target resolver and one CLI bridge; the contract exposes explicit scope/config-dir/no-op-cache semantics, never depends on first-match local-first logic, and makes the unselected stale scope visible when both scopes remain stale.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse pure Codex path helpers in the installer seam and add targeted unit regressions</name>
  <files>bin/install.js, tests/unit/install.test.js</files>
  <action>If the resolver needs Codex directory precedence from the installer, extract the relevant logic into pure helper(s) and have `bin/install.js` consume them, rather than teaching the resolver to `require('../../bin/install.js')`. Keep the installer edit tight: only share Codex global-dir/path rules and any relative-path rewrite needed for Codex-installed update artifacts. Research already identified the `./.claude/` relative-path leak in Codex-installed update workflows; close that leak here if it still exists after the resolver seam is in place.

Add focused Vitest coverage adjacent to existing installer/runtime tests. Cover: `global stale vs repo-local mirror newer -> select global`, `local stale vs global newer -> select local`, `both scopes stale -> select exactly one target and surface the other as remaining divergent scope`, `custom config dir -> include exact --config-dir`, `Codex -> cache_to_clear === null with does-not-apply reason`, and the `./.claude/` to `./.codex/` rewrite for Codex-installed update artifacts if markdown path replacement remains part of the flow. Keep this plan narrow: no hook/statusline parity work, no broader multi-runtime redesign.</action>
  <verify>`npx vitest run tests/unit/install.test.js`</verify>
  <done>Installer/runtime helper reuse is pure, unit-tested, and sufficient for the update workflow to consume without re-encoding Codex path policy in markdown.</done>
</task>

</tasks>

<verification>
- [ ] `npx vitest run tests/unit/install.test.js`
- [ ] `node get-shit-done/bin/update-target.cjs --runtime codex --cwd "$PWD"` returns JSON with explicit `install_args`
- [ ] A temp both-scopes-stale Codex fixture returns exactly one selected target plus surfaced `remaining_divergent_scope`
- [ ] No new helper imports `bin/install.js` directly for Codex update detection
</verification>

<narrowing_decisions>
## Narrowing Decisions

None -- plan covers the CONTEXT.md claims verbatim. Codex update-cache handling is implemented as the research-resolved `does-not-apply-with-reason` no-op, so hook/statusline parity is not silently deferred here.
</narrowing_decisions>

<success_criteria>

- All tasks completed
- All verification checks pass
- The shared resolver is the only Codex update-target policy owner introduced by Phase 58.1
- Unit regressions cover stale-scope selection, both-scopes-stale surfacing, non-default config-dir propagation, and no-op cache semantics
</success_criteria>

<output>
After completion, create `.planning/phases/58.1-codex-update-distribution-parity/58.1-01-SUMMARY.md`
</output>
