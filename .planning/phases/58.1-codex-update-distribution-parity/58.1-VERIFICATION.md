---
phase: 58.1-codex-update-distribution-parity
verified: 2026-04-20T22:22:04Z
status: human_needed
score: 5/5 must-haves verified
human_verification:
  - test: "Live Codex update from the source repo with a stale global install"
    expected: "The preview shows repo-local and global Codex versions, selects the stale global target, uses `npx --yes get-shit-done-reflect-cc@latest --codex --global`, and reports the Codex restart/remnant-divergence text correctly."
    why_human: "This is an interactive workflow that depends on real npm/package resolution and a live Codex runtime session."
  - test: "Live Codex update with a non-default CODEX_CONFIG_DIR"
    expected: "The preview and executed install command preserve the exact custom config dir and run `--codex --global --config-dir <dir>`."
    why_human: "The current tests simulate the fixture offline; they do not exercise a real live-runtime update against an installed Codex session."
---

# Phase 58.1: Codex Update Distribution Parity Verification Report

**Phase Goal:** Codex update surfaces stop hiding stale installs: installed-version detection enumerates project-local and active global Codex scopes correctly, update execution targets the intended runtime/config-dir/scope, and shared update indicators no longer assume Claude-only layout.
**Verified:** 2026-04-20T22:22:04Z
**Status:** human_needed
**Re-verification:** No — initial verification

Verified against current branch state on `gsd/phase-58.1-codex-update-distribution-parity` plus the claimed implementation commits `b70e66a5`, `e38a67e0`, `38fa33ac`, and `a7904470`.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Codex update targeting enumerates repo-local and active global scopes, selects exactly one target, and explains the choice instead of doing local-first masking. | ✓ VERIFIED | `get-shit-done/bin/lib/update-target.cjs:245-299` returns `local`, `global`, `selected_target`, `reason`, and `remaining_divergent_scope`; unit cases cover stale-global/newer-local, stale-local/newer-global, and both-stale selection in `tests/unit/install.test.js:162-220`; live CLI output from `node get-shit-done/bin/update-target.cjs --runtime codex --cwd "$PWD"` returned both scopes plus explicit target selection. |
| 2 | The selected Codex update command is always explicit about runtime and scope, and carries `--config-dir` when the active global Codex dir is non-default. | ✓ VERIFIED | Resolver emits `install_args` via `buildCodexInstallArgs()` and `resolveCodexUpdateTarget()` in `get-shit-done/bin/lib/update-target.cjs:86-104,245-299`; CLI bridge preserves that contract in `get-shit-done/bin/update-target.cjs:80-99`; unit and integration coverage assert `--codex --global --config-dir <dir>` in `tests/unit/install.test.js:203-213` and `tests/integration/multi-runtime.test.js:875-929`. |
| 3 | The update command/workflow now consumes the shared resolver, uses the published package instead of the local checkout, and models Codex cache handling honestly as does-not-apply. | ✓ VERIFIED | `commands/gsd/update.md:12-37` is a thin entrypoint to the workflow; `get-shit-done/workflows/update.md:53-118` shells to `update-target.cjs`, `get-shit-done/workflows/update.md:222-259` uses `npx --yes get-shit-done-reflect-cc@latest`, and `get-shit-done/workflows/update.md:118,205,258-259` explicitly treats Codex cache handling as a no-op with reason instead of inventing `.codex/cache` behavior. |
| 4 | Installed Codex update surfaces survive path rewriting: the installed skill points at the installed workflow, the installed workflow points at the installed resolver, and local Codex routing stays on `.codex` rather than `.claude`. | ✓ VERIFIED | The relative-path rewrite lives in `bin/install.js:1344-1394,1491-1493`; unit coverage asserts `./.claude/` → `./.codex/` in `tests/unit/install.test.js:538-547`; integration coverage checks installed `SKILL.md`, installed workflow, installed resolver path, and absence of `./.claude/` in `tests/integration/multi-runtime.test.js:824-873,931-977`. |
| 5 | The promised regression surface exists and currently passes for the phase’s update-parity cases. | ✓ VERIFIED | `npx vitest run tests/unit/install.test.js` passed `251/251`; `npx vitest run tests/integration/multi-runtime.test.js` passed `28/28`; the integration suite includes stale-global-vs-local-mirror, both-scopes-stale, custom-config-dir, and local `.codex` routing coverage at `tests/integration/multi-runtime.test.js:824-977`. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `get-shit-done/bin/lib/update-target.cjs` | Pure Codex update-target resolver and contract owner | ✓ VERIFIED | Substantive selection logic at `:86-299`; exports shared helpers at `:302-313`; no direct `install.js` import (`rg "install\\.js" get-shit-done/bin/lib/update-target.cjs` returned no matches). |
| `get-shit-done/bin/update-target.cjs` | Thin CLI JSON bridge for workflows and installed skills | ✓ VERIFIED | Parses `--cwd`, `--config-dir`, `--latest-version`, calls resolver, and prints JSON at `:6-99`. |
| `bin/install.js` | Shared Codex config-dir/path handling and local `.codex` rewrite support | ✓ VERIFIED | Imports `getCodexConfigDir` at `:9`; uses it in `getGlobalDir('codex')` at `:159-165`; `replacePathsInContent()` handles runtime-local rewrites at `:1344-1394`. |
| `commands/gsd/update.md` | Thin update entrypoint to the workflow | ✓ VERIFIED | Entry text and `execution_context` stay thin at `:12-37`. |
| `get-shit-done/workflows/update.md` | Runtime-aware update workflow using resolver + published package | ✓ VERIFIED | Codex resolver path/call at `:53-118`; preview and install contract at `:190-259`; runtime-correct result text at `:291-315`. |
| `tests/unit/install.test.js` | Unit regressions for resolver semantics and path rewriting | ✓ VERIFIED | Resolver cases at `:125-220`; `.claude`→`.codex` rewrite assertion at `:538-547`. |
| `tests/integration/multi-runtime.test.js` | Integration-style installed-surface and routing coverage | ✓ VERIFIED | Installed skill/workflow chain, custom config dir, and local routing checks at `:824-977`. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `get-shit-done/bin/update-target.cjs` | `get-shit-done/bin/lib/update-target.cjs` | `require()` + JSON stdout | ✓ WIRED | `get-shit-done/bin/update-target.cjs:4,87-99` calls `resolveCodexUpdateTarget()` and prints the result. |
| `bin/install.js` | `get-shit-done/bin/lib/update-target.cjs` | Shared Codex config-dir helper | ✓ WIRED | `bin/install.js:9` imports `getCodexConfigDir`; `bin/install.js:159-165` uses it for Codex global-dir resolution. |
| `commands/gsd/update.md` | `get-shit-done/workflows/update.md` | Thin execution context/process handoff | ✓ WIRED | `commands/gsd/update.md:21-37` points the command at the workflow and leaves logic there. |
| `get-shit-done/workflows/update.md` | `get-shit-done/bin/update-target.cjs` | Resolver bridge invocation before confirmation/update | ✓ WIRED | `get-shit-done/workflows/update.md:63-83` defines local/global resolver paths and the `node "$RESOLVER"` call. |
| `tests/unit/install.test.js` | `get-shit-done/bin/lib/update-target.cjs` | Imported resolver contract + assertions | ✓ WIRED | `tests/unit/install.test.js:14,152-220` imports the resolver and asserts stale-target/config-dir/cache semantics. |
| `tests/integration/multi-runtime.test.js` | Installed Codex skill/workflow/resolver chain | Temp installs inspect installed artifacts directly | ✓ WIRED | `tests/integration/multi-runtime.test.js:862-975` reads installed `gsdr-update` skill/workflow, checks resolver references, published-package callsite, `.codex` routing, and custom config-dir behavior. |

### Requirements Coverage

| Requirement | Status | Blocking Issue |
| --- | --- | --- |
| No explicit `REQUIREMENTS.md` row is mapped to Phase 58.1; verification was performed against the ROADMAP goal and both plan `must_haves` blocks. | N/A | None |
| Derived XRT-style substrate honesty for Codex update cache handling | ✓ SATISFIED | `58.1-CONTEXT.md` and `58.1-RESEARCH.md` narrow Codex hooks/statusline out of scope, and the shipped resolver/workflow keep cache handling as explicit `does_not_apply_reason` rather than a fake `.codex` cache path. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| None | - | No TODO/FIXME/placeholder/stub markers in the touched source/test artifacts; no resolver import of `bin/install.js` | ℹ️ Info | No automated blocker anti-patterns were found in the phase-owned implementation surface. |

### Human Verification Required

### 1. Live Codex Global Update

**Test:** In a real Codex session, arrange a stale global install plus a newer repo-local `.codex` mirror, then run `gsdr-update`.
**Expected:** The preview shows both versions, selects the stale global target, uses `npx --yes get-shit-done-reflect-cc@latest --codex --global`, and finishes with the Codex-specific restart text plus any remaining divergence note.
**Why human:** This requires live interactive workflow execution and real npm/package resolution, which the automated fixture intentionally avoids.

### 2. Live Non-Default Config Dir Update

**Test:** In a real Codex session with `CODEX_CONFIG_DIR` pointed at a non-default install root, run `gsdr-update`.
**Expected:** The preview/result keeps the exact custom config dir and executes `npx --yes get-shit-done-reflect-cc@latest --codex --global --config-dir <dir>`.
**Why human:** The integration test proves offline fixture behavior, but not a real live Codex runtime invoking the published package.

### Gaps Summary

No automated implementation gaps were found. The shared resolver exists, the workflow is wired to it, the published-package callsite is present, Codex cache behavior is explicitly modeled as does-not-apply, the installed Codex skill/workflow/resolver chain is covered, and the targeted unit/integration suites pass in the current branch state.

The remaining unverified surface is live interactive update execution against the real npm package and a live Codex session. That is why this report is `human_needed` rather than `passed`.

---

_Verified: 2026-04-20T22:22:04Z_
_Verifier: Codex (gsdr-verifier)_
