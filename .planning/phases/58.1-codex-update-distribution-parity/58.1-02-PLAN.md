---
phase: 58.1-codex-update-distribution-parity
plan: "02"
signature:
  role: planner
  harness: codex-cli
  platform: codex
  vendor: openai
  model: gpt-5.4
  reasoning_effort: xhigh
  profile: quality
  gsd_version: 1.19.4+dev
  generated_at: "2026-04-20T21:17:17.349Z"
  session_id: "019dacbe-274b-7c63-ae1d-29c7bf73af24"
  provenance_status:
    role: derived
    harness: exposed
    platform: derived
    vendor: derived
    model: exposed
    reasoning_effort: exposed
    profile: derived
    gsd_version: derived
    generated_at: exposed
    session_id: exposed
  provenance_source:
    role: artifact_role
    harness: runtime_context
    platform: derived_from_harness
    vendor: derived_from_harness
    model: codex_state_store
    reasoning_effort: codex_state_store
    profile: config
    gsd_version: installed_harness
    generated_at: writer_clock
    session_id: env:CODEX_THREAD_ID
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - commands/gsd/update.md
  - get-shit-done/workflows/update.md
  - tests/integration/multi-runtime.test.js
autonomous: true
user_setup: []
must_haves:
  truths:
    - "The update command/workflow consults the shared Codex resolver instead of hard-coded local-first `.claude` checks."
    - "When Codex is the runtime, the emitted update command always includes explicit `--codex` plus exactly one scope flag and the resolved non-default `--config-dir` when needed."
    - "The update callsite forces published npm resolution from the installed `gsdr-update` surface, so running the update from the source repo cannot accidentally resolve the local dev checkout."
    - "Integration regressions prove a stale repo-local `.codex` mirror cannot mask a stale active global Codex install, local Codex routing uses `.codex` rather than `.claude`, and the installed Codex `gsdr-update` skill still routes through installed workflow -> installed resolver after path rewriting."
    - "When both local and global Codex scopes are stale, one invocation still selects exactly one target and surfaces the other divergent scope instead of silently ignoring it."
  artifacts:
    - path: "commands/gsd/update.md"
      provides: "Thin update entrypoint that points at the source update workflow"
      contains: "workflows/update.md"
    - path: "get-shit-done/workflows/update.md"
      provides: "Thin workflow that shells to the shared resolver, runs explicit Codex install args, and forces published npm resolution"
      contains: "get-shit-done-reflect-cc@"
    - path: "tests/integration/multi-runtime.test.js"
      provides: "Codex update-routing regression fixtures for divergent scopes, installed entrypoint integrity, and custom config dirs"
      contains: "gsdr-update"
  key_links:
    - from: "commands/gsd/update.md"
      to: "get-shit-done/workflows/update.md"
      via: "Thin command execution_context/process points at the source workflow"
      pattern: "update\\.md"
    - from: "get-shit-done/workflows/update.md"
      to: "get-shit-done/bin/update-target.cjs"
      via: "Workflow shells to resolver JSON bridge before confirmation and execution"
      pattern: "update-target\\.cjs"
    - from: "tests/integration/multi-runtime.test.js"
      to: "commands/gsd/update.md"
      via: "Temp-install assertion proves installed `.codex/skills/gsdr-update/SKILL.md` still points at the installed update workflow after Codex path rewriting"
      pattern: "gsdr-update|workflows/update\\.md"
    - from: "tests/integration/multi-runtime.test.js"
      to: "get-shit-done/workflows/update.md"
      via: "Temp-install fixtures assert explicit Codex args, published npm callsite, single-target stale-scope selection, and custom config-dir propagation"
      pattern: "--codex --global|--codex --local|get-shit-done-reflect-cc@"
---

<objective>
Rewrite the user-facing update surface to consume the shared Codex resolver, then prove the behavior with focused integration-style runtime fixtures.

Purpose: Make the shipped update command/workflow stop hiding stale Codex installs while keeping Phase 58.1 narrowly scoped to update/distribution parity.
Output: Thin update command/workflow wiring plus integration coverage for divergent local/global/custom-dir Codex installs.
</objective>

<execution_context>
@./.codex/get-shit-done-reflect/workflows/execute-plan.md
@./.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58.1-codex-update-distribution-parity/58.1-CONTEXT.md
@.planning/phases/58.1-codex-update-distribution-parity/58.1-DISCUSSION-LOG.md
@.planning/phases/58.1-codex-update-distribution-parity/58.1-RESEARCH.md
@AGENTS.md
@commands/gsd/update.md
@get-shit-done/workflows/update.md
@hooks/gsd-check-update.js
@hooks/gsd-statusline.js
@tests/integration/multi-runtime.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keep the command/workflow thin and runtime-correct for Codex updates</name>
  <files>commands/gsd/update.md, get-shit-done/workflows/update.md</files>
  <action>Rewrite the update entrypoint so the command stays a thin wrapper and the workflow shells out once to the shared resolver produced by Plan 01. Remove hard-coded local-first `.claude` detection from the workflow and instead parse the resolver JSON to show local/global Codex versions, selected target scope, config dir, and reason. The workflow must execute the resolver's returned install args verbatim and those args must always include explicit `--codex` plus the selected `--local` or `--global`, with `--config-dir <resolvedDir>` whenever the active global Codex directory is non-default. If the resolver reports `remaining_divergent_scope`, surface that explicitly in the update preview/result text so the user can see which stale scope was not updated by this invocation.

Absorb the research-resolved dogfooding fix at the same seam: force published npm resolution at the update callsite so running `gsdr-update` from the source repo does not accidentally install the local dev checkout. Use an explicit published-package invocation such as `npx --yes get-shit-done-reflect-cc@latest ...` or equivalent; do not leave a runtime-correct but source-repo-local `npx get-shit-done-reflect-cc ...` callsite behind. Keep the workflow honest about substrate limits. Codex cache clearing is `does-not-apply-with-reason` in this phase; clear update caches only for runtimes that actually own a check-update hook surface. Do not edit `hooks/gsd-check-update.js` or `hooks/gsd-statusline.js`, and do not introduce Codex hook/statusline parity. Also fix the user-facing runtime wording in the update messages so Codex users are not told to restart Claude Code.</action>
  <verify>`rg -n "update-target\\.cjs|--codex|config-dir|does-not-apply-with-reason|Codex|get-shit-done-reflect-cc@" commands/gsd/update.md get-shit-done/workflows/update.md`</verify>
  <done>The update command/workflow is thin, explicit, Codex-aware, surfaces the unselected stale scope when both scopes diverge, and uses an explicit published-package update callsite rather than a source-repo-local `npx` invocation.</done>
</task>

<task type="auto">
  <name>Task 2: Add integration regressions for divergent scopes and custom Codex config dirs</name>
  <files>tests/integration/multi-runtime.test.js</files>
  <action>Reuse the existing temp-install fixture style in `tests/integration/multi-runtime.test.js` to verify the new behavior end-to-end without live npm mutation. Add one fixture that installs Codex globally, creates a stale project-local `.codex/get-shit-done-reflect/VERSION` mirror, invokes the shared resolver or the workflow preflight from the project root, and asserts the selected target is global with explicit `--codex --global` args and a reason showing why the repo mirror lost. Add one both-scopes-stale fixture that seeds both local and global Codex installs below a stubbed latest version, asserts the workflow/resolver selects exactly one target, and asserts the output surfaces the other stale scope instead of silently ignoring it. Add one custom-config-dir fixture that installs Codex with `--global --config-dir <dir>` and proves the selected update command preserves that exact directory. Add one local-install fixture proving the selected local VERSION path and emitted local install command use `.codex`, not `.claude`.

Add one installed-entrypoint integrity assertion as part of the same suite: temp-install Codex, read the installed `.codex/skills/gsdr-update/SKILL.md`, confirm it still points at the installed update workflow after Codex path rewriting, then read the installed `.codex/get-shit-done-reflect/workflows/update.md` and assert it shells to the installed resolver bridge and keeps the explicit published-package callsite. This is the blocker proof that `commands/gsd/update.md -> installed workflow -> resolver` survives installation rather than only working in source files.

Keep these tests tight and offline. They should validate routing, argument selection, and resolver/workflow contract shape, not perform a real published-package upgrade.</action>
  <verify>`npx vitest run tests/integration/multi-runtime.test.js`</verify>
  <done>Integration fixtures cover the user-reported masking bug, the both-scopes-stale single-target behavior, custom global-dir propagation, local `.codex` targeting, explicit published-package invocation, and the installed Codex `gsdr-update` entrypoint chain.</done>
</task>

</tasks>

<verification>
- [ ] `npx vitest run tests/unit/install.test.js`
- [ ] `npx vitest run tests/integration/multi-runtime.test.js`
- [ ] `rg -n "update-target\\.cjs|--codex|config-dir|get-shit-done-reflect-cc@" get-shit-done/workflows/update.md`
- [ ] A temp-installed Codex `gsdr-update` skill points at the installed workflow, and the installed workflow points at the installed resolver bridge after path rewriting
- [ ] A both-scopes-stale Codex fixture proves one invocation selects exactly one target and surfaces the other stale scope
</verification>

<narrowing_decisions>
## Narrowing Decisions

None -- plan covers the CONTEXT.md claims verbatim. The only adjacent surface explicitly left untouched is Codex hook/statusline parity, which RESEARCH.md already resolved as out of scope and `does-not-apply-with-reason` for this phase rather than a silent defer introduced by planning.
</narrowing_decisions>

<success_criteria>

- All tasks completed
- All verification checks pass
- Codex update execution is explicit about runtime, scope, config-dir, and published npm package source
- Integration regressions prove repo-local mirrors no longer mask stale active global Codex installs, both-stale selection stays single-target, and the installed Codex `gsdr-update` chain remains intact
</success_criteria>

<output>
After completion, create `.planning/phases/58.1-codex-update-distribution-parity/58.1-02-SUMMARY.md`
</output>
