---
phase: 46-upstream-module-adoption
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/gsd-tools.cjs
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All 8 non-fork init subcommands route through init.cjs module functions, not inline definitions"
    - "cmdInitTodos remains inline as a fork override (priority/source/status fields)"
    - "The 8 removed inline cmdInit* function definitions no longer exist in gsd-tools.cjs"
    - "All 534 tests pass after routing and removal changes"
  artifacts:
    - path: "get-shit-done/bin/gsd-tools.cjs"
      provides: "Thin dispatcher with init.cjs routing for non-fork subcommands"
      contains: "init.cmdInitNewProject"
  key_links:
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/init.cjs"
      via: "init.cmdXxx() calls in dispatcher case block"
      pattern: "init\\.cmdInit(NewProject|NewMilestone|Quick|Resume|VerifyWork|PhaseOp|MilestoneOp|MapCodebase)"
---

<objective>
Close 2 verification gaps from 46-VERIFICATION.md: route 8 non-fork init subcommands through init.cjs module functions and remove the ~340 lines of inline cmdInit* duplicates.

Purpose: init.cjs is currently an orphaned import -- required at line 39 but never called. The 8 non-fork init subcommands still route to inline function copies instead of the upstream module. This contradicts Phase 46's goal of routing all upstream commands through modules.

Output: gsd-tools.cjs with init.cjs fully wired and ~340 lines of dead inline code removed.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-upstream-module-adoption/46-VERIFICATION.md
@.planning/phases/46-upstream-module-adoption/46-02-SUMMARY.md
@get-shit-done/bin/gsd-tools.cjs
@get-shit-done/bin/lib/init.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route 8 non-fork init subcommands through init.cjs and remove inline duplicates</name>
  <files>get-shit-done/bin/gsd-tools.cjs</files>
  <action>
In the init case block (around line 3270), replace 8 inline function calls with init.cjs module calls. The calling conventions match exactly (same param count and order).

Replace these dispatcher lines:
- `cmdInitNewProject(cwd, raw)` with `init.cmdInitNewProject(cwd, raw)`
- `cmdInitNewMilestone(cwd, raw)` with `init.cmdInitNewMilestone(cwd, raw)`
- `cmdInitQuick(cwd, args.slice(2).join(' '), raw)` with `init.cmdInitQuick(cwd, args.slice(2).join(' '), raw)`
- `cmdInitResume(cwd, raw)` with `init.cmdInitResume(cwd, raw)`
- `cmdInitVerifyWork(cwd, args[2], raw)` with `init.cmdInitVerifyWork(cwd, args[2], raw)`
- `cmdInitPhaseOp(cwd, args[2], raw)` with `init.cmdInitPhaseOp(cwd, args[2], raw)`
- `cmdInitMilestoneOp(cwd, raw)` with `init.cmdInitMilestoneOp(cwd, raw)`
- `cmdInitMapCodebase(cwd, raw)` with `init.cmdInitMapCodebase(cwd, raw)`

DO NOT change these 4 fork override routes (they must stay calling inline functions):
- `cmdInitExecutePhase(cwd, args[2], includes, raw)` -- fork override with 4-param includes signature
- `cmdInitPlanPhase(cwd, args[2], includes, raw)` -- fork override with 4-param includes signature
- `cmdInitTodos(cwd, args[2], raw)` -- fork override adding priority/source/status fields
- `cmdInitProgress(cwd, includes, raw)` -- fork override with includes param

Then delete the 8 inline function definitions that are now dead code. These are approximately at:
- `cmdInitNewProject` (starts ~line 852, ~55 lines)
- `cmdInitNewMilestone` (starts ~line 908, ~26 lines)
- `cmdInitQuick` (starts ~line 935, ~46 lines)
- `cmdInitResume` (starts ~line 982, ~26 lines)
- `cmdInitVerifyWork` (starts ~line 1009, ~28 lines)
- `cmdInitPhaseOp` (starts ~line 1038, ~31 lines)
- `cmdInitMilestoneOp` (starts ~line 1135, ~60 lines)
- `cmdInitMapCodebase` (starts ~line 1196, ~33 lines)

Keep `cmdInitTodos` (starts ~line 1070) -- it is a fork override.
Keep `cmdInitExecutePhase`, `cmdInitPlanPhase`, `cmdInitProgress` -- they are fork overrides defined elsewhere in the file.

After deletion, verify the `const init = require('./lib/init.cjs')` at line 39 is still present.
  </action>
  <verify>
1. Run `grep -c 'init\.cmdInit' get-shit-done/bin/gsd-tools.cjs` -- should return 8 (the 8 module calls in the dispatcher)
2. Run `grep -c 'function cmdInitNewProject\|function cmdInitNewMilestone\|function cmdInitQuick\|function cmdInitResume\|function cmdInitVerifyWork\|function cmdInitPhaseOp\|function cmdInitMilestoneOp\|function cmdInitMapCodebase' get-shit-done/bin/gsd-tools.cjs` -- should return 0 (all inline definitions removed)
3. Run `grep -c 'function cmdInitTodos' get-shit-done/bin/gsd-tools.cjs` -- should return 1 (fork override kept)
4. Run `wc -l get-shit-done/bin/gsd-tools.cjs` -- should be ~3,170 lines (3,513 minus ~340 removed)
  </verify>
  <done>8 init subcommands route through init.cjs module, 8 inline definitions removed (~340 lines), cmdInitTodos remains inline as fork override</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to verify behavioral equivalence</name>
  <files>get-shit-done/bin/gsd-tools.cjs</files>
  <action>
Run the complete test suite to confirm no behavioral regressions from the routing change:

1. Run `npm test` (350 vitest tests)
2. Run `npm run test:upstream` (174 upstream node:test tests)
3. Run `npm run test:upstream:fork` (10 fork-specific tests)

All 534 tests must pass. The init.cjs module functions should produce identical output to the inline copies for all 8 subcommands since upstream modules were adopted wholesale in plan 01.

If any test fails:
- Check if the failure is related to the init routing change (regression) vs environmental (ETIMEDOUT, resource contention)
- For regressions: the upstream init.cjs function may have a different return shape. Compare the failing test's expected output against init.cjs's actual output. Fix the dispatcher call if needed.
- For environmental failures: re-run the specific failing test in isolation to confirm.

Note: VERIFICATION.md documents that upstream init.cjs has improvements over the inline copies (e.g., collision-resistant IDs in cmdInitQuick, ROADMAP.md fallback in cmdInitPhaseOp, additional return fields in cmdInitResume). These are intentional upgrades, not regressions. If tests fail because they expected old behavior, that would indicate the test needs updating -- but since tests invoke via CLI subprocess and check JSON output structure, they should pass with the richer output.
  </action>
  <verify>
1. `npm test` exits 0 with all tests passing
2. `npm run test:upstream` exits 0
3. `npm run test:upstream:fork` exits 0
4. Total: 534 tests pass
  </verify>
  <done>All 534 tests pass, confirming behavioral equivalence after init.cjs routing and inline code removal</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep 'init\.cmdInit' get-shit-done/bin/gsd-tools.cjs` shows 8 init.cjs module calls in the dispatcher
2. No inline cmdInit* definitions remain except cmdInitTodos (fork override)
3. `wc -l get-shit-done/bin/gsd-tools.cjs` shows ~340 fewer lines than pre-gap-closure
4. All 534 tests pass
5. `node get-shit-done/bin/gsd-tools.cjs init new-project --raw 2>/dev/null | node -e "process.stdin.on('data',d=>console.log(Object.keys(JSON.parse(d)).length))"` returns a number (init.cjs is actually being called, not erroring)
</verification>

<success_criteria>
- init.cjs is no longer an orphaned import -- 8 dispatcher routes call init.cmdXxx()
- ~340 lines of dead inline cmdInit* functions removed from gsd-tools.cjs
- cmdInitTodos remains inline as the sole init fork override
- All 534 tests pass with zero regressions
- gsd-tools.cjs is ~3,170 lines (down from 3,513)
</success_criteria>

<output>
After completion, create `.planning/phases/46-upstream-module-adoption/46-04-SUMMARY.md`
</output>
