---
phase: 46-upstream-module-adoption
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - get-shit-done/bin/gsd-tools.cjs
autonomous: true

must_haves:
  truths:
    - "gsd-tools.cjs is a thin dispatcher that requires lib/*.cjs modules for upstream command routing"
    - "Fork-specific commands (manifest, backlog, automation, sensors, health-probe) are routed to inline functions"
    - "Fork's 3 init functions (execute-phase, plan-phase, progress) use parseIncludeFlag and the 4-param signature"
    - "All upstream commands are routed through module functions, not inline definitions"
    - "The dispatcher supports --cwd and multi-word commit messages (upstream improvements)"
    - "All dead upstream function definitions are removed from gsd-tools.cjs"
  artifacts:
    - path: "get-shit-done/bin/gsd-tools.cjs"
      provides: "Thin CLI dispatcher + inline fork functions"
  key_links:
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "require('./lib/core.cjs')"
      pattern: "require.*lib/core\\.cjs"
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/state.cjs"
      via: "require('./lib/state.cjs')"
      pattern: "require.*lib/state\\.cjs"
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/init.cjs"
      via: "require('./lib/init.cjs')"
      pattern: "require.*lib/init\\.cjs"
---

<objective>
Rewrite gsd-tools.cjs from a 6,651-line monolith into a thin dispatcher (~3,000 lines) that requires upstream modules for command routing while retaining fork-specific functions inline.

Purpose: This is the core transformation of Phase 46. The monolith's ~3,100 lines of upstream function definitions (lines 146-612 shared helpers, 613-3776 command functions) are replaced by require() calls to the 11 lib/*.cjs modules from Plan 01. Fork-specific functions (~2,300 lines) remain inline until Phase 47 extracts them.

Output: A gsd-tools.cjs that is roughly half its current size, with the dispatcher section adopting upstream's structure (--cwd support, multi-word commit messages, new commands) and fork extensions.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-upstream-module-adoption/46-RESEARCH.md
@.planning/phases/46-upstream-module-adoption/46-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite gsd-tools.cjs as dispatcher + fork functions</name>
  <files>get-shit-done/bin/gsd-tools.cjs</files>
  <action>
    Rewrite `get-shit-done/bin/gsd-tools.cjs` with the following structure. The file must have FIVE sections in this order:

    **Section 1: Shebang + header comment + module requires (~30 lines)**

    Keep the shebang line. Update the header comment to match upstream's format but note fork extensions. Add module requires matching upstream's pattern exactly:

    ```javascript
    #!/usr/bin/env node
    // ... header comment ...
    const fs = require('fs');
    const path = require('path');
    const { execSync } = require('child_process');
    const { error, output, safeReadFile, loadConfig, findPhaseInternal, resolveModelInternal,
            pathExistsInternal, generateSlugInternal, getMilestoneInfo, normalizePhaseName,
            loadManifest, loadProjectConfig, atomicWriteJson, parseIncludeFlag } = require('./lib/core.cjs');
    const state = require('./lib/state.cjs');
    const phase = require('./lib/phase.cjs');
    const roadmap = require('./lib/roadmap.cjs');
    const verify = require('./lib/verify.cjs');
    const config = require('./lib/config.cjs');
    const template = require('./lib/template.cjs');
    const milestone = require('./lib/milestone.cjs');
    const commands = require('./lib/commands.cjs');
    const init = require('./lib/init.cjs');
    const frontmatter = require('./lib/frontmatter.cjs');
    ```

    Note: Destructure `core` imports that fork functions call directly (error, output, safeReadFile, loadConfig, findPhaseInternal, resolveModelInternal, pathExistsInternal, generateSlugInternal, getMilestoneInfo, normalizePhaseName, loadManifest, loadProjectConfig, atomicWriteJson, parseIncludeFlag). The fork's inline functions (init overrides, manifest, backlog, automation, sensors, health-probe) call these helpers directly by name, so they must be in scope.

    Also destructure or import `execSync` from `child_process` since fork functions (like cmdCommit in the monolith had it) may reference it. Check all fork-specific functions (lines 3776-6102) for their dependency on monolith-scoped helpers and ensure all are available via require or destructuring.

    **Section 2: Fork-specific constants and helpers (~250 lines)**

    Keep the following from the current monolith, UNCHANGED:
    - `KNOWN_TOP_LEVEL_KEYS` Set (line 531-536)
    - `FEATURE_CAPABILITY_MAP` object (line 538-555)
    - `coerceValue()` function (line 559-581)
    - `validateFieldType()` function (line 513-522)
    - `validateFieldEnum()` function (line 524-527)
    - `formatMigrationEntry()` function (line 590-609)
    - `extractFrontmatter()` function (line 257-330) -- needed by fork init functions
    - `reconstructFrontmatter()` function (line 332-394) -- needed by fork state operations
    - `spliceFrontmatter()` function (line 396-403) -- needed by fork state operations
    - `parseMustHavesBlock()` function (line 405-468) -- needed by fork verification

    IMPORTANT: The fork's inline functions call `output()`, `error()`, `safeReadFile()`, `loadConfig()`, `findPhaseInternal()`, `resolveModelInternal()`, `pathExistsInternal()`, `generateSlugInternal()`, `getMilestoneInfo()` directly. These are now imported from core.cjs via destructuring (Section 1). Do NOT re-define them. Do NOT keep the fork's old copies of these functions.

    Also keep `resolveBacklogDir()` and `readBacklogItems()` helper functions (lines 3778-3816) needed by backlog commands.

    **Section 3: Fork-specific command functions (~2,300 lines)**

    Keep ALL fork-specific command functions UNCHANGED from lines 3817-6102:
    - Backlog: cmdBacklogAdd, cmdBacklogList, cmdBacklogUpdate, cmdBacklogStats, cmdBacklogGroup, cmdBacklogPromote, cmdBacklogIndex (lines 3817-4121)
    - Fork init overrides: cmdInitExecutePhase, cmdInitPlanPhase, cmdInitProgress (lines 4123-4774) -- these use the 4-param `(cwd, phase, includes, raw)` signature that upstream's init.cjs does not support
    - Manifest: cmdManifestDiffConfig, cmdManifestValidate, cmdManifestGetPrompts, cmdManifestApplyMigration, cmdManifestLogMigration, cmdManifestAutoDetect (lines 4778-5111)
    - Automation: cmdAutomationResolveLevel, cmdAutomationTrackEvent, cmdAutomationLock, cmdAutomationUnlock, cmdAutomationCheckLock, cmdAutomationRegimeChange, cmdAutomationReflectionCounter (lines 5113-5625)
    - Health probe: cmdHealthProbeSignalMetrics, cmdHealthProbeSignalDensity, cmdHealthProbeAutomationWatchdog (lines 5627-5969)
    - Sensors: cmdSensorsList, cmdSensorsBlindSpots (lines 5971-6101)

    These functions reference `output()`, `error()`, `loadConfig()`, `loadManifest()`, `loadProjectConfig()`, `atomicWriteJson()`, `findPhaseInternal()`, `resolveModelInternal()`, `pathExistsInternal()`, `generateSlugInternal()`, `getMilestoneInfo()`, `safeReadFile()` -- all now available via Section 1 destructuring.

    They also reference `extractFrontmatter()`, `reconstructFrontmatter()`, `spliceFrontmatter()`, `parseMustHavesBlock()`, `KNOWN_TOP_LEVEL_KEYS`, `FEATURE_CAPABILITY_MAP`, `coerceValue()`, `validateFieldType()`, `validateFieldEnum()`, `formatMigrationEntry()`, `resolveBacklogDir()`, `readBacklogItems()` -- all kept in Section 2.

    **Section 4: CLI dispatcher (~550 lines)**

    Adopt upstream's dispatcher structure VERBATIM from `upstream/main:get-shit-done/bin/gsd-tools.cjs` and EXTEND it with fork-specific case blocks. The dispatcher must:

    1. Use upstream's `--cwd` parsing (lines 137-155 of upstream dispatcher). The fork currently lacks `--cwd` support -- adopt it.
    2. Use upstream's `--raw` parsing.
    3. Route all upstream commands to module functions (e.g., `state.cmdStateLoad(cwd, raw)` not `cmdStateLoad(cwd, raw)`).
    4. Include upstream's new command routing: `stats`, `config-get`, `requirements mark-complete`, `validate health [--repair]`, `state json`, `roadmap update-plan-progress`.
    5. Include upstream's improved argument parsing: multi-word commit messages via join pattern, `--summary-file`/`--rationale-file` for state add-decision, `--text-file` for state add-blocker, `--include-archived` for phases list, `--archive-phases` for milestone complete.

    Then ADD fork-specific case blocks for:
    - `manifest` -- routes to inline cmdManifest* functions
    - `backlog` -- routes to inline cmdBacklog* functions
    - `automation` -- routes to inline cmdAutomation* functions
    - `sensors` -- routes to inline cmdSensors* functions
    - `health-probe` -- routes to inline cmdHealthProbe* functions

    The `init` case block is CRITICAL. It must:
    - Parse `--include` flag using `parseIncludeFlag(args)` (from core.cjs)
    - Route `execute-phase` to fork's inline `cmdInitExecutePhase(cwd, args[2], includes, raw)` (4 params)
    - Route `plan-phase` to fork's inline `cmdInitPlanPhase(cwd, args[2], includes, raw)` (4 params)
    - Route `progress` to fork's inline `cmdInitProgress(cwd, includes, raw)` (3 params)
    - Route ALL OTHER init workflows to upstream's `init.cmdXxx()` functions (3 params, no includes)

    Copy the fork's current case blocks for manifest (lines 6483-6502), backlog (6504-6563), automation (6566-6617), sensors (6619-6630), and health-probe (6632-6644) verbatim into the new dispatcher.

    **Section 5: main() call**

    ```javascript
    main();
    ```

    **DELETE everything that is NOT in Sections 1-5:**
    - All upstream helper functions that are now in core.cjs (safeReadFile, loadConfig, isGitIgnored, execGit, normalizePhaseName, etc. -- lines 154-484)
    - All upstream command functions (cmdGenerateSlug through cmdWebsearch, cmdStateLoad through cmdProgressRender, etc. -- lines 613-3776)
    - The `MODEL_PROFILES` constant (line 115-142) -- now in core.cjs
    - The `output()` and `error()` functions (lines 471-483) -- now in core.cjs
    - The old CLI router (lines 6103-6651) -- replaced by new dispatcher

    **Line count target:** Approximately 3,000-3,200 lines (Section 1: ~30, Section 2: ~250, Section 3: ~2,300, Section 4: ~550, Section 5: ~1).
  </action>
  <verify>
    Run: `wc -l get-shit-done/bin/gsd-tools.cjs` -- expect approximately 3,000-3,200 lines (down from 6,651).
    Run: `node -e "require('./get-shit-done/bin/gsd-tools.cjs')"` -- should not throw (but will exit since no args -- check it does not crash with import errors).
    Run: `grep -c "require.*lib/" get-shit-done/bin/gsd-tools.cjs` -- expect 11 (one per module).
    Run: `grep "case 'manifest'" get-shit-done/bin/gsd-tools.cjs` -- fork case block present.
    Run: `grep "case 'backlog'" get-shit-done/bin/gsd-tools.cjs` -- fork case block present.
    Run: `grep "case 'automation'" get-shit-done/bin/gsd-tools.cjs` -- fork case block present.
    Run: `grep "case 'sensors'" get-shit-done/bin/gsd-tools.cjs` -- fork case block present.
    Run: `grep "case 'health-probe'" get-shit-done/bin/gsd-tools.cjs` -- fork case block present.
    Run: `grep "parseIncludeFlag" get-shit-done/bin/gsd-tools.cjs` -- present in init case block.
    Run: `grep "\-\-cwd" get-shit-done/bin/gsd-tools.cjs` -- --cwd support adopted from upstream.
  </verify>
  <done>gsd-tools.cjs is roughly half its previous size, requires 11 upstream modules, retains all fork functions inline, dispatcher routes correctly to both modules and inline functions, --cwd and multi-word commit support adopted</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and fix any breakage</name>
  <files>get-shit-done/bin/gsd-tools.cjs</files>
  <action>
    Run all three test suites to verify behavioral equivalence after the dispatcher rewrite:

    ```bash
    npm test                      # 354 vitest tests
    npm run test:upstream         # 174 upstream node:test tests
    npm run test:upstream:fork    # 10 fork-specific tests
    ```

    All tests invoke `gsd-tools.cjs` as a subprocess via `execSync` -- they never require() modules directly. The modularization should be invisible to them as long as CLI behavior is identical.

    **Expected breakage patterns and fixes:**

    1. **Missing helper in scope:** If a fork function calls a helper that was neither destructured from core.cjs nor kept in Section 2, add it to the destructuring in Section 1 or copy it to Section 2.

    2. **Argument parsing differences:** Upstream's dispatcher parses some arguments differently (e.g., `state add-decision` now expects `{ summary, summary_file, rationale, rationale_file }` instead of `{ summary, rationale }`). If fork tests pass simpler argument shapes, the upstream module functions should still handle them (file-based args default to null). But check.

    3. **JSON output shape differences:** Upstream's `loadConfig()` now returns `nyquist_validation` field. If any test exact-matches the config object shape, it may need the extra field. However, tests typically test specific fields, not the full object.

    4. **New upstream error messages:** If upstream changed error message wording, tests matching exact error text may fail. Update the test expectations.

    Fix any failures, re-run until all pass. Do NOT modify upstream module files -- fix issues in gsd-tools.cjs or in test expectations.

    Also run the installer to verify lib/ directory is correctly handled:

    ```bash
    node bin/install.js --local
    ls .claude/get-shit-done-reflect/bin/lib/*.cjs | wc -l  # expect 11
    ```
  </action>
  <verify>
    Run: `npm test` -- 0 failures.
    Run: `npm run test:upstream` -- 0 failures.
    Run: `npm run test:upstream:fork` -- 0 failures.
    Run: `ls .claude/get-shit-done-reflect/bin/lib/*.cjs | wc -l` -- returns 11.
  </verify>
  <done>All 354 vitest + 174 upstream + 10 fork tests pass, installer correctly copies lib/ subdirectory with all 11 modules</done>
</task>

</tasks>

<verification>
- gsd-tools.cjs line count is approximately 3,000-3,200 (down from 6,651)
- The file requires all 11 lib/*.cjs modules
- Fork-specific commands (manifest, backlog, automation, sensors, health-probe) route to inline functions
- Fork init overrides use parseIncludeFlag and 4-param signature
- Upstream init subcommands route to init.cjs module functions
- --cwd support present in dispatcher
- Multi-word commit message parsing present
- All 6 new upstream commands routable (stats, config-get, requirements mark-complete, validate health, state json, roadmap update-plan-progress)
- All test suites pass (354 + 174 + 10 = 538 total)
- Installer copies lib/ subdirectory correctly
</verification>

<success_criteria>
- `wc -l get-shit-done/bin/gsd-tools.cjs` returns approximately 3,000-3,200
- `npm test` passes all 354 tests
- `npm run test:upstream` passes all 174 tests
- `npm run test:upstream:fork` passes all 10 tests
- `grep -c "require.*lib/" get-shit-done/bin/gsd-tools.cjs` returns 11
- Fork commands (manifest, backlog, automation, sensors, health-probe) work via CLI
</success_criteria>

<output>
After completion, create `.planning/phases/46-upstream-module-adoption/46-02-SUMMARY.md`
</output>
