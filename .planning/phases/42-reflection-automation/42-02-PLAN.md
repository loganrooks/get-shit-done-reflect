---
phase: 42-reflection-automation
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - get-shit-done/workflows/reflect.md
  - get-shit-done/references/reflection-patterns.md
autonomous: true

must_haves:
  truths:
    - "Maximum one auto-reflection per session -- session cooldown prevents re-triggering across multi-phase executions"
    - "Manual /gsd:reflect bypasses session cooldown entirely"
    - "reflect.md resets phases_since_last_reflect counter on completion (both manual and auto-triggered)"
    - "Reflection reports contain confidence_history for lesson candidates with directional updates"
    - "Reflector reads previous reports to find existing lesson confidence states (report-to-report chaining)"
  artifacts:
    - path: "get-shit-done/workflows/reflect.md"
      provides: "Counter reset call at reflection completion, confidence update instructions for reflector"
      contains: "reflection-counter reset"
    - path: "get-shit-done/references/reflection-patterns.md"
      provides: "confidence_history schema and directional update rules"
      contains: "confidence_history"
  key_links:
    - from: "get-shit-done/workflows/reflect.md"
      to: "get-shit-done/bin/gsd-tools.js"
      via: "reflection-counter reset call at completion"
      pattern: "reflection-counter reset"
    - from: "get-shit-done/references/reflection-patterns.md"
      to: "get-shit-done/workflows/reflect.md"
      via: "reflector agent reads patterns reference for confidence update rules"
      pattern: "confidence_history|directional.*update"
    - from: "get-shit-done/workflows/reflect.md"
      to: ".planning/knowledge/reflections/"
      via: "report-to-report chaining for prior confidence lookup"
      pattern: "reflect-.*\\.md"
---

<objective>
Add counter reset to reflect.md (both manual and auto invocations), and implement mutable lesson confidence with directional updates in the reflection system -- confidence_history schema in reflection-patterns.md and report-to-report chaining instructions in reflect.md.

Purpose: REFL-04 (session cooldown is architecturally in-memory via the postlude from Plan 01 -- this plan ensures manual reflect also resets the counter) and REFL-05 (lesson confidence evolves directionally through evidence, tracked in reflection reports rather than deprecated lesson files).
Output: reflect.md with counter reset, reflection-patterns.md with confidence_history schema, confidence update rules for the reflector agent.
</objective>

<execution_context>
@./.claude/get-shit-done-reflect/workflows/execute-plan.md
@./.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-reflection-automation/42-RESEARCH.md
@.planning/phases/42-reflection-automation/42-01-SUMMARY.md
@get-shit-done/workflows/reflect.md
@get-shit-done/references/reflection-patterns.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add counter reset and confidence update instructions to reflect.md</name>
  <files>get-shit-done/workflows/reflect.md</files>
  <action>
**1. Add counter reset step to reflect.md** -- insert a new step `reset_reflection_counter` between the existing `commit_report` step (ends around line 637) and the `empty_state_handling` section (starts around line 641):

```markdown
<step name="reset_reflection_counter">

Reset the reflection phase counter after successful reflection completion. This ensures
both manual (`/gsd:reflect`) and auto-triggered reflections reset the counter, so the
next auto-reflection is scheduled correctly.

```bash
node ~/.claude/get-shit-done/bin/gsd-tools.js automation reflection-counter reset --raw 2>/dev/null || echo "Warning: Counter reset failed (non-blocking)"
```

This step is best-effort. If it fails (e.g., no config.json), reflection still completes
successfully. The counter will be stale but will not cause incorrect behavior -- the
auto_reflect postlude re-reads the counter from config each time.

</step>
```

**2. Add confidence update instructions to the `spawn_reflector` step** -- in the existing `spawn_reflector` step (around line 261), append the following to the reflector task instructions (inside the prompt that describes what the reflector should do):

Add after the existing reflector instructions (around line 315, before the `</step>` tag), a new subsection:

```markdown
**Lesson Confidence Updates (REFL-05):**

After pattern detection and triage, update confidence on existing lesson candidates:

1. Read the most recent reflection report from `.planning/knowledge/reflections/{project}/`
   (the `persist_report` step writes reports as `reflect-YYYY-MM-DD.md`). If no prior
   report exists, skip confidence updates -- all lessons are new.

2. For each lesson candidate in the PREVIOUS report that also appears in the current
   analysis (matched by lesson title or topic):
   - **Corroborating signals:** New signals that match the lesson's predicted pattern
     (same tags, same signal_type, recurring in the expected direction) increase
     confidence one step: low -> medium, medium -> high, high -> high (ceiling).
   - **Contradicting signals:** New signals showing the problem is resolved (positive
     signal_category where the lesson predicted negative recurrence) decrease confidence
     one step: high -> medium, medium -> low, low -> low (floor).
   - **Irrelevant signals:** Signals unrelated to the lesson's domain produce no update.
     Do NOT decay confidence due to irrelevance ("untestable milestone" rule).

3. Include a `## Lesson Confidence Updates` section in the reflection report with:

```
## Lesson Confidence Updates

### Lesson: {title}
**Prior confidence:** {level} (from reflect-{date}.md)
**New evidence:** {N} {corroborating|contradicting} signal(s) ({sig-IDs})
**Update:** {from} -> {to} ({reason})
**Confidence history:**
| Date | From | To | Evidence | Reason |
|------|------|----|----------|--------|
| {date} | {from} | {to} | {sig-IDs} | {reason} |
| ... prior entries from previous report ... |
```

4. If a lesson candidate appears for the first time (not in any prior report), set
   initial confidence based on evidence strength:
   - 4+ evidence signals: medium
   - 1-3 evidence signals: low
   No initial confidence starts at high -- high is earned through corroboration.

5. Carry forward the full confidence_history from the prior report for each lesson,
   appending the new entry. This creates a complete audit trail across reflections.
```

**Important considerations:**
- The confidence update section is advisory to the reflector agent -- the reflector is an intelligent agent that interprets these instructions. The exact matching heuristics may vary.
- The reflector already reads the KB index and signal files. The additional cost is reading one prior reflection report (~small).
- Do NOT modify the `persist_report` step -- it already writes the full report including whatever sections the reflector produces.
  </action>
  <verify>
1. Verify reflect.md contains `reset_reflection_counter` step: search for `reflection-counter reset`
2. Verify the step appears after `commit_report` and before `empty_state_handling`
3. Verify reflect.md contains `Lesson Confidence Updates` instructions in the spawn_reflector step
4. Verify reflect.md contains `confidence_history` reference
5. `npm test` passes
  </verify>
  <done>
- reflect.md resets counter on completion (both manual and auto-triggered reflections)
- Counter reset is best-effort (failure does not break reflection)
- Reflector agent receives confidence update instructions with directional step rules
- Report-to-report chaining: reflector reads previous report for prior confidence state
- Confidence_history audit trail carried forward across reflections
  </done>
</task>

<task type="auto">
  <name>Task 2: Add confidence_history schema to reflection-patterns.md</name>
  <files>get-shit-done/references/reflection-patterns.md</files>
  <action>
Add a new section to reflection-patterns.md that formally documents the confidence_history schema and directional update rules. This section is referenced by the reflector agent during analysis.

**Insert as a new top-level section** (e.g., Section 7 or after the last existing section) titled `## Lesson Confidence Evolution`:

```markdown
## Lesson Confidence Evolution (REFL-05)

### Confidence Step Ladder

Lesson confidence moves directionally based on signal evidence:

```
low <-> medium <-> high
```

- **One step up:** low -> medium, medium -> high, high -> high (ceiling)
- **One step down:** high -> medium, medium -> low, low -> low (floor)

### Update Triggers

| Trigger | Direction | Description |
|---------|-----------|-------------|
| Corroborating signals | +1 step | New signals match the lesson's predicted pattern (same tags, same signal_type, recurring as expected) |
| Contradicting signals | -1 step | New signals show the predicted problem is resolved or reversed (positive category where lesson predicted negative recurrence) |
| Irrelevant signals | No change | Signals unrelated to the lesson's domain. Confidence does NOT decay due to irrelevance ("untestable milestone" rule) |
| No new signals in domain | No change | Absence of evidence is not evidence of absence |

### Initial Confidence Assignment

New lesson candidates (first appearance, not in any prior report):

| Evidence Signals | Initial Confidence |
|------------------|--------------------|
| 4+ signals | medium |
| 1-3 signals | low |

No lesson starts at high confidence. High is earned through corroboration across reflections.

### confidence_history Schema

Each lesson candidate in a reflection report includes a cumulative confidence_history:

```markdown
### Lesson: {title}
**Prior confidence:** {level} (from reflect-{YYYY-MM-DD}.md)
**New evidence:** {N} {corroborating|contradicting} signal(s) ({sig-IDs})
**Update:** {from} -> {to} ({reason})
**Confidence history:**
| Date | From | To | Evidence | Reason |
|------|------|----|----------|--------|
| 2026-03-06 | medium | high | sig-A, sig-B, sig-C | 3 signals match predicted pattern |
| 2026-03-04 | low | medium | sig-X, sig-Y, sig-Z, sig-W | Initial distillation (4 evidence) |
```

The history table is cumulative -- each reflection appends a new row and carries forward
all prior rows from the previous report.

### Report-to-Report Chaining

Confidence state is tracked across reflection reports, not in individual lesson files
(lessons are deprecated as standalone KB entries per knowledge-store.md).

The reflector reads the most recent reflection report to find:
1. Which lesson candidates exist and their current confidence level
2. The full confidence_history to carry forward
3. Which signal patterns were previously associated with each lesson

Reports are stored at `.planning/knowledge/reflections/{project}/reflect-YYYY-MM-DD.md`.
```

**After writing, run the installer** to sync to .claude/:
```bash
node bin/install.js --local
```
  </action>
  <verify>
1. Verify reflection-patterns.md contains `Lesson Confidence Evolution` section
2. Verify it documents the step ladder (low/medium/high), update triggers table, initial confidence rules, and confidence_history schema
3. Verify it references report-to-report chaining pattern
4. Verify local install syncs: `.claude/get-shit-done-reflect/references/reflection-patterns.md` contains `confidence_history`
5. `npm test` passes
  </verify>
  <done>
- reflection-patterns.md documents the full REFL-05 confidence evolution system
- Step ladder defined: low <-> medium <-> high with ceiling and floor
- Update triggers documented: corroborating (+1), contradicting (-1), irrelevant (no change)
- Initial confidence rules: 4+ signals = medium, 1-3 = low, never high initially
- confidence_history schema with cumulative table format
- Report-to-report chaining pattern documented (not lesson files)
- Local install produces working .claude/ copy
  </done>
</task>

</tasks>

<verification>
1. reflect.md contains `reset_reflection_counter` step that calls `reflection-counter reset`
2. reflect.md contains confidence update instructions for the reflector agent
3. reflection-patterns.md contains `confidence_history` schema and directional update rules
4. Local install syncs all changes to .claude/ with correct path replacement
5. `npm test` passes
</verification>

<success_criteria>
- Any reflection run (manual or auto) resets the phase counter, ensuring correct scheduling
- Session cooldown is architecturally sound: in-memory flag in postlude (Plan 01), manual reflect bypasses it
- Confidence evolves directionally with evidence (REFL-05): corroborate +1, contradict -1, irrelevant no change
- Report-to-report chaining preserves confidence audit trail across reflections
- No resurrection of deprecated lesson files -- confidence lives in reflection reports only
</success_criteria>

<output>
After completion, create `.planning/phases/42-reflection-automation/42-02-SUMMARY.md`
</output>
