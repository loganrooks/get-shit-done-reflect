---
phase: quick
plan: 260417-5ql
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.js
  - get-shit-done/templates/codex-compact-prompt.md
  - tests/unit/install.test.js
  - tests/integration/multi-runtime.test.js
autonomous: true
must_haves:
  truths:
    - "Codex config.toml uses experimental_compact_prompt_file as a top-level string path"
    - "GSD Codex installs write a stable prompt file path under the installed runtime tree"
    - "Installer updates are idempotent and preserve user-owned config.toml content"
    - "Codex global install writes the prompt override into ~/.codex/config.toml"
  artifacts:
    - path: "bin/install.js"
      provides: "Codex config block writes experimental_compact_prompt_file"
      contains: "experimental_compact_prompt_file"
    - path: "get-shit-done/templates/codex-compact-prompt.md"
      provides: "GSD-specific compaction guidance"
      contains: "Preserve continuity for an active GSD Reflect engineering session in this repo."
    - path: "tests/unit/install.test.js"
      provides: "Unit coverage for config generation and idempotency"
      contains: "experimental_compact_prompt_file"
    - path: "tests/integration/multi-runtime.test.js"
      provides: "Install-level coverage for prompt file deployment"
      contains: "codex compact prompt file should exist after Codex install"
  key_links:
    - "OpenAI Codex config reference: experimental_compact_prompt_file"
---

<objective>
Add a GSD-owned Codex compaction prompt file and configure Codex installs to use it via
`experimental_compact_prompt_file`, so auto-compaction keeps workflow-critical GSD state.
</objective>

<context>
@bin/install.js
@tests/unit/install.test.js
@tests/integration/multi-runtime.test.js
@get-shit-done/templates/codex-compact-prompt.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship the prompt file and write it into Codex config.toml</name>
  <files>bin/install.js, get-shit-done/templates/codex-compact-prompt.md</files>
  <action>
Create a stable prompt file inside the installed GSD tree at
`get-shit-done-reflect/templates/codex-compact-prompt.md`.

Update Codex config generation so the GSD-managed block writes:
- `experimental_compact_prompt_file = "/abs/path/to/.../codex-compact-prompt.md"`
- existing GSD MCP server entries

Keep the update idempotent and avoid overwriting non-GSD sections of `config.toml`.
  </action>
  <verify>
Check that:
- generated config contains `experimental_compact_prompt_file`
- generated config still contains `[mcp_servers.context7]`
- repeated generation keeps a single prompt-file entry
  </verify>
  <done>
Codex installs now point `experimental_compact_prompt_file` at a shipped GSD prompt file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cover the change with unit and integration tests, then install live</name>
  <files>tests/unit/install.test.js, tests/integration/multi-runtime.test.js</files>
  <action>
Add assertions that:
- config generation includes the prompt-file path
- repeated generation does not duplicate the key
- `--codex --global` installs the prompt file and writes the config line

After tests pass, run the local installer so the live `~/.codex/config.toml` points to the new file.
  </action>
  <verify>
Run:
- `npm test -- tests/unit/install.test.js tests/integration/multi-runtime.test.js`
- `rg -n "experimental_compact_prompt_file" ~/.codex/config.toml`
- `test -f ~/.codex/get-shit-done-reflect/templates/codex-compact-prompt.md`
  </verify>
  <done>
Tests pass and the live Codex config points to the installed GSD compaction prompt file.
  </done>
</task>

</tasks>
