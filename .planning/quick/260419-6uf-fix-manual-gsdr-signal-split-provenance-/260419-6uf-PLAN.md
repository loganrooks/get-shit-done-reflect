---
phase: quick-260419-6uf
plan: 01
quick_id: 260419-6uf
description: Fix manual gsdr-signal split provenance gap left out of 57.8
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/signal.md
  - tests/unit/install.test.js
  - .codex/skills/gsdr-signal/SKILL.md
autonomous: true
signature:
  role: planner
  harness: codex-cli
  platform: codex
  vendor: openai
  model: gpt-5.4
  reasoning_effort: xhigh
  profile: quality
  gsd_version: 1.19.4+dev
  generated_at: "2026-04-19T08:58:17.535Z"
  session_id: 019da4f4-8760-7112-b29b-b7bf49f14479
  provenance_status:
    role: derived
    harness: exposed
    platform: derived
    vendor: derived
    model: exposed
    reasoning_effort: exposed
    profile: derived
    gsd_version: derived
    generated_at: exposed
    session_id: exposed
  provenance_source:
    role: artifact_role
    harness: runtime_context
    platform: derived_from_harness
    vendor: derived_from_harness
    model: codex_state_store
    reasoning_effort: codex_state_store
    profile: config
    gsd_version: installed_harness
    generated_at: writer_clock
    session_id: env:CODEX_THREAD_ID
must_haves:
  truths:
    - "Manual `gsdr-signal` instructions teach `provenance_schema`, `about_work`, `detected_by`, and `written_by` as the canonical manual-signal schema, with flat `runtime` / `model` / `gsd_version` described only as compatibility echoes."
    - "The repo-local Codex skill regenerated from source teaches the same split-provenance manual-signal contract as the source command."
    - "Execution leaves enough verification evidence in the quick summary for the quick workflow to append a truthful row to `.planning/STATE.md` without touching `ROADMAP.md`."
  artifacts:
    - path: "commands/gsd/signal.md"
      provides: "Canonical manual signal command aligned to the 57.8 split-provenance contract"
      contains: "provenance_schema"
    - path: "tests/unit/install.test.js"
      provides: "Regression coverage that Codex skill generation preserves the manual signal provenance contract"
      contains: "convertClaudeToCodexSkill"
    - path: ".codex/skills/gsdr-signal/SKILL.md"
      provides: "Repo-local installed Codex skill synced from the updated source command"
      contains: "about_work"
  key_links:
    - from: "commands/gsd/signal.md"
      to: "agents/kb-templates/signal.md"
      via: "manual signal schema and field vocabulary"
      pattern: "provenance_schema|about_work|detected_by|written_by"
    - from: "commands/gsd/signal.md"
      to: "get-shit-done/bin/lib/provenance.cjs"
      via: "manual command instructions align to split provenance helpers and legacy echo semantics"
      pattern: "written_by|runtime|model|gsd_version"
    - from: "commands/gsd/signal.md"
      to: ".codex/skills/gsdr-signal/SKILL.md"
      via: "local Codex install regenerates skill from command source"
      pattern: "provenance_schema|about_work|detected_by|written_by"
---

<objective>
Patch the missed manual `gsdr-signal` provenance surface left out of Phase 57.8 without reopening the broader signal pipeline.

Purpose: 57.8 fixed templates, KB storage, and automated signal surfaces, but the manual command and current Codex skill still teach the pre-split flat provenance story. This quick task closes that gap at the source, proves the install path preserves it, and refreshes the repo-local runtime mirror.
Output: updated source command, focused conversion regression coverage, refreshed repo-local Codex skill.
</objective>

<execution_context>
@./.codex/get-shit-done-reflect/workflows/quick.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/57.8-signal-provenance-split-artifact-signature-blocks/57.8-CONTEXT.md
@.planning/phases/57.8-signal-provenance-split-artifact-signature-blocks/57.8-02-PLAN.md
@commands/gsd/signal.md
@.codex/skills/gsdr-signal/SKILL.md
@agents/kb-templates/signal.md
@get-shit-done/references/signal-detection.md
@agents/knowledge-store.md
@get-shit-done/bin/lib/provenance.cjs
@get-shit-done/bin/lib/kb.cjs
@bin/install.js
@tests/unit/install.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the source manual-signal contract to the 57.8 split provenance model</name>
  <files>commands/gsd/signal.md</files>
  <action>Replace the flat-only manual signal schema and write instructions with the split-provenance contract already established elsewhere in 57.8. The command must teach `provenance_schema: v2_split`, `about_work`, `detected_by`, and `written_by`; describe flat `runtime` / `model` / `gsd_version` only as deprecated compatibility echoes; and align field names/defaults with `agents/kb-templates/signal.md`, `get-shit-done/references/signal-detection.md`, `agents/knowledge-store.md`, and the `buildSignalProvenance` / `buildLegacyFlatEcho` semantics in `get-shit-done/bin/lib/provenance.cjs`. Because manual `gsdr-signal` is both observer and writer, instruct it to fill `detected_by` and `written_by` as separate roles even when they resolve to the same runtime facts. Keep `about_work: []` unless the conversation identifies the artifact under judgment, and use `not_available` rather than invented values when runtime/model/session facts are missing. Do not reopen sensor, KB migration, or broader 60.1 telemetry work.</action>
  <verify>`rg -n "provenance_schema|about_work|detected_by|written_by|compatibility echo|legacy" commands/gsd/signal.md` shows the split contract, and `rg -n "runtime: \\{detected\\}|model: \\{detected\\}|gsd_version: \\{detected\\}" commands/gsd/signal.md` no longer finds the old flat-only schema block.</verify>
  <done>`commands/gsd/signal.md` no longer teaches flat provenance as canonical manual-signal storage.</done>
</task>

<task type="auto">
  <name>Task 2: Add a narrow Codex-skill conversion regression for the manual signal surface</name>
  <files>tests/unit/install.test.js</files>
  <action>Add a focused unit test around `convertClaudeToCodexSkill` that reads `commands/gsd/signal.md` or a representative extracted fixture from it and proves the Codex skill output preserves the split-provenance manual-signal instructions. Assert presence of `provenance_schema`, `about_work`, `detected_by`, and `written_by`, and assert the old flat-only schema block is gone as the primary contract. Keep the assertion narrow to the provenance gap; do not snapshot the whole generated skill.</action>
  <verify>`npx vitest run tests/unit/install.test.js -t "gsdr-signal split provenance"` passes.</verify>
  <done>A future reinstall cannot silently regenerate a flat-provenance Codex skill from the updated source command.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate and verify the repo-local Codex skill mirror from source</name>
  <files>.codex/skills/gsdr-signal/SKILL.md</files>
  <action>After Tasks 1-2 pass, refresh the repo-local Codex runtime copy through the installer (`node bin/install.js --codex --local` or the equivalent local install path), not by hand-editing `.codex/skills/gsdr-signal/SKILL.md`. Verify the regenerated skill teaches the same split provenance fields as the source command and still points at repo-local `get-shit-done-reflect` paths. If the install path itself is broken, stop and record the blocker rather than patching `.codex/` output directly.</action>
  <verify>The local Codex install command succeeds, `rg -n "provenance_schema|about_work|detected_by|written_by" .codex/skills/gsdr-signal/SKILL.md` matches, and `rg -n "runtime: \\{detected\\}|model: \\{detected\\}|gsd_version: \\{detected\\}" .codex/skills/gsdr-signal/SKILL.md` no longer finds the old flat-only schema block.</verify>
  <done>The repo-local Codex skill is regenerated from source and reflects the split-provenance manual-signal contract.</done>
</task>

</tasks>

<verification>
- [ ] `commands/gsd/signal.md` uses the 57.8 split-provenance vocabulary and treats flat fields as compatibility echoes only
- [ ] `tests/unit/install.test.js` guards the missed manual-signal conversion surface
- [ ] `.codex/skills/gsdr-signal/SKILL.md` has been regenerated from source, not hand-edited
- [ ] Quick execution evidence is specific enough to summarize the fix and support the later `STATE.md` quick-task row
</verification>

<success_criteria>
- The source manual signal command, conversion regression, and repo-local Codex skill all agree on `v2_split` manual signal provenance.
- The old flat-only `runtime/model/gsd_version` schema block is removed from both source and installed manual-signal surfaces.
- The quick task remains narrow: no ROADMAP edits, no broader 57.8 replanning, and no direct `.codex/` patching as the primary fix.
</success_criteria>

<output>
After completion, create `.planning/quick/260419-6uf-fix-manual-gsdr-signal-split-provenance-/260419-6uf-SUMMARY.md`.

Quick-workflow closeout must then append the completion row to `.planning/STATE.md` under `### Quick Tasks Completed` and update `Last activity`. Do not update `ROADMAP.md`.
</output>
