---
phase: 01-test-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [utils.js]
autonomous: true
---

<objective>
Create a utility module with basic string helpers.

Purpose: Clean control plan with no expected deviations.
Output: utils.js with two string helper functions.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create capitalize function</name>
  <files>utils.js</files>
  <action>Create utils.js with a capitalize(str) function that capitalizes the first letter</action>
  <verify>Function works for empty string, single char, and multi-word inputs</verify>
  <done>capitalize function created.</done>
</task>

<task type="auto">
  <name>Task 2: Add reverse function</name>
  <files>utils.js</files>
  <action>Add reverse(str) function to utils.js that reverses a string</action>
  <verify>Function works for empty string and palindromes</verify>
  <done>reverse function added.</done>
</task>

</tasks>
